fix(#44): special-point meets draw ONLY the centre point, not the construction

Operator play-test feedback: "M מפגש התיכונים" should draw just the centroid,
not the median segments — same for the other centres.

The construction (diagonals / medians / altitudes / ⊥-bisectors and their helper
feet/midpoints) is now invisible scaffolding: no `segment` commands are emitted,
helper points are `~`-prefixed (unrendered per scene.ts:239, and excluded from
detection by ADR-295), and the ⊥-bisector `perpendicular-line`s are visible:false.
Only the intersection point (centroid / incentre / orthocentre / circumcentre /
diagonal crossing) is drawn. Geometry is unchanged (centroid = (A+B+C)/3, etc.).

special-point-meet.test.ts updated (command types + `~`-hidden helper assertion);
llm-contract snapshot updated. Full 2-D lane green, tsc clean.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
