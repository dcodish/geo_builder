# 09 — Implementation Plan

_Last updated: 2026-06-11._

> **Status:** Phases 0–4 complete (M1–M3). **Phase 5a complete** (quad, parallelogram, segment, line∩line; **Q1 reproduced**). **Phase 5b complete** ([ADR-019](06-decisions.md#adr-019)) — a **`Line`** object (through / bisector / perpendicular / parallel → `(anchor,dir)`), the points it produces (line∩line of two lines, foot of ⟂, midpoint), point-on-extension, and **right-triangle**; **Q2, Q3, Q4 reproduced**. **Phase 5c complete** ([ADR-020](06-decisions.md#adr-020)) — a drawn **`Circle`** object (centre + radius), circle-dependent points (on-circle/inscribed, antipode/diameter, arc-midpoint, line∩circle) + the **tangent** line spec; the renderer now draws circles; **Q5, Q6, Q7 reproduced** from typed He/En utterances. **The full v1 construct vocabulary (Q1–Q7) is now covered.** **Phase 5d** ([ADR-021](06-decisions.md#adr-021)) adds the distance/equal/parallel/perpendicular constraints (generic mechanism, ADR-014); **ADR-018 Stage 1** (pinned-vs-free + seeded `resample`) is built. **Visible construction lines** + **two-circle intersection** ([ADR-022](06-decisions.md#adr-022)) — a tangent/bisector/perpendicular/parallel can now be *drawn*, and circle∩circle is first-class. **Phase 7 (LLM fallback)** built ([ADR-023](06-decisions.md#adr-023)). **Post-corpus relational/UX layers** (2026-06-13/14): **symbolic measures** ([ADR-031](06-decisions.md#adr-031)) — variables (`AB=3x`, `∠ABC=2α`), affine/fractional/√/power/π, ratios, on-figure measure labels; **triangle congruence/similarity** ([ADR-032](06-decisions.md#adr-032)); **shapes carry their true DOF** ([ADR-033](06-decisions.md#adr-033)) so a sizing constraint reshapes rather than over-constrains; **angle marks when stated** (FR-RN-7); **named median** `AD תיכון`; **rename a point** ([ADR-035](06-decisions.md#adr-035), `שנה שם E ל-G`) + the [letter-placement guide](12-letter-placement.md); **image export** (save/copy PNG, FR-HS-5); **a drawn line named by two points creates drivable `on-line` markers** ([ADR-036](06-decisions.md#adr-036)); **`AB ⟂/∥ CD` also draws its segments** (FR-IN-7); **label readability** (white halo + a global nudge-off-lines placement pass); **two circles tangent to each other** ([ADR-037](06-decisions.md#adr-037)). **Then (2026-06-15, corpus-driven testing):** circle **radius as an honest DOF** (ADR-037 A2); **subscripted labels** `O1`→`O₁` (ASCII id, presentation-only `<tspan>`); **cyclic quadrilateral** `בר חסימה` (hidden circle, opposite angles 180°, FR-EN-14); **semicircle + quarter circle** (a drawn `Arc` primitive, FR-EN-15); a **debug session log** (`logs/debug-log.jsonl`); persistent **align-segment-horizontal** (FR-RN-6); diameter-on-a-cyclic-quad reshapes convexly + persistently; and **robust coupled-constraint solving** (degeneracy barrier + near-first/global-fallback) so a figure pinned by several givens — e.g. `AB=CB` AND `AC bisects ∠ECD` — builds convex without collapsing. **Then:** **inequalities between named measures** ([ADR-039](06-decisions.md#adr-039), FR-IN-8) — `α<β`/`x>y` *actively reshape* the figure so the relation holds visibly (a one-sided order constraint folded into every solver's joint minimisation, since an inequality owns no carrier). **Then (2026-06-15, home PC):** the **deferred backlog is cleared** — auto-draw segments for distance/equal/angle givens (FR-IN-7 extension); an explicit **merge** command (fold two points, FR-HS-8); a free **on-line marker's offset is now sampled** by "show another configuration" (ADR-036 A2); **named-line markers widened** to perpendicular/parallel lines (ADR-036 A3); and **ADR-018 Stages 2 & 3** (constraints prune sampled DOFs + a live "degrees of freedom remaining" cue, FR-ALT-4) — ADR-018 is now fully built. **Then (2026-06-16, manual-testing marathon — circle/tangent/secant corpus):** a string of constructs + fixes from real bagrut figures: a **secant from an external point** (FR-EN-17); **one & two tangents + multiple secants from a point** (FR-EN-18); **two circles intersecting at A,B** (FR-EN-19); the radius symbol **R auto-binds to the circle radius**; **"show another configuration" now resamples free DOFs AND cycles branches** (was branch-only) with **count-scaled on-circle variety**; the **DOF cue corrected** to `raw − constraints`; a **named perpendicular / perpendicular-bisector** (construct or constrain, both pre-existence arrangements); a loose segment **repositioned into a perpendicular cross** (ADR-036 A4); and **a circle's centre shows only when used or named** (FR-RN-8, incl. deterministic unnamed circles). **Then (2026-06-17):** the **"no fixed assumptions" principle elevated to [ADR-052](06-decisions.md#adr-052)** (audit fixed a single-circle radius pinned at 5 + a midpoint-pinned on-segment point); **circle radius as a free solver DOF** ([ADR-051](06-decisions.md#adr-051)); and the **collinearity family** ([ADR-050](06-decisions.md#adr-050) + 3 amendments — ordered `line ABE`, second-intersection "E on line DB", two driven-solver fixes). **Then (2026-06-18):** the **givens verifier** ([ADR-053](06-decisions.md#adr-053), `src/engine/verify.ts`) — **"green" now means VERIFIED, not just no-error** (amber cue otherwise); `tangentMeetsOtherCircle` + `lineMeetsCircle` + `פוגש`/`פגש`; and UX (scroll, responsive canvas, free-radius slider). **Then (2026-06-18, home PC):** **directional `המשך` semantics** ([ADR-054](06-decisions.md#adr-054)) — `המשך AC`→D beyond C (order A→C→D); a new `extend-onto-circle` (driven on-circle θ + `collinear` + `collinear-order`, the figure adapting / free radius growing so the extension reaches); order-aware 1-DOF root pick so a root-at-seed isn't resized away; the bagrut corpus reworded to the strict form. **Then (2026-06-18/20, manual-testing fixes):** a parse that builds nothing gets a SECOND try then an honest message ([ADR-055](06-decisions.md#adr-055), `dryRunOutcome` — now counting a pure RESHAPE like a diameter on a cyclic quad as "produced", so `AB קוטר` no longer false-escalates); and a **symmetric driven constraint nudges its DOF to the NEAREST root** ([ADR-056](06-decisions.md#adr-056)) so `OD⊥AC` keeps the quad convex instead of flinging D to the far arc-midpoint and crossing it; and a **circle tangent to two GIVEN sides** ([ADR-057](06-decisions.md#adr-057), `cornerTangentCircle`) — the inverse of "tangent from a point" (the sides are given, the circle constrained tangent), composed from primitives like the incircle (centre = a free point on the corner's bisector, radius via a circle through the foot on one side, the other tangency automatic by equidistance), so "AB ו-AD משיקים למעגל O" no longer escalates and builds nothing — and its [ADR-057](06-decisions.md#adr-057) A1 GROWS the circle (driving the centre's free DOF) when an existing vertex is then put on it ("C נמצאת על המעגל"), keeping tangency; A2 lets it auto-name a fresh circle when none is given. Plus a **√ in a segment ratio** ([ADR-058](06-decisions.md#adr-058)) — `EB/AE=√2/2` reads as the ratio √2/2 (was silently fragment-misparsed to a length `AE=√2`), with a shared ratio-LHS guard so any unreadable ratio RHS fails honestly. And an **extension-meet draws both lines to the meeting point** ([ADR-054](06-decisions.md#adr-054) A2) — "המשך CA ו-BD נפגשים בנקודה G" now draws C→G and B→G (the lines reach G) instead of stubs that stop at the inner points. And a **"show circle centres" view toggle** ([ADR-059](06-decisions.md#adr-059), ⊙ in the figure toolbar, default off) reveals every circle's centre with its label so two unnamed circles are tellable apart — a pure display preference (no engine/fact state). And **two points on one segment** ([ADR-060](06-decisions.md#adr-060)) — a free point-on-segment now seeds in the largest open gap (0.5 → 0.25 → 0.75 …) so a second "point on AC" doesn't collide with the first, plus a `pointsOnSegment` rule for "L ו-K נקודות על AC" (and a recorded `\w`-is-ASCII Hebrew-regex gotcha). And a **perpendicular that "cuts AB at E"** ([ADR-061](06-decisions.md#adr-061)) now creates the crossing E and draws the perpendicular segment EK (was dropping the cut clause and drawing only a bare line). And **coupled chained equalities** ([ADR-062](06-decisions.md#adr-062)) — `AL=LK=KC` now drives both free points jointly so they trisect AC (was erroring "unresolved dependencies" because the two equalities each referenced the other's driven point — a closed-form cycle now promoted to the numeric joint solver). And **spelled-out Greek names normalise to symbols** ([ADR-063](06-decisions.md#adr-063)) — "alpha"/"2alpha" now read as α/2α (the spelled form used to miss the variable regex and "2alpha" half-parsed to a 2° angle); guarded so uppercase point pairs ("MU") and longer words ("alphabet") are untouched. And the REAL α/2α bug — **only a FREE on-segment point is driveable** ([ADR-064](06-decisions.md#adr-064)): a `2α` relation was dragging a point the student fixed (D on the extension, t=1.3) and failing; now a stated-ratio/extension point is a given the solver leaves put, so the relation drives the figure's free shape instead. And **"show another configuration" reports when there's no different drawing** ([ADR-065](06-decisions.md#adr-065)) — `resample` compares candidates with a similarity-invariant shape fingerprint and, when the shape is determined (only size/rotation free), shows "No other configuration" instead of silently redrawing the same figure. And **"triangle circumscribes a circle" is the incircle, plus two-tangents-meet** ([ADR-066](06-decisions.md#adr-066)) — "משולש DEF חוסם את המעגל" now builds the incircle (was a wrong circumcircle), and "tangent at A and tangent at C meet at D" builds the two tangents + their crossing; with these in the catalog the LLM composes the full tangential triangle from "a tangent through each vertex … meeting at D E F". **Then (2026-06-20):** **"AB = √2R" couples a length to a FREE radius** ([ADR-071](06-decisions.md#adr-071), a `length-radius` constraint) — three stacked defects on the operator's tangent figure (parser dropping the trailing R in `√2R`; lowering freezing R to the circle's default on a free-radius circle → seed-fragile over-constraint; the joint solver giving up) fixed by a first-class constraint that drives the radius DOF **and** the witness on-circle angle (the tangent caps the radius, so a moderate radius + the right θ satisfies it), making `BO=R` a tautology; the relation holds on all 9 seeds. **1091 tests green**, build clean, on `rebuild-foundation` (GitHub backup: `dcodish/geo_builder`). **Open threads (parked):** an *area* given ("area of △ = 0.25k²") is unsupported — a new constraint class; the production proxy deploy; the differential coordinate-validation campaign; **LLM-assisted solve for non-contradiction solve-failures** ([ADR-049](06-decisions.md#adr-049), Proposed — LLM supplies structure/seed, engine refines + verifies; depends on the ADR-045 solver consolidation + the proxy deploy); and **LLM reorder-and-repair of out-of-order input** ([ADR-073](06-decisions.md#adr-073), Proposed, FR-IN-10 — reading order ≠ construction order, so the LLM reorders a whole-problem batch + inserts implicit objects, validated by the givens verifier in a bounded repair loop; the student never plans the construction; reorder/insert only, never drop a given; offline Opus-oracle tests can precede the proxy deploy). **Then (2026-06-21) the R7 binding remainder for the bagrut `∠ADB=<number>` case was CLOSED** ([ADR-074](06-decisions.md#adr-074)): it was a *binding* jam (greedy `driveOrCheck` claimed the apex while the extension point `G` was frozen), not the convergence failure first suspected — a `pointOnExtension` point now carries `extension:true` (recruitable, not eager) and the failure-path recruit "frees the blocker" (re-points `AG⊥AD` to `G`, releasing the apex for `∠ADB`), so the figure flexes to the stated angle while [ADR-064](06-decisions.md#adr-064) stays byte-identical (eager path untouched). **1105 tests green, build clean.** Still deferred: the up-front joint binding + real stay-put penalty (the larger R7 rework), and sampling the extension `t` on "show another configuration". **PRIORITY (operator directive, 2026-06-21): Phase 6 (theorems) is GATED behind a full, working, comprehensively-tested system — harden + test what exists first, no rush.** **Hardening program — Pillars 1–4 DONE (2026-06-21), 1183 green, build clean:** (1) the **givens verifier** ([ADR-053](06-decisions.md#adr-053)) is now COMPREHENSIVE — "green = verified" covers every distance/angle/equal/ratio/∥/⊥/collinear/concyclic relation, unit-proven to fire; (2) the **corpus + 56 scenarios + a 25-figure sweep all self-verify** → zero silent-wrong figures; (3) the **negative-space gate** ([ADR-047](06-decisions.md#adr-047) Tier B) rejects contradictions generatively across scales; (4a) **extension-`t` sampling** (ADR-052), (4b) a **generated additive-stability property**, (4c) **R7 binding fragility fixed at root** ([ADR-074](06-decisions.md#adr-074) Am.2 — equilateral / AAS false over-constraints, found by an evidence-first probe). **Deferred with reasons (not blockers):** the full R7(3) greedy-retirement + per-DOF stay-put penalty (no over-recruit-drift evidence found), and Tier-C `movedAncestors` generated constraint-stability (guarded by the fixed-seed snapshot). **Pillar 5 = production proxy DEPLOY only — a go-live step, not a local-correctness gate** (the LLM fallback already runs locally via the dev proxy, so the whole system is testable end-to-end now). **Phase 6 (theorems) is the milestone after the operator's own local validation pass — the automated gate is now substantially met.** **Then (2026-06-21, manual-testing fix):** **"AB tangent at F" on an EXISTING line now flexes the circle** ([ADR-075](06-decisions.md#adr-075)) — it used to error `unresolved dependencies` because the parser re-created the existing vertices A,B as markers on a new tangent line (a dependency cycle), and there was no path reading "existing line tangent to circle" as a circle-flexing constraint; now, when the line's labels and the touch point all pre-exist, `tangentLine` emits a tangency constraint `set-perpendicular(O,F,A,B)` (radius ⟂ the existing line) and the circle adapts so OF⟂AB with F on the circle (verified clean). **Then (same figure):** **"F, G, H on AB, AC, CB" (N points pairwise on N segments) now parses deterministically** ([ADR-076](06-decisions.md#adr-076)) — it used to escalate to the LLM (which built nothing on the "הישרים"/lines wording); a new `pointsOnSegments` rule maps N points one-to-one to N two-letter segments (He/En, "sides"/"lines"), so the whole figure builds without the LLM. **Then (same figure):** **"the diameter from F cuts side AC at E" lands E ON the segment** ([ADR-077](06-decisions.md#adr-077)) — the "קוטר … cuts …" phrasing used to escalate to the LLM (which built nothing), and the manual line∩line workaround put E on the *continuation* of AC; a new `diameterCutsSegment` rule emits the diameter-line (F–O) ∩ line AC PLUS a `set-line [A,E,C]` order constraint, so the figure FLEXES a free DOF (the triangle reshapes, F moving with it) to keep E on the side. **Amendment:** a BARE "AC" (not just "הצלע AC") defaults to the segment — operator principle "a segment reference means the segment, not the continuation, unless you say `המשך`"; opt-outs "המשך AC" (E beyond, dir2) and "הישר AC" (infinite line). **Then (2026-06-21, operator session `m26xv4m2`):** the **Q4 tangent/secant trapezoid now draws cleanly** ([ADR-078](06-decisions.md#adr-078)) — quad BKCD with KB∥CD, CD a chord, KB tangent at K, BD a secant cutting the circle at A, encoded via tangent–chord (K = arc-midpoint of CD) + B = (tangent at K)∩(line AD). The construction was always geometrically exact, but the clean textbook drawing (convex BKCD **and** A between B,D) was ~1-in-300 under the seeder while "show another configuration" searches only 24 seeds. Root cause: `sample.ts` set the on-circle jitter width from the **total count** of free on-circle points, so a chord (C,D) plus a lone secant point (A) read as "3 → an inscribed polygon" and was pinned tight (±30°) — the chord never shortened. Fix: scope the tight jitter to **genuine inscribed polygons** (a declared polygon with ≥3 free-on-circle vertices), with the legacy count fallback only when no polygon touches the circle; a chord/secant point now ranges wide (±153°), lifting the clean-config rate **~100× (≈0.3 %→30 %)** so the figure is reliably reachable via "show another configuration". Real inscribed triangles/quads untouched. **1205 tests green, build clean.** **Then (same session, live-Haiku test — operator authorised one call):** the verbatim Q4 problem run through the LLM fallback decomposed in **reading order** (as predicted) and treated the circle as *given* — emitting "chord CD in circle O" / "tangent to circle O at K" / "BD cuts circle O at A" with **no step that creates the circle** — so centre O was undefined and the build collapsed with "unresolved dependencies". Fixed by **auto-materialising an IMPLICIT circle** ([ADR-079](06-decisions.md#adr-079)): the parser (`withImplicitCircles`) prepends a free circle (free centre + free radius) when a *consuming* step references a `circle-<centre>` neither in context nor defined by the same utterance; the injected circle is `ifAbsent` so the engine **skips it if the circle already exists** (never clobbers a real radius). Lives in the parser (interpreting natural input), so the engine stays strict — a referenced-but-*removed* circle still cascades honestly. The exact live decomposition now builds; the leftover "C not on circle" is the deeper trapezoid-first reorder/repair gap ([ADR-073](06-decisions.md#adr-073)), correctly flagged amber by the verifier. **1209 tests green, build clean.** **Then (same session):** that leftover "C not on circle" was fixed at root ([ADR-080](06-decisions.md#adr-080)) — declaring an EXISTING free vertex (a quad corner) "on circle O" used to add **no constraint** when the circle's radius was free (the `point-on-circle` apply path fell through to the give-up case), so the vertex stayed adrift. Now a non-pinned free vertex **converts to an on-circle point** (it slides on the circle, "shapes carry their true DOF" — [ADR-033](06-decisions.md#adr-033)), so "C,D on circle O" makes CD a genuine chord (verified: C,D land equidistant from the centre, no violation); a **pinned** point (explicit placement) is guarded and not slid. This fixes the relation generically; it does NOT rescue the whole flawed live-Haiku decomposition (which applies KB∥CD before the chord and so over-constrains trapezoid-first — now failing *honestly* instead of drawing C off the circle; the ADR-073 reorder work). **1211 tests green, build clean.** **Then (2026-06-22):** **"KB tangent at K" (a segment tangent at its OWN endpoint) now routes to a tangency relation** ([ADR-081](06-decisions.md#adr-081)) — it used to match the greedy `tangentFromExternal`, which read B as an external apex, **invented a new touch point T** (a Thales circle∩circle), drew B–T, and ignored K entirely (silently wrong: nothing asserted "KB tangent at K", so the verifier had nothing to flag). Fix: `tangentFromExternal` defers when the named "at" touch point already exists; `tangentLine` adds a case where the named segment's labels INCLUDE the touch T as an endpoint → emit `point-on-circle T` + `set-perpendicular(O,T,P,Q)` (T on the circle, radius ⟂ the segment), so the figure flexes to a real tangent (verified: K on circle, OK⟂KB). The tangent STEP now builds cleanly in the reading-order Q4 too (the build still stops at the separate KB∥CD trapezoid-first over-constraint — ADR-073). **1213 tests green, build clean.** **Then (2026-06-22, work PC):** **"KB tangent to the circle" with NO explicit touch point now parses** ([ADR-082](06-decisions.md#adr-082)) — the operator's natural "KB משיק למעגל" (vs ADR-081's "KB tangent *at K*") was the form actually failing (`not-understood` in the log); `tangentLine` gated the touch point behind an explicit "at X" clause, so it bailed. Fix: infer the touch from the segment endpoint already on the circle (`ctx.circleMembers`, exactly one); the engine builds it perfectly once it has the command (K on circle, OK⟂KB, KB∥CD, verifier clean). Locked by the operator's exact sequence as an end-to-end scenario. **1214 tests green, build clean.** **Then (same session): "one circle → no name"** ([ADR-083](06-decisions.md#adr-083)) — the operator's principle that a definite "the circle"/"המעגל" should resolve to the single circle when only one is present. The full-Q4 secant "BD חותך את המעגל בנקודה A" (unnamed) was `not-handled` because `lineMeetsCircle`/`extendOntoCircle` required a NAMED circle for both the centre and the crossing anchor. Fix: `resolveMentionedCircle` (single circle when the utterance mentions a circle at all, named or definite) + `crossingAfterCircle` (name-optional anchor), guarded so a circle-less line∩line is NOT grabbed. Full Q4 now completes deterministically. **1219 tests green, build clean.** **Then (same session): a tangent's named external point appears immediately, then a later fact drives it** ([ADR-084](06-decisions.md#adr-084)) — "מנקודה D יוצא משיק למעגל בנקודה B" used to draw the tangent but DROP D, and the defining "המשך CA נפגש עם המשיק בנקודה D" was not-handled. Operator chose "D appears at step 2": `tangentLine` now creates the named apex D as a free `point-on-line` marker on the tangent, and a tight `extensionMeetsExistingPoint` rule reads the follow-up as `set-line [C,A,D]` (D already on the tangent → only "D on the CA extension" is new, driving its DOF to the crossing). No parse-context plumbing. Verified end-to-end (DB⟂OB, D on CA extension beyond A, verifier clean). **1220 tests green, build clean.** **Then (same session): scalene default + lone-marker sign** ([ADR-085](06-decisions.md#adr-085)) — two symptoms of one ADR-052 flaw on the tangent figure: (a) the one-shot "tangent at B meets CA at D" ERRORED "lines parallel" because the inscribed triangle defaulted to ISOSCELES (golden-angle's two equal gaps put B at the arc-midpoint of AC → tangent@B ∥ CA); (b) D always landed on the same side of B. Fixes: a bounded alternating skew in `nextTheta` makes the default a GENERIC scalene triangle (tangent ∦ chord), and the sampler now flips a LONE on-line marker's sign (a ±pair keeps its signs). Fallout was 2 intended coordinate snapshots (`-u`) + the ADR-036 marker test (now asserts both-sides). **1220 tests green, build clean.** **Then (same session): "C on the extension of DA" keeps the ORDER for an EXISTING point** ([ADR-086](06-decisions.md#adr-086)) — "C נמצאת על המשך DA" aligned D,A,C but put C BETWEEN D and A (near intersection), not beyond A. Root: `pointOnExtension` always emitted `point-on-segment … extension`, but C already existed as an on-circle vertex, so it stayed on the circle as a bare collinearity at the near root, losing the order. Fix: when the named point EXISTS, emit an ordered `set-line [D, A, C]` instead (drive the existing point, on its current carrier, beyond the far end in order). C now lands beyond A (D→A→C), still on the circle. **1223 tests green, build clean.** **Then (same session): two operator feature requests** — **set a circle's radius by value** ([ADR-087](06-decisions.md#adr-087)): a `set-radius {circle,value}` command + `setRadius` rule ("רדיוס מעגל P הוא 4" / "radius of circle P is 4") sizes an EXISTING circle with NO segment drawn and no point invented (a through-radius incircle flexes the figure; a free/length radius is set); and **hide a circle** ([ADR-088](06-decisions.md#adr-088)): a `hiddenCircles` display set + `toggleCircleHidden`, with circles now clickable → the on-canvas menu → hide/show (mirrors segment hiding; engine untouched). **1228 tests green, build clean.** **Then (same session): typo escalation gate** ([ADR-089](06-decisions.md#adr-089)) — "מנוקדה D …" (typo of מנקודה) made the tangent rule match partially and silently DROP the "from D" apex, committing a partial figure instead of escalating. New `droppedNewLabels` gate in `App.submit`: a successful parse that leaves a NEW input label unreferenced is treated as weak → escalate to the LLM (typos are the LLM's job, not the grammar's). Zero false positives across the corpus (an existing label a command doesn't re-name is NOT flagged). **1232 tests green, build clean.** **Then (same session): a circle DEFINED BY its diameter** ([ADR-090](06-decisions.md#adr-090)) — the operator tried 5+ ways to make a circle with AB as diameter, all failed (define phrasings not-handled; "AB קוטר של מעגל O" misrouted to `diameter` and errored on existing A,B). New `circleOnDiameter` rule for the define phrasings (of/with/whose-diameter, He+En) → segment + midpoint(centre) + circle-through, so the centre is the midpoint of AB; the "diameter X IN circle O" add-phrasing still routes to `diameter`. **1235 tests green, build clean.** **Then (same session): midpoint endpoints + circle-by-centre/radius diameter** ([ADR-091](06-decisions.md#adr-091)) — "B אמצע הקטע AC" on an empty figure errored ("unresolved deps") because `midpoint` didn't create A,C; now it prepends a segment when an endpoint is new. And "AB קוטר במעגל שמרכזו O ורדיוסו R" misrouted to `diameter` ("'B' already defined"); circleOnDiameter's define-markers now include the centre/radius spec (שמרכזו/ורדיוסו/centered/radius) so it's recognized as defining a circle. Both were standard constructs that wrongly fell to the LLM (which built nothing). **1235 tests green, build clean.** **Then (same session): a GIVEN diameter defines the circle** ([ADR-092](06-decisions.md#adr-092)) — "AB קוטר במעגל O" (and bare "AB קוטר") with A,B already placed still errored ("'B' already defined") / was not-handled. circleOnDiameter now fires for a given diameter (both endpoints exist + no existing circle to attach to), not just for define-marker phrasings; the cyclic add-a-diameter and new-points "diameter DE in circle O" paths stay with `diameter`. **1237 tests green, build clean.** **Then (same session): tangent at a diameter endpoint** ([ADR-093](06-decisions.md#adr-093)) — "EA משיק למעגל בנקודה A" where A is the diameter circle's through-point ERRORED "unresolved dependencies" (a dependency cycle): the tangent emitted `point-on-circle A`, but A defines the circle's radius, and `pointOnCircle` didn't count a through-point as on the circle, so A was converted to on-circle → A↔circle-O cycle. Fix: `pointOnCircle` now recognises the through-point as on its circle → idempotent no-op, only the tangency constraint (OA⟂EA) is added. **1239 tests green, build clean.** **Then (same session): two tangents from E where a touch point already exists** ([ADR-094](06-decisions.md#adr-094)) — "מנקודה E יוצאים משיקים למעגל בנקודות A ו D" with A an existing diameter endpoint ERRORED "'A' is already defined" (the Thales circle∩circle re-creates A). Fix: when either touch exists, fall back to the tangency-constraint form (point-on-circle idempotent + set-perpendicular(O,P,E,P) per touch); all-new keeps the Thales construction. **Verified through an `App.submit`-faithful harness** (parse → gates → execute), per operator feedback that parse→replay alone doesn't prove the app commits. **1240 tests green, build clean.** **Then (same session): a constraint on a secant crossing can drive UPSTREAM free DOFs** ([ADR-095](06-decisions.md#adr-095)) — "∠CAE = 45" on a tangent/secant figure falsely reported "over-constrained" though a scan proved 45° reachable (∠CAE ranges ~2–143° over the free apex B / radius). Root: the ancestor walker had no case for `line-circle`/`circle-circle` points (pointParents → default []), so `recruitFreeDofs` dead-ended (cand=[]) and never recruited B. Fix: traverse a secant crossing (`line-circle`) through its line to the apex; `circle-circle` deliberately NOT traversed (over-recruits, broke the ADR-093 collinear). The angle now solves to 45° with the other givens intact. **1242 tests green, build clean.** **Then (2026-06-24) the parked "differential coordinate-validation campaign" is BUILT** ([ADR-109](06-decisions.md#adr-109), `src/validation/`) — a differential check (vs the invariants campaign + givens verifier): seeded recipes pin their base points so the frame is fixed, an independent closed-form oracle (imports nothing from the engine) recomputes the derived coordinates, and they're diffed directly. Dev/CI-only, applies only to 0-shape-DOF figures (~11% of real logged figures); zero engine changes. **Finding: engine matches the oracle to ~machine epsilon (worst residual 5.6e-15 over a 13,760-figure sweep). 1286 tests green, build clean.** Remaining parked: area-given constraint class, production proxy deploy, [ADR-049](06-decisions.md#adr-049); coordinate-campaign next slices (Procrustes/global-solve mode, set-angle-driven, circle-via-intersection). **Then (2026-06-24): theorem-vocabulary gap closed** — an operator audit of the bagrut theorem list vs. the construct vocabulary ("is there no דלתון?") found the missing nameable shapes and the n-gon gap. **Named shapes as parser MACROS** ([ADR-110](06-decisions.md#adr-110)) — **kite·דלתון** (also עפיפון), **isosceles·שווה שוקיים**, **equilateral·שווה צלעות**, **isosceles trapezoid·טרפז שווה שוקיים**, **triangle midsegment·קטע אמצעים** — each decomposes to already-supported commands (`quadrilateral`/`triangle`/`trapezoid` + equal-side/midpoint constraints), so the constraint solver does the work with **no new engine construct**; catalog entries auto-feed the LLM prompt, the i18n term corrected `עפיפון`→`דלתון`. **Regular polygon** ([ADR-111](06-decisions.md#adr-111)) — a generic `{type:'polygon'; ids:Id[]}` command + `polyEdges` + a `regularPolygon` rule placing n equally-spaced vertices on a hidden free-radius circle (He `מחומש/משושה/…`, En `pentagon…dodecagon`, `regular N-gon`); n=3→equilateral, n=4→square, n≥5→generic; irregular n>4 stays on the LLM segment-loop net. **DOF-cue correctness fix** ([ADR-112](06-decisions.md#adr-112), amends ADR-101) — a pinned parametric (on-circle/on-segment) vertex contributes **0** DOF, so the shape-DOF ladder reads exactly: **free quad 4 → cyclic quad 3 → inscribed square 0 → regular polygon 0** (fixes a pre-existing over-report on all inscribed shapes; "✓ fully determined" now fires). New `engine/__tests__/named-shapes.test.ts` + scenarios `kite-named-shape`/`regular-hexagon`. **1322 tests green, build clean.** **Then (2026-06-24, work PC): a constraint now flexes a shape DOF reached *through* a parametric point** ([ADR-113](06-decisions.md#adr-113)) — operator-reported `GE⊥AB` (rhombus, `E` mid `AB`, `G` on the extension of diagonal `BD`) failed "over-constrained: GE ⟂ AB cannot hold". The figure is constructible only if the rhombus angle flexes below 60° so `G` lands strictly beyond `D` (at 60° the unique solution is the degenerate `G=D`); but the drivable-ancestor walk **stopped at the free param carrier `G`** and never reached the rhombus angle (carried by `G`'s parent `D`), so the recruiter could only slide `G` onto `D`. A `git bisect` proved this never worked (back to 2026-06-17 the same figure failed with "D and G would be at the same point"). Fix: in `drivable` mode (the failure-path recruiter only) record a free on-segment carrier **and keep walking past it** to the shape DOFs behind its segment; the regulariser keeps surfaced-but-unneeded parents at seed. General fix for the class (any constraint that must flex an upstream shape DOF reached through a parametric point). Locked by scenario `perpendicular-from-midpoint-flexes-rhombus`. **1323 tests green, build clean.** **Then (2026-06-24): "isosceles" picks a SOFT default equal-pair that yields to an explicit one** ([ADR-114](06-decisions.md#adr-114)) — operator-reported: `משולש שווה שוקיים ABC` hard-coded `|AB|=|AC|` ("apex = first vertex"), so the student's own `AB=BC` stacked into an **equilateral** triangle they never asked for. "Isosceles" only asserts SOME two sides equal — which pair is the student's to state (ADR-052). Fix: the macro tags its default equality `soft`; `replay` drops a soft equality when an explicit (non-soft) `set-equal` on the same triangle is present, so the stated pair wins (lone isosceles still draws `|AB|=|AC|`). Scoped to the isosceles triangle; equilateral/kite/iso-trapezoid stay hard. Locked by scenario `isosceles-explicit-pair-overrides-default`. **1324 tests green, build clean.**
>
> **Session 2026-06-11 banked a large hardening + UX pass** (all from a code review + manual stress-testing, no new corpus coverage): ADR-014 generic constraint solving + DOF-selection fix; parser misparse defense; ADR-015 edit-a-fact-in-place; ADR-016 snap-to-intersection; renderer lines-only + smart label placement; categorized commands coverage-map panel; and a deep **shape-composition** sweep — ADR-013 amendments (similarity-fit so composed shapes don't degenerate; vertex-order independence), ADR-017 (no two nodes may coincide + flip to avoid), empty-side composition + `flip` flag so any shape lands away from existing geometry, and textbook-clean upright templates. Plus **ADR-018** (direction only, not built): *alternatives = samples of the figure's residual freedom* — cycling should re-sample free DOFs so an underdetermined figure visibly wanders and converges as facts accumulate.
>
> **CURRENT (2026-06-18, end of session) — 1004 tests green, build clean, on `rebuild-foundation`.** Bagrut two-circle figure (two circles meeting at A,B; mutual tangents; two secants) + a trust pass. Added: parser constructs `tangentMeetsOtherCircle` + `lineMeetsCircle`; `פוגש`/`פגש` into the shared `INTERSECT_KW`; an apply-time fix so re-defining an existing point as on-circle becomes the second-intersection (no silent no-op); the **givens verifier** ([ADR-053](06-decisions.md#adr-053)) — `checkGivens` re-derives the relations the COMMANDS assert and checks them against the final coordinates, surfaced as `Derived.violations` + an amber cue, so **"green" means VERIFIED, not just no-error**; and UX from manual-test feedback — page **scroll restored** (dead `overflow:hidden` removed), a **responsive canvas** (fills the screen), and **playable radius-slider DOFs** (first slice, scratchpad cleared on resample). **★ Resume here — directional `המשך` semantics:** `המשך AC … D` must place D BEYOND the 2nd letter C (order A→C→D) and **adapt the figure** (drive the free radius so an extension intersection exists); today `lineMeetsCircle` is order-agnostic (E lands between A,C). Full plan + the key finding (default radii have NO extension root → resizing is required) + caveats are in [PROJECT-MEMORY.md](PROJECT-MEMORY.md) (2026-06-18 entry, "★ NEXT STEP"). After that: extend the verifier (constraint residuals + collinearity) and DOF sliders (on-circle/on-segment/free points); then **Phase 6 (theorems)**. Parked: area-given constraint class, production proxy deploy, coord-validation campaign, [ADR-049](06-decisions.md#adr-049).
>
> **CURRENT (2026-06-17, end of session) — 989 tests green, build clean, on `rebuild-foundation`.** This session, driven by manual testing of the two-circles / secant corpus, added: **collinearity** ([ADR-050](06-decisions.md#adr-050)) — `set-collinear` + a parser rule ("E on line AC", "line CE passes through A", "A,B,C collinear") + a redefine→collinear reinterpretation; **Am.1** a binding-aware seed + "keep an accepted candidate" fix in the driven solver (a chained collinearity that returned a false over-constraint); **Am.2** the second-intersection pattern (a collinearity onto a circle through a co-circular point becomes a line∩circle that AVOIDS the shared point — deterministic, never the degenerate crossing); **Am.3** an ordered multi-point line **"line ABE"** (collinear AND in the named order, via a one-sided `collinear-order` constraint). Then **circle radius as a free solver DOF** ([ADR-051](06-decisions.md#adr-051)) — a shape-scalar a constraint sizes and "show another configuration" varies (with a distinctness guard so resampled sizes don't collapse two points); the renderer recovers the driven radius from a point on the circle. Then the operator elevated **"no fixed assumptions"** to a guiding principle ([ADR-052](06-decisions.md#adr-052)): every unstated magnitude is a free DOF, never a fixed value — fixed two violations (ALL unstated-radius circles now free, not just two-circles; `point G on AB` with no ratio now SLIDES instead of pinning at the midpoint). **Resume here:** (a) optionally a **systematic DOF-conformance sweep** for ADR-052 (any value counted by `rawMovableDof` but absent from `freeDofs` = a default masquerading as fixed); then **Phase 6 (theorems)** is the headline. Parked: area-given constraint class, production proxy deploy, coord-validation campaign, [ADR-049](06-decisions.md#adr-049) LLM-assisted solve.
>
> **Next (resume here):** corpus fully reproduced (Q1–Q7); the construct vocabulary + the relational/UX layers above are in. The operator is **"almost ready for the next phase"** → **Phase 6 (theorems)** is the headline next milestone. **The smaller deferred directions are now ALL DONE (2026-06-15, home PC):** (a) extend the "draw its segments" convenience (FR-IN-7) to distance/equal/angle givens — ✅; (b) an explicit **merge** command (`מזג F ל-E`) distinct from rename (FR-HS-8) — ✅; (c) make a free (un-driven) `on-line` marker's offset samplable by "show another configuration" ([ADR-036](06-decisions.md#adr-036) A2) — ✅; (d) widen the "name a line by two points → markers" beyond the tangent rule to perpendicular/parallel ([ADR-036](06-decisions.md#adr-036) A3; bisector already had its own `line-intersection` path) — ✅; (e) ADR-018 Stage 2/3 (constraints prune sampled DOFs + a DOF-remaining cue, FR-ALT-4) — ✅; **(f) subscripted point labels (`O₁`, `O₂`) — ✅; (g) circle radius as a flexible DOF — ✅ (ADR-037 A2).** Still parked (not in this batch): a single-bisector / free-point-driver carrier and drawing a bare/clipped line (ADR-019 leftovers). **Still in progress (pre-existing):** a large differential coordinate-validation campaign (LLM-generated fully-determined diagrams → predicted coords → build → assert node coordinates vs an independent reference).

## Purpose

The phased plan for building v1: what each phase delivers, what it depends on, which requirements it satisfies, and the gate that closes it. This is the authoritative plan; [Design §10](04-design.md) is the one-line summary and [Testing §Per-step gates](08-testing-strategy.md) holds the detailed acceptance criteria.

## Approach

- **De-risk the core first.** The constructive engine is the make-or-break unknown — it is Phase 1, proven on fixtures before any UI or parser exists.
- **Gate-driven, not time-boxed.** A phase is done when its acceptance gate (Testing doc) is green — tests pass, `tsc`/build clean, results shown. No phase is "ready" otherwise (ADR-008).
- **Vertical proof, then breadth.** Phases 1–3 prove the full pipeline on a thin slice; Phase 5 widens coverage to all of v1.
- No time estimates here — sequencing and dependencies only. (Estimates can be added if scheduling is needed.)

## Validation corpus (real questions drive the build)

Correctness is judged against **real bagrut questions** (text + official image) in [`sample questions/`](sample%20questions/), not only self-authored fixtures. For each, we reproduce the **figure from the givens** — never the algebra (solving is a non-goal; see [Vision](01-vision.md)).

- **Multi-stage.** A question's later parts (ב, ג, ד…) add givens — the figure accumulates them, which is the incremental model itself (FR-EN-7). A corpus entry captures the figure at each relevant stage; the engine must stay stable as stages accumulate.
- **Comparison is visual/structural, human-judged**, up to the figure's free transforms (placement, rotation, scale, reflection) and the chosen alternative branch — not pixel-matching. "Match" = a valid drawing of the givens, consistent with the official image.
- **Before the parser (Phase 4):** questions are **hand-encoded** into commands (we play the parser); the engine → render → eyeball loop validates the engine early. After Phase 4 the same corpus becomes an automatic text→image test.
- Each reproduced figure is frozen as a **golden fixture**.

**Current corpus:** 7 questions — two parallelograms, one rhombus, one right triangle (Q1–Q4), and three circle problems (Q5–Q7: a triangle inscribed with an arc-midpoint; a chord and diameter intersecting, plus a parallel-line∩circle point; an inscribed triangle with a tangent at a point). Together they exercise the full v1 construct vocabulary (table in Phase 5).

## Phases

### Phase 0 — Foundation ✅ (complete)

Scaffold, archive of the old implementation, the `docs/` set, lint config, and the foundation commit.

---

### Phase 1 — Engine core (make-or-break) ✅ complete

- **Status:** done — `src/engine/` + `src/engine/__tests__/phase1.test.ts` (6/6 green); milestone M1 reached. Supported constructs so far: `square`, point-on-segment, circle∩circle intersection, angle-check.
- **Goal:** prove the constructive model end-to-end on fixtures, no parser/UI.
- **Builds:** the dependency-graph data model; `applyCommand` reducer; topological evaluation; point kinds (free / on-segment / intersection); branch selection + cycle; over-constraint detection; structural stability (persistent DOF + branch indices).
- **Depends on:** Phase 0.
- **Requirements:** FR-EN-1, -2, -6, -7, -8, -9; FR-ALT-1, -2, -3.
- **Gate:** [Testing §Step 1](08-testing-strategy.md) — F1 (build + stability), F2 (alternatives), F3 (over-constraint), determinism; typecheck/build clean.
- **Risk:** R1 (engine expressiveness). If this phase doesn't come together cleanly, the whole approach is reconsidered before investing in UI.

---

### Phase 2 — Renderer (SVG) ✅ complete

- **Status:** done — `src/render/` (`transform.ts`, `scene.ts`, `Figure.tsx`) + `src/render/__tests__/phase2.test.tsx` (9/9 green); typecheck/build clean. `App.tsx` wires the engine → renderer as a live F2 demo with a working "show another configuration" toggle (drag-pan / scroll-zoom / reset).
- **Goal:** draw the engine's output as interactive SVG.
- **Builds:** world→screen transform + fit (isotropic, centred, Y-flipped); points, segments, polygons, labels; pan/zoom/reset. **Deferred to when a corpus question needs them** (kept out of v1 slice until exercised): circles, angle arcs, right-angle marks, equal-side ticks, dashed special lines (Phase 5c), smooth animation of moved points (Phase 8 drag).
- **Depends on:** Phase 1 (consumes computed figures).
- **Requirements:** FR-RN-1, -2, -3, -4.
- **Gate:** ✅ transform unit tests; scene-builder "figure → expected nodes"; DOM-free static render of `<Figure>` via `react-dom/server` (F1 nodes/labels, F2 branch differs); typecheck/build clean.
- **Enables the validation loop:** with the renderer, hand-encoded corpus questions can be rendered and compared to their official images — validating the engine *before* the parser exists.
- **Risk:** low — pure consumer of engine output; renderer is swappable.
- **Note:** the renderer is a **pure consumer** — `scene.ts` resolves a `Construction` + `positions` into flat primitives (no React), tested headlessly; `Figure.tsx` is a thin declarative map over that. Components are rendered to static markup in tests (no jsdom dependency).

---

### Phase 3 — Store & app shell ✅ complete

- **Status:** done — `src/store/geoStore.ts` (Zustand + `zundo` temporal) + `src/store/__tests__/phase3.test.ts` (13/13 green); `App.tsx` rebuilt as the store-driven shell (canvas + fact list + undo/redo/clear + alternatives toggle + language toggle, Hebrew default / RTL); typecheck/build clean. Milestone M2 reached. **The fact list is the source of truth and the figure is derived by replay** — each fact can be selected (highlighted on canvas), deselected (kept but off; dependents auto-drop, reversibly), or deleted (ADR-010).
- **Goal:** a usable loop — drive the engine + renderer through real app state.
- **Builds:** Zustand store (+ `zundo`); `execute` pipeline (apply→evaluate→keep-prior-on-error→log); undo/redo; clear; error-step handling (keep prior figure on failure, surface message, cleared by next success); minimal UI shell (canvas, step list, controls); i18n wired (Hebrew default, RTL via `document.dir`).
- **Depends on:** Phases 1–2.
- **Requirements:** FR-HS-1, -2, -3; FR-EN-10; FR-I18N-1, -2; US-5, US-6, US-8.
- **Gate:** ✅ store integration tests (replay pipeline, stability, keep-prior-on-error, select/deselect/delete + dependent auto-drop, undo/redo incl. undo-a-deselect, clear, alternatives); i18n key-parity test; build clean. **Manual e2e:** the text input is disabled (parser is Phase 4); a "quick facts" row drives the same store pipeline so the loop is exercisable in the browser now.
- **Note:** the fact list is the source of truth; the figure (and per-fact status) is **derived by `replay`** in the view — positions are never stored, so undo can't desync coordinates from facts. Selection is excluded from temporal history (`partialize` + `equality` guard).

---

### Phase 4 — Grammar parser ✅ complete

- **Status:** done — `src/parser/parse.ts` (the `utterance → command[]` boundary) + `src/parser/__tests__/phase4.test.ts` (25/25 green); `App.tsx` text input is **live** and example chips run through the parser; typecheck/build clean. Milestone M3 reached.
- **Goal:** real natural-language input, free and offline.
- **Builds:** Hebrew/English grammar parser → commands; input affordances (clickable examples; "couldn't read that" hint on miss); replaced the hardcoded command lists. (LLM fallback is Phase 7.)
- **Depends on:** Phase 3 (commands flow into the store).
- **Requirements:** FR-IN-1, -3, -4, -5; FR-IN-2 (local path); US-1, US-7.
- **Gate:** ✅ parser table tests (He + En for square, point-on-segment, point-by-distances, free point, angle); negative cases return `not-handled` (the fallback boundary); parse→engine end-to-end; coverage measure on the in-grammar sample. Build clean.
- **Scope note:** the grammar covers the **engine's current vocabulary** only. Higher-level phrasings ("triangle ABC", circles, parallel/perpendicular, …) deliberately return `not-handled` and widen **in lock-step with the engine in Phase 5** — the grammar table grows as each construct lands. Genuine miss-rate against real inputs is measured once the corpus is automatable (post-Phase-5).
- **Risk:** R2 (parser coverage vs fallback rate / cost).

---

### Phase 5 — Full v1 coverage (corpus-driven)

Widen the engine and renderer (and, after Phase 4, the parser) to the construct vocabulary the real corpus demands. Derived from the current 4 questions:

| Construct | Appears in | In engine now |
|---|---|:--:|
| General quadrilateral (parallelogram, rhombus; → rectangle / square / trapezoid / kite) | Q1, Q2, Q3 | ✓ (5a) |
| Arbitrary segment between two points (diagonal, cevian) | all | ✓ (5a) |
| Point on a segment at a given ratio | Q1, Q3 | ✓ |
| Angle bisector (constructive line) | Q2, Q4 | ✓ (5b) |
| Line–line intersection point | Q2, Q4 | ✓ (5a points; 5b two `Line`s) |
| Point on an extension / ray (t ∉ [0,1]) | Q2, Q3 | ✓ (5b) |
| Perpendicular + foot / distance to a line | Q3, Q4 | ✓ (5b foot) |
| Right-triangle construction | Q4 | ✓ (5b) |
| Parallel-line construction | Q6 (+ quads) | ✓ (5c parser: line∥AB ∩ circle) |
| Circle (center + radius) | Q5, Q6, Q7 | ✓ (5c) |
| Point on a circle / inscribed vertex | Q5, Q6, Q7 | ✓ (5c) |
| Arc midpoint | Q5 | ✓ (5c) |
| Diameter (chord through the centre) | Q6, Q7 | ✓ (5c) |
| Line∩circle intersection | Q6 | ✓ (5c) |
| Tangent to a circle at a point | Q7 | ✓ (5c) |

Sub-phases — each ends by **reproducing its corpus questions** (gated per [Testing](08-testing-strategy.md)):

- **5a — Quads & segments:** ✅ **complete** — general quadrilateral (4 free vertices), parallelogram (A,B,C free + D derived = A+C−B), arbitrary segment (undirected, idempotent id), line–line intersection (parallel ⇒ unconstructible). Grammar + renderer widened in lock-step (`isGeoPoint` is the single source of truth for point kinds). **Q1 reproduced** from typed utterances in both locales (`src/engine/__tests__/phase5a.test.ts`); structural assertions (parallelogram valid, E on AC between, segments AC/BE/BD present). Full suite 83/83, build clean.
- **5b — Bisectors, extensions, perpendiculars:** ✅ **complete** ([ADR-019](06-decisions.md#adr-019)) — a first-class **`Line`** object (`through` / `bisector` / `perpendicular` / `parallel`, resolved to an `(anchor, dir)` pair in the same topological sweep as points; **not rendered** — scaffolding only), the points it yields (`line-intersection` of two lines, `foot` of a perpendicular, `midpoint`), point-on-extension (on-segment `t > 1`), and `right-triangle` (right angle at the last vertex; A,C free + B derived ⟂). Grammar widened in lock-step (bisector-meet, foot, midpoint, extension, right-triangle; a *single* bisector and the parallel/⟂ *constraints* stay deferred to 5d). **Q2, Q3, Q4 reproduced** from typed He/En utterances (`src/engine/__tests__/phase5b.test.ts`, 11) with structural assertions (bisector-meet equidistant/incenter, foot ⟂ + collinear, ratio on segment, right angle preserved under a leg move). Catalog flipped + a guard test that every supported example parses both locales. Full suite 159/159, build clean.
- **5c — Circles:** ✅ **complete** ([ADR-020](06-decisions.md#adr-020)) — a first-class **drawn `Circle`** (centre + radius, by length or through a point; resolved in the same interleaved sweep as lines/points), circle-dependent points (`on-circle`/inscribed with golden-angle auto-spread, `antipode`/diameter, `arc-midpoint`, `line-circle`) and the **tangent** line spec (⟂ to the radius). The renderer draws circles (outline, fit-aware). An inscribed polygon reuses the existing shape rules on `on-circle` points (composition, ADR-013). Grammar widened in lock-step (circle, inscribed triangle/quad, chord, diameter, arc-midpoint, point-on-circle, tangent∩line, bisector∩segment, parallel∩circle). **Q5, Q6, Q7 reproduced** from typed He/En utterances (`phase5c.corpus.test.ts`, 6) on top of unit tests (`phase5c.test.ts`, 8); circle render test added. Full suite 173/173, build clean.
- **5d — Constraint-driven DOF ([ADR-012](06-decisions.md)):** make constraints *shape* the figure, not just validate it — when a constraint references a point with a free DOF (on-object `t`, or a free point), solve that DOF deterministically (analytic / bounded 1-D); multiple solutions become alternatives; over-constraint still fires when nothing is free. Sequenced alongside the constructs that need it (e.g. angle/length driving a point-on-segment or free point). Today's behaviour (constraints = checks over fully-determined points) holds until this lands.

- **Depends on:** Phases 1–4 (engine + renderer; parser for the automatic path).
- **Requirements:** FR-EN-3, -4, -5 (full).
- **Gate:** every corpus figure reproduced (visual match up to free transforms / branch); solve-correctness on fixtures (incl. constraint-driven DOF: a referenced free parameter is solved to satisfy the constraint, with branches); stability holds.

---

### Phase 6 — Theorems

- **Goal:** surface relevant theorems as the figure is built — **structurally**, from what the student typed (ADR-038).
- **Builds:** **structural** matchers against the catalog ([07](07-theorem-reference.md)), one per theorem keyed on construct + parent-relationship (the [Pedagogy §4 trigger map](10-pedagogy.md#4-construction--theorem-triggers-the-pedagogical-payload) is the spec), **re-run over the whole accumulated figure each step** (a hypothesis can span facts; a later fact can complete it or change its tier); an **accumulating theorem feed** beside the canvas that surfaces the **diff** — newly-satisfied theorems and confidence/relevance changes — each entry attributed to its completing fact, deduped/updated-in-place, **confidence-tier ordered/coloured** (certain/possible/recall), traceable (highlights the satisfying objects), bilingual, citable by official number.
- **Depends on:** Phase 5 (needs the full construct vocabulary in the dependency graph). Does **not** depend on coordinate analysis — that is Phase 9.
- **Requirements:** FR-TH-1, -2, -3, -4, -5, -6; US-4.
- **Gate:** theorem-detection tests vs the catalog — expected **P/C** IDs surface with the correct **tier**; **O**-tagged items, definitions, and area/perimeter formulas **never** surface; each surfaced entry traces to its triggering fact.

---

### Phase 7 — API fallback + proxy + cost controls

- **Goal:** handle phrasings the grammar can't, safely and cheaply.
- **Builds:** server-side proxy (key custody, gate, per-IP rate limit); Haiku client; boundary escalation wired to the proxy; client bundle never holds the key. Console spend cap + prepaid credits configured.
- **Depends on:** Phase 4 (the boundary it plugs into).
- **Requirements:** FR-IN-2 (fallback path); NFR-SE-1, -2; NFR-CT-1, -2, -3.
- **Gate:** boundary dispatch test (parser-first; fallback mocked); **bundle check — built client contains no key**; calls target the proxy; optional manual live smoke (env-gated).
- **Why after Phase 5 (sequencing rationale, confirmed 2026-06-10):** the LLM can only emit commands the **engine** supports, so its value scales with the vocabulary — with the few constructs available pre-Phase-5 the grammar already covers most natural phrasings, and freeform input has little to map onto. Until then the **grammar is the primary path** (parser-first, ADR-002) and is kept *tight-but-forgiving*: it absorbs cheap **structural** variation (word/keyword order, spacing, synonyms) but is **not** hand-extended toward freeform phrasing — that long tail is precisely the LLM's job, not more regex.

---

### Phase 8 — Export, polish, deploy

- **Goal:** the author use-case and release readiness.
- **Builds:** image export (SVG/PNG) for authors; local persistence (survive reload); drag free/on-object points to explore; accessibility pass; deploy.
- **Depends on:** Phases 2–6.
- **Requirements:** FR-HS-4, -5; FR-RN-5; NFR-AC-1, -2; US-9, US-10, US-12.
- **Gate:** export produces a valid image of the figure; persistence survives reload; headline flows pass (Playwright); accessibility checks.

---

### Phase 9 — Reveal / figure unmasking (deferred)

- **Goal:** let a student, **on demand**, unmask what the figure geometrically *is* — equal segments, equal/right angles, measured lengths/angles — without ever being clued unbidden (Pedagogy §5.1).
- **Builds:** **geometric (coordinate) analysis** of the computed figure (the layer deferred from theorem detection in [ADR-038](06-decisions.md#adr-038)); a **Reveal** toggle that annotates the canvas with tick/arc/right-angle marks and measures; strictly opt-in and reversible.
- **Depends on:** Phase 5 (computed coordinates); independent of Phase 6 — theorems and Reveal ship separately, neither blocks the other.
- **Requirements:** FR-RV-1, -2, -3.
- **Gate:** nothing is annotated until Reveal is pressed; pressing it marks all equal segments/angles and measures the figure exhibits; toggling off clears them. No geometric fact is ever surfaced automatically.

---

### Phase 10 — Analytic geometry (coordinate mode) (deferred)

- **Goal:** an **opt-in coordinate-entry mode** for *analytic ("analytical") geometry*, where the coordinate system itself is the object of the exercise — the student places points/objects by explicit coordinates (`A = (3, 4)`) and works on a coordinate grid, rather than describing synthetic relations.
- **Builds:** a coordinate grid + axes with read-outs; first-class coordinate data-entry (the engine is already coordinate-native internally, and a single `point A at (x,y)` already parses — this promotes that from an incidental pin to a real mode); analytic primitives as scope allows (slopes, line equations, distance/midpoint read-outs). Kept deliberately apart from the natural-language synthetic-construction flow so everyday construction never requires coordinates.
- **Depends on:** Phase 5 (the coordinate-native engine); independent of Phases 6/9.
- **Requirements:** FR-IN-9.
- **Notes:** This is a separate product decision, not just a phase — it widens scope past the v1 non-goal of "not a general dynamic-geometry system." Synthetic-geometry construction must stay coordinate-free (students describe relations, not numbers); analytic mode is an additive, opt-in surface. Sequenced after the headline milestones (rework → Phase 6 → Phase 8) and the other parked threads.
- **Gate:** TBD — define when the capability is taken on.

## Milestones

| Milestone | Reached at | Meaning |
|---|---|---|
| M1 — Engine proven | end of Phase 1 | The constructive approach works; safe to build on. |
| M2 — Interactive build | end of Phase 3 | A figure can be built and seen, driven by app state. |
| M3 — Natural-language input | end of Phase 4 | Students can type facts in Hebrew/English. |
| M4 — Feature-complete v1 | end of Phase 6 | All v1 shapes/constraints/specials + theorems. |
| M5 — Deployable | end of Phase 8 | Export, persistence, proxy, polish — shippable. |

## Requirement → phase coverage

| Area | Phase(s) |
|---|---|
| Engine / construction (FR-EN-\*) | 1, 5 |
| Alternatives (FR-ALT-\*) | 1 |
| Rendering (FR-RN-\*) | 2 (RN-5 in 8) |
| History / session (FR-HS-1/2/3) | 3 · (HS-4/5 in 8) |
| Input & parsing (FR-IN-\*) | 4 (fallback path in 7) |
| Theorems (FR-TH-\*) | 6 |
| i18n (FR-I18N-\*) | 3 |
| Security / cost (NFR-SE/CT) | 7 |
| Accessibility (NFR-AC) | 8 |
| Stability (NFR-ST) | 1 (held to throughout) |

Every "Must" requirement lands by **M4**; the rest by **M5**.
