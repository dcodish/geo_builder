# 09b — Status Log (archived)

_Archived 2026-07-16 from the status blockquote block that had accumulated at the top of [09-implementation-plan.md](09-implementation-plan.md)._

**What this is.** For months the plan carried its running status as a stack of appended blockquote snapshots (`**Status:** …`, `**Then (date): …**`, `**CURRENT (date) — …**`). That stack grew to ~81 KB on a **single physical line** and could no longer be opened by tooling. It is preserved here **verbatim** so nothing is lost; the plan now carries a short live pointer instead. Going forward, per-session progress belongs in [PROJECT-MEMORY.md](PROJECT-MEMORY.md) (session log) + the ADR log ([06-decisions.md](06-decisions.md)) + [DEPLOY-LOG.md](DEPLOY-LOG.md) — **not** as blockquotes appended here or in the plan.

**Ordering.** The main narrative below is oldest→newest, frozen at **2026-07-08** (its last recorded session). The extra dated snapshots that follow are reproduced in the order they appeared in the file (which was *not* strictly chronological). The authoritative *current* status is the plan's Status pointer; the historical detail is fully covered by the ADR log + DEPLOY-LOG + PROJECT-MEMORY.

---

## Full status narrative (Phases 0–4 → 2026-07-08, oldest → newest)

**Status:** Phases 0–4 complete (M1–M3). **Phase 5a complete** (quad, parallelogram, segment, line∩line; **Q1 reproduced**). **Phase 5b complete** ([ADR-019](06-decisions.md#adr-019)) — a **`Line`** object (through / bisector / perpendicular / parallel → `(anchor,dir)`), the points it produces (line∩line of two lines, foot of ⟂, midpoint), point-on-extension, and **right-triangle**; **Q2, Q3, Q4 reproduced**. **Phase 5c complete** ([ADR-020](06-decisions.md#adr-020)) — a drawn **`Circle`** object (centre + radius), circle-dependent points (on-circle/inscribed, antipode/diameter, arc-midpoint, line∩circle) + the **tangent** line spec; the renderer now draws circles; **Q5, Q6, Q7 reproduced** from typed He/En utterances. **The full v1 construct vocabulary (Q1–Q7) is now covered.** **Phase 5d** ([ADR-021](06-decisions.md#adr-021)) adds the distance/equal/parallel/perpendicular constraints (generic mechanism, ADR-014); **ADR-018 Stage 1** (pinned-vs-free + seeded `resample`) is built. **Visible construction lines** + **two-circle intersection** ([ADR-022](06-decisions.md#adr-022)) — a tangent/bisector/perpendicular/parallel can now be *drawn*, and circle∩circle is first-class. **Phase 7 (LLM fallback)** built ([ADR-023](06-decisions.md#adr-023)). **Post-corpus relational/UX layers** (2026-06-13/14): **symbolic measures** ([ADR-031](06-decisions.md#adr-031)) — variables (`AB=3x`, `∠ABC=2α`), affine/fractional/√/power/π, ratios, on-figure measure labels; **triangle congruence/similarity** ([ADR-032](06-decisions.md#adr-032)); **shapes carry their true DOF** ([ADR-033](06-decisions.md#adr-033)) so a sizing constraint reshapes rather than over-constrains; **angle marks when stated** (FR-RN-7); **named median** `AD תיכון`; **rename a point** ([ADR-035](06-decisions.md#adr-035), `שנה שם E ל-G`) + the [letter-placement guide](12-letter-placement.md); **image export** (save/copy PNG, FR-HS-5); **a drawn line named by two points creates drivable `on-line` markers** ([ADR-036](06-decisions.md#adr-036)); **`AB ⟂/∥ CD` also draws its segments** (FR-IN-7); **label readability** (white halo + a global nudge-off-lines placement pass); **two circles tangent to each other** ([ADR-037](06-decisions.md#adr-037)).

**Then (2026-06-15, corpus-driven testing):** circle **radius as an honest DOF** (ADR-037 A2); **subscripted labels** `O1`→`O₁` (ASCII id, presentation-only `<tspan>`); **cyclic quadrilateral** `בר חסימה` (hidden circle, opposite angles 180°, FR-EN-14); **semicircle + quarter circle** (a drawn `Arc` primitive, FR-EN-15); a **debug session log** (`logs/debug-log.jsonl`); persistent **align-segment-horizontal** (FR-RN-6); diameter-on-a-cyclic-quad reshapes convexly + persistently; and **robust coupled-constraint solving** (degeneracy barrier + near-first/global-fallback) so a figure pinned by several givens — e.g. `AB=CB` AND `AC bisects ∠ECD` — builds convex without collapsing. **Then:** **inequalities between named measures** ([ADR-039](06-decisions.md#adr-039), FR-IN-8) — `α<β`/`x>y` *actively reshape* the figure so the relation holds visibly (a one-sided order constraint folded into every solver's joint minimisation, since an inequality owns no carrier).

**Then (2026-06-15, home PC):** the **deferred backlog is cleared** — auto-draw segments for distance/equal/angle givens (FR-IN-7 extension); an explicit **merge** command (fold two points, FR-HS-8); a free **on-line marker's offset is now sampled** by "show another configuration" (ADR-036 A2); **named-line markers widened** to perpendicular/parallel lines (ADR-036 A3); and **ADR-018 Stages 2 & 3** (constraints prune sampled DOFs + a live "degrees of freedom remaining" cue, FR-ALT-4) — ADR-018 is now fully built.

**Then (2026-06-16, manual-testing marathon — circle/tangent/secant corpus):** a string of constructs + fixes from real bagrut figures: a **secant from an external point** (FR-EN-17); **one & two tangents + multiple secants from a point** (FR-EN-18); **two circles intersecting at A,B** (FR-EN-19); the radius symbol **R auto-binds to the circle radius**; **"show another configuration" now resamples free DOFs AND cycles branches** (was branch-only) with **count-scaled on-circle variety**; the **DOF cue corrected** to `raw − constraints`; a **named perpendicular / perpendicular-bisector** (construct or constrain, both pre-existence arrangements); a loose segment **repositioned into a perpendicular cross** (ADR-036 A4); and **a circle's centre shows only when used or named** (FR-RN-8, incl. deterministic unnamed circles).

**Then (2026-06-17):** the **"no fixed assumptions" principle elevated to [ADR-052](06-decisions.md#adr-052)** (audit fixed a single-circle radius pinned at 5 + a midpoint-pinned on-segment point); **circle radius as a free solver DOF** ([ADR-051](06-decisions.md#adr-051)); and the **collinearity family** ([ADR-050](06-decisions.md#adr-050) + 3 amendments — ordered `line ABE`, second-intersection "E on line DB", two driven-solver fixes).

**Then (2026-06-18):** the **givens verifier** ([ADR-053](06-decisions.md#adr-053), `src/engine/verify.ts`) — **"green" now means VERIFIED, not just no-error** (amber cue otherwise); `tangentMeetsOtherCircle` + `lineMeetsCircle` + `פוגש`/`פגש`; and UX (scroll, responsive canvas, free-radius slider).

**Then (2026-06-18, home PC):** **directional `המשך` semantics** ([ADR-054](06-decisions.md#adr-054)) — `המשך AC`→D beyond C (order A→C→D); a new `extend-onto-circle` (driven on-circle θ + `collinear` + `collinear-order`, the figure adapting / free radius growing so the extension reaches); order-aware 1-DOF root pick so a root-at-seed isn't resized away; the bagrut corpus reworded to the strict form.

**Then (2026-06-18/20, manual-testing fixes):** a parse that builds nothing gets a SECOND try then an honest message ([ADR-055](06-decisions.md#adr-055), `dryRunOutcome` — now counting a pure RESHAPE like a diameter on a cyclic quad as "produced", so `AB קוטר` no longer false-escalates); and a **symmetric driven constraint nudges its DOF to the NEAREST root** ([ADR-056](06-decisions.md#adr-056)) so `OD⊥AC` keeps the quad convex instead of flinging D to the far arc-midpoint and crossing it; and a **circle tangent to two GIVEN sides** ([ADR-057](06-decisions.md#adr-057), `cornerTangentCircle`) — the inverse of "tangent from a point" (the sides are given, the circle constrained tangent), composed from primitives like the incircle (centre = a free point on the corner's bisector, radius via a circle through the foot on one side, the other tangency automatic by equidistance), so "AB ו-AD משיקים למעגל O" no longer escalates and builds nothing — and its [ADR-057](06-decisions.md#adr-057) A1 GROWS the circle (driving the centre's free DOF) when an existing vertex is then put on it ("C נמצאת על המעגל"), keeping tangency; A2 lets it auto-name a fresh circle when none is given. Plus a **√ in a segment ratio** ([ADR-058](06-decisions.md#adr-058)) — `EB/AE=√2/2` reads as the ratio √2/2 (was silently fragment-misparsed to a length `AE=√2`), with a shared ratio-LHS guard so any unreadable ratio RHS fails honestly. And an **extension-meet draws both lines to the meeting point** ([ADR-054](06-decisions.md#adr-054) A2) — "המשך CA ו-BD נפגשים בנקודה G" now draws C→G and B→G (the lines reach G) instead of stubs that stop at the inner points. And a **"show circle centres" view toggle** ([ADR-059](06-decisions.md#adr-059), ⊙ in the figure toolbar, default off) reveals every circle's centre with its label so two unnamed circles are tellable apart — a pure display preference (no engine/fact state). And **two points on one segment** ([ADR-060](06-decisions.md#adr-060)) — a free point-on-segment now seeds in the largest open gap (0.5 → 0.25 → 0.75 …) so a second "point on AC" doesn't collide with the first, plus a `pointsOnSegment` rule for "L ו-K נקודות על AC" (and a recorded `\w`-is-ASCII Hebrew-regex gotcha). And a **perpendicular that "cuts AB at E"** ([ADR-061](06-decisions.md#adr-061)) now creates the crossing E and draws the perpendicular segment EK (was dropping the cut clause and drawing only a bare line). And **coupled chained equalities** ([ADR-062](06-decisions.md#adr-062)) — `AL=LK=KC` now drives both free points jointly so they trisect AC (was erroring "unresolved dependencies" because the two equalities each referenced the other's driven point — a closed-form cycle now promoted to the numeric joint solver). And **spelled-out Greek names normalise to symbols** ([ADR-063](06-decisions.md#adr-063)) — "alpha"/"2alpha" now read as α/2α (the spelled form used to miss the variable regex and "2alpha" half-parsed to a 2° angle); guarded so uppercase point pairs ("MU") and longer words ("alphabet") are untouched. And the REAL α/2α bug — **only a FREE on-segment point is driveable** ([ADR-064](06-decisions.md#adr-064)): a `2α` relation was dragging a point the student fixed (D on the extension, t=1.3) and failing; now a stated-ratio/extension point is a given the solver leaves put, so the relation drives the figure's free shape instead. And **"show another configuration" reports when there's no different drawing** ([ADR-065](06-decisions.md#adr-065)) — `resample` compares candidates with a similarity-invariant shape fingerprint and, when the shape is determined (only size/rotation free), shows "No other configuration" instead of silently redrawing the same figure. And **"triangle circumscribes a circle" is the incircle, plus two-tangents-meet** ([ADR-066](06-decisions.md#adr-066)) — "משולש DEF חוסם את המעגל" now builds the incircle (was a wrong circumcircle), and "tangent at A and tangent at C meet at D" builds the two tangents + their crossing; with these in the catalog the LLM composes the full tangential triangle from "a tangent through each vertex … meeting at D E F".

**Then (2026-06-20):** **"AB = √2R" couples a length to a FREE radius** ([ADR-071](06-decisions.md#adr-071), a `length-radius` constraint) — three stacked defects on the operator's tangent figure (parser dropping the trailing R in `√2R`; lowering freezing R to the circle's default on a free-radius circle → seed-fragile over-constraint; the joint solver giving up) fixed by a first-class constraint that drives the radius DOF **and** the witness on-circle angle (the tangent caps the radius, so a moderate radius + the right θ satisfies it), making `BO=R` a tautology; the relation holds on all 9 seeds. **1091 tests green**, build clean, on `rebuild-foundation` (GitHub backup: `dcodish/geo_builder`). **Open threads (parked):** an *area* given ("area of △ = 0.25k²") is unsupported — a new constraint class; the production proxy deploy; the differential coordinate-validation campaign; **LLM-assisted solve for non-contradiction solve-failures** ([ADR-049](06-decisions.md#adr-049), Proposed — LLM supplies structure/seed, engine refines + verifies; depends on the ADR-045 solver consolidation + the proxy deploy); and **LLM reorder-and-repair of out-of-order input** ([ADR-073](06-decisions.md#adr-073), Proposed, FR-IN-10 — reading order ≠ construction order, so the LLM reorders a whole-problem batch + inserts implicit objects, validated by the givens verifier in a bounded repair loop; the student never plans the construction; reorder/insert only, never drop a given; offline Opus-oracle tests can precede the proxy deploy).

**Then (2026-06-21) the R7 binding remainder for the bagrut `∠ADB=<number>` case was CLOSED** ([ADR-074](06-decisions.md#adr-074)): it was a *binding* jam (greedy `driveOrCheck` claimed the apex while the extension point `G` was frozen), not the convergence failure first suspected — a `pointOnExtension` point now carries `extension:true` (recruitable, not eager) and the failure-path recruit "frees the blocker" (re-points `AG⊥AD` to `G`, releasing the apex for `∠ADB`), so the figure flexes to the stated angle while [ADR-064](06-decisions.md#adr-064) stays byte-identical (eager path untouched). **1105 tests green, build clean.** Still deferred: the up-front joint binding + real stay-put penalty (the larger R7 rework), and sampling the extension `t` on "show another configuration". **PRIORITY (operator directive, 2026-06-21): Phase 6 (theorems) is GATED behind a full, working, comprehensively-tested system — harden + test what exists first, no rush.** **Hardening program — Pillars 1–4 DONE (2026-06-21), 1183 green, build clean:** (1) the **givens verifier** ([ADR-053](06-decisions.md#adr-053)) is now COMPREHENSIVE — "green = verified" covers every distance/angle/equal/ratio/∥/⊥/collinear/concyclic relation, unit-proven to fire; (2) the **corpus + 56 scenarios + a 25-figure sweep all self-verify** → zero silent-wrong figures; (3) the **negative-space gate** ([ADR-047](06-decisions.md#adr-047) Tier B) rejects contradictions generatively across scales; (4a) **extension-`t` sampling** (ADR-052), (4b) a **generated additive-stability property**, (4c) **R7 binding fragility fixed at root** ([ADR-074](06-decisions.md#adr-074) Am.2 — equilateral / AAS false over-constraints, found by an evidence-first probe). **Deferred with reasons (not blockers):** the full R7(3) greedy-retirement + per-DOF stay-put penalty (no over-recruit-drift evidence found), and Tier-C `movedAncestors` generated constraint-stability (guarded by the fixed-seed snapshot). **Pillar 5 = production proxy DEPLOY only — a go-live step, not a local-correctness gate** (the LLM fallback already runs locally via the dev proxy, so the whole system is testable end-to-end now). **Phase 6 (theorems) is the milestone after the operator's own local validation pass — the automated gate is now substantially met.**

**Then (2026-06-21, manual-testing fix):** **"AB tangent at F" on an EXISTING line now flexes the circle** ([ADR-075](06-decisions.md#adr-075)) — it used to error `unresolved dependencies` because the parser re-created the existing vertices A,B as markers on a new tangent line (a dependency cycle), and there was no path reading "existing line tangent to circle" as a circle-flexing constraint; now, when the line's labels and the touch point all pre-exist, `tangentLine` emits a tangency constraint `set-perpendicular(O,F,A,B)` (radius ⟂ the existing line) and the circle adapts so OF⟂AB with F on the circle (verified clean).

**Then (same figure):** **"F, G, H on AB, AC, CB" (N points pairwise on N segments) now parses deterministically** ([ADR-076](06-decisions.md#adr-076)) — it used to escalate to the LLM (which built nothing on the "הישרים"/lines wording); a new `pointsOnSegments` rule maps N points one-to-one to N two-letter segments (He/En, "sides"/"lines"), so the whole figure builds without the LLM.

**Then (same figure):** **"the diameter from F cuts side AC at E" lands E ON the segment** ([ADR-077](06-decisions.md#adr-077)) — the "קוטר … cuts …" phrasing used to escalate to the LLM (which built nothing), and the manual line∩line workaround put E on the *continuation* of AC; a new `diameterCutsSegment` rule emits the diameter-line (F–O) ∩ line AC PLUS a `set-line [A,E,C]` order constraint, so the figure FLEXES a free DOF (the triangle reshapes, F moving with it) to keep E on the side. **Amendment:** a BARE "AC" (not just "הצלע AC") defaults to the segment — operator principle "a segment reference means the segment, not the continuation, unless you say `המשך`"; opt-outs "המשך AC" (E beyond, dir2) and "הישר AC" (infinite line).

**Then (2026-06-21, operator session `m26xv4m2`):** the **Q4 tangent/secant trapezoid now draws cleanly** ([ADR-078](06-decisions.md#adr-078)) — quad BKCD with KB∥CD, CD a chord, KB tangent at K, BD a secant cutting the circle at A, encoded via tangent–chord (K = arc-midpoint of CD) + B = (tangent at K)∩(line AD). The construction was always geometrically exact, but the clean textbook drawing (convex BKCD **and** A between B,D) was ~1-in-300 under the seeder while "show another configuration" searches only 24 seeds. Root cause: `sample.ts` set the on-circle jitter width from the **total count** of free on-circle points, so a chord (C,D) plus a lone secant point (A) read as "3 → an inscribed polygon" and was pinned tight (±30°) — the chord never shortened. Fix: scope the tight jitter to **genuine inscribed polygons** (a declared polygon with ≥3 free-on-circle vertices), with the legacy count fallback only when no polygon touches the circle; a chord/secant point now ranges wide (±153°), lifting the clean-config rate **~100× (≈0.3 %→30 %)** so the figure is reliably reachable via "show another configuration". Real inscribed triangles/quads untouched. **1205 tests green, build clean.**

**Then (same session, live-Haiku test — operator authorised one call):** the verbatim Q4 problem run through the LLM fallback decomposed in **reading order** (as predicted) and treated the circle as *given* — emitting "chord CD in circle O" / "tangent to circle O at K" / "BD cuts circle O at A" with **no step that creates the circle** — so centre O was undefined and the build collapsed with "unresolved dependencies". Fixed by **auto-materialising an IMPLICIT circle** ([ADR-079](06-decisions.md#adr-079)): the parser (`withImplicitCircles`) prepends a free circle (free centre + free radius) when a *consuming* step references a `circle-<centre>` neither in context nor defined by the same utterance; the injected circle is `ifAbsent` so the engine **skips it if the circle already exists** (never clobbers a real radius). Lives in the parser (interpreting natural input), so the engine stays strict — a referenced-but-*removed* circle still cascades honestly. The exact live decomposition now builds; the leftover "C not on circle" is the deeper trapezoid-first reorder/repair gap ([ADR-073](06-decisions.md#adr-073)), correctly flagged amber by the verifier. **1209 tests green, build clean.**

**Then (same session):** that leftover "C not on circle" was fixed at root ([ADR-080](06-decisions.md#adr-080)) — declaring an EXISTING free vertex (a quad corner) "on circle O" used to add **no constraint** when the circle's radius was free (the `point-on-circle` apply path fell through to the give-up case), so the vertex stayed adrift. Now a non-pinned free vertex **converts to an on-circle point** (it slides on the circle, "shapes carry their true DOF" — [ADR-033](06-decisions.md#adr-033)), so "C,D on circle O" makes CD a genuine chord (verified: C,D land equidistant from the centre, no violation); a **pinned** point (explicit placement) is guarded and not slid. This fixes the relation generically; it does NOT rescue the whole flawed live-Haiku decomposition (which applies KB∥CD before the chord and so over-constrains trapezoid-first — now failing *honestly* instead of drawing C off the circle; the ADR-073 reorder work). **1211 tests green, build clean.**

**Then (2026-06-22):** **"KB tangent at K" (a segment tangent at its OWN endpoint) now routes to a tangency relation** ([ADR-081](06-decisions.md#adr-081)) — it used to match the greedy `tangentFromExternal`, which read B as an external apex, **invented a new touch point T** (a Thales circle∩circle), drew B–T, and ignored K entirely (silently wrong: nothing asserted "KB tangent at K", so the verifier had nothing to flag). Fix: `tangentFromExternal` defers when the named "at" touch point already exists; `tangentLine` adds a case where the named segment's labels INCLUDE the touch T as an endpoint → emit `point-on-circle T` + `set-perpendicular(O,T,P,Q)` (T on the circle, radius ⟂ the segment), so the figure flexes to a real tangent (verified: K on circle, OK⟂KB). The tangent STEP now builds cleanly in the reading-order Q4 too (the build still stops at the separate KB∥CD trapezoid-first over-constraint — ADR-073). **1213 tests green, build clean.**

**Then (2026-06-22, work PC):** **"KB tangent to the circle" with NO explicit touch point now parses** ([ADR-082](06-decisions.md#adr-082)) — the operator's natural "KB משיק למעגל" (vs ADR-081's "KB tangent *at K*") was the form actually failing (`not-understood` in the log); `tangentLine` gated the touch point behind an explicit "at X" clause, so it bailed. Fix: infer the touch from the segment endpoint already on the circle (`ctx.circleMembers`, exactly one); the engine builds it perfectly once it has the command (K on circle, OK⟂KB, KB∥CD, verifier clean). Locked by the operator's exact sequence as an end-to-end scenario. **1214 tests green, build clean.**

**Then (same session): "one circle → no name"** ([ADR-083](06-decisions.md#adr-083)) — the operator's principle that a definite "the circle"/"המעגל" should resolve to the single circle when only one is present. The full-Q4 secant "BD חותך את המעגל בנקודה A" (unnamed) was `not-handled` because `lineMeetsCircle`/`extendOntoCircle` required a NAMED circle for both the centre and the crossing anchor. Fix: `resolveMentionedCircle` (single circle when the utterance mentions a circle at all, named or definite) + `crossingAfterCircle` (name-optional anchor), guarded so a circle-less line∩line is NOT grabbed. Full Q4 now completes deterministically. **1219 tests green, build clean.**

**Then (same session): a tangent's named external point appears immediately, then a later fact drives it** ([ADR-084](06-decisions.md#adr-084)) — "מנקודה D יוצא משיק למעגל בנקודה B" used to draw the tangent but DROP D, and the defining "המשך CA נפגש עם המשיק בנקודה D" was not-handled. Operator chose "D appears at step 2": `tangentLine` now creates the named apex D as a free `point-on-line` marker on the tangent, and a tight `extensionMeetsExistingPoint` rule reads the follow-up as `set-line [C,A,D]` (D already on the tangent → only "D on the CA extension" is new, driving its DOF to the crossing). No parse-context plumbing. Verified end-to-end (DB⟂OB, D on CA extension beyond A, verifier clean). **1220 tests green, build clean.**

**Then (same session): scalene default + lone-marker sign** ([ADR-085](06-decisions.md#adr-085)) — two symptoms of one ADR-052 flaw on the tangent figure: (a) the one-shot "tangent at B meets CA at D" ERRORED "lines parallel" because the inscribed triangle defaulted to ISOSCELES (golden-angle's two equal gaps put B at the arc-midpoint of AC → tangent@B ∥ CA); (b) D always landed on the same side of B. Fixes: a bounded alternating skew in `nextTheta` makes the default a GENERIC scalene triangle (tangent ∦ chord), and the sampler now flips a LONE on-line marker's sign (a ±pair keeps its signs). Fallout was 2 intended coordinate snapshots (`-u`) + the ADR-036 marker test (now asserts both-sides). **1220 tests green, build clean.**

**Then (same session): "C on the extension of DA" keeps the ORDER for an EXISTING point** ([ADR-086](06-decisions.md#adr-086)) — "C נמצאת על המשך DA" aligned D,A,C but put C BETWEEN D and A (near intersection), not beyond A. Root: `pointOnExtension` always emitted `point-on-segment … extension`, but C already existed as an on-circle vertex, so it stayed on the circle as a bare collinearity at the near root, losing the order. Fix: when the named point EXISTS, emit an ordered `set-line [D, A, C]` instead (drive the existing point, on its current carrier, beyond the far end in order). C now lands beyond A (D→A→C), still on the circle. **1223 tests green, build clean.**

**Then (same session): two operator feature requests** — **set a circle's radius by value** ([ADR-087](06-decisions.md#adr-087)): a `set-radius {circle,value}` command + `setRadius` rule ("רדיוס מעגל P הוא 4" / "radius of circle P is 4") sizes an EXISTING circle with NO segment drawn and no point invented (a through-radius incircle flexes the figure; a free/length radius is set); and **hide a circle** ([ADR-088](06-decisions.md#adr-088)): a `hiddenCircles` display set + `toggleCircleHidden`, with circles now clickable → the on-canvas menu → hide/show (mirrors segment hiding; engine untouched). **1228 tests green, build clean.**

**Then (same session): typo escalation gate** ([ADR-089](06-decisions.md#adr-089)) — "מנוקדה D …" (typo of מנקודה) made the tangent rule match partially and silently DROP the "from D" apex, committing a partial figure instead of escalating. New `droppedNewLabels` gate in `App.submit`: a successful parse that leaves a NEW input label unreferenced is treated as weak → escalate to the LLM (typos are the LLM's job, not the grammar's). Zero false positives across the corpus (an existing label a command doesn't re-name is NOT flagged). **1232 tests green, build clean.**

**Then (same session): a circle DEFINED BY its diameter** ([ADR-090](06-decisions.md#adr-090)) — the operator tried 5+ ways to make a circle with AB as diameter, all failed (define phrasings not-handled; "AB קוטר של מעגל O" misrouted to `diameter` and errored on existing A,B). New `circleOnDiameter` rule for the define phrasings (of/with/whose-diameter, He+En) → segment + midpoint(centre) + circle-through, so the centre is the midpoint of AB; the "diameter X IN circle O" add-phrasing still routes to `diameter`. **1235 tests green, build clean.**

**Then (same session): midpoint endpoints + circle-by-centre/radius diameter** ([ADR-091](06-decisions.md#adr-091)) — "B אמצע הקטע AC" on an empty figure errored ("unresolved deps") because `midpoint` didn't create A,C; now it prepends a segment when an endpoint is new. And "AB קוטר במעגל שמרכזו O ורדיוסו R" misrouted to `diameter` ("'B' already defined"); circleOnDiameter's define-markers now include the centre/radius spec (שמרכזו/ורדיוסו/centered/radius) so it's recognized as defining a circle. Both were standard constructs that wrongly fell to the LLM (which built nothing). **1235 tests green, build clean.**

**Then (same session): a GIVEN diameter defines the circle** ([ADR-092](06-decisions.md#adr-092)) — "AB קוטר במעגל O" (and bare "AB קוטר") with A,B already placed still errored ("'B' already defined") / was not-handled. circleOnDiameter now fires for a given diameter (both endpoints exist + no existing circle to attach to), not just for define-marker phrasings; the cyclic add-a-diameter and new-points "diameter DE in circle O" paths stay with `diameter`. **1237 tests green, build clean.**

**Then (same session): tangent at a diameter endpoint** ([ADR-093](06-decisions.md#adr-093)) — "EA משיק למעגל בנקודה A" where A is the diameter circle's through-point ERRORED "unresolved dependencies" (a dependency cycle): the tangent emitted `point-on-circle A`, but A defines the circle's radius, and `pointOnCircle` didn't count a through-point as on the circle, so A was converted to on-circle → A↔circle-O cycle. Fix: `pointOnCircle` now recognises the through-point as on its circle → idempotent no-op, only the tangency constraint (OA⟂EA) is added. **1239 tests green, build clean.**

**Then (same session): two tangents from E where a touch point already exists** ([ADR-094](06-decisions.md#adr-094)) — "מנקודה E יוצאים משיקים למעגל בנקודות A ו D" with A an existing diameter endpoint ERRORED "'A' is already defined" (the Thales circle∩circle re-creates A). Fix: when either touch exists, fall back to the tangency-constraint form (point-on-circle idempotent + set-perpendicular(O,P,E,P) per touch); all-new keeps the Thales construction. **Verified through an `App.submit`-faithful harness** (parse → gates → execute), per operator feedback that parse→replay alone doesn't prove the app commits. **1240 tests green, build clean.**

**Then (same session): a constraint on a secant crossing can drive UPSTREAM free DOFs** ([ADR-095](06-decisions.md#adr-095)) — "∠CAE = 45" on a tangent/secant figure falsely reported "over-constrained" though a scan proved 45° reachable (∠CAE ranges ~2–143° over the free apex B / radius). Root: the ancestor walker had no case for `line-circle`/`circle-circle` points (pointParents → default []), so `recruitFreeDofs` dead-ended (cand=[]) and never recruited B. Fix: traverse a secant crossing (`line-circle`) through its line to the apex; `circle-circle` deliberately NOT traversed (over-recruits, broke the ADR-093 collinear). The angle now solves to 45° with the other givens intact. **1242 tests green, build clean.**

**Then (2026-06-24) the parked "differential coordinate-validation campaign" is BUILT** ([ADR-109](06-decisions.md#adr-109), `src/validation/`) — a differential check (vs the invariants campaign + givens verifier): seeded recipes pin their base points so the frame is fixed, an independent closed-form oracle (imports nothing from the engine) recomputes the derived coordinates, and they're diffed directly. Dev/CI-only, applies only to 0-shape-DOF figures (~11% of real logged figures); zero engine changes. **Finding: engine matches the oracle to ~machine epsilon (worst residual 5.6e-15 over a 13,760-figure sweep). 1286 tests green, build clean.** Remaining parked: area-given constraint class, production proxy deploy, [ADR-049](06-decisions.md#adr-049); coordinate-campaign next slices (Procrustes/global-solve mode, set-angle-driven, circle-via-intersection).

**Then (2026-06-24): theorem-vocabulary gap closed** — an operator audit of the bagrut theorem list vs. the construct vocabulary ("is there no דלתון?") found the missing nameable shapes and the n-gon gap. **Named shapes as parser MACROS** ([ADR-110](06-decisions.md#adr-110)) — **kite·דלתון** (also עפיפון), **isosceles·שווה שוקיים**, **equilateral·שווה צלעות**, **isosceles trapezoid·טרפז שווה שוקיים**, **triangle midsegment·קטע אמצעים** — each decomposes to already-supported commands (`quadrilateral`/`triangle`/`trapezoid` + equal-side/midpoint constraints), so the constraint solver does the work with **no new engine construct**; catalog entries auto-feed the LLM prompt, the i18n term corrected `עפיפון`→`דלתון`. **Regular polygon** ([ADR-111](06-decisions.md#adr-111)) — a generic `{type:'polygon'; ids:Id[]}` command + `polyEdges` + a `regularPolygon` rule placing n equally-spaced vertices on a hidden free-radius circle (He `מחומש/משושה/…`, En `pentagon…dodecagon`, `regular N-gon`); n=3→equilateral, n=4→square, n≥5→generic; irregular n>4 stays on the LLM segment-loop net. **DOF-cue correctness fix** ([ADR-112](06-decisions.md#adr-112), amends ADR-101) — a pinned parametric (on-circle/on-segment) vertex contributes **0** DOF, so the shape-DOF ladder reads exactly: **free quad 4 → cyclic quad 3 → inscribed square 0 → regular polygon 0** (fixes a pre-existing over-report on all inscribed shapes; "✓ fully determined" now fires). New `engine/__tests__/named-shapes.test.ts` + scenarios `kite-named-shape`/`regular-hexagon`. **1322 tests green, build clean.**

**Then (2026-06-24, work PC): a constraint now flexes a shape DOF reached *through* a parametric point** ([ADR-113](06-decisions.md#adr-113)) — operator-reported `GE⊥AB` (rhombus, `E` mid `AB`, `G` on the extension of diagonal `BD`) failed "over-constrained: GE ⟂ AB cannot hold". The figure is constructible only if the rhombus angle flexes below 60° so `G` lands strictly beyond `D` (at 60° the unique solution is the degenerate `G=D`); but the drivable-ancestor walk **stopped at the free param carrier `G`** and never reached the rhombus angle (carried by `G`'s parent `D`), so the recruiter could only slide `G` onto `D`. A `git bisect` proved this never worked (back to 2026-06-17 the same figure failed with "D and G would be at the same point"). Fix: in `drivable` mode (the failure-path recruiter only) record a free on-segment carrier **and keep walking past it** to the shape DOFs behind its segment; the regulariser keeps surfaced-but-unneeded parents at seed. General fix for the class (any constraint that must flex an upstream shape DOF reached through a parametric point). Locked by scenario `perpendicular-from-midpoint-flexes-rhombus`. **1323 tests green, build clean.**

**Then (2026-06-24): "isosceles" picks a SOFT default equal-pair that yields to an explicit one** ([ADR-114](06-decisions.md#adr-114)) — operator-reported: `משולש שווה שוקיים ABC` hard-coded `|AB|=|AC|` ("apex = first vertex"), so the student's own `AB=BC` stacked into an **equilateral** triangle they never asked for. "Isosceles" only asserts SOME two sides equal — which pair is the student's to state (ADR-052). Fix: the macro tags its default equality `soft`; `replay` drops a soft equality when an explicit (non-soft) `set-equal` on the same triangle is present, so the stated pair wins (lone isosceles still draws `|AB|=|AC|`). Scoped to the isosceles triangle; equilateral/kite/iso-trapezoid stay hard. Locked by scenario `isosceles-explicit-pair-overrides-default`. **1324 tests green, build clean.**

**Then (ADR-115–118 — full detail in the ADR log + CLAUDE.md):** tangency-vs-an-existing-circle becomes a CONSTRAINT (never a re-built circle); the arc-measure term `קשת`/arc + implicit-circle tangent; an equilateral/isosceles qualifier on an inscribed triangle carries its equal sides; and **AREA as a first-class measure & constraint** (given / ratio / labelled).

**Then ([ADR-119](06-decisions.md#adr-119), 2026-06-25, work PC): a chord's endpoints lie ON the circle in ANY phrasing.** Operator session `sflkyd0r`: after `AB קוטר במעגל`, `CD ו AF מיתרים המקבילים זה לזה` parsed to two PLAIN segments + `set-parallel` and DROPPED the on-circle membership (C,D,A,F became free points). Root cause: `parse` is first-match-wins, `parallelConstraint` (plain segments only) runs BEFORE the `chord` rule and claims the utterance; and `chord` itself handles only one chord. Fix: a centralised post-pass `withChordMembership` asserts `point-on-circle` for every SEGMENT endpoint in a chord-flavoured utterance with a resolvable circle (idempotent; a circle centre is excluded so "radius OE" keeps O off; a chord's midpoint isn't a segment endpoint) — general across ∥/⟂ chords. Locked by scenario `parallel-chords-keep-circle-membership`. _Known separate gap surfaced (operator resolved NON-actionable — students spell equality out as `AB=BC`): the WORD-form equality "X ו Y שווים"/"equal chords" isn't parsed (only symbolic `AB=CD` via `equalSegments`)._ **1345 tests green, build clean.**

**Then ([ADR-120](06-decisions.md#adr-120), 2026-06-25, work PC): the `△` triangle glyph is a parser keyword + toolbar button.** Operator: "do we support congruent/similar triangles? … we're missing the triangle symbol." Congruence (`חופף`/`≅`) and similarity (`דומה`/`~`) were ALREADY supported (ADR-032; `משולש ABC חופף למשולש GHT` builds both + makes GHT congruent to ABC by SSS — verified). Added: `△`/`▲` to the symbol palette + the base `triangle` rule keyword (so `△ABC` builds a triangle, like `∠ABC` states an angle) + `twoTriangles` strips it. Locked by scenario `congruent-triangles-word-form` + catalog-coverage (examples updated to the `△ABC ≅ △DEF` form). **1346 tests green, build clean.**

**Then ([ADR-121](06-decisions.md#adr-121), 2026-06-25, work PC): area subscript notation `S_{ABC}` / `S_ABC` + an `S_{}` toolbar button.** Operator: "how would a student enter areas? can we have an `S_{}` format?". Area (ADR-118) only read the glued `SABC`; the textbook `S_{ABC}` didn't parse. Fix: a `normalizeAreaSubscript` pass in `parse` rewrites `S_{ABC}`/`S_ABC` → `SABC` (scoped to `S_` + 3–4 uppercase labels, so every area path inherits it and nothing else is touched), plus an `S_{}` palette button (`insertSymbol` gained a caret offset so the caret lands between the braces). Glued + verbose forms unchanged. Locked by `area-subscript.test.ts` + catalog-coverage. **1355 tests green, build clean.**

**Then ([ADR-122](06-decisions.md#adr-122), 2026-06-25, work PC): swap two labels (A ↔ B) + a token-aware utterance relabel.** Operator (chord figure): wanted C and D on opposite sides; "show another" can't (a relabeling isn't a different shape) and rename C→D errored "target-taken"; the temp-label workaround corrupted the display (`CC1`→`DD1`, a substring `split/join` relabel bug). Fix: (a) a first-class `swap(a,b)` store action (exchange two labels everywhere, one undo; positions visibly switch); (b) `relabelUtterance` token-aware (`from+'(?!\\d)'`) replaces the substring relabel in rename/merge so `C1` is never corrupted; (c) a "Swap endpoints" action on the **segment** menu (`SceneSegment` now carries endpoint ids) + a typed `parseSwap` ("swap C and D" / "החלף בין C ל-D"); `swap` removed from `parseRename` (it used to route there → the error). Locked by `swap.test.ts` + `swap-parse.test.ts`. **1371 tests green, build clean.**

**Then (2026-06-25, work PC): area-ratio on a kite ([ADR-121](06-decisions.md#adr-121) Am. + [ADR-123](06-decisions.md#adr-123)).** Operator session `id4dn4a2`: `S_{ACD}=4S_{NCE}` didn't parse (a coefficient glued to the marker broke the `\b`-before-`S` in the ref regex AND `normalizeAreaSubscript`; fixed with `(?<![A-Za-z])S`), and `שטח NCE = רבע שטח ACD` errored "O and N would be at the same point." **Diagnosis:** △NCE~△ACD is structural (right kite ⇒ ∠ADC=90°=∠NEC, shared ∠C), so the area ratio ¼ ⟺ CN/CA=½; and AC is a diameter (Thales) so O is its midpoint ⇒ CN/CA=½ places N EXACTLY on O. The solver was fine (0.3/0.4/0.6/0.7 all converge) — only the coincidence guard hard-failed at the ½ (N=O) value. **Fix (ADR-123, operator rule):** a FORCED coincidence is ALLOWED with a notice (`evaluateCore` collects `coincidences`, store/UI surface "X and Y converge"); default collisions stay avoided upstream (sampler + `chooseComposition` prefers a coincidence-free placement, errors only when both sides stack). The earlier abandoned recruiter/similarity-reduction work proved unnecessary — the guard was the only blocker. Locked by scenario `area-ratio-converges-points-allowed`.

**Then (same session) — the freeze (ADR-123 Am.):** allowing the coincidence unmasked an inconsistency — `meetsRequirements` still required `pointsDistinct`, which N=O fails, so `autoResolve`→`findValidConfig` looped ~136 replays (~5 s each) hunting a distinct-points config that can't exist → hang. Fix: `pointsDistinct(c,pos,allowed)` skips the figure's `coincidences`; `meetsRequirements`/`resample` pass `fig.coincidences` → meetsRequirements true → no loop. Locked by `coincidence.test.ts`. _Perf follow-up: a single `replay` of this coupled figure is ~1.5 s (area-ratio over-recruits incl. gauge DOFs); bounded/usable, a leaner recruitment would speed it up._ **1378 tests green, build clean.**

**Then (2026-06-25, work PC) — [ADR-124](06-decisions.md#adr-124): `extend-onto-circle` onto an EXISTING target point is a CONSTRAINT, never a silent no-op.** Operator "check why C-A-D was not respected" (session `v7veg7sc`): `המשך CA חותך מעגל P ב D` did **nothing** because D already existed (from the earlier "chord AD") — `addObj` keeps the first definition and the shared-endpoint extend path pushes no constraints, so the directional C→A→D coupling was dropped (replayed collinear residual 0.38). Fix: an existing extend target is now a **constraint** (collinear + beyond-the-2nd-letter order), driving a free on-circle D's θ to the far crossing; locked by an `extend-onto-circle.test.ts` unit test (buildable variant: collinear residual 0, D beyond A). The engine change was **committed in `8b098fa` by a concurrent session, verbatim** — revalidated intact (build clean, full suite green). _Surfaced a deeper truth (proved by tangent-chord algebra + a 15k-sample engine-free sweep): the operator's exact figure is **over-determined** — "AD tangent to O at A" + "CB tangent to P at B" make C-A-D collinear impossible non-degenerately, so the step now reports `over-constrained` honestly instead of hiding it._ **PARKED:** the end-to-end scenario for the operator's *exact* sequence is held pending their review of the bagrut source (is "AD tangent" a *given* or the *conclusion*?).

**Then (2026-06-28, session `vk346px4`):** ([ADR-143](06-decisions.md#adr-143)) **a CHORD of one circle tangent to ANOTHER** (`המיתר AD במעגל P משיק למעגל O בנקודה A`) is now a tangent-chord construct — a new `tangentChord` rule (host circle before the tangent keyword, tangency circle + touch point after) creates D on the host circle, draws the chord, and makes radius⟂chord (tangent at the shared point); previously it fell to `circlesTangent` and silently **dropped D + the chord** while asserting a contradictory mutual tangency (green ✓, nothing drawn). A `chord`/`מיתר` guard on `circlesTangent` closes the class. Locked by scenario `chord-tangent-to-other-circle-at-endpoint`. **1445 tests green, build clean.**

**Then (2026-06-28, session `23vqi9u8`):** ([ADR-144](06-decisions.md#adr-144)) **two TANGENT circles carry FREE radii, tangency is a constraint over them** (`|OP| = r1±r2`) — the operator built "שני מעגלים משיקים מבחוץ" then got **"over-constrained: |OP| = 8 cannot hold"** on their own `OP=4`, because the unnamed phrasing escalated to the LLM, which pinned the radii at 5/3 (ADR-052 violation: |OP| rigid at r1+r2=8, M rigid at |OM|=5). Fix: `circlesTangent` materialises the circles with **free** radii (distinct seeds, `ifAbsent` keeps a *stated* radius) + handles the unnamed plural + an auto-named touch point; the engine builds external tangency as `coincide(M, hidden-witness)` with M=`radial-toward(c1→c2)` and witness=`radial-toward(c2→c1)` (residual `||OP|−(r1+r2)|`), both free radii as **permanent drivers**, so `OP=4` resizes the radii (`r1+r2=4`) and `OM=4` sets `r1=4` — no over-constraint; the recruiter reaches the radii via `radial-toward` ancestry (`circlesOfPoint`/`pointParents`); the verifier checks `|OP|=r1±r2`. Stated radii still use the unchanged fixed path. **Am.1 (renderer):** the figure still DREW wrong (circle at its SEED size, touch point floating inside) — `scene.ts`'s `pointOnCircleId` (recovers a free circle's drawn radius from a point on it) didn't list `radial-toward`, the only on-circle kind for tangent circles; engine positions were always correct, so only a SCENE-level test catches it. Locked by scenario `two-tangent-circles-then-size-given-flexes-radii` + a scene test. **1448 tests green, build clean.**

**Then (2026-06-28):** ([ADR-146](06-decisions.md#adr-146)) **production usage analytics + a password-protected admin dashboard** — production recorded almost nothing about user activity (the SPA's actions never hit the server; the rich `logDebug` session log was dev-only). Added a lean prod event channel through the existing client chokepoint (`src/debug/sessionLog.ts`, no `App.tsx` change), a server sink that **hashes the visitor IP** and appends `events.jsonl` (`server/eventLog.ts`), and a Hebrew-RTL dashboard at `/geo-builder/admin` (`server/admin.ts`, isbot-style username/password login over a stateless signed cookie) aggregating visitors / sessions / daily traffic / parse-outcomes / language split / top utterances. Shared `server/http.ts` (clientIp / rateLimit / readBody). 28 server tests; **1472 tests green, build clean.** **Remaining: deploy** — add `IP_HASH_SALT`/`ADMIN_*` to the root-only `geo-proxy.env`, push `dist/`+`proxy.mjs`, add the two Apache proxy rules (`deploy/README.md`).

**Then (2026-06-29) first-real-traffic parser batches** ([ADR-147](06-decisions.md#adr-147)/[ADR-148](06-decisions.md#adr-148), phrase/keyword coverage from the production usage log) and a **named-altitude fix** ([ADR-149](06-decisions.md#adr-149)) — "CD גובה" (a NAMED altitude segment, C=apex/vertex, D=foot) now keeps its foot D instead of auto-renaming it CF: the `altitude` rule reads a leading apex+foot name and honours the foot, scoped to the height keyword only so "EF אנך ל AB" stays the ⟂ constraint. Then a **named-derived-point audit** ([ADR-150](06-decisions.md#adr-150)) of the whole bug class (every `freeLabel` call site): fixed the **midsegment** sibling ("PQ קטע אמצעים …" now keeps endpoints P,Q, was renaming to M,N) and completed the altitude **keyword-first** order ("הגובה CD" / "the altitude CD"); a shared `isUpperLabel` gate stops a lowercase connector ("to"/"in") being misread as labels.

**Then ([ADR-149](06-decisions.md#adr-149) Am., the SECOND root cause found by a live retest):** the operator's actual utterance was the bare **"CD גובה"** (triangle already drawn) which STILL escalated → foot F, because the `altitude` rule required the opposite side to be STATED and had no figure-context fallback (unlike `median`); now it takes `ctx` and derives the side from the existing triangle, so "CD גובה" parses deterministically to foot D (no LLM). Locked by `phase4.test.ts` + scenarios `named-altitude-keeps-its-foot-name` (rewritten to the exact bare sequence) / `named-altitude-keyword-first-keeps-foot` / `named-midsegment-keeps-its-endpoint-names`. **1533 tests green, build clean.**

**Then (2026-06-29):** ([ADR-151](06-decisions.md#adr-151)) **the incircle marks all THREE tangency points, not just the radius foot** — operator (screenshot): an incircle on an existing triangle ABC drew only one tangency mark while it visibly touches all three sides. The general `incircle` branch built a single ⟂ foot `F` (only because `circle-through` needs one point for the radius); now it materialises a foot on each side (`F` on AB = the through-point, `G` on BC, `H` on CA), the latter two landing on the circle automatically (the incentre is equidistant). No new engine construct. Locked by scenario `incircle-has-three-tangency-points`. **1550 tests green, build clean.**

**Then (2026-06-30):** ([ADR-161](06-decisions.md#adr-161)) **shape-detection badges (FR-SH)** — an on-press "detect shapes" control classifies every polygon/circle into a named shape (kite/rhombus/isosceles-triangle/…), reported only when **forced across sampled configs** (emergent kites caught, coincidental ones not), each shown as a badge whose popup **links to that shape's page in the geometry book** (`themathbible.com/.../571/geometry/<slug>`). Purely additive (new `src/engine/detectShapes.ts` + `src/shapes/shapeCatalog.ts`, a `shapes` store slice mirroring `relations`, a button/badges/popup in `App.tsx`, a `shapes` i18n group; no existing engine/parser/render path touched). Locked by `detectShapes.test.ts` + `shapes-slice.test.ts`. **1645 tests green, build clean.**

**Then (2026-06-30, operator follow-up):** ([ADR-162](06-decisions.md#adr-162)) **detect IMPLICIT geometry** — the operator hit the real root cause: detection considered only DECLARED objects, so a parallelogram formed BETWEEN segments (no polygon object) and the equal radii `OA`/`OB` of a diameter (halves of `AB`, never drawn as segments) were invisible. Fix: a shared implicit-edge universe `figureEdges` (drawn segments + polygon edges + on-host splits + visible-line edges — the same set the equal-ANGLE pass already used) now feeds BOTH the equal-SEGMENT pass and a new emergent triangle/quad cycle detector in `detectShapes` (conservative: named special types only, simple in every sample). Changes existing "view relations" behaviour (more implicit equalities surface — the operator's ask). Locked by `relations.test.ts` (OA=OB), `detectShapes.test.ts` (emergent parallelogram), scenario `emergent-parallelogram-between-segments`. **1653 tests green, build clean.**

**Then (2026-06-30):** ([ADR-163](06-decisions.md#adr-163)) a **"right triangle ABC"'s right-angle vertex is an unstated default that yields to an explicit angle** — `ABC משולש ישר זוית` then `זווית ABC = 90` was refused "over-constrained" because the right angle is pinned structurally at the LAST vertex C; a `replay` pre-scan now reorders the triangle ids so an explicitly-90° vertex becomes the structural right-angle vertex (same shape as the ADR-114 soft default). ([ADR-164](06-decisions.md#adr-164)) a **single-vertex angle `∠B = 90`** resolves its arms from the figure when the vertex has exactly two edges (one possible angle), else returns a new `ambiguous-angle` clarification asking for three letters (a third `ParseResult` variant, NOT an LLM escalation).

**Then (same day, operator follow-up on the fix):** ([ADR-163](06-decisions.md#adr-163) Am.) the reseat also moves the right-angle **MARK** (the knee was drawn at the un-reseated last vertex C → "weird image" of two knees; now a single knee at the actual right-angle vertex). ([ADR-165](06-decisions.md#adr-165)) a **constraint that morphs a named shape** (`טרפז ישר זווית ABCD` + `∠ABC = 90` → a rectangle) is **allowed but flagged amber** — the givens verifier raises `figure.v.trapezoidMorph` when a declared trapezoid's both opposite-side pairs become parallel (operator's allow-but-flag choice; ADR-157 only guards re-declaring a shape WORD).

**Then (2026-06-30, bagrut Q9):** ([ADR-166](06-decisions.md#adr-166)) **two SEGMENTS that meet must meet ON the segments, not the continuation** — `AE ו BF נפגשים בנקודה G` (rectangle + equilateral triangles on the sides) built clean but MIRRORED: the apexes sampled OUTWARD so the segments diverge, and `line-line-intersection` placed G at the infinite-line crossing on the backward extension (param ≈ −1). Fix: the parser flags a plain meet `onSeg` (no `המשך`/`הישר`) → the verifier (`figure.v.meetOnSegment`, amber) + `meetsRequirements` require the crossing within both segments; AND the apex side — an unstated discrete DOF (ADR-052) the continuous sampler hit only ~2% of seeds — becomes an explorable **reflection DOF** encoded in the seed's high bits (`applyReflections`/`reflectableFreePoints`/`withReflectMask`), so `firstSatisfyingSeed`/`findValidConfig` mirror the apexes inward and bring the crossings onto the segments. **Am. (operator follow-up):** the emergent `EGFH` rhombus is now DETECTED — `onHostEdges` splits an `onSeg` crossing's operand segments (G↔A,E,B,F, the ADR-162 node-definition class for a new node kind), `viewRelations`/`detectShapes` sample the requirement-satisfying config (`firstSatisfyingSeed`, not seed 0), and a shared `convergedSamples` filter drops numerically-diverged solves (~2/16 seeds blow up on the equilateral apex) that had masked the forced shape. Locked by scenario `segment-meet-lands-on-segments`. **1663 tests green, build clean.**

**Then (2026-07-01):** ([ADR-167](06-decisions.md#adr-167)) **the implicit-edge universe is GEOMETRIC, not a whitelist of point kinds** — a deep-review of "emergent shapes/lines that are shown on the diagram but never declared keep going undetected." `figureEdges` was `pointNeighbors + onHostEdges + visibleLineEdges`, with `onHostEdges` a hand-maintained list of point KINDS allowed to split their host (on-segment/midpoint/foot, and — added by ADR-166 — `onSeg` intersection); each addition was "the node-definition issue, again." Root fix: `onHostEdges` deleted, replaced by `collinearSplits` — any point COLLINEAR on a drawn segment/polygon-edge in every sample AND within its span in some valid config splits it, **regardless of construction kind** (an intersection, foot, midpoint, `set-line` collinear, or purely emergent coincidence all split the carrier they land on; sub-segments like `G–C` become first-class edges). The topological edge set stays generous; the "forced in every sample" classifier still gates real shapes — so a first attempt requiring within-span in EVERY sample regressed the ADR-166 rhombus (the sampler visits mirror configs where a crossing sits past an endpoint) and the collinear-in-every / within-in-some criterion restores it. Engine-only per the operator's steer (renderer/build untouched); the sub-segment **highlight** (App resolves shape edges to stored `segment` objects only; `Figure` lights by object id) is a separate renderer concern parked as a follow-up. Locked by `collinear-splits.test.ts` + scenario `emergent-shapes-through-crossings`. **1667 tests green, build clean.**

**Then (renderer, same session):** ([ADR-167](06-decisions.md#adr-167) Am.) the operator opted into the **sub-segment highlight** (a shape's boundary is highlighted as point-PAIRS via `Figure.highlightEdges`, so a portion of a longer segment like `G–C` ⊂ `EC` lights up; `transform.toWorld` added) and raised the mark-clutter problem → **hover-to-focus** for "view relations": the resting figure is clean and hovering a side/angle ON THE CANVAS reveals only its equality class (`relationAt`/`relationsForPick`). Plus a double-label fix — `relationMarks` keys a definite-angle value by its WEDGE not point IDs, so a corner forced-60° under two names (`∠AFD`==`∠GFH`) draws "60°" once. **1672 tests green, build clean.**

**Then (2026-07-01, admin dashboard):** ([ADR-168](06-decisions.md#adr-168)) **one PRODUCTION `submit` per user submission** — the operator saw a construction they'd entered appear as TWO rows (`נותח (LLM)` + `חלקי / נשמט`, same timestamp) in the usage dashboard. Root cause: the grammar's `weak` (partial/built-nothing) diagnostic step and the LLM's FINAL outcome are BOTH `kind:'input'`, so `sessionLog`'s PROD branch emitted an analytics `submit` for both — double-counting the utterance and painting a phantom weak row beside the real one. Fix: the two `weak` `logDebug` calls are tagged `intermediate:true`; a pure `analyticsSubmit(event)` drops non-`input` kinds AND intermediate steps (DEV debug trace unchanged — it keeps the escalation detail). Root fix at emission, not a dashboard dedup, so `events.jsonl` only ever holds real submissions; historical pre-fix rows keep their duplicates (use the `rel=` release filter). Locked by `src/debug/__tests__/sessionLog.test.ts`. **1677 tests green, build clean.**

**Then (2026-07-01, session `sub2ys2a`):** ([ADR-169](06-decisions.md#adr-169)) **a trapezoid's height drops to the OPPOSITE PARALLEL BASE** — `CE גובה בטרפז` (right trapezoid ABCD) escalated to the LLM and returned not-understood while `CD גובה` works on a triangle. The `altitude` rule resolved the opposite side with triangle-only logic (the apex's two neighbours joined into a triangle); in a trapezoid the apex C's neighbours B,D are a **diagonal**, not an edge, so no triangle → not-handled. Fix: the parser gains a figure-derived `parallels` hint (vertex-disjoint parallel edge-pairs, via a new `parallelEdgePairs` engine helper), and `altitude` resolves the target to the **unique opposite parallel base** (C on base DC ∥ AB → foot on AB, CE ⟂ AB); zero or several parallel pairs (a triangle, or a parallelogram's two heights) defers per ADR-052. Triangle altitudes unchanged (empty `parallels`). Locked by scenario `altitude-in-trapezoid-drops-to-opposite-base` + `phase4.test.ts`. **1681 tests green, build clean.**

**Then (2026-07-02, session `z5dkmbla`):** ([ADR-196](06-decisions.md#adr-196)) **a midsegment named AFTER the triangle resolves its triangle from the figure** — `משולש ABC` then, in a LATER step, `GE קטע אמצעים מקביל ל AB` (the app's primary incremental flow) fell through to the parallel-constraint rule → a **plain parallel segment with no midpoints**, because `midsegment` required the triangle NAMED in the SAME utterance; so G,E were free, the sides carried no equal halves, and "view relations" correctly showed nothing (the operator's report — the engine's relations layer was never at fault). Fix: `midsegment` resolves the triangle from the figure (`ctx.neighbors`, apex = the unique vertex adjacent to both base endpoints) when it isn't named in-utterance — the same context inference altitude/single-vertex-angle use — so `GE` decomposes to `midpoint(C,A)=G`, `midpoint(C,B)=E`, `segment GE`, and the equal halves + corresponding angles surface. Locked by scenario `incremental-midsegment-resolves-triangle-from-figure`. **1782 tests green, build clean.**

**Then (2026-07-02, session `n6zuhw65`):** ([ADR-199](06-decisions.md#adr-199)) **a base-less midsegment ("EG קטע אמצעים" with E on a side) is a 2-variant construct, cyclable** — `משולש ABC` → `E על AC` → `EG קטע אמצעים` (no parallel base) fell through to a bare `segment E-G` with G undefined. A midsegment joins two MIDPOINTS, so E is pinned to the midpoint of AC and G is the midpoint of one of the two OTHER sides — AB (⇒ EG ∥ CB) or CB (⇒ EG ∥ AB) — and WHICH is genuinely unstated (ADR-052), so it's a cyclable alternative (operator: "G should have been placed on either side CB or AB with ability to alternate"). Fix: a `midsegmentBaseless` parser branch resolves E's host side from a new `ctx.onSegment` hint + the triangle from `ctx.neighbors`, emitting a new `midsegment` `shape-variant` `[P,Q,R,E,G]`; `expandShapeVariant` dispatches to pin E (`set-equal`) + place G on PR/QR by variant; `VARIANT_COUNT.midsegment = 2` reuses the kite/isosceles cycle plumbing so "show another configuration" flips G between the sides. Locked by scenario `baseless-midsegment-places-G-on-a-side-and-alternates`. **1787 tests green, build clean.**

**Then (2026-07-02): hardening plan Phase D complete** — ([ADR-200](06-decisions.md#adr-200)) `evaluate`'s coupled-solve cycle detection now walks the full object graph via a single **exhaustive** `objectParents` (compile-guarded), retiring the hand-scraped `PT_FIELDS` that silently dropped `to`/`toward`/`line`/`circle1,circle2` edges (a line∩circle-mediated cycle went undetected → false "unresolved dependencies"); ([ADR-201](06-decisions.md#adr-201)) `evaluate` **publishes** its resolved `circles` all the way to the renderer (`Derived.circles` → `<Figure>` → `buildScene`), so a solver-driven free radius is drawn from the published value and the six-kind `pointOnCircleId` reconstruction is **deleted** (renderer is now a pure consumer). Both are the kind-whitelist anti-pattern; no behaviour change for existing figures. **1790 tests green, build clean.** Phase D done; remaining hardening: Phase E (store robustness) + Phase F (renderer/UX).

**Then (2026-07-03/04) the Phase-6 theorem ground-truth corpus was AUTHORED + operator-REVIEWED and is now the phase gate** — [sample questions/theorem-ground-truth.md](sample%20questions/theorem-ground-truth.md) (per-question givens + solution sketch + the three id lists `expectSurfaced`/`solutionUses`/`mustNotSurface`) covers Q5–Q7 + the 571-booklet B-series (question 4 of every exam), 25 usable questions after the operator removed B5. Twelve review verdicts adjudicated: B8/B11/B17/B22/B23 transcription CONFIRMED, B2 "M is circumcenter" is a GIVEN, **B12 kept ∥ (booklet's printed ⊥ is a source typo — geometrically impossible; part ג's tangency forces OC∥AD)**; four pedagogy calls folded into [16-theorems-plan.md](16-theorems-plan.md) §10 — relevancy is non-monotonic + Appendix/definitions may appear as supplemental (B0), the tool never sees the question so **detected shapes drive theorems** (B1, revises D5), #87 GREEN on ≥4 stated-concyclic points (B2c), no sub-question exception (B3c). Docs-only.

**Then (2026-07-04): Phase 6a — the live theorem feed — is BUILT** ([ADR-208](06-decisions.md#adr-208)): a pure, coordinate-free `src/theorems/` spine (`detectTheorems` folds an authored `THEOREM_TABLE` over a symbolic `MatchCtx`, runs LIVE each step, "help-don't-reveal" enforced by keeping the derived-premise theorems 68/69/70/71/76 out of the table), an in-App feed (tier dots, ● new-badge, per-family background fold, click-a-row→highlight the premise), gated by `integrity`/`matchers`/`corpus`(Q5–Q7).

**Then (2026-07-04, first operator manual-test):** ([ADR-209](06-decisions.md#adr-209)) **configuration-specific theorems for an inscribed trapezoid** — `טרפז ABCD חסום במעגל` surfaced only generic inscribed-angle theorems because every circle matcher keyed off raw member COUNT, blind to the shape. Added **#87** (cyclic-quad opposite-angles=180, gated on a real quad command + concyclic vertices), the **parallels family #4/#6/#8** (Phase-6b pulled forward per operator, gated on stated parallels — `set-parallel` or any parallel-sided shape), **#94 broadened** to any equal-chord evidence, and a new **#201** ("an inscribed trapezoid is isosceles") in a new **200-band Appendix C** of 07 for composed teaching corollaries. Established working mode: **the operator names theorems during debug and each becomes a permanent table entry** (like an ADR). Q5–Q7 corpus unchanged (dormant). **2033 tests green, build clean.**

**Then ([ADR-210](06-decisions.md#adr-210)):** same trapezoid figure, next operator ruling — *"background will show only if the shape itself justifies it. here there is no triangle and no line between the parallel lines. זוית מרכזית should appear only if the center is given."* Root cause: matchers keyed off a **precondition** (a stated parallel pair; a raw ≥N-concyclic-point count) not the theorem's **premise configuration**. Three reusable coordinate-free gates: a **transversal** gate on #4/#6 (a triangle or a `line` cuts the parallels — #8 stays on the parallels alone, a trapezoid leg being its own same-side transversal); a **centre-given** gate on the central-angle family #92/#97/#98/#99 (centre **named** OR **used** — mirrors the renderer's FR-RN-8 show-centre rule, via a new `autoCenter` on `MatchCtx.circles`); and a **triangle** gate on #84 only ("every *triangle*…") while #91 ("three *points*…") is gated on a `circumcircle` CONSTRUCTED through 3 points (operator: "no use in questions I remember" → dormant in every inscribe, live only for the rare explicit circumcircle). Q6 ground truth tightened (a diameter+chord with no triangle drops #84 and #91). **2038 tests green, build clean.**

**Then ([ADR-211](06-decisions.md#adr-211)):** the **special-quadrilateral PROPERTY families** are tabled (07 #39/41/43/46/48/50/52/55/56) — a drawn מקבילית surfaced only the generic co-interior #8, so its own properties (opposite sides/angles, bisecting diagonals, consecutive-angle sum) never appeared; each is folded into the `quad` background family, gated by a class-hierarchy `quadClassPresent` (rectangle/rhombus/square inherit the parallelogram bundle + their own; generic trapezoid and triangle get none). Converse recognitions (type C) stay OUT for the Phase-6b gated-prompt layer.

**Then ([ADR-212](06-decisions.md#adr-212)):** salience is made **context-dependent** — a matcher may override its static salience, and the diagonal-property theorems (#46/#52/#55/#56/#41) jump from the background bundle to **headline** the moment the diagonals are actually DRAWN (a `quadHasDiagonal` geometric test on `neighbors`, keyword-free), so "the diagonal theorems get more weight" when the student puts the diagonals on the figure.

**Then ([ADR-213](06-decisions.md#adr-213), amends ADR-211/212):** the operator asked for the RELEVANT quad theorems as **main** entries, not additional — so the whole special-quad property family is now static **`headline`** (reverses ADR-211’s background fold), and the diagonal boost moves from a salience override to **attribution**: drawing the diagonals folds the diagonal-drawing facts + `segment` objects into the diagonal theorem (`quadHasDiagonal`→`quadDiagonals`), so it leads the quad headlines by recency, marks ● new, and highlights the diagonals when clicked — the non-diagonal properties stay main. The ADR-212 context salience-override plumbing (`TheoremMatch.salience?`) is removed (one static salience source).

**Then (ADR-214, Turn-5, session tzqfaub6):** a dropped **height/`foot` is now a right-angle premise** everywhere (Pythagoras #28 surfaces when a student drops a height, and #28 is promoted to a MAIN headline); theorem **#3** ("the distance between two parallel lines is constant") added, firing when a foot drops between two STATED parallels (its base = one parallel edge, apex on the opposite one; coordinate-free via a new `parallelPairs` helper); and a new **guiding-principle "concept" feed** (`src/theorems/concepts.ts`, sibling of the theorem feed, rendered in its own 💡 section) whose first entry is "right triangle → name one acute angle α, the other 90−α" — concepts carry a string-slug id, are NOT sourced from 07, and share a factored-out `makeAttributor`. **2113 tests green, build clean.** ([ADR-215](06-decisions.md#adr-215)) a **30-60-90 triangle is now its own detected shape sub-type** (an angle-magnitude axis in `classifyTriangle`: a right triangle with a 30° interior angle in every sample) and **theorems #33/#34** (the 30°-leg = ½-hypotenuse relation + its converse) are added to `THEOREM_TABLE`, firing whenever such a triangle is detected (operator session `x73i1cpx`: a parallelogram height + `DC=2CE` forces CDE to 30-60-90). Two independent causes sharing the shapes layer — no angle-magnitude axis + the theorems absent from the table; operator chose “always surface when detected” (a named-special-triangle study aid, extending ADR-214's shape-justifies-property to the triangle side). **2130 tests green (+17), build clean.** ([ADR-216](06-decisions.md#adr-216)) the shape→theorem gate is now **one general `shapeClassPresent(ctx, kinds)`** for triangles AND quads (operator: “are these principles applied to all cases? we need architectural solutions and not case by case”): the bespoke ADR-215 `special306090` matcher and the quad-only `QUAD_CMD` are deleted, #33/#34 become ordinary table rows keyed to a `kinds` set like every quad-property theorem, and attribution anchors to the shape's DEFINING fact (stated command, else — emergent-only — the latest vertex-touching fact) so the ADR-212 diagonal lead is preserved and the 30-60-90 stays ● new on its forcing given. **2130 tests green, build clean.** ([ADR-217](06-decisions.md#adr-217)) the **Appendix (practice-only / removed-curriculum) theorems A2–A6 + B3 are now tabled as SUPPORTING-ONLY** (operator: “keep A2, A3, A4, A5, A6, B3 — they should never appear in the main theorems, only as supporting”): type-`O` is allowed in `THEOREM_TABLE` but the structural invariant **`O ⇒ salience:'background'`** (integrity-guarded) makes each fold into its family row and never headline; `TheoremId` widened to `number | string` so the rows key on their 07 labels (`A2…B3`). Six coordinate-free matchers — intersecting chords (A2), two secants / secant+tangent from an external hub (A3/A4, near-point set excludes tangency points), the right-triangle altitude-to-hypotenuse geometric-mean pair (A5/A6), and two *visible* intersecting circles (B3, gated off the Thales hidden aux circle).

**Then (2026-07-04):** ([ADR-218](06-decisions.md#adr-218)) the theorem feed now recognises a premise **ENTAILED by the construction graph**, not only a typed shape/relation (the middle "Entailed" discovery level): `isoscelesEvidence` gains a circle-radii source (a circle centre + two members drawn as a triangle -> isosceles apex, |CX|=|CY|=r by definition, no equality typed) and a new `kiteEvidence` (declared kite OR two circles sharing two points with the four kite sides drawn -> kite of the two centres+points); **kite theorems 37/38 added** (a pure table gap). The **DRAWN gate** (sides must be in the neighbour graph) keeps it from flooding, so this stays structural-certainty recognition, not coordinate-coincidence detection. Frames the three-level model **L1 Declared / L2 Entailed (built here) / L3 Observed (numeric, next)**. The operator kite-of-two-circles sequence is locked as scenario `two-circles-kite-surfaces-kite-and-isosceles-theorems`. **2179 tests green, build clean.** **2169 tests green, build clean.** **Next: Phase 6b proper (median 15–17, midsegment 62/63, similarity/bisector converse-recognition prompts, the B-series booklet corpus).**

**Then (ADR-219):** an on-screen **levels-of-discovery dial** (Display options, cumulative L1 Declared / L2 Entailed / L3 Observed, default L1) grades every surfaced theorem by HOW its premise was known — a typed given (L1), a coordinate-free construction entailment (L2, ADR-218), or a coordinate-observed emergent shape/relation (L3, opt-in, auto-enables `detectShapes`). `detectTheorems` stays level-complete; the App filters `level ≤ selected`. Emergent special-quad/30-60-90 theorems reclassify to L3 (hidden at the L1 worksheet default); declared shapes stay L1. **2192 tests green, build clean.**

**Then (ADR-220, Phase B):** the discovery dial exercised on the operator's מקבילית ABCD / E על DC / המשך BE ו AD נפגשים בנקודה F figure drove two fixes — (1) alternate/corresponding parallels **#4/#6** now recognise a transversal STRUCTURALLY (structuralTriangles = any 3-cycle in the neighbour graph, replacing the ADR-167-smell kind-whitelist), so the drawn triangle FAB surfaces them at **Declared L1** while a bare trapezoid/parallelogram (a 4-cycle) still suppresses them (ADR-210 held) and keeps co-interior #8; (2) **AA-similarity #69 + ratios #71** admitted to the table as **Entailed L2** (relaxing ADR-208) *only* under similarityEvidence — a stated parallel that is a side of a triangle whose apex is off the other parallel (the Thales/line-parallel-to-a-side config), so the corpus (no parallels) never trips them; plus a guiding **concept** “if there are parallel lines, look for similar triangles”. General L3-*observed* similarity for non-parallel configs is DEFERRED (needs coordinates plumbed into the coordinate-free spine). Locked by scenario parallelogram-cut-triangle-surfaces-parallels-and-similarity. **2209 tests green, build clean.**

**Then (2026-07-04):** ([ADR-222](06-decisions.md#adr-222)) **the trapezoid midsegment / median** (`קטע האמצעים בטרפז`) — the triangle-only `midsegment` rule had no apex for a trapezoid (its legs don't meet) and escalated to the LLM; it now resolves the two bases from the figure's unique vertex-disjoint parallel edge-pair (`ctx.parallels`, the ADR-169 hint) and the legs via adjacency, joining their midpoints into the median (∥ both bases, length = their average) — no new engine construct (two `midpoint`s + a `segment`); a self-contained named form ("midsegment of trapezoid ABCD") builds the trapezoid inline. Locked by scenario midsegment-in-trapezoid-joins-leg-midpoints.

**Then (2026-07-04, bagrut Q8):** ([ADR-223](06-decisions.md#adr-223)) **a right triangle's vertex order is SEMANTIC — the right angle is at the LAST id — so it is never cyclically rotated** (operator session `avs58sfn`: two right triangles ABC, ABD sharing hypotenuse AB; the second knee showed but ∠ADB was 52°, and "AC and BD cannot meet"). Root cause: `right-triangle` sat in `DERIVED_SLOTS`, so `normalizeShapeComposition` rotated `[A,B,D]`→`[B,D,A]`, silently moving ∠90 from D to A. Removed it from the rotation set; the apply case now **swaps** the interchangeable hypotenuse endpoints so a fresh one is the derived perp-offset, and when the whole hypotenuse pre-exists it asserts the right angle as a **constraint** driving the NEW vertex onto the Thales circle (not a pre-existing leg's shape DOF — a stability fix), the vertex made **reflectable** across its hypotenuse (`reflectAnchors` now handles a shared-vertex right angle) so the sampler flips it to the side where the legs cross. Locked by scenario `two-right-triangles-share-hypotenuse`. **2216 tests green, build clean.**

**Then (2026-07-05, bagrut Q8b):** two follow-ups on the two-right-triangles figure. ([ADR-223](06-decisions.md#adr-223) Am.) a right-angle **knee** is now drawn only where the angle ACTUALLY measures ~90° (was placed from the command's declared vertex, so a figure that failed to realise the right angle showed a false 90° square); "show another configuration" (`resample`) now gates on the SAME `meetsRequirements` bar as the initial display, so it never offers a config where the on-segment crossing E can't be where instructed; and the "detect shapes" button no longer freezes the UI — detection is chunked/async (yields between sample batches) because the driven-⟂ solve is ~15 ms/evaluate × 16 samples. ([ADR-224](06-decisions.md#adr-224)) **similar / congruent triangle detection** — the opt-in "detect shapes" panel now surfaces similar/congruent triangle CLASSES (union-find over the forced-across-samples similarity relation, so a square's 8 right-isosceles triangles are one row not 28 pairs), on the same student-initiated reveal boundary as the shape badges (△DEG ~ △CEF for Q8b); the always-on theorem feed's no-reveal rule stands. Locked by scenarios `two-right-triangles-share-hypotenuse` + `q8-similar-triangles-detected`. **2223 tests green, build clean.** ([ADR-227](06-decisions.md#adr-227)) **a perpendicular helper's loose end is a reflectable SIDE DOF** — right triangle + `DF ⟂ AB` + `AC ו DF נחתכים בנקודה E` (session nc207foh) built with no error but E landed OFF both segments (the operator: "DF should have moved so it fits the input"); F, the loose end of the perpendicular, had no reflection axis (only a shared-vertex right angle did) so its side was frozen anti-correlated with the triangle shape, and reflecting it BEFORE the sample shifted the free-cluster spin centroid and re-shaped the triangle. Fix: the loose end of a cross-segment perpendicular reflects across the other segment's line, and a **direction-helper** (⟂/∥ loose end, no metric constraint) reflects AFTER the continuous sample (apex reflections stay before). Locked by scenario `perpendicular-helper-flips-to-reach-crossing`.

**Then (2026-07-05):** ([ADR-228](06-decisions.md#adr-228)) **size givens unified across shapes** — (A) `O_1`/`O_{1}` point-label subscripts normalise to the glued `O1` (the underscore had truncated the label to `O`); (B) a circle sized by its **circumference** (`שהיקפו 6π` ⇒ r=C/2π=3) or **area** (`ששטחו 9π` ⇒ r=√(A/π)=3) lowers to a NUMERIC radius, reusing the fixed-radius path; (C) a polygon **perimeter** (`היקף ABC = 20`) / perimeter-ratio is a first-class constraint (the sibling of area, ADR-118) — `set-perimeter` drives Σ of the sides to the given and the verifier checks it. `היקף` (one Hebrew word for circumference AND perimeter) is disambiguated by whether the utterance names a circle. Locked by scenarios `circle-circumference-sizes-radius` + `polygon-perimeter-sizes-figure`.

**Then (2026-07-05, cont.):** ([ADR-229](06-decisions.md#adr-229)) **freeze-and-co-drive** — the coupled "one point, two conditions" class is solved: a free point claimed by ONE constraint keeps a SPARE DOF its manifold allows, which a second failing constraint can now consume (`SolveDirective.also`), solved over a FROZEN bake of the already-valid subsystems with multi-start host seeds; `recruitFreeDofs` stops at the first fully-valid configuration. Bagrut Q11 (two tangents from an apex + the apex line pinned through A) builds end-to-end to the closed-form answer (|AB|=30, |BC|=18, |AD|=12). Copula-less circle sizes (`היקף מעגל O1 6π`) parse deterministically. **2264 tests green, build clean.** **A size given on a circle's RADIUS drives the radius, not the free centre** ([ADR-230](06-decisions.md#adr-230)) — `O1M=9` where M is the tangency (radial) point on O1 is a radius given, but with the free radius busy driving the tangency coincide, `driveOrCheck` fell through to the useless free CENTRE, bloating the later tangent solve to 6 coupled DOF and falsely over-constraining; now routed to pin the radius (shared `applyRadiusGiven`) and hand the coincide's centre-gap to a free centre, while an available free radius (intersecting circles) stays flexible. **2267 tests green, build clean.**

**Then (2026-07-06): the operator-commissioned review + [ADR-231](06-decisions.md#adr-231)** — the [design-rules doctrine](17-design-rules.md) (operator authority, read before any bug fix) + the review fixes: M1 existing-id lowering (the `already defined` class closed at the mechanism), M2 ownership/order-independence (HOIST dual of ADR-104 — Q11 builds in ANY entry order), M3 shared budgeted sampling + evaluate memo, honesty F5/F6/F8. Client-server for detection REJECTED; the Web Worker split + geoStore pure-function extraction is the parked follow-up (with full F4 routing unification and solver early-outs).

**Then (2026-07-06): save/load a figure to a portable `.geo.json` file — FR-HS-10 REALISED** ([ADR-232](06-decisions.md#adr-232)) — the file is the store's replay inputs (facts with utterance+lowered command, seed, dialed radii, display styling; never positions), save/load buttons in the sidebar, load = one undoable step with graceful bilingual refusals; plus the **fixtures regression net** (`src/__tests__/fixtures.test.ts`): every `fixtures/*.geo.json` replays through the real load path asserting build-green + verifier-clean + parser-drift (stored utterances must still lower to the stored commands), zero per-figure authoring — a manual session's saved figure becomes permanent regression coverage by dropping the file in the folder (seeded with 4 corpus figures). **ADR-238** (anti-collapse retry in both driven solvers + HOIST from pending — the operator's sq9lt4fj two-tangent-circles sizes now land in either entry order) and **ADR-239** (the common-tangent construct, both variants, softPair pairing) are in.

**Then (2026-07-06, home PC): the saved-file / edit honesty triple (ADR-240/241/242)** — an edited step re-lowers against its PREFIX context (+ `replaceGroup` seed parity + the diameter existing-endpoints branch asserts membership); a multi-subject on-circle statement asserts EVERY named point + the LLM commit path re-checks `droppedNewLabels`; and a loaded `.geo.json` gets a budgeted drift+dropped-labels audit (`src/store/loadAudit.ts`) with a persistent note pointing at ✎ (the per-step re-lower). **2026-07-08 (session ad66x493, bagrut kite-EMKO):** [ADR-253](06-decisions.md#adr-253) default placements land in GENERAL POSITION (the one-anchor template spin — a bare ``AM`` after ``AB קוטר`` no longer stacks M onto B / collinear, which had made the kite givens hard-fail at the only composition the apply gate judges) + [ADR-254](06-decisions.md#adr-254) ``point-circle-side`` — ``M מחוץ למעגל / בתוך המעגל`` parses to a free point seeded on the stated side, the side a verifier/meetsRequirements requirement. Scenarios ``kite-EMKO-outside-point`` / ``kite-EMKO-degenerate-default``. Same day (`gaawv4fr`): [ADR-255](06-decisions.md#adr-255) — a stated segment-meet re-seats a genuinely loose endpoint (constraint-free, non-pinned) so the segments really cross, preserving its circle-sides; scenario `stated-meet-relocates-loose-point`. And (`ntzdgqn2`): [ADR-256](06-decisions.md#adr-256) — the detection layers' sample pool is gated on the figure's stated configuration requirements (`requirementSamples` + `extensionsClear`, the `firstSatisfyingSeed` bar), so forced relations (∠AMO=∠EMO; △CAK~△OMK) are no longer suppressed by invalid samples; scenario `ntzdgqn2-kite-detection-honours-requirements`. Follow-up [ADR-257](06-decisions.md#adr-257): a congruent SUB-GROUP inside a mixed similarity class is emitted as its own congruent row (△OEM≅△OMK no longer vanishes when △ACK joins the class).

**Then (2026-07-08):** [ADR-258](06-decisions.md#adr-258) — **analytic / coordinate-geometry input** (axes, slope, coordinates, line equations, origin, cartesian; He+En) is a new classifyOutOfScope category, refused instantly with a pedagogical "this is a synthetic-construction tool; a coordinate-geometry tool is planned" message and a scope:analytic analytics tag, short-circuited **before** the LLM call (it can never build). Coordinate free-point placement A = (3,5) stays supported (grammar builds it, never reaches the classifier). [ADR-259](06-decisions.md#adr-259): a circle sized by its **diameter** (מעגל קוטר 10 -> radius 5) + the **עיגול** synonym (normalized to מעגל at the parse chokepoint) — a prod-triage real gap. [ADR-260](06-decisions.md#adr-260): a failed **compound** utterance (several statements in one line) now advises breaking it into smaller steps (looksCompound + input.tooManyParts), only on the parse+LLM failure path.


---

## Additional session snapshots (as-found order, verbatim)

### **CURRENT (2026-07-08) — the question export omits scaffolding steps ([ADR-252](06-decisions.md#adr-252)):** `questionLines` semantically filters the .docx givens list — pure-ink steps (bare `segment`/`line-through`) and value-less NEW-point markers are omitted unless a later kept step references what they introduced (reverse cascade); existing-point statements (M1) and any stated value always stay. Export-only; locked by real-parser tests over the operator's `ufxrtyp2` sequence.

### **PREVIOUS (2026-07-07) — FR-HS-11 built ([ADR-251](06-decisions.md#adr-251)):** "download question" — a real `.docx` (client-side `docx` library, lazy chunk) of the verbatim enabled utterances as a numbered RTL "נתון:" list **beside** the clean figure PNG (borderless `visuallyRightToLeft` table, real Word numbering, David/Arial fonts). Deterministic, no LLM — distinct from FR-HS-9. Pending operator gate: open a real export in desktop Word and confirm the mixed Hebrew/Latin bidi rendering.

### **Session 2026-06-11 banked a large hardening + UX pass** (all from a code review + manual stress-testing, no new corpus coverage): ADR-014 generic constraint solving + DOF-selection fix; parser misparse defense; ADR-015 edit-a-fact-in-place; ADR-016 snap-to-intersection; renderer lines-only + smart label placement; categorized commands coverage-map panel; and a deep **shape-composition** sweep — ADR-013 amendments (similarity-fit so composed shapes don't degenerate; vertex-order independence), ADR-017 (no two nodes may coincide + flip to avoid), empty-side composition + `flip` flag so any shape lands away from existing geometry, and textbook-clean upright templates. Plus **ADR-018** (direction only, not built): *alternatives = samples of the figure's residual freedom* — cycling should re-sample free DOFs so an underdetermined figure visibly wanders and converges as facts accumulate.

### **CURRENT (2026-07-09) — a polygon INSCRIBED IN A POLYGON ([ADR-262](06-decisions.md#adr-262)).** The bagrut *מעוין חסום במשולש* (rhombus inscribed in a triangle) family: `מעוין BDEF חסום במשולש ABC` / `rectangle inscribed in triangle ABC` (also square/parallelogram; triangle **and** quad containers). A general `inscribe` command lowers (ADR-110 macro pattern, **no new engine construct**) to on-segment riders on the container's sides + the shape's equal-side/right-angle constraints; shared labels coincide with their container vertex (M1). The unstated mirror/base-side is a **cyclable variant** (ADR-052/M4, shared `variants.ts`), and an explicit `point-on-segment` given pins it. Also fixed a **silent misparse** — `מעוין חסום במשולש` used to build the triangle's incircle (a circle), dropping the rhombus (`incircle` now requires a circle noun; the new rule runs first). See the CLAUDE.md running log for the headline state.

### **CURRENT (2026-06-18, end of session) — 1004 tests green, build clean, on `rebuild-foundation`.** Bagrut two-circle figure (two circles meeting at A,B; mutual tangents; two secants) + a trust pass. Added: parser constructs `tangentMeetsOtherCircle` + `lineMeetsCircle`; `פוגש`/`פגש` into the shared `INTERSECT_KW`; an apply-time fix so re-defining an existing point as on-circle becomes the second-intersection (no silent no-op); the **givens verifier** ([ADR-053](06-decisions.md#adr-053)) — `checkGivens` re-derives the relations the COMMANDS assert and checks them against the final coordinates, surfaced as `Derived.violations` + an amber cue, so **"green" means VERIFIED, not just no-error**; and UX from manual-test feedback — page **scroll restored** (dead `overflow:hidden` removed), a **responsive canvas** (fills the screen), and **playable radius-slider DOFs** (first slice, scratchpad cleared on resample). **★ Resume here — directional `המשך` semantics:** `המשך AC … D` must place D BEYOND the 2nd letter C (order A→C→D) and **adapt the figure** (drive the free radius so an extension intersection exists); today `lineMeetsCircle` is order-agnostic (E lands between A,C). Full plan + the key finding (default radii have NO extension root → resizing is required) + caveats are in [PROJECT-MEMORY.md](PROJECT-MEMORY.md) (2026-06-18 entry, "★ NEXT STEP"). After that: extend the verifier (constraint residuals + collinearity) and DOF sliders (on-circle/on-segment/free points); then **Phase 6 (theorems)**. Parked: area-given constraint class, production proxy deploy, coord-validation campaign, [ADR-049](06-decisions.md#adr-049).

### **CURRENT (2026-06-17, end of session) — 989 tests green, build clean, on `rebuild-foundation`.** This session, driven by manual testing of the two-circles / secant corpus, added: **collinearity** ([ADR-050](06-decisions.md#adr-050)) — `set-collinear` + a parser rule ("E on line AC", "line CE passes through A", "A,B,C collinear") + a redefine→collinear reinterpretation; **Am.1** a binding-aware seed + "keep an accepted candidate" fix in the driven solver (a chained collinearity that returned a false over-constraint); **Am.2** the second-intersection pattern (a collinearity onto a circle through a co-circular point becomes a line∩circle that AVOIDS the shared point — deterministic, never the degenerate crossing); **Am.3** an ordered multi-point line **"line ABE"** (collinear AND in the named order, via a one-sided `collinear-order` constraint). Then **circle radius as a free solver DOF** ([ADR-051](06-decisions.md#adr-051)) — a shape-scalar a constraint sizes and "show another configuration" varies (with a distinctness guard so resampled sizes don't collapse two points); the renderer recovers the driven radius from a point on the circle. Then the operator elevated **"no fixed assumptions"** to a guiding principle ([ADR-052](06-decisions.md#adr-052)): every unstated magnitude is a free DOF, never a fixed value — fixed two violations (ALL unstated-radius circles now free, not just two-circles; `point G on AB` with no ratio now SLIDES instead of pinning at the midpoint). **Resume here:** (a) optionally a **systematic DOF-conformance sweep** for ADR-052 (any value counted by `rawMovableDof` but absent from `freeDofs` = a default masquerading as fixed); then **Phase 6 (theorems)** is the headline. Parked: area-given constraint class, production proxy deploy, coord-validation campaign, [ADR-049](06-decisions.md#adr-049) LLM-assisted solve.

### **Next (resume here):** corpus fully reproduced (Q1–Q7); the construct vocabulary + the relational/UX layers above are in. The operator is **"almost ready for the next phase"** → **Phase 6 (theorems)** is the headline next milestone. **The smaller deferred directions are now ALL DONE (2026-06-15, home PC):** (a) extend the "draw its segments" convenience (FR-IN-7) to distance/equal/angle givens — ✅; (b) an explicit **merge** command (`מזג F ל-E`) distinct from rename (FR-HS-8) — ✅; (c) make a free (un-driven) `on-line` marker's offset samplable by "show another configuration" ([ADR-036](06-decisions.md#adr-036) A2) — ✅; (d) widen the "name a line by two points → markers" beyond the tangent rule to perpendicular/parallel ([ADR-036](06-decisions.md#adr-036) A3; bisector already had its own `line-intersection` path) — ✅; (e) ADR-018 Stage 2/3 (constraints prune sampled DOFs + a DOF-remaining cue, FR-ALT-4) — ✅; **(f) subscripted point labels (`O₁`, `O₂`) — ✅; (g) circle radius as a flexible DOF — ✅ (ADR-037 A2).** Still parked (not in this batch): a single-bisector / free-point-driver carrier and drawing a bare/clipped line (ADR-019 leftovers). **Still in progress (pre-existing):** a large differential coordinate-validation campaign (LLM-generated fully-determined diagrams → predicted coords → build → assert node coordinates vs an independent reference).
