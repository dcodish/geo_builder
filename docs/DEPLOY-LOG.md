# DEPLOY LOG — what is live on themathbible.com

Append-only, newest first. One entry per deploy, written **at deploy time** ([docs/22 §5](22-workflow.md)). Every entry pairs with a git tag `prod/YYYY-MM-DD[-n]` on the deployed commit. This file is canonical — deploy notes no longer accumulate in PROJECT-MEMORY. Pre-adoption deploy history (2026-07-04 … 2026-07-10) lives in PROJECT-MEMORY's operational notes.

| Date | Tag | Commit | App(s) | Bundle(s) | What changed |
| --- | --- | --- | --- | --- | --- |
| 2026-07-20 | `prod/2026-07-20-2` | `1c3998c` | 2-D (static only; proxy untouched) | `index-yx8AsGPl.js` | **The overnight P2 quartet (operator: "anything simple enough to fix over night" → morning "commit and deploy"): #162 + #156 + #168 + #193 (ADR-118 Am., ADR-371, ADR-372, ADR-373).** #162 — `dryRunOutcome.labelCount` counts all THREE label kinds, so a lone symbolic area label («שטח משולש AFO הוא 9b») commits instead of «already on the figure». #156 — a driving constraint coincidentally satisfied at the seed («∠EOF=90» on square-side midpoints) is `produced` via the new free-DOF-count-reduction signal; vacuous re-statements stay no-ops; the pinned angle holds at every displayed seed. #168 — a second «תיכון» no longer escalates: the median resolves its opposite side from the apex's POLYGON (`oppositePolygonEdges`, the ADR-263 mechanism), not a raw point count; the ADR-335 scenario replays the log's bare keystrokes again. #193 — the angle universe's degenerate-wedge exclusion flipped ANY-sample → ALL-samples (structural only), so Q9's part-א classes ∠ACE=∠ABE / ∠AFD=∠ABD surface; #194 (keeping squashed drawings off the canvas) stays open as the display-side complement. Full 2-D lane 3865 + engine 877 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-20 | `prod/2026-07-20` | `a088794` | 2-D (static only; proxy untouched) | `index-BmID3aKf.js` | **The circle-reference & tangency mega session (operator-tested on a local all-PRs server, then "commit and deploy"): 8 bugs + 3 features.** Bugs on main — #215 P1 (ADR-363, `resolveCirclePair`: a definite plural circle reference binds/completes/introduces/defers, never invents; «מנקודה» out of the touch alternation; the catalog «משיק משותף» example re-owned by commonTangent), #180 (ADR-364, the `through`-signal "reference is not a definition" guard), #213 (ADR-365, anonymous-centre-aware pickers + the quantified semicircle bulge), #152 (ADR-366, endpoint-is-centre routes «EO קוטר» to the new Thales circle), #159 (ADR-367, `resolveOrIntroduceCircle` — named-absent creates, empty-figure introduces, free radius), #221 (ADR-366 Am., membership resolution + cross-membership creates the circle on that diameter with a hidden centre), #222 (ADR-365 Am., the bare-adverb side words «מבחוץ»/«בחוץ»/«מבפנים» bind a semicircle's bulge). Features via merged PRs #218/#219/#220 (play-approved) — #184 (ADR-368/FR-IN-11: auto-name the unnamed construct — «אמצע AB», «משיק למעגל», «קוטר», «נתון מעגל», «מרכז המעגל», the unnamed secant; catalog +7), #203 (ADR-369: «AB משיק למעגל C» with no touch named = a tangency constraint, touch = the ⟂ foot as an anonymous dot; the quarter-circle-in-a-right-triangle figure), #214 (ADR-370 + Am.: tangents from a named apex to both circles — the from-marker binds the apex role, a new two-tangent apex is DERIVED at the crossing so «שני מעגלים זרים» stays disjoint; a 2nd standalone free circle splits its default radius, the ADR-253 general-position principle radius edition). 12 new scenarios, ~85 new tests. Full suite 4535 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200 (new code verified in the served JS). |
| 2026-07-19 | `prod/2026-07-19` | `9b3e783` | 2-D (static only; proxy untouched) | `index-vx1Tez9n.js` | Fix #212 (P1, ADR-359 Am. 9): a KIND adjective on a singular tangent noun («משיק פנימי/חיצוני/אלכסוני/מבחוץ/מבפנים», with or without labels) is a common-tangent request — the משותף-only trigger sent it to the LLM, whose guess REPOSITIONED stated disjoint circles into mutual tangency (wrong figure, green). Kind doubles as the trigger; the plural circles-participle keeps its circles-tangent meaning; droppedWordRelations gains the kind-tangent member. Full suite green + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18-3` | `897d3a5` | 2-D (static only; proxy untouched) | `index-Cuj_koAf.js` | The two-circle bundle (PR #211, ADR-358..362 + ADR-359 Am. 2-8): mutual positions «זרים»/«מוכל» as requirements + the bare-«שני מעגלים» case-toggle + apart seating with the stated-crossing re-seat (#196); the word-relation honesty gate (#210); common tangents — all kind phrasings (חיצוני/פנימי/אלכסוני/מבחוץ/מבפנים), label-less auto-naming, the plural pair, basin toggling, position-aware capacity with position-accurate refusals (contained 0 / int-tangent 1 / intersecting 2 / ext-tangent 3 / disjoint 4), the touch tangent auto-build + «בנקודת ההשקה» role form, the named tangents' meet «המשיקים נפגשים בנקודה K», naming dots at drawn-line × segment crossings, and the seat-before-constrain perf fix (38.3 s → 67 ms) (#197); the four-point secant one-liner (#191); ordinal circle references (#192). Operator: «fix this and commit and push and deploy» (play-approve waived per docs/22 §4). Full suite 4441 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18-2` | `b2502b4` | 2-D (static only; proxy untouched) | `index-JGniHryB.js` | The quarter-circle bundle + the sector: ADR-354 (#202 — membership conversions gated by wouldInvertDependency, רבע מעגל OCD ≡ ODC → r=6), ADR-355 (#205 — quarter/semicircle naming honesty: stated centre read, no label hijack, inscription stops), ADR-356 (#170 P1 — span-aware arc identity: spanDeg + view parity + scenePositions dedupe, no more 270° quarters), ADR-357 + Am. (#171, PR #209 — the גזרה sector: free/pinned/reflex central angle, שווה/=/° value forms, middle-centre naming with the O-family override, central-angle vertex resolution). Operator: "when complete testing, commit and deploy" (play-approve waived per docs/22 §4). Full suite 4399 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18` | `09a9294` | 2-D (static only; proxy untouched) | `index-DzvkFxJJ.js` | **PR #206 (feature #201, ADR-353, operator-approved + "merge and deploy"): diameter-THROUGH-a-point phrasings.** Operator ruling 2026-07-18: «קוטר ב/מנקודה A» means the diameter THROUGH A — the existing ADR-270 construct reached by more phrasings, widened at the `diameterFromPoint` from-marker only (the ADR-3D-026 phrasing-class discipline): «[ה]עובר בנקודה A» (the verbatim prod utterance carried a leading «הוסף» — tolerated), bare «בנקודה A», the «במנקודה» stacked-prefix slip, «דרך [נקודה] A», En "diameter through [the point] A" / "add a diameter through…". No-theft unchanged (cut compounds → diameterCutsSegment, «E על הקוטר» → point-on-carrier, two labels → named-antipode `diameter`; M1 membership + fresh antipode byte-identical); one REVIEWED shadow pair added to the hard allowlist (`diameterFromPoint → circle` — the circle rule also reads «העובר», rule order wins). Closes #201. Locks: `diameter-from-point.test.ts` (13 forms), scenario `diameter-through-point-imperative`, catalog +1. Full suite green on the deployed tree (4362 passed / 4 standing skips) + tsc + build clean; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-17 | `prod/2026-07-17-3` | `a02ff88` | 2-D + 3-D + **proxy** (restarted) | `index-Dr5mGWX2.js` / `3d-DE1grWOS.js` / `proxy.mjs` | **The P3 fix session (operator "park 192 and do the rest", then "deploy all"): #185 + #181 + #182 + #183.** **#185 (PR #195, operator-approved + merged; ADR-350/ADR-351)** the nine-parser-gap batch from the 2026-07-17 triage — definite references (generic polygon noun binds THE polygon; «נקודה G על הקו» resolves / «קו ועליו נקודה A» creates the unnamed line; «מקביל לבסיסים» via the `parallels` hint; «קטע מרכזים»/«מרכז מעגלים») + word forms at the `normalizeUtterance` chokepoint (cardinal degree words «שלושים מעלות» → digits; angle/arc «שווה ל» → `=`, narrowing ADR-119 per the operator ruling; the parenthesized appositive «(AB=AC)» as an ADR-264 clause; «ריבוע ABCD שצלעו הוא 1» + «צלע אחת 10 צלע שניה 5»); 11 scenarios, catalog +5. **#181 (ADR-3D-045)** 3-D lowercase labels in LABEL POSITION parse like uppercase (`upliftLowercaseLabels` — anchored, so x/y/z, k/m/t, u/v/w, R/r stay case-significant). **#182 (ADR-3D-046)** the 3-D sink logs the LLM's committed canonical lines + delete/show-another/undo/redo/clear/load actions; the triage `session3d` follows them (the #84/#189 mirror — post-fix llm sessions replay without `degraded`). **#183 (ADR-352)** the admin gap card consumes the triage VERDICT MAP (`triage.mjs` uploads `verdicts-<app>.json` beside the events file): verified-built rows move to «תוקן מאז», open rows carry a current-verdict column, the header names the verdict revision; no map reads as unknown — the first map lands on the next `/log-triage` run. Full suite green on the deployed tree (4352 passed / 4 standing skips) + tsc + all three builds clean; both live pages serve the new bundles (200), both admin dashboards 200, proxy restarted + healthz ok. |
| 2026-07-17 | `prod/2026-07-17-2` | `2924295` | 2-D (static only; proxy untouched) | `index-DfIj4qrc.js` (worker unchanged: `geoWorker-B_uK9fI5.js`) | **The parser-distribution P2 package (fix session, operator "go ahead" then "deploy"): #163 + #151 + #160 (ADR-343/344/345).** **ADR-343 (#163):** a CHAINED statement's tail value distributes to EVERY member — operator ruling «AB=BC=8 means AB=8 and BC=8»; lengths/angles/symbolic all through the one owner (`chainedEquality`); the entailed set-equal KEPT (measured green vs the ADR-272 gate; ADR-234 `pinsSoftVariant` reads it); `symbolic.test.ts` re-derived. **ADR-344 (#151):** plural chord/diameter declarations distribute over the conjunction («AB ו DC מיתרים» → all 4 memberships + both segments; sequential uppercase pairing, odd counts fall back byte-identically, intersect compounds never pair-read); catalog +1. **ADR-345 (#160):** a COUNT digit is a quantifier, not a magnitude — `droppedGivenNumbers` blanks count slots (He …ים/…ות / En …s morphology) so «2 משיקים» ≡ «שני משיקים»; magnitudes stay gated (פי-prefix, פעמים/times, decimals, digit-after-noun). #159 (same report, no-circle-yet tangent bail) deliberately stays open. Closes #163/#151/#160. 3 scenarios + 18 class tests; both catalog snapshots re-derived (the new entry). Full suite green on the deployed tree (2-D 3631 + 3-D 739 / 4 standing skips) + tsc + build clean; live page serves the new bundle (200, text/javascript, worker chunk 200), proxy healthz ok (untouched). |
| 2026-07-17 | `prod/2026-07-17` | `577719c` | 2-D (static only; proxy untouched) | `index-DZncnaol.js` + `geoWorker-B_uK9fI5.js` | **The #177 anonymous-centres batch (P1, operator play-tested + "commit and deploy"): ADR-342 + Am. + Am. 2.** **#177:** an unnamed circle's centre POINT is anonymous ('@ctr-O') — the auto letter never squats the student's namespace (prod session `gxccyt2n`: «P על המשך BA» bound the invisible second centre); the LETTER stays the circle's reference token («מעגל O», `circle-O` — byte-unchanged). The binding boundary is PLACING vs REFERENCING (two operator rulings): metric givens («OP=4», «OA=5») and bare segments («OP», «PA») bind-and-promote (the locked `23vqi9u8` + kite prod flows); placing statements create a fresh point. Naming/promotion via `name-center` + a replay pre-scan; `nameCentre` token-driven; slider labels show the token. **#178 (play-test):** naming with TWO circles reads the size qualifier («מרכז מעגל קטן הוא O1», articles optional) via the #102 core + locks the roles — no more LLM-invented arbitrary point. **#179 (play-test):** the lock carries POST-rename ids, so `orderedBelow` stamps and the ADR-309 sliders enforce the stated big/small in both directions. Closes #177/#178/#179. Full 2-D suite green (3614 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, worker too), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-4` | `dae3196` | 2-D (static only; proxy untouched) | `index-C5vEcdZz.js` + `geoWorker-CtdT7eOv.js` | **The two open P1s (fix session, operator "fix the 2 P1" then "deploy").** **ADR-340 (#175):** "show another configuration" searches and applies a VALIDATED COMPOSITE view — facts × seed × branch × variant accepted only when `meetsRequirements` holds on the whole (`searchAnotherView`; worker + sync fallback; ONE `applyView`, the App's post-hoc `cycleAlt`/`cycleVariant` deleted; both store actions gated the same way). The prod session `gxccyt2n` break (branch flip onto a valid seed broke the stated internal tangency, «B ו-D נפלו על אותה נקודה») is locked deterministically (seed 2 + D-flip) and verified RED pre-fix. **ADR-341 (#173):** a trapezoid's LONG BASE — a stated «AB < CD» flips the TEMPLATE (ids rotated by two; first draw k=1/0.6≈1.667, never the k≈1.08 boundary grind) and the sampler straddles k=1 (both branches reachable when unstated — the ADR-052 smell closed; a stated order pins the branch, min sampled 1.205). Closes #175/#173. Full 2-D suite green (3605 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, worker chunk too), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-3` | `da45964` | 2-D (static only; proxy untouched) | `index-CWwOSOBS.js` + `geoWorker-BmPXB4uM.js` | **The inscribe triple (operator "commit and deploy" after play-testing): #167 P1 + #166 + #176.** **ADR-337 (#167):** a fact's multi-command lowering is TRANSACTIONAL — the fold builds into a trial and commits only if EVERY command succeeds, so a failed macro leaves ZERO trace (was: half-built expansion rendered from a red step, prior figure moved, verifier blind). **ADR-338 (#166):** a macro's defining constraints are solved as ONE COUPLED SYSTEM (`applyCoupledStep` attaches all N then evaluates once; `applyUnits` groups consecutive relation commands structurally) — square/rectangle inscribed in a right triangle now BUILDS (all 36 shape×variant×vertex configs verified; the corner square matches the closed-form oracle exactly; no new numerics — resolveMixedCarriers/jointCostTerm already composed). **ADR-339 (#176, the play-test follow-up):** a cyclable variant's DEFAULT settles at commit to the first cleanly-building configuration — the reported sequence now draws the general-position square (all 4 vertices ON the sides), the degenerate corner square stays reachable by cycling with its ADR-123 notice. Filed #174 (P3, 1-of-18 marginal NM convergence, pinned by the MARGINAL set). Full 2-D suite green (3597 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-2` | `a9aa832` | 2-D (static only; proxy untouched) | `index-Cm9luzzL.js` | **PR #165 (feature #161, ADR-336, operator-approved waiver): the default triangle draws textbook-oriented** — A at the apex, BC the horizontal base (B bottom-right), via the one-literal `triangle` template change in `applyCommand`; apex (2, 3.6) chosen so the seed-sweep/isosceles scenarios keep their basins. Merge note: both of today's sessions had claimed ADR-335 — the compound-measures ADR (already live as `prod/2026-07-16`) keeps 335; the triangle ADR is renumbered **ADR-336** (docs/06 + its PROJECT-MEMORY note). Deploy-audit context: 3-D (`3d-qKv80QAv.js`) and the proxy verified CURRENT — no undeployed commits in either lane; this PR was the only outstanding deployable. Closes #161. Full 2-D suite green on the merged tree (3578 passed / 4 standing skips) + tsc + build clean; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16` | `ad26d30` | 2-D (static only; proxy untouched) | `index-3d9zeIlf.js` | **ADR-335 — compound measure relations (PR #169, operator commit-and-deploy): the #153/#145 P1 honesty class closed at BOTH mechanisms + the #154/#144 capability.** A relation over ≥2 measure terms was TRUNCATED by the first-match-wins relation rules to a wrong binary constraint with every gate silent («קשת AC + קשת BE = קשת AD + קשת BC» built ∠AOC=∠AOD; «DM/ME=BM/DM» built set-equal; unreported siblings: segment sums, angle sums, «קשת AB=40» as a chord LENGTH). Now: `set-measure-sum` (sums of segments/angles/arcs, NUMEXPR coefficients, numeric targets, ADR-164 single-vertex arms) + `set-length-product` (power-of-a-point products/quotients/squares, log-domain residual) + the structural `droppedCompoundRelation` gate (slot-count accounting, 3 commit chokepoints, catalog-wide false-positive guard) + `arcValue` («קשת AB=40» → central angle; value printed ON the arc, never a wedge at the hidden centre — operator display rule) + the `⌢{}` toolbar template + over-arc MathML rendering (#155 folded in). Bagrut Q22 reproduces end-to-end with the REAL exam given (S_CFG=S_CGH): HG⊥AB forced, cos=0.0000. Closes #153/#145/#154/#144+#155; filed #168. 6 scenarios; full 2-D suite green (3576 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, set-measure-sum/set-length-product/arcOf markers verified in the served JS), proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15-3` | `f4a7d3f` | 2-D (static only; proxy untouched) | `index-DSMc9BKD.js` | The **P3 bundle** (PR #149, ADR-334, operator commit-and-deploy). **#146** «נקה הכל» now also clears the input box + all transient notes + any in-progress edit (`clearAll`; prefs untouched). **#147** the successful-load "N steps refreshed" note now auto-clears after 6 s (was on the persistent `fileNote` lane with no timer/dismiss → hung forever) — re-routed through `noteFileProblem`. **#46** bare «קו XY» → segment synonym of «ישר XY» (orientation/style adjectives like «קו אופקי» never match → escape to the guidance classes). **#47 (forms 1/2/4; form 3 → #148)** the «ב F» spaced crossing-marker class-fix (`crossingAfterCircle`, `ב-?`) unblocks «ההמשך של BE … ב F», + the copula «המשך CD היא נקודה A»; the plural distribute («המשכי הגבהים …») split to #148. **#107** «גובה לצלע AB» (altitude to a named side) drops from the opposite vertex (mirror of the #71 vertex-less median). Closes #146/#147/#46/#107, partially #47. Full 2-D suite green (3510) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15-2` | `8e6520f` | 2-D (static only; proxy untouched) | `index-C_fv6fLj.js` | Sync dev↔prod: the secant + tangent session's remaining work (all client-only parser/context). **#136 (ADR-332, PR #137)** the apex+far secant «AD חותך למעגל [בנקודה B] [דרך P]» + the from-point phrasing «מנקודה A יוצא חותך למעגל בנקודה D» (`secantFarPoint`; D a free-θ far crossing, near an anonymous @-dot or named, guarded off the diameter family). **#142 (ADR-333, PR #143)** two «tangent from A» statements now land on the two DISTINCT touch points — the second takes branch 1 (`ctx.tangentAuxes`), fixing «B ו-C נפלו על אותה נקודה». (#138's tangent verb-gate fix rides along, already live from `-1`.) Operator "deploy all so dev and prod sync" (play-approve waived; PRs #137/#143 are the record). Full 2-D suite green (3497) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), all three fixes verified in the served JS, proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15` | `841d3a6` | 2-D (static only; proxy untouched) | `index-COcYafDk.js` | **P1 prod-regression fix — #138 (ADR-292 Am., PR #139).** The singular/plural external tangent (`מנקודה A יוצא משיק למעגל בנקודה B`, `…יוצאים שני משיקים`) parsed OK in prod on 2026-07-11 but broke by 2026-07-15 (weak:dropped:משיק/tangent → LLM → built-nothing). Cause: the ADR-292 `משיק` verb honesty gate (deployed `prod/2026-07-12-2`) didn't recognise `tangentFromExternal`/`tangentsFromExternal`'s Thales AUX-CIRCLE construction (`tanaux-`/`~tanmid-`, no literal `tangent` object), so it flagged the CORRECT parse as dropping משיק. Fix: `tanaux-\|tanmid-` added to the gate's satisfied set (a genuinely dropped tangent still blocks). Confirmed against prod `events.jsonl` (worked 07-11, broke 07-15). Full 2-D suite green (3478) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-5` | `65d2559` | 2-D + 3-D (static only; proxy untouched) | 2-D `index-DVVKUgG8.js` + `geoWorker-Dfh4WyFk.js` / 3-D `3d-qKv80QAv.js` | The P2 feature batch (7 PRs, operator "deploy all"): **#127 (ADR-327)** a drawn parallel line reads the "from a point" origin (`מנקודה A ישר מקביל ל-DO`) via a FROM_PT anchor on the parallel-line rule. **#104 (ADR-328)** a bare free point `נקודה A`/`point A` builds a 2-DOF sampled point positioned by the next statement (re-exposes the original primitive; the `bare-point` scope guidance narrowed to a lone letter). **#44 (ADR-329)** named special-point meets — `מפגש האלכסונים/התיכונים/חוצי הזוויות/הגבהים/האנכים האמצעיים` (diagonals/centroid/incentre/orthocentre/circumcentre) as ADR-110 macros; only the CENTRE point is drawn, the construction is `~`-hidden scaffolding (operator play-test fix). **#29 (ADR-330)** a semicircle on EVERY side of a polygon (`על כל צלע … יש חצי מעגל`). **#134 (ADR-331)** a semicircle oriented OUTSIDE/INSIDE a shape (`חצי מעגל על צלע AB מחוץ למשולש`) — the arc gets a render-time `bulgeRef`. **#94 (ADR-3D-043)** a named angle `∠SDB`/`∠SDB=α` is a highlightable 3-D marker, not a valueless-query refusal. **#117 (ADR-3D-044)** right prisms over parallelogram/quad/square/pentagon/hexagon bases + the oblique `מקבילון` (parallelepiped). Merged to main, full suite green (4139 2-D+3-D) + tsc + both builds clean on the deployed tree; both live pages serve the new bundles (200, text/javascript), proxy healthz up (untouched). |
| 2026-07-14 | `prod/2026-07-14-4` | `be96230` | 2-D (static only; proxy untouched) | `index-Y-6dsT7t.js` | PR #125 (features #98/#39/#126, operator commit-and-deploy after play-test). **#98 (ADR-324)** on-canvas measure labels render math as PURE SVG — a radical gets a real overline/vinculum, a fraction a bar, coefficients kept (`MathSvg`); MathML was rejected because the PNG/docx export can't rasterize `foreignObject`. Plain labels unchanged. **#39 (ADR-325)** selecting a size/ratio step shows its value MEASURED on the drawing under the row (`S(DBCA)=62.2, S(GAD)=4.1 → ratio 15.0 ✓`); the ratio is the seed-invariant verdict, absolutes are per-drawing context; read-only, no engine change. **#126 (ADR-326, folded in from play-test)** the "view relations" hover picker recognized only equality classes — a right triangle's forced 90° (or any determined angle/length with no equal pair) was undisplayable; now `detectRelationsAcross` also emits `definiteLengths` (honest: empty on a free-scale figure, populated once a size given pins the scale), the picker selects definite angles+lengths, and hovering a determined side reads its length / a wedge reads its angle (a forced 90° stays a knee, operator's choice). Also answers #39's "show what AC is" for lengths. Full 2-D suite 3442 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-3` | `920af6a` | 2-D + 3-D (static only; proxy untouched) | `index-ux5odOvc.js` / `3d-ZZZ0_e1_.js` | The "finish the open bug queue" batch — all 5 open bugs (0 remained) + one P2 feature. **#1 (ADR-320)** the dry-run trial now models the commit's dedup, so re-typing `AB=AC` reads "already drawn" instead of a phantom ~0.75 shift (`trialFacts` drops exact enabled-duplicates → prefold/dry-run/commit agree). **#24 (ADR-321)** the ADR-242 load-audit note is DERIVED from live facts (`liveAuditFindings`) so it self-clears when its rows are deleted/toggled/re-lowered, plus a × dismiss. **#33 (ADR-322)** a run-on given-list that a permissive circle rule claimed across clause boundaries is refused honestly (`droppedGivenRelations` joins the winning-parse chokepoint) instead of garbage `ok:true`. **#55 (ADR-3D-041)** radical coefficient in a 3-D vec-rel (`AB=√2·OD` parses like `A'K=4/5 DN`; the filed ambiguity diagnosis was corrected) + a coefficient bare-pair length RHS. **#116 (ADR-3D-042)** the 3-D "right triangle" qualifier — build/constrain (M1 idempotency for a polygon on existing ids + a soft middle-vertex right angle that yields to an explicit ∠=90). **#106 (ADR-323, PR #124, commit-and-deploy)** the central angle «זוית מרכזית COD» / «נשענת על (קשת) CD» — resolves the centre by name or implicitly, draws the radii (centre shows), a value → `set-angle`, valueless → a new `mark-angle` display primitive; the subtends verb alone signals the endpoints (operator play-test widening). Full suite (2-D 3426 + 3-D 709) green + tsc + both builds clean on the deployed tree; both pages serve the new bundles (200), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-2` | `bebafa0` | 2-D + **proxy** (restarted) | `index-CTXOpkeN.js` + `proxy.mjs` | The backlog-triage batch (bugs #90/#95 + feature #105 to `main`/PR #123 + debt #84). **#90 (ADR-316)** minor/major arc qualifier — `D אמצע הקשת הקטנה AB` no longer lands on the chord; MAJOR = the far arc (branch 1 / a new `on-circle` `major` flag). **#95 (ADR-317)** `E בין A ל-B` (between) builds a point on segment AB. **#105 (ADR-318)** verbose/relational size-givens — `שורש N → √N`, `אורך X הוא √N`, and the operator's Q5 `אורך AC גדול פי √(3) מהקטע CO` now build (`ratioConstraint` moved before `segment` so `מהקטע`'s `קטע` can't half-parse the ratio; the vague unnamed-sides form gets a guided message). **#84 (ADR-319, the server half)** prod analytics now logs the LLM's committed `commands` + a lean `ev:'action'` line per store interaction (edit/slider/show-another/delete) — so a reported session reconstructs; the proxy was rebuilt + restarted (verified `/api/log` accepts `ev:'action'` → 204). Full suite 3412 green + tsc + client & proxy builds clean; live page serves the new bundle (200), healthz ok. |
| 2026-07-14 | `prod/2026-07-14` | `92df7b8` | 2-D (static only; proxy untouched) | `index-C8Qf6xr5.js` + `geoWorker-xh4TwN_y.js` | The bagrut-Q5 session batch (all client-only). **#114 (ADR-310)** `√()` radical as a ratio COEFFICIENT — `ratioConstraint`/`segmentRatio`/`measureRatioK` migrated to the shared NUMEXPR atom so `AC=√(3)CO` / `גדול פי √(3) מ` parse deterministically (was an LLM detour). **#115 (ADR-311)** a point-on-object rider defaults into GENERAL POSITION — `freeSegT` is position-aware, so `E על CB` never lands on an existing point. **#118 (ADR-312)** the live math preview follows the input's direction (`dir={textDir}`), so a Hebrew sentence containing a radical no longer renders word-reversed. **#119 (ADR-313)** a stable within-segment SELECTION for line∩circle (`onSegment`) — `המעגל חותך את BO בנקודה K` keeps K on segment BO after a later `CK=√63` (was flipping beyond the centre); the tangent/secant sibling solves every seed (sidesteps #3, no `order` contention). **#120 (ADR-314)** load auto-re-lowers DETERMINISTIC steps against the current parser, so an old save picks up fixes on load (LLM steps kept as saved — never re-escalate). **#121 (ADR-315)** saving a NAMED figure asks overwrite-vs-copy. PR #122 (features #120/#121, operator play-approved + merged); #114/#115/#118/#119 committed direct (bugs). Full 2-D suite 3377 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-6` | `57729b9` | 2-D (static only; proxy untouched) | `index-DYSh3kBc.js` + `geoWorker-FWQh-hps.js` | Operator follow-up to #113 (ADR-309): the two radius sliders under a stated R>r order now share ONE common scale (min = smaller base×0.3, max = larger base×2.2), so R's thumb always sits to the RIGHT of r's on the same axis — the order is now VISIBLE, not just enforced (superseding the per-slider clamp, which kept the ranges incomparable). Verified across seeds (r thumb 11-25%, R thumb 30-45%). The bidi similar/congruent row fix (prod-4) confirmed working in the operator's screenshot. Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-13 | `prod/2026-07-13-5` | `93a112b` | 2-D (static only; proxy untouched) | `index-BhN11CZ_.js` + `geoWorker-FWQh-hps.js` | Operator follow-up to #113 (ADR-309): the radius slider RANGE is now clamped to a stated R>r order — the inner circle's max is the outer's current radius minus the verifier gap, the outer's min is the inner's radius plus that gap — so it's MECHANICALLY impossible to drag the small circle past the big one (the value guard rejected it but the free range let the thumb travel there and snap back, reading as a violation). Confirmed the engine already gates resample/"show another" on the order (seed sweep). Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-13 | `prod/2026-07-13-4` | `1fecb24` | 2-D (static only; proxy untouched) | `index-CGwr8gYh.js` + `geoWorker-FWQh-hps.js` | Two fixes from continued testing of the 2025-bagrut figure (ADR-309): **#113** the radius slider now respects a stated R>r order — `setRadius` rejects a value that introduces a `radius-order`/`radius-ratio` verifier violation (was only rejecting a hard error), so the slider STOPS at the boundary instead of dragging the small circle past the big one; **#26/#89** the similar/congruent triangle rows («△AMO ~ △AOE») rendered garbled as «AMO ~ △AOE△» under the RTL document — the LTR math notation now forces `dir=ltr` + `unicodeBidi: isolate` (radius-slider label gets `dir` too). Confirmed against the operator's screenshot. Closes #113/#26/#89. Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-3` | `936e465` | 2-D (static only; proxy untouched) | `index-DeUeXXZh.js` + `geoWorker-CoYgsosW.js` | The three fixes from the 2025-bagrut testing pass: **#112 (ADR-306)** name/rename an auto-assigned circle centre — «מרכז המעגל הוא P» renames the hidden centre O→P + reveals it (via the new `parseNameCenter`/`nameCentre` store op), never a second circle; **#111 (ADR-307)** a definite plural «(שני) המעגלים» / «the circles» binds the two EXISTING circles (`definiteTwoCircles`), no invented third; **#110 (ADR-308)** «M אמצע OK» on the AE-extension∩OK crossing now flexes E so M bisects OK — a collinear midpoint lowers to the 1-D equidistance |OM|=|MK| driven by the upstream free carrier found past soft-order carriers (`ancestors`/`freeCarrierAncestor` `'soft-order'` mode + `driveHardOn`), replacing the unsolvable 2-D coincide. The full 2025 exam incl. the part-ב midpoint given now builds end-to-end. Closes #110/#111/#112. Full 2-D suite 3343 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-2` | `ea95051` | 2-D (static only; proxy untouched) | `index-5aFllYLB.js` + `geoWorker-smWq7buc.js` | Feature #102 (PR #103, ADR-305): «המעגל הגדול/הקטן» between two INDEPENDENT circles both REFERS and ASSERTS — a definite size qualifier resolves to the concrete circle (recorded `set-radius-order`/`Circle.orderedBelow` roles first, else assigned from the drawn sizes) and a first assigning use appends the locking `set-radius-order` (the operator's "translate it to a R>r like constraint" ruling), so sampling can never swap which circle is bigger; the creation adjective «מעגל קטן…» seeds a smaller radius (free DOF). Concentric pairs keep ADR-244; 0/1/3+ escalate. The 2025 bagrut's PUBLISHED wording now builds end-to-end (scenario `bagrut-2025-verbatim-unnamed-circles`). Operator commit-and-deploy after play-testing. Full 2-D suite 3323 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). Known follow-ups filed from the same session: #110 (M-midpoint solve), #111 (plural circle ref), #112 (name/rename an auto-centre). |
| 2026-07-13 | `prod/2026-07-13` | `ecfeef5` | 2-D (static only; proxy untouched) | `index-0AIMIH_k.js` + `geoWorker-CGkr4dk3.js` | The 2025-bagrut two-circle batch (PR #101, operator commit-and-deploy): **#54 (ADR-304)** per-circle RADIUS SYMBOLS — bind at creation («שרדיוסו r», any letter, case-sensitive) via the `withRadiusSymbolBinding` chokepoint post-pass or after the fact («רדיוס מעגל O הוא R»); relations `R > r` (radius-order requirement, independent circles), `R = 1.5r` / `R/r = 2√7/5` (`set-radius-ratio` → plain `ratio` over centre-witness pairs, zero new solver code); `AB = √2R` couples to the BOUND circle; sliders show the letter. **#99 (ADR-303)** a point's side of a POLYGON region («הנקודה E נמצאת על מעגל O בתוך המשולש KAO») — `point-polygon-side` requirement + verifier + `regionSideFallback` + `droppedRegionSubject` gate. **#100 (ADR-302, was main-only)** tangent THROUGH an on-circle point + the definite «המשיק» back-reference. **Two play-test rounds folded in (Am. 1+2, session `yla2d4xo`):** the submit-gate false-positives (`droppedNewLabels` measureSymbols mask; verb gate accepts a `tan-` line reference), the one-sentence compound «דרך A עובר משיק למעגל O שחותך את מעגל P בנקודה K», the region→membership order (the (c2) conversion lands at the point's own bearing), and requirement re-statements commit (exact duplicates stay no-ops). Closes #54/#99/#100. Full 2-D suite 3314 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-12 | `prod/2026-07-12-4` | `7403ce3` | 2-D (static only; proxy untouched) | `index-GY7oYSVK.js` + `geoWorker-ptyHBf-J.js` | The batch since `-3`: **#77+#40 (PR #92, ADR-298)** radical/fraction VALUES as givens (`BC=35/√32`, `35/2`, `√32/5`) with EXPLICIT `√()` grouping (disambiguates `√(2/3)` vs `√2/3`), the `√()` toolbar button, and **MathML rendering** (live input preview + step rows); **#45 (PR #93, ADR-299)** right-angle glyph/word/homoglyph forms (∡/∢→∠, ⁰→°, Cyrillic→Latin, «ישרה»/«right angle» word, lowercase vertex); **#96 (ADR-300)** the abbreviated «מ-B» external-point cue (secant/tangent from external point); **#97 (ADR-301)** a stated «D על המעגל» membership survives a relation clause (was silently dropped). Closes #40/#77/#45/#96/#97. Full 2-D suite 3271 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-12 | `prod/2026-07-12-3` | `e000e06` | 2-D (static only; proxy untouched) | `index-BYovH_N5.js` + `geoWorker-ptyHBf-J.js` | The 2-D fix + feature batch since `-2`: **ADR-295** the detection-honesty cluster (#49 `~`-scaffold excluded from the detection universe, #50 `distinctSamples` drops unforced point-collapse samples, #88 a definite VALUE needs a non-starved pool); **ADR-291 Am.** (#86) the circumscribing circle in a CUT sentence stays hidden (scaffolding); **ADR-296** (#87) config searches get `WORKER_SEARCH_BUDGET_MS` (12s) off the main thread; **ADR-297** (#32, PR #91, operator play-approved) anonymous promotable points — an incircle's touch feet are `@`-anonymous clickable dots the student promotes to a letter, so no auto-minted `F`/`G`/`H` hijacks the student's namespace. Closes #49/#50/#86/#87/#88/#32. Full 2-D suite 3212 green + tsc + build clean on the deployed tree; live page references the new bundle, page + bundle + worker chunk 200; proxy healthz ok (untouched). |
| 2026-07-12 | `prod/2026-07-12-2` | `e95f1c9` | 2-D (static only; proxy untouched) | `index-D3tcGzQp.js` + `geoWorker-DHZcuPJ-.js` | The CEFO cluster (operator "fix both"): ADR-291 circumscribing-circle REFERENCE resolution at the resolver chokepoint + show-circle (fixes #82 P1 silent tangent drop, #83 duplicate-circle re-create, closes #81 book circumcircle-cut phrasing); ADR-292 the VERB honesty gate (droppedGivenVerbs, both commit paths); ADR-294 the המשך kaf-inflection sweep (18 sites — plural המשכי parsed to the OPPOSITE onSeg constraint, fixes #79); ADR-293 view-level keep-prior (never-blank canvas + stale notice, fixes #85). 4 scenarios with exact prod/book utterances + verb-gate.test.ts. 2-D lane 3196 green + tsc + build clean; page + bundle + worker chunk 200. |
| 2026-07-12 | `prod/2026-07-12` | `ac57d39` | 2-D (static only; proxy untouched) | `index-DrL3d2Ti.js` + `geoWorker-D5RL8Lgk.js` | Fix #41 (ADR-290): the geometry WORKER — show-another/auto-resolve searches + the cold FOLD (submit, LLM, and file-load prefolds) run off the main thread with fold transplants (content-keyed primeFoldFor); live k/24 progress + a ✕ cancel (worker termination = real preemption); no-Worker environments keep the sync path. Closes the page-unresponsive class; the load half is the #67 core. Operator validated locally (dev server play). 2-D lane 3182 green + tsc + build clean on the deployed tree; page + main bundle + worker chunk all 200. |
| 2026-07-11 | `prod/2026-07-11-7` | `2a8ed20` | 2-D + 3-D (static only; proxy untouched) | `index-BoJLFRwZ.js` / `3d-C8ZwhmWn.js` | The baseline-triage batch (operator approved + "merge and deploy all"): #34 parallel-default-segments fix (ADR-287, on main directly), PR #74 (#72 3-D phrasing batch, ADR-3D-039), PR #75 (#71 2-D phrasing batch, ADR-288), PR #76 (#43 2-D guidance register, ADR-289), PR #78 (#73 3-D guidance register, ADR-3D-040). Guidance families short-circuit before the LLM in both apps; no-theft catalog sweeps locked. Full suite 3799 green (4 standing skips) + tsc + both builds clean on the deployed tree; both pages serve the new bundles (200). |
| 2026-07-11 | `prod/2026-07-11-6` | `130acb7` | 3-D (static only; proxy untouched) | `3d-D2rI8hG4.js` | Feature #69 (PR #70, ADR-3D-038): MULTI-LINE NAMING — digit-indexed line names ℓ1/ℓ2 (typed l1/l2, subscript tolerated; no primes, operator ruling); the "single-ℓ model" was a parser artifact — widened LINE_NAME + canonicalLine, ~10-rule sweep binding each matched name (was hardcoded ℓ), named-line operands in common-perp/projection; the 2010-Q3 form (two parametric lines + common perpendicular) builds closed-form He+En. Closes the deferred ADR-3D-028 enabler. Operator waived play-and-approve (commit-and-deploy). Full suite 3715 green (4 standing skips) + tsc + build:3d clean on the deployed tree; live page serves the new bundle (200). |
| 2026-07-11 | `prod/2026-07-11-5` | `0679227` | 2-D + 3-D (static only; proxy untouched) | `index-c8HfhWBZ.js` / `3d-Bq2OIDXu.js` | Feature #42 (PR #66, ADR-286 / ADR-3D-037): a persistent figure NAME — one inline-editable header input is both the name field and the visible title; save uses a set name directly (a prompted name is adopted); load derives the name from the FILENAME (embedded name = provenance only); outside undo history, clear resets; the 2-D docx question export takes the name as its title. Operator waived play-and-approve (build-now-and-deploy). Full suite 3701 green + both builds clean on the deployed tree; /healthz ok, both pages + bundles 200. |
| 2026-07-11 | `prod/2026-07-11-4` | `6adb13a` | 2-D (static only; proxy untouched) | `index-Dici3XwB.js` | Fix session — the honesty cluster + the #27 leftover guard: ADR-282 (#27 P1 — SHAPE_LEFTOVER gains side refs/כל quantifier/polygon nouns, quantified per-side semicircle escalates instead of half-parsing), ADR-283 (#31/#38 — the ב container marker governs over the חוסם/חסום verb letter, normalizeInscriptionSlip at the utterance boundary), ADR-284 (#28 — a semicircle on existing endpoints attaches closed-form: centre = midpoint, radius through an endpoint; freeRadius/autoCenter migration + apply (c3)/(c4)), ADR-285 (#52 — radical-aware ratio coefficients, AB=√2*OD). Closes #27/#28/#31/#38/#52. Full suite 3690 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-11 | `prod/2026-07-11-3` | `6131563` | 2-D + 3-D + **proxy** (restarted) | `index-Zy0I7UH3.js` / `3d-CbDe9Kuf.js` | ADR-278 privacy posture (external-review follow-up): retention default-on 7 d — prod env set 30→7 per operator ruling; IP salt never falls back to the committed constant (random per-boot + warning); in-app He/En privacy note (2-D About modal, 3-D footer); NFR-SE-3/US-4 reconciled. 3-D "גרסת פיתוח V0" tagline dropped. CI now builds the proxy bundle (#56). Closes #56/#57/#58. Full suite 3659 green on the deployed tree; /healthz ok, log path exercised live (204 + event landed), no salt warning in journal. |
| 2026-07-11 | `prod/2026-07-11-2` | `f8aa09e` | 2-D + 3-D (static only; proxy untouched) | `index-_wyA3c6e.js` / `3d-BPaU7JCv.js` | Save-name prompt copy → the operator's student-friendly wording (תנו שם לקובץ - מועד בחינה, עמוד וספר, או כל דבר אחר), both apps He+En. PR #25, closes #23. |
| 2026-07-11 | `prod/2026-07-11` | `b54b155` | 2-D + 3-D (static only; proxy untouched) | `index-BUwREL-D.js` / `3d-Cyj7eEy-.js` | Fix session closing the issue queue: #22 per-operand segment semantics (ADR-268), #17 noun cut operands + Q4 (ADR-269), #21 קוטר מנקודה F (ADR-270), #18 narrow-angle hover pick (ADR-271), #7 vacuous-collapse gate (ADR-272), #2 word-magnitude honesty (ADR-273), #20 named save files (ADR-274/3D-036), #6 ADR-124 unparked. |
| 2026-07-10 | `prod/2026-07-10-3` | `499675f` | 3-D (static only; proxy untouched) | `3d-BGQndf-i.js` | ADR-3D-034 — V8-j: a point on a segment positioned so a derived pyramid is RIGHT (apex above the base centre, closed-form t; honest no-solution when the segment misses the centre). **Completes V8** (full legacy-572 input coverage). PR #12, closes #11. |
| 2026-07-10 | `prod/2026-07-10-2` | `e39679a` | 3-D (static only; proxy untouched) | `3d-C0CHZumu.js` | ADR-3D-033 — a membership statement about an existing point DRIVES the figure (stage-4 member-pin re-solve, warm-started, transactional); fixes #9 (session `n6lmx1rj`, `M על מישור DCC'D'` refused not-on-plane). |
| 2026-07-10 | `prod/2026-07-10` | `6168d0c` | 2-D + 3-D (static only; proxy untouched) | `index-42UWO93R.js` / `3d-BJG80kY4.js` | 2-D: ADR-262 Am.1, ADR-264 (+Am.1/2) — clause split, honesty gates, hover/canvas fixes. 3-D: ADR-3D-030/031/032 — plane-eq as M1 given, pair-named parametric line, derived plane equations, symbolic coordinate. (Tag added retroactively at workflow adoption.) |
