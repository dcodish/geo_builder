# DEPLOY LOG — what is live on themathbible.com

Append-only, newest first. One entry per deploy, written **at deploy time** ([docs/22 §5](22-workflow.md)). Every entry pairs with a git tag `prod/YYYY-MM-DD[-n]` on the deployed commit. This file is canonical — deploy notes no longer accumulate in PROJECT-MEMORY. Pre-adoption deploy history (2026-07-04 … 2026-07-10) lives in PROJECT-MEMORY's operational notes.

| Date | Tag | Commit | App(s) | Bundle(s) | What changed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-29 | `prod/2026-08-29-2` | `b996d28` | **3-D** (static only; proxy untouched — no `server/` change in the range) | 3-D `3d-Dh3_yAuy.js` | **P1 prod-honesty fix — [#801](https://github.com/dcodish/geo_builder/issues/801) ([ADR-3D-174](06b-decisions-3d.md#adr-3d-174)).** On the operator's bagrut prism (`AA'=(k-1,k-7,k+1)` … panel «k = 2»), the exercise's own line «משוואת הישר AC היא x=(8,-1,-1)+t(k+1,0,k-3)» was refused `not-on-line: A` — blaming a correct given — while its bare form built GREEN with ℓ drawn at a sampled **k ≈ 0** and the panel flipping to «k = ?»: one letter with two values in one drawing. Root cause: the engine has two symbol-resolving mechanisms (the algebraic `c.param` root-find and the pivot's pin-symbol namespace) and #794 guarded only one direction, so `line3`/`plane3` opened a SECOND lane for a letter the pivot already owned. The letter now routes to its owner in one place (`paramLane`); a pin-symbol equation records its lane and is resolved from `pivot.pinSymbols` after every applied solution, never pre-pivot at another lane's value; the ADR-3D-033 membership drive — about MEMBERSHIP, but enumerating planes — gains `symLine`/`symPlane` member pins whose equation is evaluated at the trial symbol each LM iteration, so gauge, dims and k solve jointly; and the landing funnel's placement gates now count those memberships (they did not, so the funnel re-sampled the placement and slid the figure back off the line the drive had just put it on). Entry order no longer decides for the fact set (M2 re-homing). Siblings: the unreported PLANE cell fixed with it; 2-D and complex audited, class absent. Follow-up filed: [#815](https://github.com/dcodish/geo_builder/issues/815) (the algebraic lane has no membership drive at all — P2). Operator played and confirmed both forms before shipping. Gates on the deployed tree: full suite GREEN (530 files / 9626 tests, `sha=b996d28`), `tsc -b` + `build:3d` clean; live page serves the new bundle (200, `text/javascript`, byte-identical to the local build), proxy `/healthz` ok (untouched). |
| 2026-08-29 | `prod/2026-08-29` | `ad0f193` | **2-D + 3-D** (static only; proxy untouched — no `server/` change in the range) | 2-D `index-DjZ9L2cz.js` · 3-D `3d-W5cL4wpx.js` | **Fix-round [#800](https://github.com/dcodish/geo_builder/issues/800) complete — eight issues, operator-played before shipping.** Three landed direct on `main`: **2-D [#762](https://github.com/dcodish/geo_builder/issues/762) ([ADR-463](06-decisions.md#adr-463), `640ebdf`)** — `SHAPE_LEFTOVER` had no AREA/PERIMETER token, so «שני מעגלים משיקים מבחוץ ששטחם שווה» committed with the equal-areas relation silently dropped and a green ✓; the size vocabulary now joins the leftover accountant. **3-D [#754](https://github.com/dcodish/geo_builder/issues/754) ([ADR-3D-171](06b-decisions-3d.md#adr-3d-171), `8b5eb61`)** — a stated magnitude now pins the gauge-frozen solid's SCALE uniformly, so «קובייה» + «|AB| = 4» builds and reports real sizes instead of refusing. **3-D [#555](https://github.com/dcodish/geo_builder/issues/555) ([ADR-3D-173](06b-decisions-3d.md#adr-3d-173), `ea2b97e`)** — the 3-D LLM seam gains the SEQUENCE gate its 2-D twin already had. Five shipped as PRs: **[#760](https://github.com/dcodish/geo_builder/issues/760)/PR #804 ([ADR-464](06-decisions.md#adr-464))** a membership and its stated magnitude lower TOGETHER; **[#775](https://github.com/dcodish/geo_builder/issues/775)/PR #805 ([ADR-465](06-decisions.md#adr-465))** a side named by its ROLE («תיכון ליתר») resolves against the declared shape; **[#776](https://github.com/dcodish/geo_builder/issues/776)/PR #806 ([ADR-466](06-decisions.md#adr-466))** the terse label-first intersection «D מפגש המעגל עם AB»; **[#808](https://github.com/dcodish/geo_builder/issues/808)/PR #809 ([ADR-467](06-decisions.md#adr-467))** equal areas/perimeters on a circle pair ARE the equal-radii given — the #762 refusal becomes the capability, operator-directed mid-play; **[#774](https://github.com/dcodish/geo_builder/issues/774)/PR #807 ([ADR-3D-172](06b-decisions-3d.md#adr-3d-172))** a MIXED shape-declaration run has an owner (the refusal no longer blames a correctly-named apex). Gate: full suite GREEN at `528d622` (529 files / 9616 tests; 4 skips are all `skipIf(!env)` report generators), `tsc -b` + both builds clean, prod bundles verified serving `text/javascript`. Merge-pass notes: the five PRs were stacked, so each conflicted against `main` on `reports/test-tiers.json` alone (resolved to the trunk's measurement, then regenerated by the pre-deploy run) — filed as [#812](https://github.com/dcodish/geo_builder/issues/812); and a `--delete-branch` on the stack base auto-closed the next PR, which needed its base branch restored before it could be reopened and retargeted. |
| 2026-08-27 | `prod/2026-08-27` | `0f4f96b` | **2-D + 3-D** (static only; proxy untouched — the only `server/` change since the last deploy is a TEST file, loopback `/healthz` `ok`) | 2-D `index-CCrU30B4.js` · 3-D `3d-BVYAHgRp.js` | **Fix-round [#799](https://github.com/dcodish/geo_builder/issues/799), operator-validated before shipping — five items, seven issues, all honesty or parser-reach defects.** **2-D [#782](https://github.com/dcodish/geo_builder/issues/782) ([ADR-461](06-decisions.md#adr-461), `02f8330`):** the honesty-gate battery was a BLOCK OF CODE inside `submitPipeline`, so the ✎ EDIT seam — which never contained it — had none. Every `dropped*` gate added since ADR-089 was silently bypassable via ✎: editing a step to «מרכזו O. שתי נקודות על המעגל A ו B» committed with the two stated points gone. The battery is now one function (`app/honestyGates`) and BOTH seams call it; `commitEdit` moved out of the component to `app/editPipeline` (a seam nothing can call directly is a seam nothing tests, which IS the bug's mechanism) and refuses inline, no LLM on the edit path. **2-D [#784](https://github.com/dcodish/geo_builder/issues/784) + [#785](https://github.com/dcodish/geo_builder/issues/785) ([ADR-462](06-decisions.md#adr-462), `0f4f96b`):** both gates pattern-matched the lowering's OUTPUT — wrong for any lowering that TRANSFORMS what it read. Four operator-reported, corpus-locked constructions parsed correctly and were then refused at the commit seam and sent to the paid LLM: a DERIVED magnitude («היקף … 6» → `set-radius 0.9549`, so the stated 6 appeared nowhere; only «6π» escaped, by a SYMBOL exemption rather than a handled class) and three verbs encoded STRUCTURALLY (a referenced tangent, the midsegment theorem, the corner-tangent circle). The rule now DECLARES what it consumed and the gate ASKS. `KNOWN_GATE_FALSE_BLOCKS` is empty rather than silenced. **3-D [#766](https://github.com/dcodish/geo_builder/issues/766) + [#765](https://github.com/dcodish/geo_builder/issues/765) ([ADR-3D-169](06b-decisions-3d.md#adr-3d-169), `467d5f9`):** `volumePolyClaim` gated on "exactly 4 uppercase tokens + `=`" and `claims.ts` computed `|triple product| / 6` — correct only for an actual tetrahedron, selected BY LETTER COUNT. On «פירמידה ישרה מרובעת ABCDS», ABCD is the coplanar BASE, so a true given came back «check your arithmetic» while «= 0» was ACCEPTED. The subject now resolves against the DECLARED figure (full run or a FACE run; noun alone when unambiguous; ASK when not; refuse by name when nothing matches), and volume comes from the resolved solid's own faces. Corrects ADR-457's sibling-audit line, which had concluded 3-D needed no port. **3-D [#773](https://github.com/dcodish/geo_builder/issues/773) ([ADR-3D-170](06b-decisions-3d.md#adr-3d-170), `b5ee20d`):** a Hebrew↔Latin script transition is now a token boundary at the normaliser. «Eעל BB'» renders identically to «E על BB'» in an RTL box and only the second parsed — prod 2026-08-19…24 shows it reaching the paid LLM. #530 had fixed this at ONE rule; #494 fixed the mirror direction at the normaliser, and this is that fix's other half. **workspace [#750](https://github.com/dcodish/geo_builder/issues/750) ([ADR-W-033](06w-decisions-workspace.md#adr-w-033), `4aa00fa`):** the suite writes `reports/suite-verdict.json` — «green» is READ, never inferred from an exit code a pipeline destroys (`npm run test:full 2>&1 | tail -40` reads `tail`'s 0; that line gated the prod/2026-08-25-2 deploy). **This deploy is the first gated by that artifact:** `green:true, mode:full, sha:0f4f96b (= HEAD), dirty:false` — 523 files passed | 2 skipped, 9568 tests passed | 4 skipped, 0 failed; `tsc -b` + both builds clean. Live pages serve the new bundles (200), and the new code is stat-proven present in both (`גופים שמתאימים למשפט` in the 3-D bundle, `נשאר בלי שימוש` in the 2-D). **Known boundary shipped deliberately:** a stated size/area/volume still does not PIN the figure's scale, so «נפח הפירמידה ABCDS = 10» is understood and resolved to the right solid but still reported as not holding — [#754](https://github.com/dcodish/geo_builder/issues/754), skipped from the round and armed for the next one. |
| 2026-08-26 | `prod/2026-08-26-2` | `0469b33` | **2-D + 3-D + complex** (static only; proxy untouched — `server/` unchanged, loopback `/healthz` `ok`) | 2-D `index-CME-AcXs.js` · `geoWorker-C-32ZFcj.js` · 3-D `3d-Dt1VyJqz.js` · complex `complex-DASxxDM0.js` | **Same-day second deploy: the work-PC session's output, all of it operator-validated before shipping.** **3-D P1 honesty:** [#793](https://github.com/dcodish/geo_builder/issues/793) ([ADR-3D-167](06b-decisions-3d.md), `6bda87c`) — `injectionList` was a scan-and-harvest rule with no total-coverage requirement: it `matchAll`'d items and validated only the trailing tail, so text before the first item and between items was silently dropped by construction, and the item alternation could bind mid-run — «נתון: AB = (1,2,3)» matched `B = (…)` and reinterpreted a stated pair-vector as point coordinates, committing a wrong figure with no error. Two honesty invariants inverted at once. Every gap must now be separators only or the whole utterance defers, and a lookbehind stops an item starting inside a longer run. **complex:** [#788](https://github.com/dcodish/geo_builder/issues/788) ([ADR-CX-031](06d-decisions-complex.md), `ce4c8f8`) — stage 3e computed a measure verdict but `unsatisfied` was filtered to `deferred-*` residuals, and BOTH refusal surfaces read `unsatisfied`, so «אורך z1z2 = 99» on a determined figure was accepted as an ordinary fact with its ✗ hidden in the opt-in panel; violated measures now join that channel, so the gate refuses beside the input like 2-D (ADR-417). [#716](https://github.com/dcodish/geo_builder/issues/716) ([ADR-CX-030](06d-decisions-complex.md), `8af204b`, 25 files) — student-facing reasons were COMPOSED as Hebrew strings at the point of decision inside `solve/`, `model/`, `replay/` and the App, so the English UI answered in Hebrew and no toggle could reach the words; the engine now publishes reason CODES + params (`model/why.ts`) and the ADR-CX-015 reading seam words them through the product i18n, with an exhaustive switch making a missing wording a type error. **Merged PRs, played and approved:** [#525](https://github.com/dcodish/geo_builder/issues/525) (PR [#795](https://github.com/dcodish/geo_builder/pull/795), [ADR-W-032](06w-decisions-workspace.md)) — one `SymbolRow` on every text surface: the query boxes, the ask box and the fact-list editor can now type ∠ α ° ⊥ √, not just the main input, across all three builders. [#791](https://github.com/dcodish/geo_builder/issues/791) (PR [#792](https://github.com/dcodish/geo_builder/pull/792), [ADR-CX-033](06d-decisions-complex.md)) — capital letters are POINTS, case-sensitively: the exam's «הנקודות A ו-B מציגות את המספרים» register, with `d_{AB}` distances and the dual-name «A (z₁)» display. [#789](https://github.com/dcodish/geo_builder/issues/789) (PR [#790](https://github.com/dcodish/geo_builder/pull/790), [ADR-CX-032](06d-decisions-complex.md)) — the complex ask lane moves into the data panel, 3-D query-lane parity. Also `48bbb10` — the [ADR-460](06-decisions.md#adr-460) amendment recording the [#786](https://github.com/dcodish/geo_builder/issues/786) ruling (drawing wins when the tool can faithfully build). Gate: full suite on the deployed tree — **516 files / 9323 tests passed, 2 files / 4 tests skipped** (the standing skips), 252s, read from the summary lines rather than the exit code ([#750](https://github.com/dcodish/geo_builder/issues/750)); `tsc -b` + all three builds clean. Verified live: each page references this build's hash and each bundle returns `200 text/javascript` at its built byte size. **NOT in this deploy:** PR [#796](https://github.com/dcodish/geo_builder/pull/796) ([#794](https://github.com/dcodish/geo_builder/issues/794) + [#797](https://github.com/dcodish/geo_builder/issues/797)) is unmerged and was never playable this session — it has no operator play behind it. |
| 2026-08-26 | `prod/2026-08-26` | `e2911b0` | **2-D + 3-D + complex** (static only; proxy untouched — `server/` unchanged, `geo-proxy` `active` and loopback `/healthz` `200`) | 2-D `index-Cv1fyJxO.js` · `geoWorker-C-32ZFcj.js` · 3-D `3d-BEtTP8KL.js` · complex `complex-Cuq_1ZL1.js` | **All of fix-round [#783](https://github.com/dcodish/geo_builder/issues/783) — six fixes across three products, operator-played and validated — plus [#714](https://github.com/dcodish/geo_builder/issues/714) (PR [#787](https://github.com/dcodish/geo_builder/pull/787), played and approved).** **2-D honesty/parser:** [#771](https://github.com/dcodish/geo_builder/issues/771) ([ADR-458](06-decisions.md#adr-458), `191a443`) — JS `` is INERT around Hebrew letters, so every gate word was a bare substring test and `מקבילית` (parallelogram) matched `מקביל` (parallel): every parallelogram utterance was read as stating a parallel relation and correct parses were escalated to the paid LLM, which answered *not-understood* three times in prod for a sentence the grammar had already parsed. The boundary now lives in `lexicon.ts` (the registered home for Hebrew morphology) and the gates compose from it; `satisfied` became whole-token, so `parallelogram` is evidence by intent rather than by luck. [#772](https://github.com/dcodish/geo_builder/issues/772) ([ADR-459](06-decisions.md#adr-459), `b67243e`) — the radius statement's value slot was number-only without a copula, so «רדיוס המעגל 5» built and «רדיוס המעגל R» did not; a student had to re-declare the whole circle to attach its radius (session `ah1kqxz5`). One slot, two value KINDS. [#763](https://github.com/dcodish/geo_builder/issues/763) ([ADR-460](06-decisions.md#adr-460), `8e8443e`) — an utterance packing INDEPENDENT constructs is now TAUGHT one fact at a time instead of being decomposed and built by the LLM seam («שני מעגלים משיקים מבחוץ ואלכסון AB» used to draw two circles plus a segment belonging to nothing, green ✓). Derived from the tool itself — a clause stands alone when it parses AND builds from an empty figure — never a noun list. Ships four KNOWN, recorded catalog false positives that the seam's placement makes unreachable, with a net that fails if any becomes reachable. **3-D:** [#780](https://github.com/dcodish/geo_builder/issues/780) ([ADR-3D-165](06b-decisions-3d.md#adr-3d-165), `541248b`) — the typed crossing forked on the PLANE's form, so a point-run plane re-created a solid's EDGE as an unbounded line (a full-height `CC'` the student never asked for). A segment operand now references drawn ink whatever the plane's form, the crossing is bounded to the ink (new `crossing-off-segment` status), and apply draws a not-yet-drawn carrier as a bounded segment. [#613](https://github.com/dcodish/geo_builder/issues/613) ([ADR-W-031](06w-decisions-workspace.md#adr-w-031), `83b08e8`) — restating a fact appended a duplicate row (M1 idempotency was at apply; the store appended anyway). Now it SUCCEEDS, adds no row, and says so. Cross-product rule; 2-D measured already conformant and gained the conformance test. [#714](https://github.com/dcodish/geo_builder/issues/714) ([ADR-3D-166](06b-decisions-3d.md#adr-3d-166), `ff9be77`) — named view presets (front/top/side/isometric) beside `↺`, the ALIGN half orbit does not give; `top` clamped to `MAX_PITCH` because straight down degenerates the frame, `iso` is TRUE isometric and deliberately not the ¾ home view. **complex:** [#688](https://github.com/dcodish/geo_builder/issues/688) ([ADR-CX-029](06d-decisions-complex.md#adr-cx-029), `c7f6aaa`) — «z1 מדומה טהור» drew z₁ at 189°, contradicting what the student typed, while the panel honestly read `unknown`. A claim whose relevant DOF is FREE now lowers to a constraint (`imaginary` → arg ∈ {90°,270°}); over a determined subject it still only checks, so «z1 = 3+4i» + the claim stays refuted at 53.13°. Gate: `npm run test:full` on the deployed tree — **511 files passed / 2 skipped, 0 failed; 9252 tests passed / 4 skipped**; `tsc -b`, `build`, `build:3d`, `build:complex` clean. |
| 2026-08-25 | `prod/2026-08-25-2` | `13edd1d` | **2-D** (static only; proxy untouched — `server/` unchanged, `/healthz` verified `ok`) | `index-0OHUVgfL.js` · `geoWorker-C-32ZFcj.js` | **The two P1 fixes of fix-round [#781](https://github.com/dcodish/geo_builder/issues/781), operator-played and validated before deploy.** Both are honesty defects where the tool silently asserted something the student never stated. **[#779](https://github.com/dcodish/geo_builder/issues/779) ([ADR-456](06-decisions.md#adr-456) + Am., `fae0835`):** the `dropped*` honesty gates match `[A-Z]` while the parser upper-cases on the build path, so a lowercase-labelled given tripped **nothing** and committed green with the stated points gone — `מרכזו O. שתי נקודות על המעגל A ו B` and its lowercase twin produced the identical broken lowering, but only the uppercase one escalated (prod logged the bad branch as `parser/ok`, session `qouua77n`). The gate layer is now case-blind and lowercase is **TAUGHT** — refused with a short note whose suggestion is the student's own text uppercased ([ADR-W-030](06w-decisions-workspace.md#adr-w-030)), never silently rewritten; measure letters (`שרדיוסו r`) keep building, with a note. Two operator rulings were taken live mid-round and recorded as the ADR's amendment. **[#770](https://github.com/dcodish/geo_builder/issues/770) ([ADR-457](06-decisions.md#adr-457), `10efc58`):** a definite shape reference resolved on ring *shape* rather than declared *kind*, so `אלכסוני הריבוע` drew the **trapezoid's** diagonals under a green ✓ — a square that is not in the figure borrowing another shape's geometry. The figure now remembers what each ring was **declared** to be (a new optional `declaredAs` on the base commands, since the cyclic-declaration route lowers named quads to the generic base), and refuses by name when the shape is absent; two quads in one figure each bind their own diagonals. The En mirror exposed a #779-class bug inside the qualifier regex itself ("meet" read as labels M,E,E,T), fixed to uppercase-run capture. Gate: `npm run test:full` GREEN on the deployed tree — 503 files passed / 2 skipped, 9140 tests passed / 4 skipped, **0 failed**; `tsc -b` + 2-D build clean. Live page references the new bundle; page + both chunks 200 (`text/javascript`); proxy `/healthz` ok. Round [#768](https://github.com/dcodish/geo_builder/issues/768) closed the same session (its items validated after the fact — they shipped in `prod/2026-08-25`). Filed from this round, NOT deployed: [#782](https://github.com/dcodish/geo_builder/issues/782) (the ✎ edit seam runs none of the gate battery). |
| 2026-08-25 | `prod/2026-08-25` | `fed09ac` | **2-D + 3-D + complex** (static only; proxy untouched — `server/` unchanged since `prod/2026-08-18`, verified still active from that date) | `index-DDhrNDgZ.js` · `3d-CIU3Q0qh.js` · `complex-DA06rnDC.js` | **Alignment deploy — the two landed items of fix-round [#768](https://github.com/dcodish/geo_builder/issues/768), which had sat undeployed since 2026-08-19, plus the triage session's ADR.** Prod was found six commits behind while triaging: the live bundles were `index-CNzel0Ep.js` (2-D, from `prod/2026-08-19-3`) and the `prod/2026-08-19-2` bundles for 3-D and complex, so neither fix commit was serving. **[#751](https://github.com/dcodish/geo_builder/issues/751) ([ADR-W-029](06w-decisions-workspace.md#adr-w-029), `16e3f1a`):** a stored utterance holds what the STUDENT stated — the example chips baked i18next bidi isolates (U+2066–U+2069) into the utterance they submitted, so presentation characters reached the fact list, saved files, the prod usage logs and printed as missing-glyph boxes in the `.docx`. The invariant is enforced at each product's fact-ingest chokepoint **including on load**, so files already in the wild clean up; `QuickChips` gained a label≠command contract; one shared `stripFormatControls` in `shell/bidi.ts` replaces three copies. Amends [ADR-3D-144](06b-decisions-3d.md#adr-3d-144), whose reasoning had held that the stored fact stays raw. **Operator-validated on this deploy's tree.** **[#755](https://github.com/dcodish/geo_builder/issues/755) + [#756](https://github.com/dcodish/geo_builder/issues/756) ([ADR-3D-164](06b-decisions-3d.md#adr-3d-164), `1b95d8c`):** one root cause — the 3-D crossing capability was written against NAMED tokens while a student's crossing line is drawn ink. #755: three rules split the line∩plane cell and each generalised one side, leaving the common square (segment × point-run, noun frame) empty; one rule now owns it and classifies both operands through `readOperand`, so kinds decide and nouns never. #756: `openCrossings3` looped only NAMED lines, so the clickable-crossing offer was structurally dead on any solid figure — the candidate set is now derived from named lines + solids' edges + auxiliary segments, with segments bounded `0 < t < 1`. **Partial validation, deployed knowingly:** the operator played this deploy's tree and validated #751 and the #756 offer (the clickable dot on `CC'`), but found the **typed** crossing form draws a new unbounded line for an operand that is already an edge — «G נקודת חיתוך של CC' עם מישור ADE» adds a full-height `CC'`, and «הקטע» is consumed and discarded. Filed as [#780](https://github.com/dcodish/geo_builder/issues/780) (P2) — it is the *drawing* half ADR-3D-164 explicitly deferred to the operator's eye. Deployed on the operator's explicit call: the affected utterances are **refused** on the previous build, so nothing working regresses, and the offer half is a clear gain. Also carried: **[ADR-W-030](06w-decisions-workspace.md#adr-w-030)** (docs only) — the operator's ruling that non-canonical input is TAUGHT, never silently accepted, workspace-wide. Gates on the deployed tree: `npx vitest run` **501 files / 9097 tests passed, 0 failed** (2 files + 4 tests skipped, pre-existing), `VITEST_EXIT=0`; `tsc -b` clean; all three builds clean. **The gate was run bare rather than via `npm run test:full`** — `test-tiers.mjs` is the subject of open [#750](https://github.com/dcodish/geo_builder/issues/750), whose escalation established that the status is destroyed at the CALL SITE by a `| tail` pipeline, so a piped `test:full` cannot be trusted as green. Verified live: all three pages 200 serving the new bundles as `text/javascript`; proxy `active` since 2026-08-18 20:51 UTC, i.e. not restarted. Baseline at deploy: no open PRs, tree clean at `fed09ac`. |
| 2026-08-19 | `prod/2026-08-19-3` | `e12cb74` | **2-D only** (static; proxy untouched — `server/` unchanged since `prod/2026-08-18`; 3-D and complex not rebuilt, their bundles unchanged) | `index-CNzel0Ep.js` | **The #659 span-accounting arc, end to end, plus the P1 it surfaced.** **[#659](https://github.com/dcodish/geo_builder/issues/659) steps 1–2:** the accountant's "76 missing words" turned out to be **three MATCHER defects**, not vocabulary — greedy Hebrew prefix stripping («במעגל» → «עגל»), the final-letter trap («תיכון» could never prefix-match «התיכונים»), and English stems stored as inflected forms (`cuts` never matched `cutting`). Debt to zero; the locks target the mechanisms, not the word list. **[#659](https://github.com/dcodish/geo_builder/issues/659) step 3 — the operator's enforcing flip (PR [#759](https://github.com/dcodish/geo_builder/pull/759), [ADR-453](06-decisions.md#adr-453)):** span accounting now REFUSES on hard spans (label/number/relation) at both submit seams. It **joins** the `dropped*` gate family rather than replacing it — the replacement was implemented, measured, and backed out when the ADR-264 relation class proved invisible to the accountant (one global `hasConstraint` flag), so retirement got a differential test as its criterion instead of an intention ([#758](https://github.com/dcodish/geo_builder/issues/758)). `unknown-word` stays a report bucket: enforcing it would false-refuse working input. **[#757](https://github.com/dcodish/geo_builder/issues/757) — P1, found by probing off-corpus input ([ADR-454](06-decisions.md#adr-454)):** «שני מעגלים משיקים מבחוץ ברדיוסים שווים» built GREEN with radii 5 and 3.6, the stated equality silently dropped and no gate firing — `circlesTangent` read a tangency kind and a circle pair and discarded everything else. Fixed at the class with the ADR-024 leftover guard (strip what the rule consumes, decline whole if anything geometry-significant remains), closing unreported siblings too. **[#761](https://github.com/dcodish/geo_builder/issues/761) (PR [#764](https://github.com/dcodish/geo_builder/pull/764), [ADR-455](06-decisions.md#adr-455)):** the capability half — the equal-size modifier now BUILDS, lowering to `set-radius-ratio(k=1)` across the two-circle family; INTERNAL tangency + equal radii is refused from the WORDS per operator ruling, because at the degenerate collapse every stated relation holds (ratio IS 1, coincident circles are internally tangent in the limit) so no verifier downstream could catch it. Gates: full suite 496 files / 9059 tests, 0 failed; `tsc` + build clean. Baseline check at deploy: all 34 remote branches merged into `main`, no open PRs, tree clean. |
| 2026-08-19 | `prod/2026-08-19-2` | `0428dd4` | **2-D + 3-D + complex** (static only; proxy untouched — `server/` unchanged since `prod/2026-08-18`) | `index-BiawWkR2.js` · `3d-CGRqzEeM.js` · `complex--1P6UVPk.js` | **Fix-round [#752](https://github.com/dcodish/geo_builder/issues/752) (4 items) + the question document reaching the 3-D builder (PR [#746](https://github.com/dcodish/geo_builder/pull/746)).** **[#640](https://github.com/dcodish/geo_builder/issues/640)+[#642](https://github.com/dcodish/geo_builder/issues/642), closes [#504](https://github.com/dcodish/geo_builder/issues/504) ([ADR-3D-160](06b-decisions-3d.md#adr-3d-160)):** a defining statement's HEAD is read once — noun, article, name, separator — so the exam's own «ישר l x=…» parses deterministically instead of costing a paid LLM call for a capability shipped in 2024-Q2; measuring the matrix also found `planeByEquation` reading a spaced dash as a UNARY MINUS, so «מישור π1 - x+…=0» silently built a different plane than the student wrote, and #504's «= 0»-less form now builds. The query lane's missing subject noun («נקודה A» was «לא זוהה») fixed at the gate, plus three swept siblings incl. the ASCII `\w*` that could never match «קואורדינטות». **[#639](https://github.com/dcodish/geo_builder/issues/639) ([ADR-3D-161](06b-decisions-3d.md#adr-3d-161)):** the knowledge frame-gate was an ENUMERATION one member short for the whole algebraic lane — a figure framed by an equation plane + parametric line had every knowledge surface withheld while the engine held A=(2,0,−10) exactly, and the query lane answered «לא נקבע על ידי הנתונים» on the exam part that asks for it. It now asks the placement funnel's own question, with the over-reach guard locked (the cube's vertices stay silent while the Lane-A crossing prints). **[#571](https://github.com/dcodish/geo_builder/issues/571) ([ADR-3D-162](06b-decisions-3d.md#adr-3d-162)):** «מישור BB'DD'» was refused `not-coplanar` — false — because the Newell normal is order-sensitive and a bowtie order cancels to zero; one shared order-free run normal, orientation and every refusal preserved, and the drawn 'face' patch takes the non-crossing reorder. **[#723](https://github.com/dcodish/geo_builder/issues/723) partial ([ADR-W-026](06w-decisions-workspace.md#adr-w-026)):** 2-D and 3-D drop their private rounders for `shell/fmtNum`; two cells escalated to the operator (the 3-D canvas's three decimals under #491, and the complex `value/` layer which cannot import `shell/`), so #723 stays open. **[#745](https://github.com/dcodish/geo_builder/issues/745) ([ADR-W-027](06w-decisions-workspace.md#adr-w-027), [ADR-3D-163](06b-decisions-3d.md#adr-3d-163)):** «הורידו שאלה» in the 3-D builder over a SHARED composer + rasteriser moved to `shell/export/`; the 3-D givens list is each enabled fact verbatim; complex's n/a is asserted by test, not left as a gap. Operator played and approved. Gates on the deployed tree: `npm run test:full` **495 files / 9034 tests passed, 0 failed** (2 files / 4 tests skipped, pre-existing); `tsc -b`, `build`, `build:3d`, `build:complex` all clean. All three live pages serve the new bundles (200, text/javascript); the served 3-D JS carries the new catalog forms and the «הורידו שאלה» string. Proxy untouched. Known follow-ups filed from this work: [#751](https://github.com/dcodish/geo_builder/issues/751) (chip bidi isolates reach the .docx — the display defect seen while playing #746), [#753](https://github.com/dcodish/geo_builder/issues/753), [#754](https://github.com/dcodish/geo_builder/issues/754). |
| 2026-08-19 | `prod/2026-08-19` | `dbe4efe` | 3-D (static only; proxy untouched — `server/` unchanged since `prod/2026-08-18`) | `3d-BDetH78N.js` · `3d-DkVMJLWs.css` | **A segment ratio stated as its OWN fact now determines an on-segment rider** ([#748](https://github.com/dcodish/geo_builder/issues/748), PR [#749](https://github.com/dcodish/geo_builder/pull/749), [ADR-3D-159](06b-decisions-3d.md)). Operator prod report: «מקבילון» / «E על AA'» / «AE=2*EA'» refused the third line with «הטענה לא מתקיימת בציור» — a given that is not merely satisfiable but closed-form DETERMINED (t = ⅔), refuted against a configuration the tool had sampled itself (the ADR-052 cardinal sin). Root cause: the ratio→t reading existed and was correct since the first 3-D commit, but `ratioT` was called only from inside the `onSegment` parser rule against the DECLARATION string, so the same ratio typed as its own fact never reached it — a capability bound to one code path rather than to the concept. Not a missing DOF: a bare «E על AA'» already emits `t` absent. Fixed by giving the arithmetic one home (`engine/onSegmentRatio.ts`, shared by parse3 and apply) and normalizing a rider ratio into the `point-on-segment3` given it is at the one `applyCommand3Inner` entry; a stated `t` on an existing FREE rider is now a definition update rather than the vec-rel claim that refuted it. No solver change — the statement fixes `t` exactly. **Second commit, from the operator's play:** a LENGTH pair is unordered, so «|AE|=2|A'E|» is the same given as «|AE|=2|EA'|» — the first cut had imposed the vec-rel chain form on every shape and refused the whole length family (`|A'E|=0.5|AE|`, `אורך AE = 2*A'E`, `AE:A'E = 2:1`, `A'E:AE = 1:2`). Orientation is now the caller's business: only `vec-rel` is `directed`. The bare `AE = 2*A'E` still refuses — genuinely ambiguous. Also carried: the already-on-main [#747](https://github.com/dcodish/geo_builder/issues/747) docs/chore `-next` retirement (`802f2e3`, no 3-D artifact impact). Gates on the deployed tree: full suite **489 files / 8940 tests green** (4 pre-existing env-gated skips), `tsc --noEmit` clean, `build:3d` clean; 26 locks in `issue-748-rider-ratio.test.ts`; `shadow-matrix3` winner snapshot grew by exactly the two rows of the new catalog entry with `shadow-allowlist3.json` untouched. Live page 200 and the served asset hashes match the built bundle exactly. |
| 2026-08-18 | `prod/2026-08-18` | `521ea20` | **2-D + 3-D + complex static AND proxy** (`server/` changed) | `index-BP4o8gbn.js` · `3d-BdGMb37E.js` · `complex-CErJunWt.js` · `proxy.mjs` | **THE SWITCHOVER — the unified interface becomes the canonical build, and the `-next` channel is torn down** ([#747](https://github.com/dcodish/geo_builder/issues/747), [ADR-W-025](06w-decisions-workspace.md#adr-w-025); operator: *"deploy the changes… the -next hidden page is no longer needed, turn this code to the baseline"*). 73 commits since `prod/2026-08-17-4` — the whole Track B unification now on the student URLs: shared design tokens, three-column Workbench + mobile stacking (#737), the shared header/product switcher, one input area, the fact list, the data panel, the under-canvas row CONTRACT (#738, #739, #743) and the canvas chrome contract (#742); the complex builder's clean default canvas + cartesian view (#703, #722); 3-D canvas fit + canvas-relative orbit (#718, #724); **the 2-D theorem surface ships DISABLED** (#740 — flag only, engine stays live); server side, the product registry + operator config admin surface (#661, #662) — hence the proxy rebuild + `systemctl restart geo-proxy`. `unify/ui` was fully merged into `main` first, so this was an ordinary Standard deploy: **the ADR-W-020 exception (a channel deploying branch state) is now closed and `main` → canonical is the only deploy path.** Teardown done at deploy time: `geo-builder-next/` + `3d-builder-next/` `rm -rf`'d (both now 404), `build:next:*` scripts deleted, RUNBOOK section reduced to a historical pointer. **Operator's remaining step: remove the two `-next` api mappings from the Plesk directives field** (inert now — they map to nothing). Gates on the deployed tree: `test:full` 488 files / 2 skipped, **8910 passed / 4 skipped, exit 0**; `check:siblings: PASS`; builds clean. Verified live: all three pages 200 serving the new hashes, `/healthz` `ok`, both admin routes answer through Apache. |
| 2026-08-18 | `next/2026-08-18-2` | `0063183` | **`-next` channel ONLY** (canonical byte-untouched, stat-proven again: both 2026-08-15 20:45) | `index-CJZKC-vG.js`-era rebuilds of both `-next` paths | **The MOBILE fix on the evaluation channel** ([#737](https://github.com/dcodish/geo_builder/issues/737); operator: "on mobile the sites look crap"). Root cause at the one layout component: the Workbench's desktop three-column row collapsed to ~117px slivers at phone width. Below 900px it now STACKS (canvas card → input → data) with native page scroll; the frame's suite/tool rows wrap; the builder strip takes `maxWidth:100%` + `overflow-x:auto` so it can never drag the page sideways; the title block shrinks instead of clipping. Verified 390×844 ×3 tools: `scrollWidth == innerWidth`. Gates: `test:full` 483 files / 8875 passed / 4 skipped / exit 0 on the deployed commit; `sibling-safety: PASS`. `unify/ui` fast-forwarded to the same commit (the channel's source per the runbook). Degraded-api caveat unchanged. |
| 2026-08-18 | `next/2026-08-18` | `cea103e` | **`-next` channel ONLY** — `/geo-builder-next/` + `/3d-builder-next/` (canonical paths byte-untouched, stat-proven: both still 2026-08-15 20:45) | `index-Bj0mPg9U.js` (2-D), `3d-Nu2Js6rG.js` (3-D) | **THE UNIFIED INTERFACE on the parallel evaluation channel** (ADR-W-020; programme [#648](https://github.com/dcodish/geo_builder/issues/648) complete; merged to `main` as PR [#736](https://github.com/dcodish/geo_builder/pull/736) at whole-interface acceptance). Everything Track A+B built, operator-played PR by PR: the shared frame (suite bar + builder strip + curriculum names + About/privacy, ADR-W-019 both flips executed), the shared input area (palette, maths/bidi preview, wrap-selection), the shared fact list (mute/edit/delete with the 2-D selection accent), the shared data panel (D8 skeleton, DOF-count status, pull-only values), the level model (figure actions under the canvas), the one-page Workbench layout ([#734](https://github.com/dcodish/geo_builder/issues/734) — zone geometry parity-locked at the pixel level across all three tools), the manual screens ([#665](https://github.com/dcodish/geo_builder/issues/665)+[#672](https://github.com/dcodish/geo_builder/issues/672) — catalog-backed, six representatives per section, click-builds), tab titles = tool names. Gates on the deployed tree: `test:full` 483 files / 8875 passed / 4 skipped / exit 0; `PARITY: PASS` incl. zone probes; scroll probes 850/850 ×3. **Degraded-path caveat (runbook §-next):** the Apache `/geo-builder-next/api` + `/3d-builder-next/api` mappings are NOT yet configured, so the `-next` builds run without logging/LLM-fallback and with the static roster — fine for layout/interaction evaluation; map the api paths (one-time Plesk step) to lift it. Canonical prod runs the pre-unification builds until the operator's switchover. |
| 2026-08-17 | `prod/2026-08-17-4` | `ab8beca` | **complex ONLY** (static; proxy untouched) | `complex-BBdyugu7.js` | **THE CUTOVER — the prototype engine is deleted, and v2 is what students get** (PRs [#692](https://github.com/dcodish/geo_builder/pull/692), [#693](https://github.com/dcodish/geo_builder/pull/693), [#695](https://github.com/dcodish/geo_builder/pull/695), [#696](https://github.com/dcodish/geo_builder/pull/696), [#697](https://github.com/dcodish/geo_builder/pull/697); closes [#616](https://github.com/dcodish/geo_builder/issues/616) and [#624](https://github.com/dcodish/geo_builder/issues/624); ADR-CX-025/026/027). Every deploy before this one ran the **C0 prototype**: a float evaluator with per-fact sweeps. This one runs the log-polar constraint engine — exact linear algebra over ℚ on two vector spaces, branch enumeration as the exam's «כל האפשרויות», and the acceptance gate above it. **−3,943 lines**: `engine/complex.ts`, `engine/model.ts`, `parser/parse.ts`, `model/fact.ts`, `render/GaussPlane.tsx`, `bridgeFacts`, `sceneFromDerived2`, the store's second submit path, and the `?engine=v2` switch with everything that branched on it. One entry path now, which is the fix for a class that cost three issues (#658, #680, #686): *a capability reachable from only one of two paths is invisible to tests aimed at the other.* **It also carries a P1 honesty fix the cutover measurement found** ([#690](https://github.com/dcodish/geo_builder/issues/690)): a quadrant or argument-window given about a name that Gaussian elimination made *dependent* was **silently dropped** — «z3 ברביע הראשון» + «arg z3 + arg z2 = 0» drew z3 at 219.52° with every honesty signal clean. It cost the docs/27 §2b capstone, which built at 243.44° with a perimeter of 72.8452r where it must build at arctan ½ and answer **60r**. Fixed at the chokepoint (LADDER-CX 2b projects the window onto the basis coordinate that carries it) plus a stage-3e backstop that re-verifies every filter against the DRAWN direction, reported through `unsatisfied` so the gate blames the offending line. Plus four grammar-parity gaps ([#691](https://github.com/dcodish/geo_builder/issues/691)): quadrant word order («ברביע הראשון z2», «נמצא»), the spaced generic polar form («z1 = r cis θ»), «|z1| = 9w» refused instead of silently re-read, and «w1 = |z1|» read as the definition it is rather than a modulus-only relation. Gates: `test:full` 469 files / 8719 tests / 0 failures on the merged commit; `check:siblings` PASS — 2-D and 3-D untouched. Named gaps left named: [#688](https://github.com/dcodish/geo_builder/issues/688), [#694](https://github.com/dcodish/geo_builder/issues/694), and the rest of the 572 booklet's Q3s as fixtures. |
| 2026-08-17 | `prod/2026-08-17-3` | `eab0427` | **complex ONLY** (static; proxy untouched) | `complex-BK__zZh4.js` | **Nothing to cycle: the button reads one published answer** (PR [#683](https://github.com/dcodish/geo_builder/pull/683), [#680](https://github.com/dcodish/geo_builder/issues/680), ADR-CX-020). Operator ruling, 2026-08-17, answering #680's design question: *«if there are no dofs left, the button can be disabled»*. That settles more than it was asked — the button's meaning is not «walk the branch index» but **«is there another drawing to show?»**, one question with one answer whatever produces the alternatives. `Derived2.canCycle` publishes it (`configCount > 1 || remainingDof > 0`) and the component READS it rather than recomputing, for the reason every count in this engine is published once (ADR-CX-006): the DOF cue, the knowledge gates and this button must not be able to disagree about how free the figure is. Today a determined figure greys the button out — it could not change the picture, and a control that visibly does nothing tells a student their figure might be wrong when it is simply *determined*. For #680 it unblocks materialising «z³ = 8» as three NAMED points in ONE configuration: the three solutions stop being three configurations and the button switches itself off for that figure, so the roots case needs no rule of its own. The rest of #680 — the enumeration, the reserved bare letter, the anonymous set of §2b part ד — is still to build and is still why [#616](https://github.com/dcodish/geo_builder/issues/616) cannot close (ADR-CX-019). **Prod default UNCHANGED — behind `?engine=v2`.** Gates: `npm run test:full` green — 466 files / 8728 passed, 4 skipped (pre-existing); `tsc -b` clean; `check:siblings` PASS. Verified live: served `index.html` references the new hash, JS 200 `text/javascript`, `/geo-builder/` + `/3d-builder/` 200, proxy untouched. |
| 2026-08-17 | `prod/2026-08-17-2` | `fb437e7` | **complex ONLY** (static; proxy untouched) | `complex-AxBxC7fq.js` | **S7's cutover gate, MEASURED — eight capabilities rebuilt, one blocker named** (PR [#682](https://github.com/dcodish/geo_builder/pull/682), [#624](https://github.com/dcodish/geo_builder/issues/624), ADR-CX-019). The gate on deleting the prototype is *«if v2 cannot cover something the prototype covers, STOP and report it rather than deleting the capability»* — so it was measured rather than argued: every form the prototype's 76-test suite exercises, run through BOTH grammars, difference printed. **That measurement is now a permanent test** (`parser/__tests__/cutover-parity.test.ts`) — a form the prototype reads and v2 does not FAILS the suite. Nine gaps found. **Eight were grammar and are built:** «z1 מספר מרוכב» (F1 spelled out — and the only way to declare a non-z/w name complex at all) · the word-spelled operators «הצמוד של», «ההופכי של», «החלק הממשי של», rewritten at the orthography chokepoint beside the combining overline · θ/α/β normalized to their Latin spellings (two spellings of one parameter were two different parameters) · `z1 = 2cis(θ)`, which states the MAGNITUDE and leaves the direction free instead of refusing the line and dropping what the student said · **`arg z2 < 45` and `90 < arg z1 < 180` — F4's INEQUALITY half**, where the engine has carried `BranchFilter.range` since S2 and the sampler its window since S3, so only the sentence was missing and the §2b capstone's own branch selector could not be typed · `re(z1)`/`im(z1)` as real projections lowered to `(z ± z̄)/…` · **a bare expression is a QUESTION** answered by the knowledge rule — the prototype's calculation panel rebuilt honestly, «|z1-z2|» reading `15r` over the §2b givens with the degree MEASURED under the scale symmetry rather than declared. **Two defects found by the measurement, both fixed at root:** `im(z1)` lexed as the NAME `im` times z1, so a stated projection silently became a product with an invented parameter (the same defect the TOKEN comment already records for `2cis150`, in a second place); and the first bare-expression rule read «triangle Oz1z2z3» — which the shape rule REFUSES on purpose for the wrong vertex count — as an implicit product, a catch-all quietly undoing an honesty refusal. **The ninth gap is not grammar and BLOCKS the cutover:** a solution set is not referencable in v2 (the prototype's «z³ = 8» names z₁,z₂,z₃; v2 draws one point and walks three configurations), three prototype tests depend on it, and ADR-CX-005 specifies the replacement that is not implemented — deleting the prototype today would delete a capability rather than replace it, so **[#616](https://github.com/dcodish/geo_builder/issues/616) stays open and the prototype remains the default engine** until [#680](https://github.com/dcodish/geo_builder/issues/680) is built (it needs an operator ruling: what branch cycling cycles once all n roots are drawn at once). **Prod default UNCHANGED — all of it behind `?engine=v2`.** Gates: `npm run test:full` green on the merged tree — 8723 passed, 4 skipped (pre-existing); `tsc -b` clean; `check:siblings` PASS; complex tree 510 → 548 tests. Verified live: served `index.html` references the new hash, JS 200 `text/javascript`, `/geo-builder/` + `/3d-builder/` 200, proxy `/healthz` ok (untouched). |
| 2026-08-17 | `prod/2026-08-17` | `19ba270` | **complex ONLY** (static; proxy untouched) | `complex-cq0QUOo6.js` | **The visualization + knowledge batch: S5, the S6 remainder, and two silent-drop root causes** (PRs [#676](https://github.com/dcodish/geo_builder/pull/676) · [#677](https://github.com/dcodish/geo_builder/pull/677) · [#678](https://github.com/dcodish/geo_builder/pull/678) · [#679](https://github.com/dcodish/geo_builder/pull/679); [#616](https://github.com/dcodish/geo_builder/issues/616), [#622](https://github.com/dcodish/geo_builder/issues/622), [#623](https://github.com/dcodish/geo_builder/issues/623), ADR-CX-015…018). **[#675] a plotted number carries a READING, composed once** (ADR-CX-015) — «z1 = 3+4i» drew a bare `z₁` while the banner an inch above printed `z₁ ≈ 5·cis53.1301°`: the renderer was deciding a presentation it had nothing to decide with (`exact` nullable → fallback to *nothing*), and `exactLabel === null` was read as «nothing to say» when it means «no symbolic rendering exists». Stage 5d now composes one non-null `reading` (`=` closed form · `≈` forced but decimal · `~` on a sampled half) and BOTH surfaces print it; `v2Labels` reads that field instead of re-deriving it, which is the half that prevents recurrence. Three drifted copies of `prettyName` collapsed to one. **S5 — the visualization layer** (ADR-CX-016, closes #622): TermSpiral (a geometric sequence IS a log-spiral; a circle when |q|=1, a ray when arg q=0, a line when arithmetic), SumChain (the limit point when |q|<1, the closing polygon when the sum is 0), RotationArc (multiplication as turn-and-stretch), ValueCycle + the `n` stepper (period DECIDED from the exact carriers — a sampled 59.9999° has no period), Region (inside/on/outside, the §2b ד counting picture). The polar/cartesian toggle is now PROVEN display-only over the whole catalog, both languages, through the real store. **S6 remainder** (ADR-CX-017): «הביעו באמצעות r» answered — `15r`, `54r²`, `36r` — by a checked predicate («the remaining freedom is exactly the symmetry group»: scale and rotation are APPLIED and verified against every live residual, never inferred from sampling variance); the official formula sheet transcribed to `docs/29` and byte-matched BOTH ways to `formulas/table.ts` (a one-way check would let the table grow a fourth formula the sheet does not carry), surfaced structurally with each row naming its premises; F12 quantified claims — «לכל n טבעי w^(4n) ממשי» and «ה-n המינימלי … הוא 5» — decided by congruence on turns, with an n that works but is not least refuted *naming the least*. **G7 + G8** (ADR-CX-018) and the defect they uncovered: «z1 + z2 = 5+2i» produced NO drive, NO refusal and NO row — #675's root cause in a second place (an exact carrier with no symbolic form read as having no value) plus an unevaluable relation dropped from the live system without being reported. Both fixed at root; `Derived2.undecided` now exists and the banner prints it. **Prod default is UNCHANGED — all of it is behind `?engine=v2`.** Known open, filed rather than implied: [#680](https://github.com/dcodish/geo_builder/issues/680) (solution SETS as first-class — the substrate G4/G5/G6 need), [#681](https://github.com/dcodish/geo_builder/issues/681) (G9 + the F13 locus layer), and **S7 ([#624](https://github.com/dcodish/geo_builder/issues/624)) is untouched — the prototype is still the default engine and #616 is NOT closed.** Gates: `npm run test:full` green on the merged tree — 8673 passed, 4 skipped (pre-existing); `tsc -b` clean; `check:siblings` PASS; complex tree 370 → 510 tests. Verified live: served `index.html` references the new hash, JS 200 `text/javascript` + CSS 200, `/geo-builder/` and `/3d-builder/` both still 200, proxy `/healthz` ok (untouched). |
| 2026-08-16 | `prod/2026-08-16-5` | `80cc3be` | **complex ONLY** (static; proxy untouched) | `complex-9g6raE__.js` | **The foundation batch: #658 + four families + the numeric tier (PR [#674](https://github.com/dcodish/geo_builder/pull/674), [#616](https://github.com/dcodish/geo_builder/issues/616), [#623](https://github.com/dcodish/geo_builder/issues/623)).** **[#658] the ACTIVE engine now gatekeeps input** (ADR-CX-010): the store kept only the PROTOTYPE's facts and the app rebuilt the v2 input from them, so the retiring parser gatekept the input box — a line it refused never became a fact and never reached v2 at all. Every v2-only form was unreachable in the running app however well the engine handled it; the operator's «z1 מדומה טהור» prod report was one symptom. Same class as #653 one layer higher, and it is S7's cutover arriving early rather than a patch. The store now owns the ordered LINE list as the source of truth, the engine switch is read there (a v2 session rehydrated through the prototype's yes/no would silently lose every v2-only line on reload), and the lock drives `useComplexStore.addLine` — #658's own diagnosis of why S6 shipped broken was that its tests called `deriveLines` directly and never crossed the store (#535's lesson: a solver that works when called and never gets called). **Measured family coverage F1–F5,F8 → F1–F9.** **F9 sequences** (ADR-CX-011) stated by term POSITIONS, so «בהתאמה» is the general case and no ratio is invented as an unknown; the geometric case is exact and its integer turn-unknown IS «מנת הסדרה — כל האפשרויות». **F6 objects** (ADR-CX-012) — segments, polygons, circles, the origin O available everywhere and an unknown nowhere; they claim NOTHING about shape (that is F11), because the corpus's own figures are frequently non-convex. **F7 measures + TIER 2** (ADR-CX-013) — the numeric residue (LADDER-CX stage 3a/3b/3c/3e) that ADR-CX-006 specified and nothing had built: residuals over the free basis tier 1 leaves, LM with a numeric Jacobian and DETERMINISTIC multi-start. One sentence form, and the arithmetic decides whether it drives or checks. **The knowledge panel** (ADR-CX-014) — a number prints only when the givens force it, from one STRUCTURAL predicate, never an inference from sampling variance (ADR-421's P1 shape, which inverts at N=1). Defects found and root-fixed on the way: `freeDof` was published from tier 1, which only sees names a CONSTRAINT mentions, so a merely-declared number counted as zero DOF — two definitions of one quantity, surfacing as both a solver bug and an honesty bug; the Hebrew final-letter trap twice («הראשונים» medial nun, «היקף» final pe — the guard checked three of five final forms, now five); a root sitting exactly on a scan node was skipped, and exam answers are integers; and a plotted point printed its winding (`cis-190440°`). **Prod default is UNCHANGED** — all of this is behind `?engine=v2`. Known open: [#675](https://github.com/dcodish/geo_builder/issues/675) (P2, filed) — a number written `3+4i` gets no canvas label, because its angle is an opaque atom and the renderer has no fallback; LADDER-CX stage 3d (obligation preservation) is named as pending, not built. Gates: `npm run test:full` green — 8533 passed, 4 skipped (pre-existing); `tsc -b` clean; `check:siblings` PASS; complex tree 253 → 370 tests. Verified live: served `index.html` references the new hash, JS+CSS 200, proxy `/healthz` ok (untouched). |
| 2026-08-16 | `prod/2026-08-16-4` | `2688336` | **complex ONLY** (static; proxy untouched) | `complex-DZsWclfO.js` | **S6 part 1 ([#623](https://github.com/dcodish/geo_builder/issues/623), PR [#657](https://github.com/dcodish/geo_builder/pull/657)) — CLAIMS.** Family F10, present in five of the eleven sampled exams: «w ממשי», «w מדומה טהור», «z₁ ו-z₂ צמודים זה לזה». **Claims never drive** — the figure is built from the givens and a claim is checked against it; a claim that could move the figure would make every answer correct. **Decided, not sampled:** «מדומה טהור» is `2·arg w ≡ ½ (mod 1)`, an integer question true for EVERY configuration or for none, which is what the turns carrier exists for. **`unknown` is a first-class verdict** — a claim about a direction the givens leave free is unanswered, not wrong. **The test that mattered:** «z1 = 3+4i, z2 = 3-4i, צמודים» first came out REFUTED. They are conjugates, but each carries its own opaque base angle and nothing in the exact core can prove one is the negative of the other; refuting a TRUE claim is the costly direction, so an opaque angle now yields *unknown*. That failure also exposed a real regression — the v2 parser did not constant-fold, so `z1 = 3+4i` (an ADDITION) routed to the numeric tier that does not exist yet; the S3 bridge folded constants and the parser did not. A constant expression is a literal whatever its syntax. The **lexical ratchet** caught the new rules inlining «ו-» and «הוא» and sent them to lexicon atoms — back at ZERO. Riding along: the measured test-tier refresh (ADR-394/ADR-W-009 — membership is derived, so the refresh is the mechanism working). Gates: full suite green (253 in-tree), `tsc -b` clean, `check:siblings` PASS. Verified live: served hash matches the local build, `/geo-builder/` and `/3d-builder/` both still 200. |
| 2026-08-16 | `prod/2026-08-16-3` | `c2130f0` | **complex ONLY** (static; proxy untouched) | `complex-BWbQR_wz.js` | **[#653](https://github.com/dcodish/geo_builder/issues/653) (bug, PR [#654](https://github.com/dcodish/geo_builder/pull/654)) — operator report from prod play.** The #607 sequence BUILT, z1 and z3 were determined and drawn, and the row for `-2z1 = conj(z3)` was marked **red**. **Class: two surfaces answering the same question with different engines.** `?engine=v2` re-pointed the CANVAS at the rebuilt engine, but the fact list kept reading `scene.errors`/`scene.checks` from the prototype's evaluation — and the prototype is precisely what refuses that line (#607, the reason the rebuild exists). The figure said "built" and the row said "failed", about the same fact, at the same moment. Same honesty class the siblings paid for in [ADR-3D-157](06b-decisions-3d.md), where the canvas printed a line's numbers while the app's own status line said five free DOF. **Fix:** when v2 drives the picture it drives the verdict. Rows style from `derived2.untranslated`, keyed by the student's own LINE, so a red mark lands on the row v2 could not read and on no other; the prototype's check/error chrome is suppressed under v2 rather than left to compete, and a genuinely-refused row shows v2's own reason instead of a different engine's error code. Gates: full suite green (242 in-tree), `tsc -b` clean, `check:siblings` PASS. Verified live: served hash matches the local build, `/geo-builder/` and `/3d-builder/` both still 200. |
| 2026-08-16 | `prod/2026-08-16-2` | `3858175` | **complex ONLY** (static; proxy untouched) | `complex-R69VXCqK.js` | **[#651](https://github.com/dcodish/geo_builder/issues/651) (bug, PR [#652](https://github.com/dcodish/geo_builder/pull/652)) — found by the operator within minutes of the previous deploy.** «z1 ברביע הראשון» alone drew z1 at `~1·cis~0°`, **on the +Re axis, which is in no quadrant at all**: the figure contradicted its own stated given while every check passed. **Class, not the quadrant:** an inequality PRUNES enumerated branches, but a direction the givens never pin is a sampled degree of freedom, not a branch, so pruning could not reach it — the same hole would hit `arg z < 45` and any region given. A second defect rode with it: a name with no linear unknown fell back to a fixed `1·cis0°`, a default that never moves (the ADR-052 smell in its plainest form). **Fix:** a filter does TWO jobs — it prunes the configurations the equations produced AND bounds the sampling of what they left free, both being the same statement about the same direction. A free direction is sampled **strictly** inside its window, because a quadrant is an OPEN region and 0°/90° are axes belonging to neither neighbour. A **determined** direction is still only verified — `z^3 = 8` + quadrant 1 still finds NO configuration, and that is in the lock so the fix cannot drift into satisfying a filter by moving a value the givens forced. **Sibling audit (ADR-W-004):** class not present in `src/` or `src3d/` — both run a seed search against `meetsRequirements`/`seedForRequirements`; this product had the filter but not the search. Gates: full suite green (240 in-tree), `tsc -b` clean, `check:siblings` PASS. Verified live: served hash matches the local build, `/geo-builder/` and `/3d-builder/` both still 200. |
| 2026-08-16 | `prod/2026-08-16` | `d41e302` | **complex ONLY** (static; **`server/` changes were TEST files only, so the proxy was NOT rebuilt or restarted**). **2-D and 3-D deliberately NOT redeployed** — nothing of theirs is undeployed, and the parallel session owns anything that appears | `complex-BaX0JXkw.js` | **Operator: "i'm on a remote session so i cannot test it locally. should we deploy to prod and i will test it there? there are no users of this tool so no risk."** The complex-numbers FOUNDATION REBUILD ([#616](https://github.com/dcodish/geo_builder/issues/616)) goes live behind **`?engine=v2`** — off by default, so the prototype remains what a visitor gets and this deploy changes nothing user-facing. **S1 ([#618](https://github.com/dcodish/geo_builder/issues/618), PR [#633](https://github.com/dcodish/geo_builder/pull/633)):** the exact value layer — modulus as a ℚ-exponent vector over prime/parameter atoms, argument as rational TURNS plus symbolic angle atoms. Of eleven sampled 572 Q3s, **zero** can be verified by floats. **S2 ([#619](https://github.com/dcodish/geo_builder/issues/619), PR [#637](https://github.com/dcodish/geo_builder/pull/637), ADR-CX-006):** the tier-1 exact linear solver — **[#607](https://github.com/dcodish/geo_builder/issues/607) closes by ELIMINATION, not by the joint Newton its own body proposed**: in log-polar the system is linear, `|z1| = √2` exactly, `4·t1 = k − ½` gives four branches, the quadrant given prunes to 45°, zero iterations. The branch set IS the exam's «כל האפשרויות». **S3 ([#620](https://github.com/dcodish/geo_builder/issues/620), PR [#636](https://github.com/dcodish/geo_builder/pull/636)) + the `?engine=v2` switch (PR [#638](https://github.com/dcodish/geo_builder/pull/638)):** replay over the solver, plus a deliberately throwaway bridge so the engine could be played before the parser existed. **[#643](https://github.com/dcodish/geo_builder/issues/643) (bug, PR [#644](https://github.com/dcodish/geo_builder/pull/644)), found by the operator in play:** an under-determined figure drew NOTHING — the engine plotted only magnitudes the givens forced, so every figure mid-typing rendered empty. Two rules had been conflated; *do not print an unknown value as knowledge* is right and moved to the LABEL, *do not draw it* was wrong (ADR-CX-001 D3 always-visualise, ADR-052's default-as-a-STARTING-value). Free DOFs are now sampled and everything draws, with `~` marking what is sampled. **S4 ([#621](https://github.com/dcodish/geo_builder/issues/621), PRs [#645](https://github.com/dcodish/geo_builder/pull/645)/[#646](https://github.com/dcodish/geo_builder/pull/646)/[#649](https://github.com/dcodish/geo_builder/pull/649), ADR-CX-009):** the v2 parser drives the figure — the orthography chokepoint and **total span accounting ENFORCING** built *before rule one* (no `dropped*` gate exists and none will), lexicon atoms with the ratchet at **zero**, an executable catalog, and `app/deriveLines.ts` as the submit path. One fold, two sources; the tests assert the parser path and the bridge agree point for point. **S5 part 1 ([#622](https://github.com/dcodish/geo_builder/issues/622), PR [#650](https://github.com/dcodish/geo_builder/pull/650)):** the POLAR SUBSTRATE — the operator's first requirement for this product. Every number now draws as a **length and a direction**: radius vector, argument arc swept from +Re, modulus ring, over rings and 30° rays. Equal magnitudes share ONE ring. Honesty travels on the primitive (`known:false` renders dashed), so always-visualise and never-invent stop fighting. **Workspace, riding along:** [#634](https://github.com/dcodish/geo_builder/issues/634)/ADR-W-017 **sibling safety is a CHECK** (`npm run check:siblings` — refuses sibling edits, builds both siblings regardless of the diff; verified non-vacuous), and [#617](https://github.com/dcodish/geo_builder/issues/617) **the complex CI lane this product had never had** (ci.yml had zero occurrences of "complex"). **Not shipped, and named:** the sequence spiral, partial-sum chain and value cycle — all need grammar that does not exist yet (F9, symbolic exponents), so building them would have shipped pictures with nothing to draw. Gates on the exact deployed tree: `npm run test:full` **green, 0 failures**; `tsc -b` clean; `check:siblings` PASS. Verified live: served bundle hash matches the local build byte-for-byte, `/complex-builder/` 200, the shipped bundle contains the `engine=v2` switch, and **`/geo-builder/` and `/3d-builder/` both still 200** (untouched by this deploy). |
| 2026-08-15 | `prod/2026-08-15-3` | `e7499d3` | **3-D** (2-D re-uploaded, byte-identical — `src/` unchanged since `prod/2026-08-15-2`); static only, **`server/` unchanged so the proxy was NOT restarted**. **`src-complex/` deliberately NOT deployed** — six files of the parallel session's work sit on `main` unshipped, theirs to release | `index-Ds-ONOPU.js` + `geoWorker-DazKZGhI.js` (2-D, unchanged) / `3d-C774DPaZ.js` | **Operator: "I want a deployment tonight of all the work we did on 2d and 3d."** The whole of **fix-round [#596](https://github.com/dcodish/geo_builder/issues/596)** — five planned items, four merged PRs, plus three defects found and fixed during the operator's own play pass. **[#587](https://github.com/dcodish/geo_builder/issues/587) (PR [#604](https://github.com/dcodish/geo_builder/pull/604), ADR-3D-152):** a stated flat QUAD SHAPE builds — one `quad-shape` command, three apply arms dispatched at APPLY because the parse is context-free; seven constraint sets authored against `quadBaseRing` and asserted against `QUAD_BASE_DIMS`; `rect-complete` absorbed as its rectangle instance. Operator ruled option (a). **[#333](https://github.com/dcodish/geo_builder/issues/333) (PR [#608](https://github.com/dcodish/geo_builder/pull/608), ADR-3D-153):** `ישר החיתוך` becomes ONE rule — the rule registry SHRANK — with a total tail reader (strip the Hebrew, the operands are what is left) and apply-side auto-indexing of a colliding `ℓ` plus a notice. **[#577](https://github.com/dcodish/geo_builder/issues/577) + [#558](https://github.com/dcodish/geo_builder/issues/558) (PR [#609](https://github.com/dcodish/geo_builder/pull/609), ADR-3D-154):** the data panel's relation scan becomes ONE partitioned universe and ONE loop, replacing two hand-built ones — the mixed LINEAR×PLANE column now reports (`relDeviation`, the same reading the verifier uses), vectors/arrows enter the universe, solid edges pair with named lines, and a contained segment gets its own «מוכל» row instead of a false «parallel». **[#533](https://github.com/dcodish/geo_builder/issues/533) (PR [#610](https://github.com/dcodish/geo_builder/pull/610), ADR-3D-155):** the canvas can be PANNED — a third view-gauge component applied as one screen-space `<g translate>`, so `scene3.ts` stays pure; zoom now zooms about the pointer, `↺` clears all three. **[#559](https://github.com/dcodish/geo_builder/issues/559) (ADR-3D-156):** the data panel is bidi — no list-wide `dir`, direction per row via `textDir3` (never `dir="auto"`, the ADR-312 first-strong trap). Found and fixed DURING the play pass: **[#611](https://github.com/dcodish/geo_builder/issues/611) (P1, ADR-3D-157)** — the canvas printed a derived line's SAMPLED numbers as if given («ℓ: x = (0.678, 0.467, 0) + …» on a 5-DOF figure); the echo's honesty gate was an enumeration of line KINDS, so `plane-plane`/`through`/`common-perp`/`line-projection` all leaked. Now one predicate: numbers print only when an absolute frame exists AND nothing is free. **[#612](https://github.com/dcodish/geo_builder/issues/612) + [#615](https://github.com/dcodish/geo_builder/issues/615) (ADR-3D-158)** — a shape is what it says and only what it says: a statement the figure already forces is REDUNDANT (notice, figure untouched) and a less specific name is a NAMING ERROR (operator ruling); and, the other half, a declared shape no longer DRAWS as a special case of itself (measured: «מקבילית» drew at 89.4°, a rectangle, at seed 0) — as a PREFERENCE tier in one sweep, so givens that force the special case still draw. Gates on the deployed commit: `npm run test:full` **8224 passed / 4 skipped / 447 files, 0 failures**; `tsc -b`, `npm run build`, `npm run build:3d` all clean. Verified live: both bundle hashes match local, both return 200, `/healthz` → `ok`, and the served 3-D bundle contains `quad-shape`, `shape-less-specific`, `pan-group`, «מוכל» and «ישר החיתוך». Open follow-ups from the same play pass, NOT in this deploy: [#613](https://github.com/dcodish/geo_builder/issues/613) (restating a fact duplicates its row — pre-existing, general), [#614](https://github.com/dcodish/geo_builder/issues/614) (containment has no INPUT form though the panel now prints it), [#601](https://github.com/dcodish/geo_builder/issues/601) (kite corner completion). |
| 2026-08-15 | `prod/2026-08-15-2` | `6347e0a` | 2-D + 3-D + **complex (NEW, first deploy)** (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-ZvUnM6Xu.js` + `geoWorker-DazKZGhI.js` (unchanged) / `3d-B-CIOKpK.js` / `complex-Q06AyjSs.js` | **Operator: "I want to deploy all changes … my goal is to have all code in prod."** Everything on `main` since `prod/2026-08-15`, plus the complex tool merged for this deploy. **A THIRD PRODUCT goes live: `https://themathbible.com/complex-builder/`** — the C0 Gauss-plane walking skeleton ([#585](https://github.com/dcodish/geo_builder/issues/585), PR [#588](https://github.com/dcodish/geo_builder/pull/588), `ADR-CX-001…005`), built by the parallel session. Merged as a **merge commit**, not a rebase, so that session's branch and worktree were left untouched; its branch was verified clean and fully pushed first. Purely static — no proxy calls, so no Apache change beyond creating the directory (`mkdir` + `chown root:root` + `chmod 755`, matching its siblings). **2-D: [ADR-451](06-decisions.md) + Am. 1 ([#591](https://github.com/dcodish/geo_builder/issues/591), PR [#594](https://github.com/dcodish/geo_builder/pull/594))** — «ריבוע שצלעו 4» / «משולש שווה צלעות שצלעו 4» build with NO letters (operator: *"just like we can write ריבוע with no letters"*). The clause moved from an utterance REWRITE (`normalizeShapeSide`, which had to NAME the edge and so required letters the student had not written) to the shape MACRO, where the auto-minted ids exist; the rewrite is retired, so one owner rather than two split by label-presence. Am. 1 fixes a regression the operator caught in play: the value is now lowered **by delegation** (re-parsing `<v0><v1> = <value>`) rather than hand-built, because «AB = <value>» is not one lowering — a plain number becomes `set-distance`, a radical becomes a `measure-length` carrying `expr.text`, and that text is what the canvas prints. Hand-rolling it had regressed «שצלעו √2» from «√2» to «1.41»; the first byte-identical check missed it by using an integer, where the two routes agree. **2-D: [ADR-452](06-decisions.md) ([#595](https://github.com/dcodish/geo_builder/issues/595))** — `autoVertexLabels` is now TOTAL: past Z it mints `A1, B1, …`. It used to return a SHORT array when the alphabet ran out, silently, and its 13 call sites all assume length `n`, so with 23 points placed «ריבוע» committed `{square, ids:['X','Y','Z',undefined]}` with a green ✓. Fixed at the CONTRACT so all 13 callers became correct untouched, rather than adding 13 guards; the 26-point ceiling went with it. Not prod-reachable (needs 23+ named points) — fixed for the shape of the failure. **Workspace: [#593](https://github.com/dcodish/geo_builder/issues/593)** — the shared absolute vite `cacheDir` is gone from all three configs (it was a Dropbox-era leftover, obsolete since the 2026-07-23 move) and `dev` gained `--strictPort`. That defect had voided two operator play attempts earlier the same day: a dev server left running from the PREVIOUS DAY held 5173, vite silently bound elsewhere, and the reported URL served old code while `curl` returned 200 throughout. The complex config arrived carrying the same override and was stripped in the merge rather than reintroducing it. Also riding: the `complex`-out-of-scope operator rule recorded in git-tracked memory. Gates on the exact deployed tree: full suite **8028 passed / 4 standing skips** (441 files) + `tsc -b` + all three builds clean. Verified live: all three pages reference the new bundles and all three return 200 `text/javascript`; CSS 200; both admin dashboards 200; `/healthz` ok (proxy untouched). RUNBOOK updated with the complex app's artifact row and deploy steps. |
| 2026-08-15 | `prod/2026-08-15` | `b111edc` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-DSkseqGa.js` + `geoWorker-DazKZGhI.js` (unchanged) / `3d-DIrRPA0l.js` | **Round [#589](https://github.com/dcodish/geo_builder/issues/589) (validated, closed) — four work items, six issues, all P2, all operator-played.** **#586 ([ADR-3D-149](06b-decisions-3d.md), `648cfa0`):** the inscription ring names itself — `polygonCircle3`'s polygon NOUN is now optional («מעגל חוסם את ABCD», the operator's own request), its container marker read off the RUN when no noun carries it; and its ARITY MAP — an enumeration of three nouns that forgot every quad noun `POLY_WORDS_HE3` admits — is replaced by the ring's LENGTH, closing the #440 half-drop where «מעגל חוסם את ריבוע ABCD» emitted the circle alone and refused `unknown-point A` as an opening move. Riding with it, the **silent-drop half of #587**: `droppedShapeNoun3` was scoped to solid BASES, so «המרובע ABCD הוא ריבוע» committed a green ARBITRARY quadrilateral (the #424/#330 class, quad edition); the gate is now bound to the EVENT rather than the solid context and runs on the DETERMINISTIC path too (the ADR-3D-147/#530 doctrine), with `מקבילית` joining the watched nouns. That half was *forced* by the arity fix — without it the newly-reachable path would have created a fresh instance of the wrong-build class. **#549 ([ADR-3D-150](06b-decisions-3d.md), `4c2986f`):** every primed label rendered mirrored (`′C`, not just the reported one) — SVG `<text>` inherits the RTL shell's CSS `direction` and U+2032 PRIME is bidi-neutral, so as a trailing character it took the paragraph level. Fixed at the ROOT (`direction:'ltr'` on the `<svg>`), because the existing per-node `ltr()` isolates are OPT-IN and the most common primed run on the canvas never opted in; deliberately not `bidi-override`, so a strong-RTL run still lays out correctly. **#496 + #317 ([ADR-3D-151](06b-decisions-3d.md) + Am. 1, PR [#590](https://github.com/dcodish/geo_builder/pull/590), `4399a4c`+`6fb72c6`):** the query lane gains its two missing kinds — a bare POINT label answers its coordinates (the lane could already answer «m» but not «A»), and a PLANE answers its equation (the exam's «מצאו את משוואת המישור» asked as a QUESTION instead of entered as a figure-changing fact). Both answer through the derivations the ארגון נתונים panel ALREADY uses — `dataView.pointCoords` called directly, and `canonicalPlaneEq` extracted so both call it — never a private formatter (#481). **Am. 1, from the operator's play:** a plane now gives BOTH representations, `parametricPlaneForm` extracted likewise; *"if possible"* is load-bearing — the parametric form needs a stable anchor and stable spanning edges, and an equation-given plane has no run, so the equation stands alone rather than a sampled parametrisation being invented. **#458 ([ADR-450](06-decisions.md), PR [#592](https://github.com/dcodish/geo_builder/pull/592), `0e014e4`):** the DIMENSIONS phrasing — `מידות` appeared NOWHERE in the parser, in either locale, while «ריבוע במידות 4*4» is a live prod utterance. Read in the shape MACRO (one reader, every quad) and consumed BEFORE the `shapeLeftover` gate that used to escalate the whole sentence; magnitudes lower to ordinary `set-distance` givens, first number = base edge. Scoped to QUADS, and the single-side «שצלעו» form deliberately LEFT with its existing owner (`normalizeShapeSide`, restricted to equal-sided shapes because a rectangle's "its side" is an unstated pick) rather than gaining a second, laxer reader. **Escalated, not shipped: #587's capability half** — its plan said to mirror constraint sets the solid lane never emits (quad bases are realised STRUCTURALLY via `quadBaseRing`), and `rect-complete` owns «ABCD מלבן» with corner-completion semantics no sibling noun has; operator ruled **option (a)** (one `quad-shape` command, three apply arms) and the rewritten plan is armed for a future round. **Filed, not shipped:** #591 (the label-less «ריבוע שצלעו 4» has no owner), **#593** (dev-server play is unreliable — a shared absolute `cacheDir` + no `--strictPort`; a stale server from the previous day served old code and voided two play attempts). Deployed commit also carries a parallel session's complex-tool docs (`9e7a84f`, ADR-CX-005, docs-only), reconciled explicitly rather than absorbed. Gates on the exact deployed tree: full suite **7937 passed / 4 standing skips** (438 files) + `tsc -b` + both builds clean. Verified live: both pages reference the new bundles and both return 200 `text/javascript`; CSS 200; both admin dashboards 200; `/healthz` ok (proxy untouched). |
| 2026-08-14 | `prod/2026-08-14-2` | `13e1db1` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-DGQ1lIN_.js` + `geoWorker-DazKZGhI.js` / `3d-CcLJSEF6.js` | **Round #582 (validated, closed) + the same-day play finding — five fixes, all landed on main and operator-played.** **#579 ([ADR-3D-146](06b-decisions-3d.md), `dea342e`):** «SO גובה הפירמידה» names the NEW foot — a ⟂-to-plane statement with exactly one new letter delegates to the `height-to-face` funnel (creation, not a reference error); the superseded #14 lock now asserts the creation. **#535 ([ADR-3D-147](06b-decisions-3d.md), `7d5a3e3`):** the deterministic submit path runs the `droppedNewLabels3`/`droppedGivenNumbers3` honesty gates (the #530 generator closed); measurement-first — one false positive (the structural `=0` of a standard-form equation) fixed at its root in the gate; 2-D twin verified already wired (ADR-089/250). **#581 ([ADR-447 Am. 1](06-decisions.md), `4bd4e82`):** operator ruling — a coincidence notice with a machinery-minted member is not shown at all (`visibleCoincidences` at the display seam). **#580 ([ADR-449](06-decisions.md), `a5f1b4c`):** the «מחפשים תצורה…» searching notice is a centred upper-third canvas banner; the stale chip keeps its corner. **#584 ([ADR-3D-148](06b-decisions-3d.md), `b1c2ae9` — the round's play finding, operator-instructed fix-now):** plane-run materialisation is ONE rule (`materializePlaneRun`) — the claim carriers (`plane-eq` «המישור ABS: x=0», `coord-plane-rel`, `line-plane-angle`, `mutual-rel` runs) now draw their patch and carry the full/face/hidden tick. Deployed commit also carries the parallel session's complex-tool docs (#583, docs-only). Play follow-ups filed + armed for the next round, not shipped: #586 (bare-run «מעגל חוסם את ABCD»), #587 (flat-quad shape semantics — «ABCD ריבוע»). Gates: full suite **7901 passed / 4 standing skips** (435 files) run on the exact deployed tree + both builds clean. Verified live: both pages serve the new bundles (200 `text/javascript`); `/healthz` ok (proxy untouched). |
| 2026-08-14 | `prod/2026-08-14` | `142869b` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-yaChNbDf.js` + `geoWorker-DazKZGhI.js` / `3d-fJiNUaGU.js` | **Round #576 (validated, closed) — the rescue-flow polish batch, all four items landed directly on main and operator-played.** **#572+#573 ([ADR-446](06-decisions.md), `9d25431`):** the config-search + honest-note flow binds to the display EVENT — a LOADED figure that fails its requirements now runs the same seat rescue the submit path has (the operator's saved collapse file self-corrects on load), and while any search is pending the ADR-293 keep-prior slot renders the last good view (dimmed + «מחפשים תצורה…») instead of painting the failing one for ~5 s. **#574 ([ADR-447](06-decisions.md), `f97f669`):** anonymous ids never reach a rendered string — `anonPointDescriptor` derives «נקודת ההשקה על CA» from the defining object (a foot from a circle's centre IS its tangency); the coincidence notice adopts. **#531 ([ADR-3D-144](06b-decisions-3d.md) + [ADR-448](06-decisions.md), `d17bf1d`):** invisible bidi controls strip at `normalize3` — copy-paste from the app's own fact list parses; NBSP + doubled spaces fold at both parse seams; locked by the round-trip property `parse3(isolateLtrRuns3(u)) ≡ parse3(u)` over the whole catalog. **#529 ([ADR-3D-145](06b-decisions-3d.md), `142869b`):** «המרחק מ X ל-Y» joins «בין X ל-Y» at the distance rule. Play follow-ups filed + armed, not shipped: #580 (centre the searching notice), #581 (operator ruling — suppress anonymous-member coincidence notices), #528 bumped P2 (second distance pins the normal). Gates: full suite **7869 passed / 4 standing skips** (433 files) at the deployed commit + `tsc -b` + both builds clean. Verified live: both pages serve the new bundles (200 `text/javascript`); `/healthz` ok (proxy untouched). |
| 2026-08-13 | `prod/2026-08-13-3` | `9b11365` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-D17PbdXJ.js` + `geoWorker-DazKZGhI.js` / `3d-BmktO0T7.js` | **Round #561 (validated, closed) + the P1 its play surfaced + the first agent-certified scenario — the same-day third batch, operator "close and merge / deploy".** **The round's four features**, all operator-played (PRs [#562](https://github.com/dcodish/geo_builder/pull/562)/[#563](https://github.com/dcodish/geo_builder/pull/563)/[#564](https://github.com/dcodish/geo_builder/pull/564)/[#575](https://github.com/dcodish/geo_builder/pull/575), the last superseding the auto-closed #565): **#546/ADR-443** — the anonymous circle beside SEVERAL circles binds by the membership tie-break in all three resolver tails, and a genuinely-ambiguous reference ASKS (`ambiguous-circle-ref`) instead of burning an LLM call; **#503/ADR-3D-142** — the apex-less «גובה הפירמידה» (apex derived apex-last at apply) + the imperative/relative-clause frame; **#505/ADR-444** — a bare 3–4-letter run of NEW labels declares the shape, one behaviour across casings; **#392/ADR-3D-143** — bare «מנסרה ABCA'B'C'» derives the base arity from the primed-mirror run (oblique, «המנסרה ישרה» composes). **The P1 the play caught (#566, ADR-445 + Am. 1, `88c2833`+`adfddb7`):** the unstated right-angle SEAT is now a config-search dimension (the `rot` field; the ADR-163 explicit pin always wins) — «קשת AB = קשת BC» on the defaulted right triangle used to satisfy by collapsing C onto A with every status green, prod-reachable via the single-circle prefix; the seat tier runs BEFORE the reflection tier (the mask×seed sweep ate the 12 s worker budget — the Infinity-budget suite masked it; now locked structurally via `lastConfigTier`), and an exhausted search surfaces `figure.noValidConfig` instead of silent green. **Also riding:** the exercise-sequence agent (#567/ADR-W-015, PR #568) + its first certified bagrut scenario (`bagrut-gchf-cyclic-tangent-parallel`) — workspace/test, no runtime change. Follow-ups filed, not shipped: #569 (accept-gate hardening + resample seat cycling), #572 (load path never searches), #573 (keep-prior during the search), #574 (anonymous-id leak in the coincidence notice). Gates on the deployed commit: full suite **7832 passed / 4 standing skips** (429 files) + `tsc -b` + both builds clean. Verified live: both pages serve the new bundles, both return 200 `text/javascript`; proxy `/healthz` ok (untouched). |
| 2026-08-13 | `prod/2026-08-13-2` | `1729668` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-CHy40dTI.js` + `geoWorker-Ciy7qP5Y.js` / `3d-CcvdqisM.js` | **The same-day second batch — operator "deploy all" after playing PR #540 and closing the #536 P1 in its dedicated session.** **3-D, the angle-operand cluster** ([ADR-3D-140](06b-decisions-3d.md), PR [#540](https://github.com/dcodish/geo_builder/pull/540), closes #522 #523 #524 #512 #534 #8 — operator played and approved): the operand seam derives instead of enumerating — plural/conjoined noun frames («המישורים ABC ו-SBC מאונכים»), the α-label angle form wherever the numeric form parses, `פאה`/`בסיס` as operand nouns, coordinate planes and axes as first-class `Operand3` members, the line-plane angle as a cone constraint whose ⊥/∥ are the endpoints, and the #8 in-face altitude — one gate, every relation family inherits together. **3-D, the free-standing named line** ([ADR-3D-141](06b-decisions-3d.md), PR [#557](https://github.com/dcodish/geo_builder/pull/557), #552): «ישר k» / bare «l1» declares a line pinned by later stated relations. **2-D, the #536 P1 pair** (dedicated session): the SEQUENCE honesty gate — the LLM lane may not respell a stated point run ([ADR-441](06-decisions.md), `0f38b63`), and the deferral flex probe reads the ORDER family in RELATIVE units ([ADR-442](06-decisions.md), `1729668`) — a stated collinear order is enforced and verified, never silently green. **Workspace (no runtime change):** the fix-round live ledger (ADR-W-013) and batch approval — one operator okay arms a round, `auto-ok` by transcription with an audit comment (ADR-W-014, PR [#550](https://github.com/dcodish/geo_builder/pull/550), #548). Gates on the deployed commit: full suite **7766 passed / 4 standing skips** (425 files) + `tsc -b` + both builds clean. Verified live: both pages serve the new bundles and both return 200 `text/javascript`; proxy `/healthz` ok (untouched). |
| 2026-08-13 | `prod/2026-08-13` | `4a2492a` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-Dc5clBgH.js` + `geoWorker-DjLC48Tp.js` / `3d-CtwH6djy.js` | **The accumulated 4-commit batch since `prod/2026-08-11-2`; the operator played the P1 on dev and said "deploy all".** **3-D P1 ([ADR-3D-139](06b-decisions-3d.md#adr-3d-139), #530, `80f5217`):** a point-naming marker BINDS its own label or the rule declines — prod session `rsqkx2`'s «אלכסוני A'B'C'D' נחתכים בנקודהS» (ordinary missing space) did not fail to parse, it parsed into a DIFFERENT figure (A′ as the crossing, an invented face B′C′D′S); the `already-defined` answer was a cover story. Root cause was an unanchored positional fallback after an optional marker — the same line's THIRD bite — so the fix is structural (marker present ⇒ id from the marker or decline, locked as a property over nine spellings) with the vocabulary as ONE shared `AT_POINT` fragment across all nine sites. The sibling audit caught `circleTangentLine` silently DROPPING the student's touch point K on the glued form under a green ✓. #535 (droppedNewLabels3 never asked by the deterministic path) filed, not fixed. **2-D ([ADR-438](06-decisions.md#adr-438), `1a9ce46`):** the drivable walk traverses DERIVED circle centres, and a refusal names the WHOLE statement. **2-D ([ADR-439](06-decisions.md#adr-439)/[440](06-decisions.md#adr-440), `9ec27c3`):** naming-by-use for the size lane and for auto-named points — no phantom circle, no duplicate touch point. **Workspace (`4a2492a`, ADR-W-012, #543/#544, PR #545):** /fix-round + /status-update — no product runtime change. Gates on the deployed commit: full suite **7642 passed / 4 standing skips** (422 files) + `tsc -b` + both builds clean. Verified live: both pages serve the new bundles (200) and both bundles return 200; proxy `/healthz` ok (untouched). |
| 2026-08-11 | `prod/2026-08-11-2` | `663ddb9` | **3-D static only** (`dist-3d/`; 2-D `dist/` and the proxy untouched — no `src/` or `server/` change, no restart) | `3d-DvOL0EfL.js` (sha256 `bc6c7e28…`) · `3d-DxhlYknS.css` (unchanged) | **The refusal-honesty round — five operator-selected issues, all played and confirmed before merge.** Two root causes covered the three message bugs, and both are the shape docs/17 calls *an enumeration is not a rule*: the refusal was CORRECT in substance while its EXPLANATION was picked from a list of kinds that the general finding had outgrown. [ADR-3D-134](06b-decisions-3d.md#adr-3d-134) (#492, #425): `no-roots` was gated on `cmd.type ∈ {plane-angle, line-perp-plane}` — the only pinning kinds when it was written — so S2's line relations (#378) fell through to the claim verifier and «l ∥ π1» over `d·n = 2((m−1)²+1)`, unsatisfiable for EVERY real m, was reported as «check your computation», blaming the student's arithmetic. It is now gated on the PROPERTY (did this fact own a pinning given), and the claim pass is SKIPPED in that state — a claim cannot be refuted by a figure that has no configuration. Its sibling: the pivot guard had been deliberately widened to every pin kind while its message stayed the injection-specific «no placement matches the given coordinates», told to a student whose equilateral-base pyramid (∠DAB=120 then ∠DAC=53.13, impossible in R³) contains no coordinate at all; the refusal now splits where the guard splits, both payloads NAME the conflicting statements, and blame lands on the newest statement alone (ADR-276) instead of reddening every earlier given. [ADR-3D-135](06b-decisions-3d.md#adr-3d-135) (#513): the third instance of paren-blindness in a scalar reader (#299/#300) — `√(48)` refused while `√48` parsed, costing four attempts and two paid escalations to state one magnitude. Per operator ruling, fixed at the ONE lexical atom and ONE reader #510 deliberately paired, so length givens, coordinates and coefficients gained it together; `√(4*3)` still refuses honestly (that is #509's ruled reader). The lexical ratchet (docs/24 S2.1) failed the first draft for inlining fresh number fragments and was right — they now compose from a new `UNUM` atom, sweeping parse3 **24 → 14** inlines and lowering the recorded ceiling. [ADR-3D-137](06b-decisions-3d.md#adr-3d-137) (#449, PR [#526](https://github.com/dcodish/geo_builder/pull/526)): «אלכסון תיבה AC'» — the diagonal noun carries its solid; no new construct (a space diagonal IS a segment, the #72 ruling), and the shadow-matrix snapshot moved ADDITIVELY only, which is the evidence there is no rule theft. [ADR-3D-138](06b-decisions-3d.md#adr-3d-138) (#508, PR [#527](https://github.com/dcodish/geo_builder/pull/527)): a stated DISTANCE now pins a free plane's OFFSET (`d = −n·p ± value`, the side a sampled branch that «show another configuration» flips) — before, `resolveFreePlane`'s pin set was the enumeration of kinds that existed when #487 landed, so a perfectly good given was answered `claim-refuted`, i.e. *your distance is wrong*. The CLASS half is what closes it: a claim about a plane whose relevant DOF is still SAMPLED can never be refuted — it degrades to an honest `plane-not-determined` — so a constraint kind the resolver does not yet pin costs a refusal, never a false accusation. Two mechanisms had to follow the pin, both found by MEASUREMENT after the pin alone still drew 6.75 instead of 5: the free-plane resolution became a bounded FIXPOINT (#487's two fixed passes assumed a one-way dependency; an offset pinned to a POINT makes it mutual), and the landing funnel (ADR-3D-101) now treats a distance-pinned plane as pinned, by its own doctrine that a component is sampled only when PROVABLY free. Follow-ups filed, not shipped: #528 (a SECOND distance should pin the normal — refuses honestly today), #529 (the «המרחק מ A ל…» framing is not-handled while «בין A ל…» parses). Gates on the deployed commit: full suite **7599 passed / 4 standing skips** (418 files) + `tsc -b` + `build:3d` clean; the 3-D lane ran with ZERO tests moved for #508 and exactly two for #492/#425, both ours asserting the old bare payload. Verified live: the page serves the new bundle (200, `text/javascript`), /healthz ok (proxy untouched), 2-D still on `index-CAQ-3BrU.js`, both admin dashboards 200. |
| 2026-08-11 | `prod/2026-08-11` | `2876f78` | **3-D static only** (`dist-3d/`; 2-D `dist/` and the proxy untouched — no `src/` or `server/` change, no restart) | `3d-fx85ZDM2.js` (sha256 `dbda459b…`) · `3d-DxhlYknS.css` | **The night-run pair plus the three fixes its play surfaced — five issues, all operator-played and confirmed on the work PC.** The night batch: a parametric line's RUNNING parameter is the student's letter, echoed as typed ([ADR-3D-129](06b-decisions-3d.md#adr-3d-129), #422); coordinate components take √ and fraction literals via the `VAL` atom + `literalValue` reader pair — never a widened `NUM` whose readers would commit `NaN` ([ADR-3D-130](06b-decisions-3d.md#adr-3d-130), #510, PR [#515](https://github.com/dcodish/geo_builder/pull/515)). The play's harvest: a recognized ambiguity is a TYPED refusal, never a decline — the both-roles form «l1:x=m(m-1, 5-m, -2)» had been laundered through the LLM lane into a silent m→t build; it now surfaces a clarification naming the letter and can never escalate ([ADR-3D-131](06b-decisions-3d.md#adr-3d-131), #516); the knowledge gates derive their absolute sources from the construction — a fresh `C(√2,1,0)` lands as a `'coord'` point, never a pin, and the private `c.pins.length > 0` enumerations had suppressed canvas labels, the panel and the query lane for bare injected points (shared `translationPinned3`/`vectorFramePinned3`/`scaleKnown3`; [ADR-3D-132](06b-decisions-3d.md#adr-3d-132), #517); and an undriven SCALE parks at the seed's target POST-HOC, so a cube with one pinned vertex stops answering `\|AB\| = 1` as knowledge — the base solve byte-identical, the park kept only when the primary residuals stay exact, after two full-suite calibrations ruled out seeded starts (lost branches) and in-solve anchors (displaced determined coordinates / stalled on quadratic-root directions) ([ADR-3D-133](06b-decisions-3d.md#adr-3d-133), #518). Follow-ups filed, not shipped: #519 (2-D sibling audit of ambiguity declines), #520 (LM tail-burn perf debt). Full suite **7561 passed / 4 standing skips** + `tsc -b` + `build:3d` clean on the deployed commit; deployed over Wi-Fi-bound SSH (the wired path refuses port 22 from the work network); /healthz ok (proxy untouched), the page serves the new bundle (200). |
| 2026-08-10 | `prod/2026-08-10-2` | `876d742` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-CAQ-3BrU.js` (sha256 `f331e580…`) + `geoWorker-BlZmLenC.js` / `3d-z_nClHi2.js` (sha256 `4ef2da7b…`) | **Two batches, 13 commits: the morning's P2 regressions and then the P3 backlog batch (20 issues closed).** **The P2 half:** the false «redundant given» notice on a relation that PINS a free plane — `isAbsolute` was answering two different questions, and `isSelfDetermined` now names the distinction ([ADR-3D-124 Am. 2](06b-decisions-3d.md), #500); the 3-D declaration gate fails CLOSED, so «משולש ישר זוות ABC» no longer drops the right angle under a green ✓, with the «זוות»→«זווית» and «מעויין»→«מעוין» folds and 2 divergent shadow pairs removed ([ADR-3D-125](06b-decisions-3d.md), #498); the log-triage mirror is DERIVED from the submit pipeline instead of enumerated, and its NEW/carried split now travels between the two PCs ([ADR-W-006/008](06w-decisions-workspace.md), #501/#502); session start reports merged-ness and deployed-ness, not just pushed-ness ([ADR-W-007](06w-decisions-workspace.md), #488); tier membership became a RATIO so it stops thrashing across machines ([ADR-W-009](06w-decisions-workspace.md), #484). **The P3 half:** the honesty battery gained a catalog-wide false-positive net over EVERY gate — which found a live English-only false block on «circle O and circle P are tangent internally at M» on its first run — plus `introducedNewLabels`, the mirror for what a lowering ADDS, and the ADR-156 reuse path now narrates the object it reused ([ADR-436](06-decisions.md), #140/#255/#462); the 3-D gate battery's multiset accounting + the final-nun «תיכון» slip, a detached clitic re-binding at normalize3, and a plane run of THREE OR FOUR labels (the ⟂ rule had been silently dropping a stated box face's 4th point, covering for an `apply` refusal one layer down) ([ADR-3D-126](06b-decisions-3d.md), #457/#463/#494/#380); «נקה הכל» clears the 3-D input box, and the canvas keeps 3 decimals ([ADR-3D-127](06b-decisions-3d.md), #336/#491); the instrument honesty trio ([ADR-W-010](06w-decisions-workspace.md), #489/#439/#48); and the operator-approved feature batch (PR [#514](https://github.com/dcodish/geo_builder/pull/514)) — the «ן»/«ו» connective slip ([ADR-437](06-decisions.md), #506), a reasoned refusal for a latin angle label, a FACE-ON default view for planar figures, and the leftover rotation spent on LEGIBILITY, where one seed in eight used to draw a true 90° as 1.2° ([ADR-3D-128](06b-decisions-3d.md), #394/#5/#385). Full suite 7519 green (4 standing skips) + tsc + both builds clean on the deployed tree; both pages serve the new bundles (200, text/javascript), fix markers verified IN the served JS (`latin-angle-label`, `labelsInvented`, both Hebrew messages), proxy `/healthz` ok (untouched). |
| 2026-08-10 | `prod/2026-08-10` | `bbcd2cd` | 2-D + 3-D (static only; proxy untouched) | `index-CFISzVlc.js` / `3d-CamJwf8s.js` | The two-parallel-sessions batch (operator "commit and deploy"): 2-D fix #497 (ADR-435) — the leftover gate fails CLOSED (a typo'd shape modifier escalates instead of half-parsing to the bare shape; «זוות»→«זווית» fold; labelRun uppercase preference — "draw a square ABCD" no longer builds square D,R,A,W; shadow matrix: 6 divergent pairs removed, 0 added; 3-D sibling filed #498); 3-D feature #487 (PR #499, ADR-3D-124 + Am. 1) — the FREE-standing named plane, incl. bare «π2» declaring deterministically per operator play ruling. Also carries the #495 bidi-run-boundary merge (ADR-3D-123, #482) already on main. Full suite 7431 green + tsc + both builds clean on the deployed tree; /healthz ok, both pages + bundles 200. |
| 2026-08-09 | `prod/2026-08-09-3` | `7bd3e62` | **3-D static only** (`dist-3d/`; 2-D `dist/` and the proxy untouched — no `src/` or `server/` change, no restart) | `3d-BlF7zoIs.js` (sha256 `ef040f1f…b662`), `3d-DPnbeHDB.css` | **The operator's prod session on the parametric line/plane pair, triaged and fixed.** [ADR-3D-118](06b-decisions-3d.md#adr-3d-118) (P1, [#479](https://github.com/dcodish/geo_builder/issues/479) + [#481](https://github.com/dcodish/geo_builder/issues/481)): the canvas echo asked *"is the parameter UNPINNED?"* as a proxy for *"is its value FORCED?"* — the two coincide only while every pin has a single root, so `ℓ ∥ π1` over `dir·n = 2m²−4` (m = ±√2) printed one branch's components as the line's definition and changed them on "show another configuration", asserting a magnitude the student never gave (ADR-052). `resolve3` now publishes `branches` — the roots surviving every selection given — so `branches.length === 1` IS that statement by construction; one exported `paramIsKnowledge()` is what every surface asks. Fixed a latent sibling on the way: sign givens were honoured on the `paramGivens` path and silently ignored on the plane-angle / line-⟂ / line-rel one. The canvas also dropped its private 3-decimal rounder for the panel's `cleanMag`. [ADR-3D-119](06b-decisions-3d.md#adr-3d-119) ([#480](https://github.com/dcodish/geo_builder/issues/480)): three symbol kinds lived in three fields and each surface knew a different subset, so `m` — the symbol the exam question is *about* — was the one a student could not ask for; `figureSymbolsOf()` unifies them and the parameter answers its SOLUTION SET (`±√2`), not a sampled value. [ADR-3D-120](06b-decisions-3d.md#adr-3d-120) ([#486](https://github.com/dcodish/geo_builder/issues/486), [#485](https://github.com/dcodish/geo_builder/issues/485), [#401](https://github.com/dcodish/geo_builder/issues/401)): the optional definite article / subject noun, and a relation's verb-vs-noun FRAME, are shared vocabulary rather than per-rule spellings — «B על מישור π2» and «A נקודת החיתוך של ℓ עם π1» were silent drops on capabilities the engine already had. [ADR-3D-121](06b-decisions-3d.md#adr-3d-121) ([#482](https://github.com/dcodish/geo_builder/issues/482)): the bidi isolation was bound to the i18next post-processor, so it covered what the tool says and never what the STUDENT writes; the fact list now isolates the utterance at render. [ADR-3D-122](06b-decisions-3d.md#adr-3d-122) ([#483](https://github.com/dcodish/geo_builder/issues/483) → PR [#490](https://github.com/dcodish/geo_builder/pull/490)): a determined-but-unnamed ℓ∩π crossing is OFFERED as a clickable dot that names itself through the ordinary submit path, gated on `paramIsKnowledge` so a dot never invites naming a point the givens do not fix. Deployed on the operator's "commit and deploy" (play-and-approve waived, PR kept). |
| 2026-08-09 | `prod/2026-08-09-2` | `8d4caf3` | **proxy only** (rebuilt + **restarted**; `dist/` + `dist-3d/` untouched — no static push, no 2-D/3-D runtime change) | `proxy.mjs` (sha256 `ef61d67b…8b99`) | **The admin dashboards browse the usage log BY SESSION** ([ADR-434](06-decisions.md#adr-434), [#470](https://github.com/dcodish/geo_builder/issues/470) → PR [#471](https://github.com/dcodish/geo_builder/pull/471)). The stored event has always carried `sid`, and since #84/#182 the LLM's committed `commands` and an `ev:'action'` row per store interaction — logged expressly so a session replays end-to-end. Every surface `server/admin.ts` rendered over that data was nonetheless FLAT: «פעילות אחרונה» interleaves all sessions by time, both drills group by utterance TEXT (discarding the session that produced it), and the `action` rows were displayed **nowhere**. So the one question the log answers best — what did one student try, in what order, and what came of it — was answerable only by grepping `events.jsonl` on the box. Root cause: the data was always there; no surface ever read it as a session — the ADR-352 shape, a capability present and the surface not calling it. New `sessionsOf(events, profile)` (pure, beside `aggregate`) groups by `sid`, preserves LOG order within a session, classifies each submit through the PROFILE's outcome map, and counts submits/gaps; the `session` marker bounds a session rather than being a step. `?view=sessions` makes the «כניסות» card a drill like the other two, each session expanding (`<details>`, no JS) to its ordered steps — utterance, outcome, committed commands flattened beneath, store actions in place. `?sid=` pins one session, reachable from the URL, a lookup box, and a new per-row link in «פעילות אחרונה», so a bug report's session id lands directly on it. Nothing is silently truncated: per-session and per-page overflow are stated, and an event with no `sid` is never folded into a synthetic session — it is counted and reported. One renderer, two profiles, so 2-D and 3-D both get it ([ADR-W-004](06w-decisions-workspace.md) — nothing to mirror). **Deployed against events already in prod**, so existing sessions render retroactively — no wait for new traffic. **First proxy deploy since `prod/2026-07-27-2`**; the runbook's "did `server/` change?" rule read TRUE for the first time in two weeks. Rebased onto `main` at deploy time and **renumbered ADR-432 → ADR-434**, main having taken 432 (#474) and 433 (#477) while the PR was open. Gates on the deployed commit: full suite **7237 passed / 4 skipped** (403 files), `tsc -b` clean, all three CI lanes green, server slice 101 passed (52 admin). Verified live: deployed `proxy.mjs` sha256 **identical** to the local build, `ExecMainStartTimestamp` after the copy (so the running process is that build), `/healthz` ok, both admin dashboards and both app pages 200. Play-and-approve was waived — the operator had played the pre-rebase build on the work PC ("do this for me"); PR #471 stands as the record. |
| 2026-08-09 | `prod/2026-08-09` | `e7f4032` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched, not restarted**) | `index-D6eBkWDt.js` + `geoWorker-BlZmLenC.js` / `3d-CureT1kp.js` | **The honesty-and-legibility day: 22 commits, four of them fixing defects the operator found in that same session's fixes.** **2-D honesty:** accounting is a MULTISET, so a REPEATED stated number can no longer be paid for twice — «ריבוע במידות 4*4» stopped committing a size-less square with a green ✓ ([ADR-429](06-decisions.md#adr-429), #437); the dropped-construct gate ported from 3-D asks the missing question *did a stated object materialise at all* — «מלבן ABCD עם אלכסונים» no longer commits a bare rectangle, with the ADR-156 REUSE case measured as the only false flag in 1202 corpus steps ([ADR-430](06-decisions.md#adr-430), #456); and a stated magnitude is now labelled from the FACT rather than the surviving constraint, so the «זווית GBA = 37» that DRIVES the flagship figure is finally printed on it ([ADR-432](06-decisions.md#adr-432), #474). **The tool teaches one language:** the canonical form is the ∠ glyph everywhere — hint, step row and now the .docx export — one renderer behind all three so they cannot drift ([ADR-428](06-decisions.md#adr-428) Am. 1-3, #460/#450/#465). **RTL bidi, the day's recurring lesson:** an LTR technical run inside a Hebrew sentence was being reversed («|BC| = 10» → «10 = |BC|»); fixed at a RENDER-time chokepoint covering every t() call, after a per-value attempt proved it could not generalise ([ADR-431](06-decisions.md#adr-431), #464), extended to balanced delimiters, ported to 3-D ([ADR-3D-116](06b-decisions-3d.md#adr-3d-116), #468), and expressed differently in Word — per-RUN direction, because Word prints the isolate characters as visible boxes. **New capability:** the values-panel QUERY lane — ask «∠GBC» and get 53° when it is knowledge, computed inside the panel so a query can never contradict the rows above it ([ADR-433](06-decisions.md#adr-433), #477). **3-D:** a stated OBJECT must materialise ([ADR-3D-113](06b-decisions-3d.md#adr-3d-113), #438/#440); a height is stated by its APEX, not its segment — «גובה הפירמידה מנקודה D» builds, and the phrasing gap needed no new geometry ([ADR-3D-115](06b-decisions-3d.md#adr-3d-115), #448); the bare ambiguous form answers with guidance instead of reaching the LLM ([ADR-3D-117](06b-decisions-3d.md#adr-3d-117), #467); and the shared refusal sentence stopped claiming a partial commit that no path ever makes ([ADR-3D-114](06b-decisions-3d.md#adr-3d-114), #459). Operator waived play-and-approve on the last two PRs ("commit and deploy all the changes we did today"); the PRs stand as the record. Full suite 7226 green + tsc + both builds clean on the deployed commit; /healthz ok, both pages and all three bundles 200. |
| 2026-08-08 | `prod/2026-08-08-2` | `f94090a` | 2-D (static only; **`server/` + `src3d/` unchanged — proxy untouched, no 3-D build**) | `index-CnkqSvJL.js` + `geoWorker-7y-miD_C.js` | **The ADR-428 pair — the tool teaches its own input language — plus two operator-reported fixes.** «משולש ישר זווית» now builds LABEL-LESS: it was the one member of the bare-shape-noun family that returned null and then tripped `triangle`'s leftover guard, because `rightTriangle` was hand-written and never routed through the shared `shapeMacro` factory that gives every other shape auto-labelling and leftover discipline — routed through it, so the exception disappears rather than being special-cased (shadow matrix byte-identical, the proof the refactor changed no behaviour) ([ADR-428](06-decisions.md#adr-428), #446). The bare vertex angle «A = 40» is now **accepted AND taught**: prod showed two students in the identical flow relying on the LLM to recover it — precisely the failure ADR-428 names, since whether it works tomorrow depends on the model rather than on this codebase, invisibly to the student; ADR-164 already resolved a single-vertex angle from `ctx.neighbors`, so only the noun-less spelling was missing (registered after setRadius/radiusRelation/area, uppercase-only so a lowercase symbolic unit `a = 6` is never stolen), and on a successful commit the new `src/parser/canonical.ts` surfaces «זווית DAB = 40» rendered FROM THE LOWERED COMMANDS — never by rewriting the input string, which is what keeps the hint a report of what was understood rather than a guess at what was meant (#447; foundation for the #450 canonical echo). A named shape's own equal sides are now SHOWN, marked distinctly — the concave kite printed "no necessarily-equal sides found" although its pairs are exact in every sample, because `detectRelationsAcross` pools samples across variant alternatives so what is forced is the DISJUNCTION, and the layer could say "forced"/"not forced" but not "forced, on a label set you chose"; fixed with a second channel (dashed amber ticks + `?`) never folded into the first, its pairs sourced from `expandShapeVariant` itself so it generalises over the class by construction, an explicitly-stated pair staying in the forced channel ([ADR-427](06-decisions.md#adr-427), #444). The Word export's figure is legible: `r`/`stroke`/`fontSize` were absolute pixel constants in the SOURCE canvas while the .docx prints at a fixed physical width, so printed weight was `constant × (printWidth/canvasWidth)` — and the sharper defect it exposed is that the printed figure depended on the SIZE OF THE USER'S BROWSER WINDOW; `svgToPng` now scales ink by `canvasWidth/printWidth` (a drawn circle's `r` is GEOMETRY and deliberately untouched), print width is ONE shared constant, and the columns went 55/45 → 42/58 (8cm → 9.5cm) — ADR-3D-098 in the print dimension (#451). Plus operator-verbatim i18n wording on the declared-equality note (#444). Full suite **6428 passed / 4 skipped** (394 files) |
| 2026-08-08 | `prod/2026-08-08` | `995bb05` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched**) | `index-BmRAhW50.js` + `geoWorker-C9jbvU8a.js` / `3d-B8OXmalF.js` | The log-triage fix session: **three P1 honesty defects, all one signature — the tool asserting something the student never said, with a green ✓.** **2-D:** a NEGATED statement built its own OPPOSITE — «זווית A לא ישרה» emitted `set-angle A = 90`, byte-identical to the positive form, because negation had no representation anywhere in the parser; now refused pre-parse in the `looksLikeLatex` position, vocabulary verified against all 294 catalog examples for false positives ([ADR-425](06-decisions.md#adr-425), #436). A stated CONVEXITY is honoured — «דלתון קעור» drew a CONVEX kite, both because the qualifier was in no vocabulary and because convexity was a blanket default no configuration search could satisfy; now a requirement (ADR-244 shape) with the apply-time seat reflecting a vertex across its neighbours' line, plus the general fix that `findValidConfig` now searches the REFLECTION-mask dimension (previously only continuous jitter, so mirror configurations were reachable only by luck — ~2% of seeds) ([ADR-426](06-decisions.md#adr-426), #441). **3-D:** «משולש ישר זווית» as a pyramid base was BOTH silently dropped AND read as the solid's own «ישרה», turning a free tetra into a right pyramid — the ADR-052 cardinal sin; the qualifier joins the ONE `statedTriShape` vocabulary, a solid's rightness is now tested with the base qualifier REMOVED, and `rightTriangle` gained the leftover guard it lacked (the English side was broken in all three solid rules and additionally dropped the SOLID) — shadow matrix shrank 5→4 divergent pairs ([ADR-3D-111](06b-decisions-3d.md#adr-3d-111), #435). Plus the operator-requested feature: a polygon's CIRCUMSCRIBED and INSCRIBED circle in R³ — «מעגל חוסם/חסום», roles by the ב container marker never word order (ADR-245 ported), ring may be a flat polygon or a SOLID'S FACE, incircle triangles-only with an honest refusal for a general quad, retiring a now-false `cross-app` guidance ([ADR-3D-112](06b-decisions-3d.md#adr-3d-112), #442 → PR #445). Full suite **6385 passed / 4 skipped** (390 files) |
| 2026-07-30 | `prod/2026-07-30-4` | `4702435` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched**) | `index-zvQOnl0i.js` + `geoWorker-CqG5ESaz.js` / `3d-BattQr6N.js` | The day-2 batch, operator-played ("this works well" on the ADR-424 figure) + "full deploy". **2-D:** a definite circle reference resolves through ONE seam — «חסום במעגל» after «חצי מעגל» binds THE existing circle instead of silently minting a second one ([ADR-422](06-decisions.md#adr-422), #430); which part of a circle is DRAWN is engine knowledge — a semicircle's free riders land on the ink, «אמצע הקשת» takes the arc that exists ([ADR-423](06-decisions.md#adr-423), #429); a structurally VACUOUS collinear removes no DOF — «ישר ADE» over a derived crossing no longer drives `freeDofCount` to 0, so the two-secant power-of-a-point figure keeps its genuine free angle, the values panel prints only the FORCED multiples (`AC=4a, AD=5a, BC=6a, DE=3a` beside the stated `10a`/`8a`) and withholds CD/BE/radius, and the cue stops claiming "נקבע במלואו" on an under-determined figure ([ADR-424](06-decisions.md#adr-424), #432, P1 — the residual measured-defense filed as #434). **3-D:** a stated triangle-shape QUALIFIER is read from ONE vocabulary and always lowers — «שווה צלעות»/«שווה שוקיים» in all five positions, isosceles as a soft pair, the `droppedTriShape3` honesty gate on both commit paths ([ADR-3D-110](06b-decisions-3d.md#adr-3d-110), #424). Full suite **6288 passed / 4 skipped** (386 files) + `tsc -b` + both builds clean on the deployed tree (`4702435`); /healthz ok, both pages serve the new bundles (200, `text/javascript`). **Gate note:** Actions still billing-blocked (#388) — the local full suite was the gate. |
| 2026-07-30 | `prod/2026-07-30-3` | `387b6d2` | **2-D only** (3-D unchanged, not redeployed; `server/` unchanged — proxy untouched) | `index-DhMwYhoS.js` + `geoWorker-CIH4ZA8C.js` + `index-BQ254GUV.css` | The values panel stops inventing a size, and learns the student's own symbol. **[ADR-421](06-decisions.md#adr-421) (#426, P1 honesty):** an absolute magnitude is knowledge only when the SCALE is pinned — a bare square with no size stated anywhere had been printing `AB = 5`, area `25`, perimeter `20` in the panel AND `AB = 5` on the canvas, i.e. asserting a size the question never gave ([ADR-052](06-decisions.md#adr-052)). Root cause: the gate asked "does this vary across samples?", a proxy for "is the scale pinned?", and `samplingJobs` takes ONE sample when `freeDofCount === 0` — which means *rigid up to similarity*, not *size known* — so every magnitude trivially agreed with itself. Both consumers (panel rows + `definiteLengths` canvas labels) now gate on the semantic `scalePinned`; **angles and area-ratio classes keep printing on the same figure** — that asymmetry is the point. **[ADR-420](06-decisions.md#adr-420) (#427, PR #428, operator-played):** naming a segment declares a UNIT, so `AB = a` now yields `AC = a√2`, `AG = a√10/3`, `S = a²`, `P = 4a` (and `AB = 3x` ⇒ `3x√2`, `9x²`); a pinned scale still wins, two symbols withhold, non-linear/affine bindings are not units. Full suite **6210 passed / 4 skipped** (382 files) + `tsc -b` + build clean on the deployed tree; /healthz ok, served bundle **byte-identical** (md5 `e1815b98…`) to the audited build, 3-D 200. |
| 2026-07-30 | `prod/2026-07-30-2` | `32ca64c` | **2-D only** (3-D unchanged, not redeployed; `server/` unchanged — proxy untouched) | `index-DJqFxc1i.js` + `geoWorker-CliWR2sZ.js` | The evening batch, every item operator-played ("tested and works") + "merge, commit, deploy". **Refusals:** constraint descriptions no longer leak English into Hebrew messages, verifier notices included ([ADR-416](06-decisions.md#adr-416), #413); a PROVEN metric impossibility is refused instantly instead of parked as «נרשם אך לא משפיע» — the pinned-distance path bound, checked before the ladder, **27.8 s → 0.52 s** ([ADR-417](06-decisions.md#adr-417), #420), worded as the curriculum states it («סכום שתי צלעות תמיד גדול מהצלע השלישית») with a general-cycle variant so the sentence is never false. **Panel:** היקף joins the derived values for a circle AND a polygon, and each area ratio is its own clickable row ([ADR-410](06-decisions.md#adr-410) Am. 1, #414/#415). **Input coverage:** «נקודת מפגש האלכסונים» with no label builds the crossing and auto-names it by the textbook letter — H/I/O/M ([ADR-418](06-decisions.md#adr-418), #363); the guidance register's solid vocabulary went 4/9 → 9/9 and its coordinate patterns 5/8 → 8/8, and a compound line is now TAUGHT as numbered steps instead of escalated ([ADR-419](06-decisions.md#adr-419), #109/#108). Full suite **6192 passed / 4 skipped** (381 files) + `tsc -b` + build clean on the deployed tree; /healthz ok, the page serves the new bundles (200), 3-D page unaffected (200). **Gate note:** Actions still billing-blocked (#388) — the local full suite was the gate. |
| 2026-07-30 | `prod/2026-07-30` | `8954a86` | 2-D + 3-D (static only; **`server/` unchanged — proxy untouched**) | `index-B5wz-yLz.js` + `geoWorker-BOsvTpYa.js` / `3d-Pzzqkwyo.js` | The accumulated 14-commit batch, every user-visible piece operator-played this session ("all tested and working") + "deploy now". **2-D:** the קטע-אמצעים breakage — midsegment never lowers to a bare segment, the default side yields to an explicit membership, and a declared polygon driven to ZERO AREA is refused instead of silently accepted ([ADR-411](06-decisions.md#adr-411)/[412](06-decisions.md#adr-412)/[413](06-decisions.md#adr-413), #405/#407/#408 — played as M1–M4, incl. the prod figure `bad-2d-1` where «GE=3DG» now genuinely drives F to the quarter point); a NEW label in a collinearity statement is DEFINED by it ([ADR-408](06-decisions.md#adr-408), #402, PR #406) together with the two fixes its play exposed — an engine-created carrier must declare its DOF class, the missing `extension` flag that made both follow-on statements FALSE over-constraints ([ADR-414](06-decisions.md#adr-414), closes #412 **and #404**, whose morph ratchet is retired), and the collinearity family now reads its noun on either side so «GFH ישר» ≡ «ישר GFH» ([ADR-415](06-decisions.md#adr-415), #417); a membership statement presupposes its circle ([ADR-409](06-decisions.md#adr-409), #362, PR #409); the VALUES PANEL ([ADR-410](06-decisions.md#adr-410), #217, PR #410); and two perf ADRs — fold PREFIX REUSE so appending a step pays only the new fact ([ADR-406](06-decisions.md#adr-406)) and instant refusal of futile failures ([ADR-407](06-decisions.md#adr-407), #403). **3-D:** a stated relation's point-run carrier leaves its trace — patch + knee ([ADR-3D-109](06b-decisions-3d.md#adr-3d-109), #383). Full suite **6135 passed / 4 skipped** + `tsc -b` + both builds clean on the deployed tree; /healthz ok, both pages serve the new bundles (200). **Gate note:** Actions still billing-blocked (#388), so the local full suite was again the gate. |
| 2026-07-29 | `prod/2026-07-29` | `1a1fcfc` | 2-D + 3-D (static only; proxy untouched) | `index-BDrqGqmf.js` + `geoWorker-DDg4bN64.js` / `3d-C6JvU9wQ.js` | The 2026-07-29 batch, operator-played ("all validates and work") + "deploy": the relations program S3+S5 (plane column ADR-3D-105, distances ADR-3D-106, PRs #390/#391); 2-D #389 spelling-fold class (ADR-405 — מעויין/עפיפון/שוה at the normalizeUtterance orthography boundary); the magnitude bundle (ADR-3D-107, PR #399, closes #393+#335 — chained/expression magnitudes deterministic, LLM removed from the class); the panel bundle (ADR-3D-108, PR #400, closes #384/#395/#396/#397/#398 — plane-pair + distance panel rows, redundancy notice, distance witness + knee, plane display full→face→hidden, query-row RTL + point-run notation fix). Full suite 6065 green (4 standing skips) + tsc + both builds clean on the deployed tree; /healthz ok, both pages serve the new bundles (200). |
| 2026-07-28 | `prod/2026-07-28-3` | `5967637` | 3-D only (**no `server/` change — proxy untouched**; 2-D unchanged, not redeployed) | `3d-BuJI4l1X.js` · `3d-CWha1pFm.css` | **The relations program's S1+S2+S4, all previously unreleased.** S1 the operand atom + RELATION_TABLE + battery ([ADR-3D-102](06b-decisions-3d.md#adr-3d-102), #381) · S2 the NAMED-LINE column, 14 cells — point-on-ℓ, ∥/⟂/angle with a line operand ([ADR-3D-103](06b-decisions-3d.md#adr-3d-103), #382) · S4 MUTUAL POSITIONS, 12 cells — skew/intersecting/parallel/coincident as first-class statements over the operand pair, the closed ones driving and `skew` gated by the requirement lane ([ADR-3D-104](06b-decisions-3d.md#adr-3d-104), #387). S4 also ships the data panel reporting mutual positions **derived**, not only stated, and a mechanism fix: `scalePinned` is now a total `Record` over `ScalarPin['kind']` so a new pin kind can never again default to "pins the scale" (it had made the panel print a gauge unit as a given). **Gate note:** GitHub Actions has been blocked by billing since 2026-07-25 (#388), so CI did not run for any of these merges — the gate was the local full suite (**5924 passed**, 363 files), `tsc -b`, and both vite builds, per the operator's explicit decision to proceed. |
| 2026-07-28 | `prod/2026-07-28-2` | `42510a3` | 2-D + 3-D (static only; **no `server/` change — proxy untouched**) | `index-DJznThkt.js` · `3d-CuJO-crY.js` | **The full day-of-2026-07-28 batch.** 2-D: the #357 input-convention bundle ([ADR-403/404](06-decisions.md#adr-403) — active-voice extension «מאריכים את הצלע AB עד לנקודה D», «BC בסיס» → orientation guidance naming Rotate & align, the container-less «מעגל מוכל» clarification). 3-D: the stated-⊥ knee everywhere (#308, [ADR-3D-093](06b-decisions-3d.md#adr-3d-093)) · origin-anchored parametric line `l1:x=t(…)` (#356, ADR-3D-091) + lowercase-labels nudge (ADR-3D-092) · partial point on an axis (#279, ADR-3D-094) · **the placement-honesty arc**: a solid's placement is gauge only while nothing is absolute (#367, ADR-3D-095) → view-space legibility + unit-direction line reach (#372, ADR-3D-099) → the LANDING FUNNEL classifying gauge-component freedom, closing the pair-injection/invariant-pin doors (#379, ADR-3D-101 — S0 of the relations program docs/26) · a parametric line's canvas echo shows the student's symbolic form while unpinned (#371, ADR-3D-096) · «ℓ ⟂ π» drawn as perpendicular — knee at the crossing + the patch reaches it (#373, ADR-3D-097) · knees sized in screen units (#374, ADR-3D-098, also closes #369) · **the ⟂ operand-matrix cell «מישור ACD אנך לישר ℓ1»** with the drive that rotates the figure + the noun-slip corrective notice (#375 → PR #376, ADR-3D-100). Suite 5797 green at deploy. |
| 2026-07-28 | `prod/2026-07-28` | `8689bfc` | 3-D (static only; **proxy + 2-D untouched — no `server/` and no 2-D changes**) | `3d-DDEoH9xc.js` | **The solid-modifier bundle — #349 (PR #355) + #305/#341/#358 (PR #366) — one class, landed as two PRs.** **#349** ([ADR-3D-089](06b-decisions-3d.md#adr-3d-089)) obliqueness is a MODIFIER of any prism base, not a per-base template: `prismBaseDims`/`prismBaseRing` factor the base ring out of the lateral translation (keys/ranges verbatim ⇒ right prisms bit-identical), `parallelepiped` becomes a *spelling* normalized to `prism4 + oblique` at apply (one oblique code path; old saves still load), and `make-right-prism` clears the flag for any base. `מנסרה משולשת` / `מנסרה שבסיסה משולש` — six prod submits from one user bouncing off a refusal — now build oblique with the tilt a genuine free DOF. *This PR had been open and unrebased since 2026-07-26; rebased onto main and its ADR renumbered `081`→`089` (081 had been taken by the SURD formatter meanwhile).* **#305/#341/#358** ([ADR-3D-090](06b-decisions-3d.md#adr-3d-090)) the pyramid twin: rightness is a modifier of any BASE. Root cause — rightness lived in each kind's *dim parameterization* (`pyramid4r` hard-codes the apex at `(0.5,b/2,h)`), so it could not be added by the ADR-110 macro route a base *shape* uses, and every (base × rightness) pair needed its own kind: right+rhombus deferred, **right+parallelogram silently dropped the stated «ישרה»** (#341), and kite/trapezoid/general-quad bases had no pyramid at all (#358). Per the operator ruling (2026-07-27), «ישרה» = equal lateral edges = apex over the base's **circumcentre** ⇒ the base must be **cyclic**, so a non-cyclic base is **constrained into its family's cyclic member with a build notice, never refused** (rhombus→square, parallelogram→rectangle keeping a free aspect, kite→right kite, trapezoid→isosceles, quad→cyclic). New `baseShapes.ts` defines each base ONCE (the 5 legacy kinds re-derived **bit-identical**); `ringCircumcentre` for every base also closes the latent [ADR-3D-080](06b-decisions-3d.md#adr-3d-080) bug that seated a quad apex over the *centroid*; the concyclicity residual is the **signed** `cos A + cos C = 0` (Ptolemy is non-negative ⇒ touches zero ⇒ the descent stalled a measured ~1e-3 short — the ADR-3D-006 lesson); one `statedQuadBase` vocabulary so a recognised noun is always a lowerable one (the ADR-3D-084 class cannot recur), with `droppedShapeNoun3` reading the same registry; and a NEW non-error **build-notice** channel (`notices.ts` → `Derived3.notices` → App3). Boundary held: `מעוין` + `∠DAB=60` + «ישרה» still refuses honestly — the auto-fix consumes FREE DOFs only. **PR #306 was superseded** (its #304 half already fixed differently on main by ADR-3D-084; its rightness policy predates the ruling) — its registry was harvested. Gates on the deployed tree (`8689bfc`): full suite green, 3-D lane **1501 passed / 92 files**, `tsc -b` + `vite build --config vite.config.3d.ts` clean; index.html serves the new bundle (200, `text/javascript`), 2-D page 200, proxy `/healthz` ok. Operator "commit and deploy". |
| 2026-07-27 | `prod/2026-07-27-4` | `cc782de` | 2-D only (static; no proxy restart) | `index-DL54QBEv.js` + `geoWorker-DDg4bN64.js` | **#157 + #258 — both operator-play-validated on the dev server before deploy.** **#157** ([ADR-401](06-decisions.md#adr-401)) the detection sample sweep runs in the geometry WORKER: measurement overturned the filed diagnosis (`firstSatisfyingSeed` 0 ms; `sharedSamples` ~9 s ON the main thread, and the always-on `detectCrossings` paid it after EVERY step — the "even drawing a line between two existing nodes takes long" report). One `detectAll` sweep serves relations + shapes + crossings with classification done worker-side; content-keyed sample memo (identity misses on every structured clone); two worker LANES so the background sweep never queues ahead of what the student awaits; the batched main-thread sampler DELETED. Locked by the `main-thread-sweeps.test.ts` source ratchet + `detect-lane.test.ts`; remaining main-thread costs filed with numbers as #364 (seed auto-advance, needs a UX call) and #365 (no fold prefix reuse). Operator: "it worked and was very fast." **#258** ([ADR-402](06-decisions.md#adr-402)) the PRESERVATION GATE: an obligation stored only in a carrier's solve state (case (1) embeds without listing) was DESTROYED by `settleOnFrozenPrior`'s bake (the coupled-promotion converts embedded→directive→`solve: undefined`; the ADR-229/F2 restore law knows only the directive shape) — and the destructive rescue then WON acceptance, because dropping a given makes a fold easier and the acceptance tests only examine the new constraints. Every ladder rescue accept now requires `obligationsOf(next) ⊆ obligationsOf(candidate)` (repair-as-checks, else the stage's accept is VOID — docs/LADDER.md 2h′) + sibling hygiene (`drivenConstraintsOf` += `solve.also`; the case-(1) `solve===undefined` guard; case-(D) `also` carry). The #236 book figure's sizes-early order now lands the exact book values (\|BC\|=\|BE\|=6) in every entry order; the membership-conversion KNOWN RESIDUAL flipped to `bookValues`; new `obligation-preservation.test.ts`. Gates on the deployed tree (main `cc782de`): full suite **346 files / 5,604 passed / 4 standing skips** (273 s), `tsc -b` + build clean; live page serves the new bundles (200, `text/javascript`), proxy `/healthz` ok (untouched). Rode along on main since `-3`: the operator's play-a-fix rule mechanized (`scripts/ensure-test-server.mjs` Stop hook, CLAUDE.md + memory) — workflow only, no app behaviour. |
| 2026-07-27 | `prod/2026-07-27-3` | `77ca3c8` | 2-D only (static; no proxy restart) | `index-BkOxZO4f.js` + `geoWorker-BBhfwBlX.js` | **The solver-basin pair — #150 + #359 — plus the status-attribution fix under them.** **#358/#360** ([ADR-398](06-decisions.md#adr-398)) a per-seed `evaluate` failure is attributed to its OWNING fact row: the failure was reported against whichever row the sweep happened to be on, so a real refusal read as an unrelated step's problem and the honest blame was lost. **#150** ([ADR-399](06-decisions.md#adr-399) `ensureOwnership`) a constraint that is already SATISFIED when it is accepted now claims its DOF instead of committing as an unowned check — Q27's `EF=EG` was never "converging 1/64": the default figure is symmetric to 8e-15, so the equality committed via `main:primary` with no carrier, nothing re-solved per seed, and the single valid seed was the accidental draw itself (docs/17 §2.2 verbatim — routing by observed residual). The four primary accepts now probe with the real `applySeed`, recruit at a blamed probe state through the standard recruiter, and transplant the fewest-carrier marking; genuinely redundant statements survive the probe and stay checks (Varignon guard). **#359** ([ADR-400](06-decisions.md#adr-400) `warmStartCarriers`) the displayed fold's per-seed tail `evaluate` failed at 4/15 seeds from perturbed starts and `tailChoice` silently fell back to the PENDING base fold; a failed tail now retries once from the fold's committed carrier values (the ADR-238 pattern). Measured: Q27 1/64 → **64/64 re-solve, 27/64 displayable**, labelings BE=DE 9 / AE=DE 18 (the operator's previously unreachable mirror is one "show another" away), zero displayable blow-ups; #359 reaches closed form at 16/16 seeds. Locks: `basin-ownership.test.ts`, scenario `q27-chords-equal-distances-drives`, rewritten `status-attribution.test.ts`. Gates on the deployed tree (main `77ca3c8`): full suite **343 files / 5,589 passed / 4 standing skips** (276 s), `npm run build` clean; live page serves the new bundle (200, `text/javascript`), proxy `/healthz` ok (untouched — no `server/` change). Downstream unblocked by this pair: **#258** (amber-settled re-fold — the owner maps and honest statuses are its machinery) and **#157** (perf). |
| 2026-07-27 | `prod/2026-07-27-2` | `57fc75d` | 2-D + 3-D + **proxy** (restarted) | `index-CKqSNVsR.js` + `geoWorker-CYRCMTHx.js` / `3d-BKFIj8ke.js` | **The autonomous P2 queue sweep — six rounds, seven issues, operator-validated before deploy.** **#288** ([ADR-3D-086](06b-decisions-3d.md#adr-3d-086)) the 3-D save whitelist becomes a TOTAL function over `Command3['type']` — it was a hand-maintained `Set` that could disagree with the union at all, so `inject-pair` (`BD = (-4,5,12)`) still saved cleanly and could not be re-opened *after* #288 was called fixed; the catalog-driven guard added then can only reach types some catalog example emits, so the structural fix is an exhaustive `Record` that makes an unclassified command a COMPILE error. **#309** ([ADR-3D-087](06b-decisions-3d.md#adr-3d-087)) a 3-D load reports the OUTCOME, not just the schema — `deserializeFigure3` validated the file and `loadFigure` cleared `lastError`, so a file this build cannot rebuild loaded "successfully" onto a blank canvas although `derive3` had already recorded the per-fact failure; new read-only `loadAudit3` + an amber note distinguishing "some steps are broken" from "this file does not open in this version" (the load still opens exactly as saved — never destructive). **#293** ([ADR-395](06-decisions.md#adr-395)) the 2-D LLM prompt must not invent an unstated property **nor teach one**: a few-shot demonstrated the sin outright (`"a circle with a triangle inscribed in it"` → `circle centered at O radius 5`), and measurement showed the fabricated line was *unnecessary* — the inscribe alone yields an identical object set with the radius `{via:'free'}` instead of pinned; fixed the example, added the never-invent + never-drop rules, and a MECHANICAL guard (every magnitude in an example's steps must appear in its freeform). **#348 + #347** ([ADR-396](06-decisions.md#adr-396)) two supported constructs unreachable in production: a collinearity list truncated to its first three points (`labelRun` returns exactly n; ADR-050's `set-line` is variadic and the sibling glued rule already emitted it) and the whole colon-ratio family discarded by a `droppedGivenNumbers` false positive (the gate consumed slash fractions whole but had no colon form, and `p:q` never survives as literal digits) — plus the two catalog entries whose absence is why the coverage guard never caught it (#140). **#337** ([ADR-3D-088](06b-decisions-3d.md#adr-3d-088)) one relation, every phrasing: a shared angle-phrase atom so `∠SAB` and «הזווית שבין הוקטור BE לבין הוקטור BC'» are the same operand, and the shared prefix stripper learned the given-framing «נתון ש» alongside the proof framing (renamed `stripStatementPrefix`; the 3-D shadow matrix proves no existing utterance changed which rule claims it). **#345** ([ADR-397](06-decisions.md#adr-397)) a configuration is displayable only if the figure satisfies its own COMMANDS — `meetsRequirements` judged geometry and the verifier but never consulted the step statuses, so "show another configuration" could show a seed where a stated given settled `over-constrained` (\|O2M\| = 2.9 instead of 16); the cross-seed escape class one level up. **Proxy rebuilt + restarted** because `server/parseHandler.ts` imports `src/parser/llmShared` — the runbook's "did `server/` change?" rule reads FALSE here even though proxy behaviour does change, the trap being that a static-only push would have left production running the prompt that teaches the invented radius. **#293 verified with ONE operator-approved live Haiku call against the new prompt: `["triangle ABC inscribed in circle O"]`, no radius invented.** Gates on the deployed tree (main `57fc75d`): full suite **341 files / 5,581 passed / 4 standing skips**, `tsc -b` + all three builds clean; both pages serve the new bundles (200, `text/javascript`), proxy `/healthz` ok after restart. Operator play-validated every fix on the dev server first. Filed not fixed: **#359** (the solver-basin half of #345 — ADR-397 hides the bad configs, it does not make seed 2 solvable). |
| 2026-07-27 | `prod/2026-07-27` | `fd8c148` | 3-D (static only; **proxy + 2-D untouched — no `server/` changes and no 2-D runtime changes**) | `3d-DUZ86p1J.js` | **The overnight P1 sweep (#332/#304/#330), deployed after operator play-testing.** **#332** ([ADR-3D-083](06b-decisions-3d.md)) a symbol-pin root that COLLAPSES the driven segment is a VACUOUS zero, not a solution — a ∥/⊥/length pin's residual is normalised by the driven segment length, so `EM = k·CM` + `EM ∥ plane BSC` (direction fixed ∝ CM) has only the collapse k≈0 as a zero-crossing, where a zero vector reads as trivially parallel; `evaluate.ts` selected `roots[0]` with no non-degeneracy filter, placed E exactly on M, and — E having a position — committed all 17 rows GREEN with \|EM\| ≈ 0. A shared `firstNonDegenerateRoot()` at BOTH symbol-pin root-selection sites (the main vec-defined sweep + the ADR-3D-056 deferred 2nd pass) now requires the driven segment to exceed 1e-6·figureScale; no non-degenerate root ⇒ the point is unpositioned ⇒ the store refuses honestly (`no-solution`, keep-prior). Valid roots are chosen exactly as before, so the V8-f length-rel / seg-perp / ∥-plane exam gates are unaffected. The 3-D analogue of the 2-D anti-collapse / general-position principle (ADR-238 / ADR-253). **#304** ([ADR-3D-084](06b-decisions-3d.md)) `פירמידה שבסיסה מעוין` parsed to `pyramid4gr` — a free-apex RECTANGLE base — silently dropping the equal-sides given and asserting an unstated right angle; the parser now lowers a rhombus base to `pyramidPar` + `length-rel(\|AB\|=\|AD\|)` (the ADR-3D-078 prism macro, pyramid edition, driving a genuine rhombus at every seed) and the right+rhombus combination DEFERS rather than drop the shape, with a new `droppedShapeNoun3` honesty gate at the `store3.submitSteps` LLM seam refusing any decomposition whose committed base lacks a stated shape's defining property. Rules now dispatch or defer — never emit a kind that contradicts a stated qualifier. **#330** ([ADR-3D-085](06b-decisions-3d.md)) special-line phrasings never silently mis-build — a point-LAST centroid (`תיכוני הפאה SAB נפגשים בנקודה P`, the book's exact wording) was absorbed by `planarPolygon` as a bare triangle with P DROPPED and no error, and the midpoint rule's `/אמצע/` guard matched INSIDE `אמצעי` (⊥-bisector) and `אמצעים` (midsegment), reducing `אנך אמצעי ל AB` to a bare midpoint; `centroidRule` widened to both orders (ה optional, `פאה`/`משולש`, construct-state `תיכוני`, En mirrors), the guard tightened to `/אמצע(?!י)/` so the ⊥-bisector escalates honestly, and an ADR-024 leftover guard on `planarPolygon` closes the class beyond the two reported instances (ride-along P2: altitude from a vertex of a named triangle). Also carried, tooling only with no runtime effect: the ADR-394 measured fast/slow test-tier split and the mechanical two-machine handoff (`scripts/session-sync.mjs` + the `/handoff` skill). Gates on the deployed tree (main `fd8c148`): full suite **337 files / 5,536 passed / 4 standing skips**, `tsc -b` + `build:3d` clean; live page references the new bundle (200, `text/javascript`), CSS 200, proxy `/healthz` ok (untouched), 2-D still serving its `prod/2026-07-25-5` bundle. Deferred and filed from the same fixes: **#342** (the ⊥-bisector CONSTRUCT in 3-D — needs-operator), **#343** (bisector short forms + altitude-from-vertex), **#341** (a parallelogram right pyramid still drops "right"). |
| 2026-07-25 | `prod/2026-07-25-5` | `bca8542` | 2-D + 3-D (static only; **proxy untouched — no server/ changes**) | `index-ByCYMBcd.js` + `geoWorker-ZOz1ntgP.js` / `3d-DUqGWC_D.js` | **The P3-reduction campaign, batches B2–B4 (three self-merged PRs), deployed as one.** **B2** ([ADR-392](06-decisions.md#adr-392), PR #334) — the guided-message register: `#244` «זוית abc» lowercase no longer brushed off as unrelated (the angle keyword `זו?וי` matches both vav spellings), `#329` LaTeX-pasted input (`$…$`, `\triangle`) gets a "use the symbol palette" nudge PRE-parse, `#246` a «שורש N» word-form magnitude gets "use the √ symbol" at the escalation seam (both mirrored in the log-triage harness, ADR-346). **B3** ([ADR-393](06-decisions.md#adr-393) + [ADR-3D-081](06b-decisions-3d.md), PR #338) — display consistency: `#164` ONE shared 2dp measure formatter (`src/format.ts`; `AC 7.34` was "7.3"), `#200` refusal messages no longer leak internal ids (`circle-O`→`O`, `~A`→`⟨…⟩`, floats→2dp), `#269` a SURD tier for the 3-D data panel (`|B'E| = √5`, not "2.24"). **B4** ([ADR-3D-082](06b-decisions-3d.md), PR #340) — 3-D parser/query: `#275` a bare parametric line `x=(0,2,0)+t(2,-2,0)` auto-binds ℓ, `#322` re-typing a constraint-macro no longer duplicates its ScalarPin (dedup at the apply chokepoint), `#328` «נפח המנסרה» (definite bare solid noun) resolves to the one prism. Deferred with analysis: #108 (LLM-vs-guide policy), #276 (needs an `on-axis` point kind). Gates on the deployed tree (main `bca8542`): full 2-D suite **4211 passed / 4 skipped**, 3-D suite **1368 passed**, `tsc -b` + both builds clean; both pages serve the new bundles (200, `text/javascript`), proxy `/healthz` ok (untouched). Operator "deploy all we have". |
| 2026-07-25 | `prod/2026-07-25-4` | `9cd47ca` | 2-D (static only; **proxy + 3-D untouched — no server/ changes**) | `index-Cc8bGXyo.js` | **The P3 parser-phrasing batch (PR #331, [ADR-391](06-decisions.md#adr-391)) — first slice of the P3-reduction campaign; six log-triage (2026-07-25) grammar near-misses, one PR.** A HEAD-verification pass before any edit flipped TWO from the escalations the issues described into **SILENT bugs** (worse than a refusal): **#267** «נסמן ∠CAD=A1» silently set ∠CAD = **1°** (read the "1" of "A1" as the value) — fixed with an `=` alias branch (letter+digit RHS only, so `=40` stays a value / `=∠DEF` an equality) + a `(?<![A-Za-z])` guard so a label subscript is never a degree value; **#198** «מיתר AR» (R bound as the radius symbol) built a **node R** on the circle (the operator ruling forbids it) — fixed with a new `reserved-symbol` deterministic clarification (radius RELATIONS `R>r`/`R=1.5r` never caught). The other four: **#245** «M ו N נמצאות על הצלע BC» (`pointsOnSegment` gains the `נמצאות` verb its siblings accept), **#242** «גובה בנקודה A» (keyword-anchored `LOCATIVE_APEX` on altitude+median, deliberately NOT free-floating — «בנקודה X» is the universal crossing marker), **#231** «BC על מעגל» is a CHORD + «מיתר» introduces its circle (`pairOnCircle` + `resolveOrIntroduceCircle({implied})`). **#47 closed alongside** (forms 1&4 already build + locked; form 2 folded into #148). New regexes compose from the lexicon `LABEL` atom (S2.1 ratchet flat); the shadow-matrix allowlist shrank by one (the fixed alias shadow). Operator "commit and deploy" (play-and-approve waived, PR kept as record). Gates on the deployed tree (main `9cd47ca`): full 2-D suite **256 files / 4177 passed / 4 skipped**, `tsc -b` + `vite build` clean; live page serves the new bundle (200, `text/javascript`), proxy `/healthz` ok (untouched). Locked by `p3-parser-phrasing.test.ts` (20) + the updated `multi-point-on-circle.test.ts`. |
| 2026-07-25 | `prod/2026-07-25-2` | `8f860f5` | 3-D (static only; **proxy + 2-D untouched**) | `3d-TzvKZYfq.js` | **The exam-completion day: four operator-reported fixes + three play-tested features, all from one live testing session on the pyramid bagrut figure.** Fixes: **#311** ([ADR-3D-072](06b-decisions-3d.md)) a derived vector decomposes in the DECLARED span whatever its rank — the 3-basis hard gate refused every planar-figure decomposition (`DE = 1/3·v` read «not determined»); rank-aware Gram solve with a vanishing-residual acceptance, the query lane's inline 3×3 copy deleted onto the shared core; the companion «E was not placed correctly» report verified NOT a bug (E(+) provably on SC at t=⅔, locked). **#312** ([ADR-3D-073](06b-decisions-3d.md)) vector atoms keep their notation under any coefficient syntax (`u/6`, `2v`, `(1-t)u` — the boundary-enumeration class; formatter extracted to pure `notation.ts`). **#315+Am.1** ([ADR-3D-074](06b-decisions-3d.md), P1 honesty) a pin enables only the coordinate family it DETERMINES — a pure pair injection printed the pivot's gauge origin as `A(0, 0, 0)`; points/planes now need translation pinned, vectors keep the frame+seed-stability gate (the amendment operator-validated: v = 3·DE prints, u stays gauge). **#316** ([ADR-3D-075](06b-decisions-3d.md)) «D=(8,10,-12)» ≡ «D(8,10,-12)» — the figure's freedom decides drive-vs-verify (pin AND claim, the claim the final arbiter; symbol-defined points guarded; S = 3D−2B = (18,12,−18), the book answer). Features (PRs #314, #320, operator play-tested): **#313** MathML notation (stretchy arrow over the FULL pair name, real fractions, arrow+underline named vectors; `isVectorFact3` routes so prose never gains an arrow). **#318** ([ADR-3D-077](06b-decisions-3d.md)) plane FACE/FULL display toggle per plane fact row (absent-means-full, persisted leniently). **#319** ([ADR-3D-076](06b-decisions-3d.md)) line↔plane angle: exam phrasings, a Greek value NAMES the measure (panel prints `α = X°` when stable), query kinds line-plane + plane-plane over one shared Newell helper. Filed not built: **#317** (query-lane plane equation — the fact route works). Gates on the deployed tree (main `8f860f5`): full suite **332 files / 5,329 passed / 4 standing skips**, `tsc -b` + `build:3d` clean; live page serves the new bundle (200), proxy `/healthz` ok. |
| 2026-07-25 | `prod/2026-07-25` | `a3836a5` | 2-D + 3-D (static only; **proxy untouched — no server/ changes**) | `index-YNvQ05tS.js` / `3d-BUUP73M6.js` | **The foundation-hardening program (docs/23 review → docs/24 plan → overnight execution + the morning decision batch) — deployed as ONE baseline so subsequent bug fixes aren't mixed with the big change (operator).** Six commits since `prod/2026-07-24-2`: Phase 0 `4047256` (docs/LADDER.md contract + `StepResult.ladder` trace; 35 numeric-core characterization locks; `submit()` extracted to `src/app/submitPipeline.ts` with 7 direct tests; `constraintKey()` + the directive-interning invariant), Phase 1 `4be0837` (ONE transactional `runFailureLadder()` — three divergent copies unified, recruiter cases C/D restore-on-failure per M2 law ii; the pure `src/replay/` layer extracted from geoStore with the `engine ← replay ← store` direction test), Phase 2 `9f39306` (`src/parser/lexicon.ts` atoms + the lexical ratchet; the bilingual morphology matrix in BOTH products; the 3-D shadow-matrix ordering guard + `honesty3.ts` LLM gates wired at `submitSteps`), Phases 3+4 `9e0453a` (span accounting SHADOW-ONLY wired into the dev log; theorems re-pointed to the zustand-free replay leaf), and the operator-approved batch `a3836a5` (S3.2 stages a+b: `components.ts` partition + `jointFirst` as a rescue tier BEHIND the recruit rungs — the measured stability amendment; `solveLM.ts` landed characterization-locked; span accountant → 0 hard divergences on 290 catalog + 1,084 corpus utterances; scenario corpus split into harness+4 chunks; `theorems/evidence.ts` extracted). **Behavior contract: the apps should be indistinguishable from `prod/2026-07-24-2`** — this is hardening; the one user-visible engine addition is the component-joint rescue tier (fires only where every recruit rung failed). Gates on the deployed tree (main `a3836a5`): full suite **324 files / 5,283 passed / 4 standing skips** (+344 tests over the pre-program baseline), `tsc -b` + both builds clean, hardest-fixture replay 248 ms (baseline 244 ms, budget ≤305 ms); live pages reference the new bundles (both 200), proxy `/healthz` ok (untouched). Revert point: `baseline/2026-07-24-pre-hardening`; per-phase reverts available at each listed commit. |
| 2026-07-24 | `prod/2026-07-24-2` | `637e02e` | 3-D (static only; proxy untouched) | `3d-CW7MzIYU.js` | **The symbolic-coefficient + data-panel batch** — four bugs from one operator testing session, all four the SAME class (a proxy signal standing in for the semantic fact, docs/17 §2.2). **#299** ([ADR-3D-068](06b-decisions-3d.md)): `AS=(1-t)u+0.5v+tw` was not-handled — the term tokenizer split on any `+`/`-` REGARDLESS of paren depth, shredding `(1-t)*u` into `(1` + `-t)*u`; nothing downstream was wrong (SYM_TERM already matched the intact term, the engine already evaluates `k + p·sym`) and no CAS boundary was involved. One shared depth-tracked `splitTopLevelTerms` replaces all FOUR paren-blind copies. **#300** ([ADR-3D-069](06b-decisions-3d.md)): `AM=(0.5+k/6)u+(k+3.5)w+0.5v` — a dedicated `(k/2)` whole-paren fast path meant a divided symbol never reached `parseParamExpr`, whose term regex had NO division, so `(k/6)u` parsed and `(0.5+k/6)u` did not; the carve-out was what HID the gap. Widened the shared `PARAM_TERM` to the rational forms and DELETED the fast path. The operator's «2 params» assumption was checked and CORRECTED, not built — their statement carries one param; the genuine two-unknowns-in-one-expression boundary is filed as **#301** (needs-operator). **#302** ([ADR-3D-070](06b-decisions-3d.md)): the data panel could not report `SM` — `parametricDecomp` classified parameters by PIN KIND (excluding only `rel:'value'`), but `SO⊥ABCD` DETERMINES `t` = ½ at every seed while `k` roams, so a determined symbol was counted as a parameter and every `k`-dependent row vanished. Now decided by MEASUREMENT (a def whose own vector decomposes stably is determined — reuses `basisDecompose`, no second sampler); ≥2 roaming symbols still return honest null. Panel + query lane now read `SM = (k + 3)·w + 1/6·k·u`, verified numerically against the engine at k = 0/1/2.5. **#303** ([ADR-3D-071](06b-decisions-3d.md), filed P3 → **P1 on reproduction**): `האלכסונים AC ו BD נחתכים בנקודה O` put O on the midpoint of EDGE AB, not the face centre — silently, no error: the two-diagonal branch triggered on COUNTING the word `אלכסון`, but the Hebrew plural names both diagonals in ONE word, so the labels fell through and were read as the cyclic quad A→C→B→D. Now decided by letter GROUPING (`AC ו BD` is two RUNs of two, `ABCD` is one run of four — already in the tokenizer); two phrasing seams fixed alongside (`…at point O` unmatched; the verb gate spelled `intersection` but not `intersect`). The noun-less form deliberately still defers to the LLM (ADR-052 — it would assert an unstated parallelogram) and is asserted as deferring. Gates on the deployed tree (main `637e02e`): 3-D **937** + 2-D **4059** passed (4 standing skips), `tsc -b` + `build:3d` clean; live page + new bundle 200 (`text/javascript`), proxy `/healthz` ok. **Static-only — `server/` unchanged, proxy untouched; 2-D untouched.** |
| 2026-07-24 | `prod/2026-07-24` | `bb84928` | 3-D | `3d-BnSqyTGx.js` | **Data-panel completeness — #296 + #297**, both found in one operator session on the free-scale «dd» figure (square-base pyramid, `AE=t·AS`, `EO⊥AS`, `∠SAD=∠SAB=α`). **#296** ([ADR-3D-066](06b-decisions-3d.md)): the panel's empty-guard tested `vectors`/`points`/`planes` but OMITTED `relations` — and `relations` renders only inside that guard — so a figure whose only derived knowledge is relations rendered a false "empty", HIDING `\|u\|=\|v\|` and `u·v=0`; one shared `panelIsEmpty` predicate now backs both the guard and the render (class fix, not a special case). **#297** ([ADR-3D-067](06b-decisions-3d.md), PR #298): (a) **parametric vectors for a DRIVEN parameter** — `EO⃗ = ½u + ½v − t·w`, `AE⃗ = t·w`; `t` is pinned by `EO⊥AS` (a `seg-perp` symbolPin) so its VALUE roams with the free apex and the parametric form is the only stable representation — `decomposeSym` now admits constraint-driven symbols (excluding only `rel:'value'` pins) and the k-probes REPLACE the driving pin; (b) **stated/forced angle equalities** — `∠SAD = ∠SAB` (a `cos-eq`, equal to machine precision every seed) now surfaces value-free, the scale-free twin of `\|u\|=\|v\|`, while a determined marker still prints `α = X°` and an under-determined singleton still prints nothing; (c) **the QUERY lane shares the panel's engine** — extracted `basisDecompose`/`parametricDecomp`, so querying `AE` returns `t·w` (was "depends on α") and a query can no longer diverge from its panel row. Gates on the deployed tree (main `bb84928`): full suite **4878 passed / 4 skipped**, `tsc -b` + `build:3d` clean; live page serves the new bundle (200, `text/javascript`), proxy `/healthz` ok. **Static-only — `server/` unchanged, proxy untouched; 2-D untouched.** |
| 2026-07-23 | `prod/2026-07-23-2` | `a698783` | 3-D + **proxy** | `3d-BRSpZKnV.js` / `proxy.mjs` | **PR #282 — the 3-D DATA & MEASURE PANEL — reconciled with the morning prism bundle and shipped** (the operator had dev-tested #282 but it never merged; found while diagnosing "prod is missing the α/range/query features"). Delivers: **#271** angle equality (kept #282's `angle-pair-eq`, wired to the α-label/bound system; the morning bundle's standalone `angles-equal` was the duplicate and was REMOVED — [ADR-3D-063](docs/06b-decisions-3d.md); [ADR-3D-061](docs/06b-decisions-3d.md) superseded); **#272** a value for a named measure (`α = 70` pins every angle wearing it) + **Greek letters (α β γ θ) and `<` in the palette**; **#273** numeric BOUNDS ([ADR-3D-064](docs/06b-decisions-3d.md): `60 < α < 90` / `∠SAB > 60` is a REQUIREMENT, the angle keeps its DOF — new `meetsRequirements3`/`firstSatisfyingSeed3` config-search layer); **#274** the data-panel QUERY lane ([ADR-3D-065](docs/06b-decisions-3d.md): `queries.ts`, ask `w·v`/`\|EN\|` without adding a fact); and the **#288** save-whitelist repair (a guard test round-trips every cataloged command through save→load so the whitelist can't drift — it caught `make-right-prism`, added). Reconciliation: merged `main` into the #282 branch, deduped #271, ADR renumber (#282's 052/053/057 → 063/064/065, main holds 057-062). Gates on the deployed tree (main `a698783`): full 3-D + server suite **895 passed**, `tsc -b` + `build:3d` + `build:proxy` clean; live page serves the new bundle (200, `text/javascript`), `angle-pair-eq`/`angle-bound3`/α-palette verified present in the served JS, proxy `/healthz` ok. 2-D untouched. |
| 2026-07-23 | `prod/2026-07-23` | `0292b12` | 2-D + 3-D + **proxy** | `index-CV7lYLfV.js` + `geoWorker-CTASLD-t.js` / `3d-C4INCPHk.js` / `proxy.mjs` | **The prism / ⟂-constraint bundle (operator play-tested + approved) + a full catch-up of pending main.** This session (PR #294, 3-D): #289 (ADR-3D-057) `המנסרה ישרה` makes THE existing solid a right prism (M1 — oblique `מקבילון`→`prism4` in place, honest `no-prism` refusal) instead of the LLM re-declaring vertices; #290 (ADR-3D-058) the 3-D LLM prompt forbids inventing an unstated PROPERTY (a prism not stated right is OBLIQUE, never right; 2-D sibling audit filed #293); #291 (ADR-3D-059) free parallelogram bases seed VISIBLY oblique (∠DAB ≈ 45–76°) so a `מקבילית` never renders as a מלבן; #292 (ADR-3D-060) the DOF cue is monotone non-increasing on a ⟂ (drops the free +7 gauge, pyramidPar+u⊥v 5→4); #271 (ADR-3D-061, P1) a general angle EQUALITY `∠SAB = ∠SAD` lowers to `cos-eq` (chained `= α` / reused-label assert it); #295 (ADR-3D-062, amends 058) a bare `מנסרה שבסיסה מקבילית` builds an OBLIQUE `מקבילון` (the build→pin entry point). **Caught up (already on main, undeployed):** 3-D #268 (ADR-3D-054), #284 (055), #287 (056); 2-D #277 (ADR-390). ADR collision on merge resolved (this bundle renumbered 050-055 → 057-062, origin used 054/055/056). **Proxy redeployed** for the #290/#295 3-D prompt honesty (`parseHandler` bundles `llmShared3`). Gates on the deployed tree (main `0292b12`): full suite **4063 (2-D) + 853 (3-D+server) passed / 4+2 skips**, `tsc -b` + all three builds clean; live pages serve the new bundles (200, `text/javascript`), proxy `/healthz` ok. Env note: `.git` + `node_modules` Dropbox-conflict corruption cleaned this session; `com.dropbox.ignored` set on node_modules/dist/dist-3d. |
| 2026-07-22 | `prod/2026-07-22-5` | `4b5fb0c` | 2-D (static only; proxy + 3-D untouched) | `index-Ckc2ThRM.js` + `geoWorker-DLc1PVGl.js` | **The m01ophid triage bundle — #266 (P1) + #264 + #265, operator-validated on the PR worktree server then "commit and deploy".** ADR-387 (#266, main `020cb18`): word-form SIDE relations join the dropped-given gate family — «בצדדים שונים של AB» dropped by the LLM used to COMMIT GREEN; `droppedWordRelations` gains the carrier-agnostic side-pair member (both commit paths) + `droppedRegionSubject` on the LLM stillDropped list; **rode along: 4 raw 0x08 bytes in parse.ts regexes** (the `\b` tool-escape corruption) had deadened the En triggers (circles/disjoint/contained + ordinal circle) — restored + a C0-control hygiene sweep. ADR-388 (#264, main `4de80e9`): collinear-containment ink dedup at the SCENE (`markCoveredSegments` — covered = no base ink/hit-target, the maximal container owns the run, lit overlay kept) — one click now hides/dashes the ENTIRE apex common tangent; no static lowering id choice could fix the class (near/far flips with the free radii). ADR-389 (#265, PR #270): `points-line-side` — the ADR-254/303 side family, LINE edition («C ו-D בצדדים שונים/באותו צד של AB», En mirrors; relational requirement + verifier `figure.v.lineSide*` + `meetsRequirements` gating; general-position side seeding with anchor-exclusion + phase decorrelation; M1 re-seat; catalog +1). Locks: `word-side-gate.test.ts`, `ink-dedup.test.ts`, `points-line-side.test.ts` + scenarios `apex-common-tangents-single-ink-run`, `points-on-different-sides-of-line` (the exact prod sequences). Gates on the deployed tree (the merged main in the feature worktree): full suite **4744 passed / 4 standing skips**, `tsc -b` + build clean; live index references the new bundle (200, `text/javascript`), proxy `/healthz` ok (untouched). |
| 2026-07-22 | `prod/2026-07-22-4` | `f33083b` | 2-D (static only; proxy + 3-D untouched) | `index-D7rXfJpY.js` | **#262 (P1) + #263 — the operator's first #235 play-test findings, fixed same-hour (ADR-386 Am. 1).** P1: a NAMING-shaped utterance the alias rule didn't claim fell through to the angle VALUE rule — «נסמן זוית CAD כ 1» silently set ∠CAD=1° (green row), «כ-1» even −1°, «סימון/לסמן … כ A1» read the 1 inside «A1». The ADR-024 leftover-guard fix: `angleAliasRule` ALL-OR-NOTHING (widened verbs נסמן/נסמנה/סימון/לסמן/מסמנים · denote/mark/label; connectors כ/ב/בתור · as/by; «כזוית»/∠ decor; a bare-digit name binds the canonical vertex-letter alias — CAD כ 1 ⇒ A1; unbindable naming-shapes STOP and escalate whole). #263: the wedge sign renders the DIGIT alone when the name = vertex letter + digits (the screenshot's «A1»-on-«A» collision); a different-letter name keeps full text. Locks: probe-table + display blocks in `angle-alias.test.ts`, scenario `angle-alias-bare-digit-sign` (the exact screenshot figure); the two pre-Am full-name label locks re-derived to the digit contract. Gates on the deployed tree: full suite **4700 passed / 4 standing skips**, `tsc -b` + build clean; live index references the new bundle (200, `text/javascript`), proxy `/healthz` ok (untouched). |
| 2026-07-22 | `prod/2026-07-22-3` | `05153b6` | 2-D (static only; proxy + 3-D untouched) | `index-D2Kddf7h.js` | **Angle ALIASES — the book's subscript notation (#235 → PR #261, ADR-386; shipped under the operator's "commit and deploy" waiver — prod play-test pending).** «נסמן זוית BAM כ-A1» binds the alias (a data-only `angle-alias` object, id = the name — id-registry visibility for free; arc + the "A1" label on the wedge via the FR-RN-7/measure-label streams); «זוית A1»/«∠A1»/«<A1» resolve through ONE ctx rewrite at the parse chokepoint, so value/equality/ratio/acuteness/chains all gain the notation (the #237 keyboard form composes for free); «∠B=∠C» resolves bare 2-edge vertices (the ADR-164 gate, equality edition — the mixed prod form «זוית B שווה לזוית EDF» too), ambiguous vertices clarify WITH the נסמן suggestion; namespace honesty both ways (`alias-taken` on a taken name; «נקודה A1» refuses instead of silently no-opping). Operator scoping: explicit נסמן only, no auto-numbering. Locks: `angle-alias.test.ts` (13), scenario `angle-alias-book-notation` (the ne810woo book-74 figure to book values through C1/E1, equality-first order re-locking ADR-384), catalog +1, shadow-allowlist +1 reviewed pair (`angleAliasRule → angle` — the runner-up read the 1 in «A1» as a degree value). Gates on the deployed tree: full suite **4695 passed / 4 standing skips**, `tsc -b` + build clean; live index references the new bundle (200, `text/javascript`), `angle-alias` verified present in the served JS, proxy `/healthz` ok (untouched). |
| 2026-07-22 | `prod/2026-07-22-2` | `cdb7b6b` | 2-D (static only; proxy + 3-D untouched) | `index-jM7ExVQO.js` | **The M2/honesty pair (operator play-confirmed all four probes): #236 + #207 (ADR-384/385).** #236 — a membership statement about a busy FREE point CONVERTS it to the declared on-segment rider (projection-seeded t, the ADR-140 directive carried whole incl. `also`) instead of a generic collinear claiming ANOTHER carrier; the ne810woo book-74 order builds the exact book figure (|CD|=14, |BD|=8, |BC|=|BE|=6) and the membership-first order dropped 170s→8s. Residuals filed: #258 (amber-settled constraints never re-enter deferral/HOIST — the sizes-early row, asserted known-residual), #260 (two-host rider membership, pre-existing). #207 — ONE deferral gate (`deferralWorthwhile` = the classify gate, exported): App.submit, the five submit-gate replicas, and classify share it, so the structurally impossible quarter-circle (3yrpvz14) refuses honestly with the specific relation instead of parking as «add the remaining givens»; ADR-104 deferral preserved through the same gate (the early-⟂ control + q4-commit-deferred-perpendicular). One-signed-residual «rigid» probe REJECTED with a counterexample (ADR-385/#259, with the measured ~30s concluded-infeasibility burn). Gates on the deployed tree: full suite **4681 passed / 4 standing skips** (both lanes), `tsc -b` + build clean; live index references the new bundle (200, `text/javascript` — a logic-only diff, so the hash change IS the content marker), proxy `/healthz` ok (untouched). |
| 2026-07-22 | `prod/2026-07-22` | `5447f29` | 2-D (static only; proxy + 3-D untouched) | `index-Bwwatw0p.js` | **The segment-statement package (PR #256, operator play-approved): #240 + #241 + #237 (ADR-381/382/383).** #241 — «CD חותך את AB» with NO point named lowers deterministically to the new point-free `segments-cross` REQUIREMENT (reading (a), the ADR-244 shape: ADR-255 reseat improves the default; verifier `figure.v.segmentsCross`; strict `segmentsCrossWithin` bar in `meetsRequirements`/`firstSatisfyingSeed` incl. the ADR-166 reflection search + the shared sample core; joins `REQUIREMENT_DATA`). NO label is invented — the pool gating makes the crossing FORCED, so the ADR-380 dot offers the naming; the prod coin-flip (not-understood vs an LLM-invented `M`) is dead. #240 — «CD חוצה את AB» segment bisection: the ADR-110 macro (auto/pinned/reused midpoint + `set-line` between) PLUS the #241 requirement, so the default drawing is a genuine transversal, never the degenerate all-collinear solve. #237 — keyboard prefix-`<` ≡ `∠` in the normalize seam («<ACB = 40», «<ACB=<BED»); `AB<CD`/`α<β`/`R<r`/`k<0` locked as comparisons. Scenarios `bare-crossing-statement-states-no-label` + `segment-bisection-statement` (the exact `i1mt2us8` sequences); catalog +2; follow-ups #254/#255 filed; the operator's missing-trapezoid report during play triaged as NOT-a-bug (#257, needs-operator: the quad is trapezoid in only 3 of 6 valid configs — the forced-in-every-sample doctrine holding; «CDRT טרפז» verified as the stating workaround). Gates on the deployed tree: full suite **4666 passed / 4 standing skips** (both lanes), `tsc -b` + build clean; live page serves the new bundle (200, `text/javascript`), `segments-cross` verified present in the served JS, proxy `/healthz` ok (untouched). |
| 2026-07-21 | `prod/2026-07-21-3` | `08b13b3` | 3-D (static only; proxy + 2-D untouched) | `3d-CvJghNVP.js` | **The operator-authorized overnight 3-D fix batch (4 consecutive sessions).** #243 (bug, main) — the triage 3-D lane's ADR-3D-040 mirror drift: `session3d` now consults `classifyGuidance3` + a 3-D guard in `triage-mirror.test.ts`; a full `--reverify` re-bucketed 8 false LIVE rows (3-D worklist 15→6 carried) and uploaded corrected verdicts to the admin dashboard. #247 (PR #249) — the guidance register widened: `מעויין`/`פרמידה` spellings + the `נתון` prefix (recorded trap: `נתונה?` never matches `נתון` — final ן). #199 (bug, main, ADR-3D-047) — M1 for existing-object statements: a same-kind/same-ids solid re-declare no-ops; `point-on-segment3` on an existing id lowers to the vec-rel claim (the tetra-apex median now refuses `claim-refuted`, not `already-defined`); `שווה מקצועות` on a tetra is a 5×`length-rel` macro driving the regular tetra (the qualifier was silently dropped even on fresh figures). #225 (PR #250, ADR-3D-048) — un-named `אמצע BB'` auto-labels its midpoint at APPLY (M-first free letter). #251 (PR #252, ADR-3D-049) — `זוית O ישרה`/`זווית O = 90` vertex-named angle resolves its arms at APPLY (exactly two edges → the M1-routed `angle-seg-eq`; else the new honest `ambiguous-angle` message) + the `ישרה` word-form on full triples. Gates on the deployed tree: full suite **4634 passed / 4 standing skips**, `tsc -b` + `build:3d` clean; live page + bundle 200 (`text/javascript`), `/healthz` ok (proxy untouched). **Morning play-test gate pending:** `reports/morning-test-plan-2026-07-22.md`. |
| 2026-07-21 | `prod/2026-07-21-2` | `01cd2f0` | 2-D (static only; proxy + 3-D untouched) | `index-BlwrE6pY.js` | **#228 — the crossing dot: geometric ink universe + the FORCED-only gate (ADR-380, PR #239).** Two coupled defects, one sweep. (1) The affordance's universe was a hand-maintained whitelist of pair KINDS — segment×segment (ADR-016) plus drawn-line×segment once #197 needed it — so **circle×segment (the operator's own example), circle×circle, circle×line and line×line offered no dot at all**; the ADR-167 node-definition class, affordance edition. `findInkCrossings` now intersects every pair of drawn things, in the ENGINE (the store owns the sample pool the gate needs and may not import the renderer), with branch indices from the engine's own `lineCircleIntersect`/`circleCircleIntersect` so a crossing's `branch` IS `evaluate`'s. (2) Candidates were computed from the ONE drawing on screen, so a crossing existing only at the current seed got a dot exactly like a forced one — "show another configuration" then removed it, stranding the letter given to it; widening (1) alone would have made that MORE frequent. **Operator ruling: «only if that intersection exists for sure across all configurations, we show the dot.»** `forcedCrossingKeys` keeps a pair only when it contributes the same non-zero crossing COUNT in EVERY valid sample (the ADR-167 forced-in-every-sample standard over the ADR-256/295 filtered pool; by count per operand pair, never by matching roots — root labelling is not stable as a figure flexes). **ONE definition of drawn ink:** "drawn line" was not one thing — `buildScene` TRIMS a line carrying 2+ drawn points to a segment — so `drawnPointIds`/`resolveDrawnLines`/`drawnCircles` moved into the engine and `buildScene` now consumes them (**all 123 renderer tests unchanged; scene output byte-identical**); a trimmed line is BOUNDED ink whose crossings carry a `set-line` within twin. Delivery = the operator-chosen option A: the verdict recomputes after every fact change, async over the ADR-231 shared pool, facts-keyed + stale-guarded — the figure paints at once, dots resolve a beat later (synchronous sampling on submit was never on the table; that is the freeze #157 exists for). Operator-confirmed: **gesture = observe, typing = state** (a non-forced crossing stays creatable by typing, which makes it forced) and **a starved pool withholds the dot** (ADR-295 precedent). `src/render/intersections.ts` retired into `src/engine/inkCrossings.ts`. **Expect noticeably FEWER dots than before — that is the ruling working, not a regression.** Gates on the exact deployed tree: full suite **4600 passed / 4 standing skips** (both lanes), `tsc -b` + build clean; live page + bundle 200, new code verified in the served JS (`circle-circle-intersection` present, `findSegmentCrossings` absent), proxy `/healthz` ok (untouched), 3-D 200 (untouched). |
| 2026-07-21 | `prod/2026-07-21` | `7f73452` | 2-D (static only; proxy + 3-D untouched) | `index-BkWwL4qp.js` | **The crossing-gesture honesty fix + the play-approved containment feature.** #234 (bug, ADR-379) — **a clicked crossing is a STATEMENT about the drawn ink, not a coordinate.** The dot is only ever OFFERED at a crossing strictly interior to both operands, so clicking it asserts what the typed «AN ו-DM נפגשים בנקודה O» asserts; but `markIntersection` lowered it to the bare INFINITE-line crossing, which carries no within requirement — so a later configuration was free to move the named point off the drawn segments (prod `ne810woo`: the operator named a cevian crossing O, changed configuration, and O left the figure while the letter still read as taken, with no reclaim path). The lowering moved to a shared pure seam `crossingCommands` (App + tests exercise the same code, ADR-346 discipline) and states the within meaning PER OPERAND — segment×segment → the joint ADR-166 `onSeg` requirement (asserted **byte-identical** to the typed meet form's lowering); drawn-line×segment → `set-line [c, O, d]`, since only the segment bounds an infinite line. Measured on the reported figure's shape over 16 seeds: **the old lowering left 15 displayable configurations of which 7 put O outside both cevians; the new one leaves 3, of which 0 do** — fewer configurations is the correct outcome, the discarded ones were never configurations of the drawn figure. #224 (feature, ADR-376, PR #229, play-approved) — definite-reference circle containment «מעגל מוכל בתוך המעגל הגדול»: `resolveSizeQualifier` widened to a single-circle figure (ADR-029) + the `containedNewInExisting` lane in the #196 seam; 2+ circles without a binding reference DEFER, never a guessed container. Gates on the exact deployed tree: **full suite 4584 passed / 4 standing skips** (both lanes), `tsc -b` + build clean; live page + new bundle 200 (`text/javascript`), the new code verified present in the served JS, proxy `/healthz` ok (untouched), 3-D 200 (untouched). Split out during the session: **#238** (P3, the letter-reclaim UX half of #234). NOT in this deploy: **#228** (the crossing-dot universe + the operator's forcedness ruling) — still WIP on `feat/228-crossing-dot-universe`. |
| 2026-07-20 | `prod/2026-07-20-3` | `6a56d3c` | 2-D (static only; proxy untouched) | `index-BFE6z8Ze.js` | **The tangency/membership honesty day (operator-driven fix session + play-round + "P1 before deploy"): #226 + #223 + #233 + #230 + #232 (ADR-374/375/377/378).** #226 (P1) — the verb honesty gate is OPERAND-BOUND: it had false-blocked the correct deterministic «AD משיק למעגל» lowering (escalating to the LLM) and false-passed the LLM's `tangent at:A` (AD drawn as a chord, green); the #203 foot-from-centre+membership lowering counts as evidence; a named NEW touch rides the named segment. #223 — a shape word over EXISTING points lowers to its defining constraints (`shapeConstraints`, the ADR-262 authority) at the apply boundary; declared cycles stay ADR-157-immutable; «FEDG מלבן» over four riders builds a genuine inscribed rectangle. #233 (P1, the play-round) — the ADR-075 existing-touch tangency states the FULL conjunction (membership + ⟂ + on-line-when-loose), and (#230) the point-on-circle (d) fall-through pushes an unreconcilable membership as a `length-radius` residual so the solver drives the circle to the segment («AD משיק למעגל בנקודה E» genuinely tangent; «BC מיתר» a real chord on a rectangle's derived corner). #232 (P1) — bare free points land in GENERAL POSITION (the free-point apply chokepoint probes the canned (3,2); the coincidence collector's forcedness split never certifies a both-free default stack; the submit/edit auto-advance gates fire on a distinctness break) — «נקודה d» never draws exactly on A again. 7 new scenarios (every exact prod/dev sequence). Full suite green on the deployed tree (2-D 3890 + 3-D 759 / 4 standing skips) + tsc + build clean; live page serves the new bundle (200, text/javascript), proxy alive (untouched). PR #229 (#224 containment) remains open awaiting play-approve — NOT in this deploy. |
| 2026-07-20 | `prod/2026-07-20-2` | `1c3998c` | 2-D (static only; proxy untouched) | `index-yx8AsGPl.js` | **The overnight P2 quartet (operator: "anything simple enough to fix over night" → morning "commit and deploy"): #162 + #156 + #168 + #193 (ADR-118 Am., ADR-371, ADR-372, ADR-373).** #162 — `dryRunOutcome.labelCount` counts all THREE label kinds, so a lone symbolic area label («שטח משולש AFO הוא 9b») commits instead of «already on the figure». #156 — a driving constraint coincidentally satisfied at the seed («∠EOF=90» on square-side midpoints) is `produced` via the new free-DOF-count-reduction signal; vacuous re-statements stay no-ops; the pinned angle holds at every displayed seed. #168 — a second «תיכון» no longer escalates: the median resolves its opposite side from the apex's POLYGON (`oppositePolygonEdges`, the ADR-263 mechanism), not a raw point count; the ADR-335 scenario replays the log's bare keystrokes again. #193 — the angle universe's degenerate-wedge exclusion flipped ANY-sample → ALL-samples (structural only), so Q9's part-א classes ∠ACE=∠ABE / ∠AFD=∠ABD surface; #194 (keeping squashed drawings off the canvas) stays open as the display-side complement. Full 2-D lane 3865 + engine 877 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-20 | `prod/2026-07-20` | `a088794` | 2-D (static only; proxy untouched) | `index-BmID3aKf.js` | **The circle-reference & tangency mega session (operator-tested on a local all-PRs server, then "commit and deploy"): 8 bugs + 3 features.** Bugs on main — #215 P1 (ADR-363, `resolveCirclePair`: a definite plural circle reference binds/completes/introduces/defers, never invents; «מנקודה» out of the touch alternation; the catalog «משיק משותף» example re-owned by commonTangent), #180 (ADR-364, the `through`-signal "reference is not a definition" guard), #213 (ADR-365, anonymous-centre-aware pickers + the quantified semicircle bulge), #152 (ADR-366, endpoint-is-centre routes «EO קוטר» to the new Thales circle), #159 (ADR-367, `resolveOrIntroduceCircle` — named-absent creates, empty-figure introduces, free radius), #221 (ADR-366 Am., membership resolution + cross-membership creates the circle on that diameter with a hidden centre), #222 (ADR-365 Am., the bare-adverb side words «מבחוץ»/«בחוץ»/«מבפנים» bind a semicircle's bulge). Features via merged PRs #218/#219/#220 (play-approved) — #184 (ADR-368/FR-IN-11: auto-name the unnamed construct — «אמצע AB», «משיק למעגל», «קוטר», «נתון מעגל», «מרכז המעגל», the unnamed secant; catalog +7), #203 (ADR-369: «AB משיק למעגל C» with no touch named = a tangency constraint, touch = the ⟂ foot as an anonymous dot; the quarter-circle-in-a-right-triangle figure), #214 (ADR-370 + Am.: tangents from a named apex to both circles — the from-marker binds the apex role, a new two-tangent apex is DERIVED at the crossing so «שני מעגלים זרים» stays disjoint; a 2nd standalone free circle splits its default radius, the ADR-253 general-position principle radius edition). 12 new scenarios, ~85 new tests. Full suite 4535 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200 (new code verified in the served JS). |
| 2026-07-19 | `prod/2026-07-19` | `9b3e783` | 2-D (static only; proxy untouched) | `index-vx1Tez9n.js` | Fix #212 (P1, ADR-359 Am. 9): a KIND adjective on a singular tangent noun («משיק פנימי/חיצוני/אלכסוני/מבחוץ/מבפנים», with or without labels) is a common-tangent request — the משותף-only trigger sent it to the LLM, whose guess REPOSITIONED stated disjoint circles into mutual tangency (wrong figure, green). Kind doubles as the trigger; the plural circles-participle keeps its circles-tangent meaning; droppedWordRelations gains the kind-tangent member. Full suite green + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18-3` | `897d3a5` | 2-D (static only; proxy untouched) | `index-Cuj_koAf.js` | The two-circle bundle (PR #211, ADR-358..362 + ADR-359 Am. 2-8): mutual positions «זרים»/«מוכל» as requirements + the bare-«שני מעגלים» case-toggle + apart seating with the stated-crossing re-seat (#196); the word-relation honesty gate (#210); common tangents — all kind phrasings (חיצוני/פנימי/אלכסוני/מבחוץ/מבפנים), label-less auto-naming, the plural pair, basin toggling, position-aware capacity with position-accurate refusals (contained 0 / int-tangent 1 / intersecting 2 / ext-tangent 3 / disjoint 4), the touch tangent auto-build + «בנקודת ההשקה» role form, the named tangents' meet «המשיקים נפגשים בנקודה K», naming dots at drawn-line × segment crossings, and the seat-before-constrain perf fix (38.3 s → 67 ms) (#197); the four-point secant one-liner (#191); ordinal circle references (#192). Operator: «fix this and commit and push and deploy» (play-approve waived per docs/22 §4). Full suite 4441 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18-2` | `b2502b4` | 2-D (static only; proxy untouched) | `index-JGniHryB.js` | The quarter-circle bundle + the sector: ADR-354 (#202 — membership conversions gated by wouldInvertDependency, רבע מעגל OCD ≡ ODC → r=6), ADR-355 (#205 — quarter/semicircle naming honesty: stated centre read, no label hijack, inscription stops), ADR-356 (#170 P1 — span-aware arc identity: spanDeg + view parity + scenePositions dedupe, no more 270° quarters), ADR-357 + Am. (#171, PR #209 — the גזרה sector: free/pinned/reflex central angle, שווה/=/° value forms, middle-centre naming with the O-family override, central-angle vertex resolution). Operator: "when complete testing, commit and deploy" (play-approve waived per docs/22 §4). Full suite 4399 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-18 | `prod/2026-07-18` | `09a9294` | 2-D (static only; proxy untouched) | `index-DzvkFxJJ.js` | **PR #206 (feature #201, ADR-353, operator-approved + "merge and deploy"): diameter-THROUGH-a-point phrasings.** Operator ruling 2026-07-18: «קוטר ב/מנקודה A» means the diameter THROUGH A — the existing ADR-270 construct reached by more phrasings, widened at the `diameterFromPoint` from-marker only (the ADR-3D-026 phrasing-class discipline): «[ה]עובר בנקודה A» (the verbatim prod utterance carried a leading «הוסף» — tolerated), bare «בנקודה A», the «במנקודה» stacked-prefix slip, «דרך [נקודה] A», En "diameter through [the point] A" / "add a diameter through…". No-theft unchanged (cut compounds → diameterCutsSegment, «E על הקוטר» → point-on-carrier, two labels → named-antipode `diameter`; M1 membership + fresh antipode byte-identical); one REVIEWED shadow pair added to the hard allowlist (`diameterFromPoint → circle` — the circle rule also reads «העובר», rule order wins). Closes #201. Locks: `diameter-from-point.test.ts` (13 forms), scenario `diameter-through-point-imperative`, catalog +1. Full suite green on the deployed tree (4362 passed / 4 standing skips) + tsc + build clean; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-17 | `prod/2026-07-17-3` | `a02ff88` | 2-D + 3-D + **proxy** (restarted) | `index-Dr5mGWX2.js` / `3d-DE1grWOS.js` / `proxy.mjs` | **The P3 fix session (operator "park 192 and do the rest", then "deploy all"): #185 + #181 + #182 + #183.** **#185 (PR #195, operator-approved + merged; ADR-350/ADR-351)** the nine-parser-gap batch from the 2026-07-17 triage — definite references (generic polygon noun binds THE polygon; «נקודה G על הקו» resolves / «קו ועליו נקודה A» creates the unnamed line; «מקביל לבסיסים» via the `parallels` hint; «קטע מרכזים»/«מרכז מעגלים») + word forms at the `normalizeUtterance` chokepoint (cardinal degree words «שלושים מעלות» → digits; angle/arc «שווה ל» → `=`, narrowing ADR-119 per the operator ruling; the parenthesized appositive «(AB=AC)» as an ADR-264 clause; «ריבוע ABCD שצלעו הוא 1» + «צלע אחת 10 צלע שניה 5»); 11 scenarios, catalog +5. **#181 (ADR-3D-045)** 3-D lowercase labels in LABEL POSITION parse like uppercase (`upliftLowercaseLabels` — anchored, so x/y/z, k/m/t, u/v/w, R/r stay case-significant). **#182 (ADR-3D-046)** the 3-D sink logs the LLM's committed canonical lines + delete/show-another/undo/redo/clear/load actions; the triage `session3d` follows them (the #84/#189 mirror — post-fix llm sessions replay without `degraded`). **#183 (ADR-352)** the admin gap card consumes the triage VERDICT MAP (`triage.mjs` uploads `verdicts-<app>.json` beside the events file): verified-built rows move to «תוקן מאז», open rows carry a current-verdict column, the header names the verdict revision; no map reads as unknown — the first map lands on the next `/log-triage` run. Full suite green on the deployed tree (4352 passed / 4 standing skips) + tsc + all three builds clean; both live pages serve the new bundles (200), both admin dashboards 200, proxy restarted + healthz ok. |
| 2026-07-17 | `prod/2026-07-17-2` | `2924295` | 2-D (static only; proxy untouched) | `index-DfIj4qrc.js` (worker unchanged: `geoWorker-B_uK9fI5.js`) | **The parser-distribution P2 package (fix session, operator "go ahead" then "deploy"): #163 + #151 + #160 (ADR-343/344/345).** **ADR-343 (#163):** a CHAINED statement's tail value distributes to EVERY member — operator ruling «AB=BC=8 means AB=8 and BC=8»; lengths/angles/symbolic all through the one owner (`chainedEquality`); the entailed set-equal KEPT (measured green vs the ADR-272 gate; ADR-234 `pinsSoftVariant` reads it); `symbolic.test.ts` re-derived. **ADR-344 (#151):** plural chord/diameter declarations distribute over the conjunction («AB ו DC מיתרים» → all 4 memberships + both segments; sequential uppercase pairing, odd counts fall back byte-identically, intersect compounds never pair-read); catalog +1. **ADR-345 (#160):** a COUNT digit is a quantifier, not a magnitude — `droppedGivenNumbers` blanks count slots (He …ים/…ות / En …s morphology) so «2 משיקים» ≡ «שני משיקים»; magnitudes stay gated (פי-prefix, פעמים/times, decimals, digit-after-noun). #159 (same report, no-circle-yet tangent bail) deliberately stays open. Closes #163/#151/#160. 3 scenarios + 18 class tests; both catalog snapshots re-derived (the new entry). Full suite green on the deployed tree (2-D 3631 + 3-D 739 / 4 standing skips) + tsc + build clean; live page serves the new bundle (200, text/javascript, worker chunk 200), proxy healthz ok (untouched). |
| 2026-07-17 | `prod/2026-07-17` | `577719c` | 2-D (static only; proxy untouched) | `index-DZncnaol.js` + `geoWorker-B_uK9fI5.js` | **The #177 anonymous-centres batch (P1, operator play-tested + "commit and deploy"): ADR-342 + Am. + Am. 2.** **#177:** an unnamed circle's centre POINT is anonymous ('@ctr-O') — the auto letter never squats the student's namespace (prod session `gxccyt2n`: «P על המשך BA» bound the invisible second centre); the LETTER stays the circle's reference token («מעגל O», `circle-O` — byte-unchanged). The binding boundary is PLACING vs REFERENCING (two operator rulings): metric givens («OP=4», «OA=5») and bare segments («OP», «PA») bind-and-promote (the locked `23vqi9u8` + kite prod flows); placing statements create a fresh point. Naming/promotion via `name-center` + a replay pre-scan; `nameCentre` token-driven; slider labels show the token. **#178 (play-test):** naming with TWO circles reads the size qualifier («מרכז מעגל קטן הוא O1», articles optional) via the #102 core + locks the roles — no more LLM-invented arbitrary point. **#179 (play-test):** the lock carries POST-rename ids, so `orderedBelow` stamps and the ADR-309 sliders enforce the stated big/small in both directions. Closes #177/#178/#179. Full 2-D suite green (3614 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, worker too), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-4` | `dae3196` | 2-D (static only; proxy untouched) | `index-C5vEcdZz.js` + `geoWorker-CtdT7eOv.js` | **The two open P1s (fix session, operator "fix the 2 P1" then "deploy").** **ADR-340 (#175):** "show another configuration" searches and applies a VALIDATED COMPOSITE view — facts × seed × branch × variant accepted only when `meetsRequirements` holds on the whole (`searchAnotherView`; worker + sync fallback; ONE `applyView`, the App's post-hoc `cycleAlt`/`cycleVariant` deleted; both store actions gated the same way). The prod session `gxccyt2n` break (branch flip onto a valid seed broke the stated internal tangency, «B ו-D נפלו על אותה נקודה») is locked deterministically (seed 2 + D-flip) and verified RED pre-fix. **ADR-341 (#173):** a trapezoid's LONG BASE — a stated «AB < CD» flips the TEMPLATE (ids rotated by two; first draw k=1/0.6≈1.667, never the k≈1.08 boundary grind) and the sampler straddles k=1 (both branches reachable when unstated — the ADR-052 smell closed; a stated order pins the branch, min sampled 1.205). Closes #175/#173. Full 2-D suite green (3605 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, worker chunk too), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-3` | `da45964` | 2-D (static only; proxy untouched) | `index-CWwOSOBS.js` + `geoWorker-BmPXB4uM.js` | **The inscribe triple (operator "commit and deploy" after play-testing): #167 P1 + #166 + #176.** **ADR-337 (#167):** a fact's multi-command lowering is TRANSACTIONAL — the fold builds into a trial and commits only if EVERY command succeeds, so a failed macro leaves ZERO trace (was: half-built expansion rendered from a red step, prior figure moved, verifier blind). **ADR-338 (#166):** a macro's defining constraints are solved as ONE COUPLED SYSTEM (`applyCoupledStep` attaches all N then evaluates once; `applyUnits` groups consecutive relation commands structurally) — square/rectangle inscribed in a right triangle now BUILDS (all 36 shape×variant×vertex configs verified; the corner square matches the closed-form oracle exactly; no new numerics — resolveMixedCarriers/jointCostTerm already composed). **ADR-339 (#176, the play-test follow-up):** a cyclable variant's DEFAULT settles at commit to the first cleanly-building configuration — the reported sequence now draws the general-position square (all 4 vertices ON the sides), the degenerate corner square stays reachable by cycling with its ADR-123 notice. Filed #174 (P3, 1-of-18 marginal NM convergence, pinned by the MARGINAL set). Full 2-D suite green (3597 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16-2` | `a9aa832` | 2-D (static only; proxy untouched) | `index-Cm9luzzL.js` | **PR #165 (feature #161, ADR-336, operator-approved waiver): the default triangle draws textbook-oriented** — A at the apex, BC the horizontal base (B bottom-right), via the one-literal `triangle` template change in `applyCommand`; apex (2, 3.6) chosen so the seed-sweep/isosceles scenarios keep their basins. Merge note: both of today's sessions had claimed ADR-335 — the compound-measures ADR (already live as `prod/2026-07-16`) keeps 335; the triangle ADR is renumbered **ADR-336** (docs/06 + its PROJECT-MEMORY note). Deploy-audit context: 3-D (`3d-qKv80QAv.js`) and the proxy verified CURRENT — no undeployed commits in either lane; this PR was the only outstanding deployable. Closes #161. Full 2-D suite green on the merged tree (3578 passed / 4 standing skips) + tsc + build clean; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-16 | `prod/2026-07-16` | `ad26d30` | 2-D (static only; proxy untouched) | `index-3d9zeIlf.js` | **ADR-335 — compound measure relations (PR #169, operator commit-and-deploy): the #153/#145 P1 honesty class closed at BOTH mechanisms + the #154/#144 capability.** A relation over ≥2 measure terms was TRUNCATED by the first-match-wins relation rules to a wrong binary constraint with every gate silent («קשת AC + קשת BE = קשת AD + קשת BC» built ∠AOC=∠AOD; «DM/ME=BM/DM» built set-equal; unreported siblings: segment sums, angle sums, «קשת AB=40» as a chord LENGTH). Now: `set-measure-sum` (sums of segments/angles/arcs, NUMEXPR coefficients, numeric targets, ADR-164 single-vertex arms) + `set-length-product` (power-of-a-point products/quotients/squares, log-domain residual) + the structural `droppedCompoundRelation` gate (slot-count accounting, 3 commit chokepoints, catalog-wide false-positive guard) + `arcValue` («קשת AB=40» → central angle; value printed ON the arc, never a wedge at the hidden centre — operator display rule) + the `⌢{}` toolbar template + over-arc MathML rendering (#155 folded in). Bagrut Q22 reproduces end-to-end with the REAL exam given (S_CFG=S_CGH): HG⊥AB forced, cos=0.0000. Closes #153/#145/#154/#144+#155; filed #168. 6 scenarios; full 2-D suite green (3576 passed / 4 standing skips) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript, set-measure-sum/set-length-product/arcOf markers verified in the served JS), proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15-3` | `f4a7d3f` | 2-D (static only; proxy untouched) | `index-DSMc9BKD.js` | The **P3 bundle** (PR #149, ADR-334, operator commit-and-deploy). **#146** «נקה הכל» now also clears the input box + all transient notes + any in-progress edit (`clearAll`; prefs untouched). **#147** the successful-load "N steps refreshed" note now auto-clears after 6 s (was on the persistent `fileNote` lane with no timer/dismiss → hung forever) — re-routed through `noteFileProblem`. **#46** bare «קו XY» → segment synonym of «ישר XY» (orientation/style adjectives like «קו אופקי» never match → escape to the guidance classes). **#47 (forms 1/2/4; form 3 → #148)** the «ב F» spaced crossing-marker class-fix (`crossingAfterCircle`, `ב-?`) unblocks «ההמשך של BE … ב F», + the copula «המשך CD היא נקודה A»; the plural distribute («המשכי הגבהים …») split to #148. **#107** «גובה לצלע AB» (altitude to a named side) drops from the opposite vertex (mirror of the #71 vertex-less median). Closes #146/#147/#46/#107, partially #47. Full 2-D suite green (3510) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15-2` | `8e6520f` | 2-D (static only; proxy untouched) | `index-C_fv6fLj.js` | Sync dev↔prod: the secant + tangent session's remaining work (all client-only parser/context). **#136 (ADR-332, PR #137)** the apex+far secant «AD חותך למעגל [בנקודה B] [דרך P]» + the from-point phrasing «מנקודה A יוצא חותך למעגל בנקודה D» (`secantFarPoint`; D a free-θ far crossing, near an anonymous @-dot or named, guarded off the diameter family). **#142 (ADR-333, PR #143)** two «tangent from A» statements now land on the two DISTINCT touch points — the second takes branch 1 (`ctx.tangentAuxes`), fixing «B ו-C נפלו על אותה נקודה». (#138's tangent verb-gate fix rides along, already live from `-1`.) Operator "deploy all so dev and prod sync" (play-approve waived; PRs #137/#143 are the record). Full 2-D suite green (3497) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), all three fixes verified in the served JS, proxy healthz ok (untouched). |
| 2026-07-15 | `prod/2026-07-15` | `841d3a6` | 2-D (static only; proxy untouched) | `index-COcYafDk.js` | **P1 prod-regression fix — #138 (ADR-292 Am., PR #139).** The singular/plural external tangent (`מנקודה A יוצא משיק למעגל בנקודה B`, `…יוצאים שני משיקים`) parsed OK in prod on 2026-07-11 but broke by 2026-07-15 (weak:dropped:משיק/tangent → LLM → built-nothing). Cause: the ADR-292 `משיק` verb honesty gate (deployed `prod/2026-07-12-2`) didn't recognise `tangentFromExternal`/`tangentsFromExternal`'s Thales AUX-CIRCLE construction (`tanaux-`/`~tanmid-`, no literal `tangent` object), so it flagged the CORRECT parse as dropping משיק. Fix: `tanaux-\|tanmid-` added to the gate's satisfied set (a genuinely dropped tangent still blocks). Confirmed against prod `events.jsonl` (worked 07-11, broke 07-15). Full 2-D suite green (3478) + tsc + build clean on the deployed tree; live page serves the new bundle (200, text/javascript), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-5` | `65d2559` | 2-D + 3-D (static only; proxy untouched) | 2-D `index-DVVKUgG8.js` + `geoWorker-Dfh4WyFk.js` / 3-D `3d-qKv80QAv.js` | The P2 feature batch (7 PRs, operator "deploy all"): **#127 (ADR-327)** a drawn parallel line reads the "from a point" origin (`מנקודה A ישר מקביל ל-DO`) via a FROM_PT anchor on the parallel-line rule. **#104 (ADR-328)** a bare free point `נקודה A`/`point A` builds a 2-DOF sampled point positioned by the next statement (re-exposes the original primitive; the `bare-point` scope guidance narrowed to a lone letter). **#44 (ADR-329)** named special-point meets — `מפגש האלכסונים/התיכונים/חוצי הזוויות/הגבהים/האנכים האמצעיים` (diagonals/centroid/incentre/orthocentre/circumcentre) as ADR-110 macros; only the CENTRE point is drawn, the construction is `~`-hidden scaffolding (operator play-test fix). **#29 (ADR-330)** a semicircle on EVERY side of a polygon (`על כל צלע … יש חצי מעגל`). **#134 (ADR-331)** a semicircle oriented OUTSIDE/INSIDE a shape (`חצי מעגל על צלע AB מחוץ למשולש`) — the arc gets a render-time `bulgeRef`. **#94 (ADR-3D-043)** a named angle `∠SDB`/`∠SDB=α` is a highlightable 3-D marker, not a valueless-query refusal. **#117 (ADR-3D-044)** right prisms over parallelogram/quad/square/pentagon/hexagon bases + the oblique `מקבילון` (parallelepiped). Merged to main, full suite green (4139 2-D+3-D) + tsc + both builds clean on the deployed tree; both live pages serve the new bundles (200, text/javascript), proxy healthz up (untouched). |
| 2026-07-14 | `prod/2026-07-14-4` | `be96230` | 2-D (static only; proxy untouched) | `index-Y-6dsT7t.js` | PR #125 (features #98/#39/#126, operator commit-and-deploy after play-test). **#98 (ADR-324)** on-canvas measure labels render math as PURE SVG — a radical gets a real overline/vinculum, a fraction a bar, coefficients kept (`MathSvg`); MathML was rejected because the PNG/docx export can't rasterize `foreignObject`. Plain labels unchanged. **#39 (ADR-325)** selecting a size/ratio step shows its value MEASURED on the drawing under the row (`S(DBCA)=62.2, S(GAD)=4.1 → ratio 15.0 ✓`); the ratio is the seed-invariant verdict, absolutes are per-drawing context; read-only, no engine change. **#126 (ADR-326, folded in from play-test)** the "view relations" hover picker recognized only equality classes — a right triangle's forced 90° (or any determined angle/length with no equal pair) was undisplayable; now `detectRelationsAcross` also emits `definiteLengths` (honest: empty on a free-scale figure, populated once a size given pins the scale), the picker selects definite angles+lengths, and hovering a determined side reads its length / a wedge reads its angle (a forced 90° stays a knee, operator's choice). Also answers #39's "show what AC is" for lengths. Full 2-D suite 3442 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-3` | `920af6a` | 2-D + 3-D (static only; proxy untouched) | `index-ux5odOvc.js` / `3d-ZZZ0_e1_.js` | The "finish the open bug queue" batch — all 5 open bugs (0 remained) + one P2 feature. **#1 (ADR-320)** the dry-run trial now models the commit's dedup, so re-typing `AB=AC` reads "already drawn" instead of a phantom ~0.75 shift (`trialFacts` drops exact enabled-duplicates → prefold/dry-run/commit agree). **#24 (ADR-321)** the ADR-242 load-audit note is DERIVED from live facts (`liveAuditFindings`) so it self-clears when its rows are deleted/toggled/re-lowered, plus a × dismiss. **#33 (ADR-322)** a run-on given-list that a permissive circle rule claimed across clause boundaries is refused honestly (`droppedGivenRelations` joins the winning-parse chokepoint) instead of garbage `ok:true`. **#55 (ADR-3D-041)** radical coefficient in a 3-D vec-rel (`AB=√2·OD` parses like `A'K=4/5 DN`; the filed ambiguity diagnosis was corrected) + a coefficient bare-pair length RHS. **#116 (ADR-3D-042)** the 3-D "right triangle" qualifier — build/constrain (M1 idempotency for a polygon on existing ids + a soft middle-vertex right angle that yields to an explicit ∠=90). **#106 (ADR-323, PR #124, commit-and-deploy)** the central angle «זוית מרכזית COD» / «נשענת על (קשת) CD» — resolves the centre by name or implicitly, draws the radii (centre shows), a value → `set-angle`, valueless → a new `mark-angle` display primitive; the subtends verb alone signals the endpoints (operator play-test widening). Full suite (2-D 3426 + 3-D 709) green + tsc + both builds clean on the deployed tree; both pages serve the new bundles (200), proxy healthz ok (untouched). |
| 2026-07-14 | `prod/2026-07-14-2` | `bebafa0` | 2-D + **proxy** (restarted) | `index-CTXOpkeN.js` + `proxy.mjs` | The backlog-triage batch (bugs #90/#95 + feature #105 to `main`/PR #123 + debt #84). **#90 (ADR-316)** minor/major arc qualifier — `D אמצע הקשת הקטנה AB` no longer lands on the chord; MAJOR = the far arc (branch 1 / a new `on-circle` `major` flag). **#95 (ADR-317)** `E בין A ל-B` (between) builds a point on segment AB. **#105 (ADR-318)** verbose/relational size-givens — `שורש N → √N`, `אורך X הוא √N`, and the operator's Q5 `אורך AC גדול פי √(3) מהקטע CO` now build (`ratioConstraint` moved before `segment` so `מהקטע`'s `קטע` can't half-parse the ratio; the vague unnamed-sides form gets a guided message). **#84 (ADR-319, the server half)** prod analytics now logs the LLM's committed `commands` + a lean `ev:'action'` line per store interaction (edit/slider/show-another/delete) — so a reported session reconstructs; the proxy was rebuilt + restarted (verified `/api/log` accepts `ev:'action'` → 204). Full suite 3412 green + tsc + client & proxy builds clean; live page serves the new bundle (200), healthz ok. |
| 2026-07-14 | `prod/2026-07-14` | `92df7b8` | 2-D (static only; proxy untouched) | `index-C8Qf6xr5.js` + `geoWorker-xh4TwN_y.js` | The bagrut-Q5 session batch (all client-only). **#114 (ADR-310)** `√()` radical as a ratio COEFFICIENT — `ratioConstraint`/`segmentRatio`/`measureRatioK` migrated to the shared NUMEXPR atom so `AC=√(3)CO` / `גדול פי √(3) מ` parse deterministically (was an LLM detour). **#115 (ADR-311)** a point-on-object rider defaults into GENERAL POSITION — `freeSegT` is position-aware, so `E על CB` never lands on an existing point. **#118 (ADR-312)** the live math preview follows the input's direction (`dir={textDir}`), so a Hebrew sentence containing a radical no longer renders word-reversed. **#119 (ADR-313)** a stable within-segment SELECTION for line∩circle (`onSegment`) — `המעגל חותך את BO בנקודה K` keeps K on segment BO after a later `CK=√63` (was flipping beyond the centre); the tangent/secant sibling solves every seed (sidesteps #3, no `order` contention). **#120 (ADR-314)** load auto-re-lowers DETERMINISTIC steps against the current parser, so an old save picks up fixes on load (LLM steps kept as saved — never re-escalate). **#121 (ADR-315)** saving a NAMED figure asks overwrite-vs-copy. PR #122 (features #120/#121, operator play-approved + merged); #114/#115/#118/#119 committed direct (bugs). Full 2-D suite 3377 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-6` | `57729b9` | 2-D (static only; proxy untouched) | `index-DYSh3kBc.js` + `geoWorker-FWQh-hps.js` | Operator follow-up to #113 (ADR-309): the two radius sliders under a stated R>r order now share ONE common scale (min = smaller base×0.3, max = larger base×2.2), so R's thumb always sits to the RIGHT of r's on the same axis — the order is now VISIBLE, not just enforced (superseding the per-slider clamp, which kept the ranges incomparable). Verified across seeds (r thumb 11-25%, R thumb 30-45%). The bidi similar/congruent row fix (prod-4) confirmed working in the operator's screenshot. Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-13 | `prod/2026-07-13-5` | `93a112b` | 2-D (static only; proxy untouched) | `index-BhN11CZ_.js` + `geoWorker-FWQh-hps.js` | Operator follow-up to #113 (ADR-309): the radius slider RANGE is now clamped to a stated R>r order — the inner circle's max is the outer's current radius minus the verifier gap, the outer's min is the inner's radius plus that gap — so it's MECHANICALLY impossible to drag the small circle past the big one (the value guard rejected it but the free range let the thumb travel there and snap back, reading as a violation). Confirmed the engine already gates resample/"show another" on the order (seed sweep). Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-13 | `prod/2026-07-13-4` | `1fecb24` | 2-D (static only; proxy untouched) | `index-CGwr8gYh.js` + `geoWorker-FWQh-hps.js` | Two fixes from continued testing of the 2025-bagrut figure (ADR-309): **#113** the radius slider now respects a stated R>r order — `setRadius` rejects a value that introduces a `radius-order`/`radius-ratio` verifier violation (was only rejecting a hard error), so the slider STOPS at the boundary instead of dragging the small circle past the big one; **#26/#89** the similar/congruent triangle rows («△AMO ~ △AOE») rendered garbled as «AMO ~ △AOE△» under the RTL document — the LTR math notation now forces `dir=ltr` + `unicodeBidi: isolate` (radius-slider label gets `dir` too). Confirmed against the operator's screenshot. Closes #113/#26/#89. Full 2-D suite 3345 green + tsc + build clean; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-3` | `936e465` | 2-D (static only; proxy untouched) | `index-DeUeXXZh.js` + `geoWorker-CoYgsosW.js` | The three fixes from the 2025-bagrut testing pass: **#112 (ADR-306)** name/rename an auto-assigned circle centre — «מרכז המעגל הוא P» renames the hidden centre O→P + reveals it (via the new `parseNameCenter`/`nameCentre` store op), never a second circle; **#111 (ADR-307)** a definite plural «(שני) המעגלים» / «the circles» binds the two EXISTING circles (`definiteTwoCircles`), no invented third; **#110 (ADR-308)** «M אמצע OK» on the AE-extension∩OK crossing now flexes E so M bisects OK — a collinear midpoint lowers to the 1-D equidistance |OM|=|MK| driven by the upstream free carrier found past soft-order carriers (`ancestors`/`freeCarrierAncestor` `'soft-order'` mode + `driveHardOn`), replacing the unsolvable 2-D coincide. The full 2025 exam incl. the part-ב midpoint given now builds end-to-end. Closes #110/#111/#112. Full 2-D suite 3343 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-13 | `prod/2026-07-13-2` | `ea95051` | 2-D (static only; proxy untouched) | `index-5aFllYLB.js` + `geoWorker-smWq7buc.js` | Feature #102 (PR #103, ADR-305): «המעגל הגדול/הקטן» between two INDEPENDENT circles both REFERS and ASSERTS — a definite size qualifier resolves to the concrete circle (recorded `set-radius-order`/`Circle.orderedBelow` roles first, else assigned from the drawn sizes) and a first assigning use appends the locking `set-radius-order` (the operator's "translate it to a R>r like constraint" ruling), so sampling can never swap which circle is bigger; the creation adjective «מעגל קטן…» seeds a smaller radius (free DOF). Concentric pairs keep ADR-244; 0/1/3+ escalate. The 2025 bagrut's PUBLISHED wording now builds end-to-end (scenario `bagrut-2025-verbatim-unnamed-circles`). Operator commit-and-deploy after play-testing. Full 2-D suite 3323 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). Known follow-ups filed from the same session: #110 (M-midpoint solve), #111 (plural circle ref), #112 (name/rename an auto-centre). |
| 2026-07-13 | `prod/2026-07-13` | `ecfeef5` | 2-D (static only; proxy untouched) | `index-0AIMIH_k.js` + `geoWorker-CGkr4dk3.js` | The 2025-bagrut two-circle batch (PR #101, operator commit-and-deploy): **#54 (ADR-304)** per-circle RADIUS SYMBOLS — bind at creation («שרדיוסו r», any letter, case-sensitive) via the `withRadiusSymbolBinding` chokepoint post-pass or after the fact («רדיוס מעגל O הוא R»); relations `R > r` (radius-order requirement, independent circles), `R = 1.5r` / `R/r = 2√7/5` (`set-radius-ratio` → plain `ratio` over centre-witness pairs, zero new solver code); `AB = √2R` couples to the BOUND circle; sliders show the letter. **#99 (ADR-303)** a point's side of a POLYGON region («הנקודה E נמצאת על מעגל O בתוך המשולש KAO») — `point-polygon-side` requirement + verifier + `regionSideFallback` + `droppedRegionSubject` gate. **#100 (ADR-302, was main-only)** tangent THROUGH an on-circle point + the definite «המשיק» back-reference. **Two play-test rounds folded in (Am. 1+2, session `yla2d4xo`):** the submit-gate false-positives (`droppedNewLabels` measureSymbols mask; verb gate accepts a `tan-` line reference), the one-sentence compound «דרך A עובר משיק למעגל O שחותך את מעגל P בנקודה K», the region→membership order (the (c2) conversion lands at the point's own bearing), and requirement re-statements commit (exact duplicates stay no-ops). Closes #54/#99/#100. Full 2-D suite 3314 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok (untouched). |
| 2026-07-12 | `prod/2026-07-12-4` | `7403ce3` | 2-D (static only; proxy untouched) | `index-GY7oYSVK.js` + `geoWorker-ptyHBf-J.js` | The batch since `-3`: **#77+#40 (PR #92, ADR-298)** radical/fraction VALUES as givens (`BC=35/√32`, `35/2`, `√32/5`) with EXPLICIT `√()` grouping (disambiguates `√(2/3)` vs `√2/3`), the `√()` toolbar button, and **MathML rendering** (live input preview + step rows); **#45 (PR #93, ADR-299)** right-angle glyph/word/homoglyph forms (∡/∢→∠, ⁰→°, Cyrillic→Latin, «ישרה»/«right angle» word, lowercase vertex); **#96 (ADR-300)** the abbreviated «מ-B» external-point cue (secant/tangent from external point); **#97 (ADR-301)** a stated «D על המעגל» membership survives a relation clause (was silently dropped). Closes #40/#77/#45/#96/#97. Full 2-D suite 3271 green + tsc + build clean on the deployed tree; live page serves the new bundle (200), proxy healthz ok. |
| 2026-07-12 | `prod/2026-07-12-3` | `e000e06` | 2-D (static only; proxy untouched) | `index-BYovH_N5.js` + `geoWorker-ptyHBf-J.js` | The 2-D fix + feature batch since `-2`: **ADR-295** the detection-honesty cluster (#49 `~`-scaffold excluded from the detection universe, #50 `distinctSamples` drops unforced point-collapse samples, #88 a definite VALUE needs a non-starved pool); **ADR-291 Am.** (#86) the circumscribing circle in a CUT sentence stays hidden (scaffolding); **ADR-296** (#87) config searches get `WORKER_SEARCH_BUDGET_MS` (12s) off the main thread; **ADR-297** (#32, PR #91, operator play-approved) anonymous promotable points — an incircle's touch feet are `@`-anonymous clickable dots the student promotes to a letter, so no auto-minted `F`/`G`/`H` hijacks the student's namespace. Closes #49/#50/#86/#87/#88/#32. Full 2-D suite 3212 green + tsc + build clean on the deployed tree; live page references the new bundle, page + bundle + worker chunk 200; proxy healthz ok (untouched). |
| 2026-07-25 | `prod/2026-07-25-3` | `de60737` | 3-D + **proxy** (restarted; 2-D untouched) | `3d-C_6c3M8z.js` | The operator's book-question day (PRs #323 + #327): #321 the parallelogram-family bare prisms build oblique (מעוין/מלבן/ריבוע → מקבילון + base constraints, ADR-3D-078); ADR-3D-079 coordinate-frame givens — ring ∥/⟂/on a coordinate plane/axis (`coordPlanePins` + claim), affine symbolic point components `B(2t,t,k)` left OPEN as pivot unknowns (params panel `t = ?`/`k = -3`, seed-anchored openness, sign-selects-root Am. 3), book-register prefixes + trailing-period tolerance, Hebrew variant registers; ADR-3D-080 a pyramid over existing points is a STATEMENT (right-apex seating on the carrier plane, semantic apex Am. 1) + plane riders in general position; S2.1 ratchet-compliance sweep. Proxy restarted for the llmShared3 prompt rules (never-invent-right examples, never-downgrade-base, coordinate-given examples). Gates: 3-D lane 1351 green on the deployed tree + 2-D lane 4155 green + ratchet pair verified; tsc + build:3d + build:proxy clean; /healthz ok, page + bundle 200. |
| 2026-07-12 | `prod/2026-07-12-2` | `e95f1c9` | 2-D (static only; proxy untouched) | `index-D3tcGzQp.js` + `geoWorker-DHZcuPJ-.js` | The CEFO cluster (operator "fix both"): ADR-291 circumscribing-circle REFERENCE resolution at the resolver chokepoint + show-circle (fixes #82 P1 silent tangent drop, #83 duplicate-circle re-create, closes #81 book circumcircle-cut phrasing); ADR-292 the VERB honesty gate (droppedGivenVerbs, both commit paths); ADR-294 the המשך kaf-inflection sweep (18 sites — plural המשכי parsed to the OPPOSITE onSeg constraint, fixes #79); ADR-293 view-level keep-prior (never-blank canvas + stale notice, fixes #85). 4 scenarios with exact prod/book utterances + verb-gate.test.ts. 2-D lane 3196 green + tsc + build clean; page + bundle + worker chunk 200. |
| 2026-07-12 | `prod/2026-07-12` | `ac57d39` | 2-D (static only; proxy untouched) | `index-DrL3d2Ti.js` + `geoWorker-D5RL8Lgk.js` | Fix #41 (ADR-290): the geometry WORKER — show-another/auto-resolve searches + the cold FOLD (submit, LLM, and file-load prefolds) run off the main thread with fold transplants (content-keyed primeFoldFor); live k/24 progress + a ✕ cancel (worker termination = real preemption); no-Worker environments keep the sync path. Closes the page-unresponsive class; the load half is the #67 core. Operator validated locally (dev server play). 2-D lane 3182 green + tsc + build clean on the deployed tree; page + main bundle + worker chunk all 200. |
| 2026-07-11 | `prod/2026-07-11-7` | `2a8ed20` | 2-D + 3-D (static only; proxy untouched) | `index-BoJLFRwZ.js` / `3d-C8ZwhmWn.js` | The baseline-triage batch (operator approved + "merge and deploy all"): #34 parallel-default-segments fix (ADR-287, on main directly), PR #74 (#72 3-D phrasing batch, ADR-3D-039), PR #75 (#71 2-D phrasing batch, ADR-288), PR #76 (#43 2-D guidance register, ADR-289), PR #78 (#73 3-D guidance register, ADR-3D-040). Guidance families short-circuit before the LLM in both apps; no-theft catalog sweeps locked. Full suite 3799 green (4 standing skips) + tsc + both builds clean on the deployed tree; both pages serve the new bundles (200). |
| 2026-07-11 | `prod/2026-07-11-6` | `130acb7` | 3-D (static only; proxy untouched) | `3d-D2rI8hG4.js` | Feature #69 (PR #70, ADR-3D-038): MULTI-LINE NAMING — digit-indexed line names ℓ1/ℓ2 (typed l1/l2, subscript tolerated; no primes, operator ruling); the "single-ℓ model" was a parser artifact — widened LINE_NAME + canonicalLine, ~10-rule sweep binding each matched name (was hardcoded ℓ), named-line operands in common-perp/projection; the 2010-Q3 form (two parametric lines + common perpendicular) builds closed-form He+En. Closes the deferred ADR-3D-028 enabler. Operator waived play-and-approve (commit-and-deploy). Full suite 3715 green (4 standing skips) + tsc + build:3d clean on the deployed tree; live page serves the new bundle (200). |
| 2026-07-11 | `prod/2026-07-11-5` | `0679227` | 2-D + 3-D (static only; proxy untouched) | `index-c8HfhWBZ.js` / `3d-Bq2OIDXu.js` | Feature #42 (PR #66, ADR-286 / ADR-3D-037): a persistent figure NAME — one inline-editable header input is both the name field and the visible title; save uses a set name directly (a prompted name is adopted); load derives the name from the FILENAME (embedded name = provenance only); outside undo history, clear resets; the 2-D docx question export takes the name as its title. Operator waived play-and-approve (build-now-and-deploy). Full suite 3701 green + both builds clean on the deployed tree; /healthz ok, both pages + bundles 200. |
| 2026-07-11 | `prod/2026-07-11-4` | `6adb13a` | 2-D (static only; proxy untouched) | `index-Dici3XwB.js` | Fix session — the honesty cluster + the #27 leftover guard: ADR-282 (#27 P1 — SHAPE_LEFTOVER gains side refs/כל quantifier/polygon nouns, quantified per-side semicircle escalates instead of half-parsing), ADR-283 (#31/#38 — the ב container marker governs over the חוסם/חסום verb letter, normalizeInscriptionSlip at the utterance boundary), ADR-284 (#28 — a semicircle on existing endpoints attaches closed-form: centre = midpoint, radius through an endpoint; freeRadius/autoCenter migration + apply (c3)/(c4)), ADR-285 (#52 — radical-aware ratio coefficients, AB=√2*OD). Closes #27/#28/#31/#38/#52. Full suite 3690 green + tsc + build clean on the deployed tree; /healthz ok, page + bundle 200. |
| 2026-07-11 | `prod/2026-07-11-3` | `6131563` | 2-D + 3-D + **proxy** (restarted) | `index-Zy0I7UH3.js` / `3d-CbDe9Kuf.js` | ADR-278 privacy posture (external-review follow-up): retention default-on 7 d — prod env set 30→7 per operator ruling; IP salt never falls back to the committed constant (random per-boot + warning); in-app He/En privacy note (2-D About modal, 3-D footer); NFR-SE-3/US-4 reconciled. 3-D "גרסת פיתוח V0" tagline dropped. CI now builds the proxy bundle (#56). Closes #56/#57/#58. Full suite 3659 green on the deployed tree; /healthz ok, log path exercised live (204 + event landed), no salt warning in journal. |
| 2026-07-11 | `prod/2026-07-11-2` | `f8aa09e` | 2-D + 3-D (static only; proxy untouched) | `index-_wyA3c6e.js` / `3d-BPaU7JCv.js` | Save-name prompt copy → the operator's student-friendly wording (תנו שם לקובץ - מועד בחינה, עמוד וספר, או כל דבר אחר), both apps He+En. PR #25, closes #23. |
| 2026-07-11 | `prod/2026-07-11` | `b54b155` | 2-D + 3-D (static only; proxy untouched) | `index-BUwREL-D.js` / `3d-Cyj7eEy-.js` | Fix session closing the issue queue: #22 per-operand segment semantics (ADR-268), #17 noun cut operands + Q4 (ADR-269), #21 קוטר מנקודה F (ADR-270), #18 narrow-angle hover pick (ADR-271), #7 vacuous-collapse gate (ADR-272), #2 word-magnitude honesty (ADR-273), #20 named save files (ADR-274/3D-036), #6 ADR-124 unparked. |
| 2026-07-10 | `prod/2026-07-10-3` | `499675f` | 3-D (static only; proxy untouched) | `3d-BGQndf-i.js` | ADR-3D-034 — V8-j: a point on a segment positioned so a derived pyramid is RIGHT (apex above the base centre, closed-form t; honest no-solution when the segment misses the centre). **Completes V8** (full legacy-572 input coverage). PR #12, closes #11. |
| 2026-07-10 | `prod/2026-07-10-2` | `e39679a` | 3-D (static only; proxy untouched) | `3d-C0CHZumu.js` | ADR-3D-033 — a membership statement about an existing point DRIVES the figure (stage-4 member-pin re-solve, warm-started, transactional); fixes #9 (session `n6lmx1rj`, `M על מישור DCC'D'` refused not-on-plane). |
| 2026-07-10 | `prod/2026-07-10` | `6168d0c` | 2-D + 3-D (static only; proxy untouched) | `index-42UWO93R.js` / `3d-BJG80kY4.js` | 2-D: ADR-262 Am.1, ADR-264 (+Am.1/2) — clause split, honesty gates, hover/canvas fixes. 3-D: ADR-3D-030/031/032 — plane-eq as M1 given, pair-named parametric line, derived plane equations, symbolic coordinate. (Tag added retroactively at workflow adoption.) |
