# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Geo Builder is a browser app where Israeli high-school students describe a geometry construction in natural language (Hebrew or English) and watch it render on an interactive canvas, **building the figure up one step at a time**, with relevant theorems surfaced as they build. UI is RTL Hebrew by default.

The defining interaction: a student adds information incrementally — "square ABCD" → "point G on AD" → "angle GBA = 37°" (G slides along AD until the angle holds) — and the figure forms and adapts as constraints accumulate. When a construction has more than one valid drawing, one is shown and the student can press a button to cycle to an alternative configuration.

## Current state (rebuild in progress — gate-driven, see [docs/09-implementation-plan.md](docs/09-implementation-plan.md))

The original implementation was a *shape-template matcher* that dead-ended (couldn't represent free points, points-on-objects, accumulating constraints, or alternative configurations). Rebuilding from scratch as a constructive engine. **Phases 1–4 complete (M1–M3: engine, renderer, store, parser — text box live). Phase 5a: parallelogram, general quad, segment, line∩line — Q1. Phase 5b ([ADR-019](docs/06-decisions.md#adr-019)): a `Line` object (bisector/perpendicular/parallel/through, scaffolding, not rendered) + foot, midpoint, line∩line of two lines, point-on-extension, right-triangle — Q2–Q4. Phase 5c ([ADR-020](docs/06-decisions.md#adr-020)): a drawn `Circle` object + on-circle/inscribed, antipode/diameter, arc-midpoint, line∩circle, tangent — Q5–Q7. The full v1 corpus (Q1–Q7) is reproduced from typed He/En utterances; the v1 construct vocabulary is complete. 186 tests green. Phase 5d ([ADR-021](docs/06-decisions.md#adr-021)) adds distance/equal/parallel/perpendicular constraints; ADR-018 Stage 1 (pinned-vs-free points + seeded `resample`) is built. Visible construction lines + two-circle intersection ([ADR-022](docs/06-decisions.md#adr-022)) — tangent/bisector/perpendicular/parallel can be drawn; circle∩circle is first-class. 550 tests green. **Phase 7 (LLM fallback) built** ([ADR-023](docs/06-decisions.md#adr-023)): out-of-grammar input escalates to `claude-haiku-4-5` via a server-side proxy (`server/llmProxy.ts`, `POST /api/parse`, key in `.env.local` only) that normalises freeform He/En into canonical commands the parser re-parses. 561 tests green. Remaining: production proxy deploy, implicit-circle reference, right-angle/tick/arc marks, ADR-018 Stage 2/3, then Phase 6 (theorems).**

- **`src/engine/`** — the constructive engine: dependency-graph data model (`types.ts`, with `isGeoPoint` the single source of truth for point kinds), pure `geometry.ts` (incl. `solveParam`, a generic deterministic 1-D root finder, and `footOnLine`), `solve.ts` (constraint refs/residual/description — ADR-014), `applyCommand` reducer, topological `evaluate` (over-constraint detection; resolves `Line` objects and points in one interleaved sweep), and `step` (apply/keep-prior, `commandConflict`, free-point move, `cycleAlternative`, `branchCount`). Supported constructs: the polygon family (`square`/`rectangle`/`rhombus`/`trapezoid`/`parallelogram`/`quadrilateral`/`triangle`/`right-triangle`), point-on-segment (incl. extension `t>1`), `point-by-distances` (circle∩circle), `segment` (undirected/idempotent; creates its endpoints if missing), `set-angle`; the **Phase-5b `Line` layer** ([ADR-019](docs/06-decisions.md#adr-019)) — a `Line` object (`through`/`bisector`/`perpendicular`/`parallel`/`tangent`, resolved to `(anchor,dir)`, **never rendered** — scaffolding) plus `line-intersection` (two lines, e.g. bisector∩bisector), `foot` (⟂ foot), `midpoint`, and the 5a 4-point `line-line-intersection`; and the **Phase-5c `Circle` layer** ([ADR-020](docs/06-decisions.md#adr-020)) — a drawn `Circle` (centre + radius) with `on-circle`/inscribed, `antipode`/diameter, `arc-midpoint`, `line-circle`. `evaluate` resolves circles, lines, and points in one interleaved fixed-point sweep. `set-angle` **drives a free DOF when any referenced point has one** (an on-segment `t` solved so the angle holds, branches as alternatives — ADR-012/ADR-014) and is a *check* when every referenced point is determined.
- **`src/render/`** — the SVG renderer (Phase 2, +circles in 5c): pure `transform.ts` (isotropic fit, centred, Y-flipped world→screen) and `scene.ts` (`Construction` + `positions` → flat primitives incl. `SceneCircle`, no React), with `Figure.tsx` a thin declarative SVG map over the scene plus pan/zoom/reset. **Circles are drawn** (unfilled outline, before segments; `scenePositions` adds the circle extent so the fit never clips it); lines stay scaffolding (not drawn). Tested in `src/render/__tests__/phase2.test.tsx` — **12/12 green** (transform, scene "figure → nodes", circle render, DOM-free static render via `react-dom/server`; no jsdom). The renderer is a **pure consumer** of engine output and swappable.
- **`src/store/`** — the session store (Phase 3): `geoStore.ts` is Zustand + `zundo` (temporal undo/redo). **The ordered fact list is the source of truth; the figure is derived by `replay`** (fold `applyStep` over enabled facts) — positions are never stored, so undo can't desync. Each fact is selectable (highlight on canvas), deselectable (toggle `enabled`; dependents auto-drop, reversibly), deletable (ADR-010), and **editable in place** (`update` replaces a fact's command at its position — ADR-015); `cycleAlt` edits the fact's branch; `clear`. Selection is excluded from history (`partialize` + `equality`). Tested in `src/store/__tests__/phase3.test.ts` — **13/13 green** (replay, stability, keep-prior-on-error, select/deselect/delete + dependent auto-drop, undo/redo, clear, alternatives, i18n parity).
- **`src/parser/`** — the grammar parser (Phase 4): `parse.ts` is the deterministic, offline `utterance → command[]` boundary — bilingual (He/En), keyword-order-independent rules for the polygon family, segment, point-on-segment, point-on-extension, point-by-distances, free point, line∩line, angle, the **5b** bisector-meet/foot/midpoint, and the **5c** circle family (circle by centre+radius/through, inscribed triangle/quad, chord, diameter, arc-midpoint, point-on-circle, plus the tangent∩segment, bisector∩segment, and parallel∩circle intersection compounds). A circle's id is `circle-<centre letter>` so "circle O" is referenceable by name. Unmatched input returns `{ ok:false, reason:'not-handled' }` (where the Phase-7 LLM fallback escalates) — including, on purpose, the parallel/⟂ *constraints*, a *single* bisector fact, and a 3-point (circumscribed) circle (all Phase 5d / later). `catalog.ts` is the user-facing reference **and coverage map** (drives the in-app **commands** panel): every v1 construct, grouped by `category`, each ✓ wired (clickable to try) or ○ not-yet with a `phase` tag — single source of truth, flip `supported` when a construct lands (a guard test asserts every supported example parses both locales). Tested in `phase4.test.ts`.
- **`src/` app shell** — `main.tsx`, i18n (Hebrew default, RTL via `document.dir`), styles, and `App.tsx` the **store-driven shell**: a **live text input** (parse → execute) with clickable example chips, canvas + fact list (checkbox to include/exclude, click to highlight, ✎ to edit inline, × to delete) + undo/redo/clear + alternatives + language toggle. The canvas draws **lines only** (no shape fill) and places each vertex label in the largest open wedge around it (`scene.ts` `labelDir`).
- **`archive/`** holds the entire old template-based implementation — outside `src/`, **not compiled or bundled, and excluded from tests** (`vite.config.ts`). Reference only; useful bits: the old Claude tool schema (`archive/src/services/llm/`) and theorem predicates (`archive/src/engine/theorems/`).
- **Validation corpus:** `docs/sample questions/` holds 7 real bagrut problems (text + image). The plan is corpus-driven — we reproduce each *figure* (never solve) and compare to the official image.

**Next step (resume here):** the **corpus is fully reproduced (Q1–Q7)** and the construct vocabulary is complete; on top of it the **relational + UX layers** are now in — symbolic measures ([ADR-031](docs/06-decisions.md#adr-031)), congruence/similarity ([ADR-032](docs/06-decisions.md#adr-032)), shapes-carry-true-DOF ([ADR-033](docs/06-decisions.md#adr-033)), stated angle marks (FR-RN-7), named median, **rename a point** ([ADR-035](docs/06-decisions.md#adr-035)) + the [letter-placement guide](docs/12-letter-placement.md), **image export** (FR-HS-5), **named-line `on-line` markers that are constraint-drivable** ([ADR-036](docs/06-decisions.md#adr-036)), `AB ⟂/∥ CD` also drawing its segments (FR-IN-7), and label readability (white halo + a nudge-off-lines placement pass). The whole **deferred backlog is now cleared** (2026-06-15): auto-draw segments for distance/equal/angle givens, an explicit **merge** command (FR-HS-8), a free `on-line` marker's offset now sampled by "show another configuration" (ADR-036 A2), named-line markers widened to perpendicular/parallel (ADR-036 A3), and **ADR-018 fully built** (Stages 2 & 3 — constraints prune sampled DOFs + a live "degrees of freedom remaining" cue, FR-ALT-4). **Then a long manual-testing marathon (2026-06-16) on the circle/tangent/secant corpus** added: a **secant from an external point** (FR-EN-17), **one/two tangents + multiple secants from a point** (FR-EN-18), **two circles intersecting at A,B** (FR-EN-19), **R auto-binds to the circle radius**, "show another configuration" now **resamples free DOFs AND cycles branches** with count-scaled on-circle variety, the **DOF cue corrected** to `raw − constraints`, named **perpendicular / ⊥-bisector** (construct-or-constrain), a loose segment **repositioned into a ⊥ cross** (ADR-036 A4), and **a circle's centre shows only when used or named** (FR-RN-8). The headline next milestone is **Phase 6 (theorems)**. **Then (2026-06-17, work PC):** the **"no fixed assumptions" principle elevated to [ADR-052](docs/06-decisions.md#adr-052)** (every unstated magnitude is a free DOF — audit fixed a single-circle radius pinned at 5 and "G on AB" pinned to the midpoint), **circle radius as a free solver DOF** ([ADR-051](docs/06-decisions.md#adr-051)), and the **collinearity family** ([ADR-050](docs/06-decisions.md#adr-050) + 3 amendments: ordered `line ABE`, second-intersection "E on line DB", and two driven-solver fixes). **Then (2026-06-18, work PC):** the **givens verifier** ([ADR-053](docs/06-decisions.md#adr-053), `src/engine/verify.ts`) — re-derives the relations the commands assert and checks them against the final coordinates; **"green" now means VERIFIED, not just no-error** (amber cue otherwise); two new parser constructs (`tangentMeetsOtherCircle`, `lineMeetsCircle`) + `פוגש`/`פגש` in the shared intersect keywords; and **UX** (scroll restored, responsive canvas via `ResizeObserver`, a playable free-circle-radius slider panel). The live resume pointer and parked threads (area constraint, production proxy deploy, coord-validation campaign) live in **[docs/09-implementation-plan.md](docs/09-implementation-plan.md) (Status line)** and **[docs/PROJECT-MEMORY.md](docs/PROJECT-MEMORY.md) (session log)** — read those first. **Then (2026-06-18, home PC):** **directional `המשך` semantics** ([ADR-054](docs/06-decisions.md#adr-054)) — `המשך AC`→D beyond C (order A→C→D); a new `extend-onto-circle` command (a shared-endpoint case = deterministic line∩circle avoiding the shared point; a neither-on-circle case = driven on-circle θ + `collinear` + `collinear-order`, the free radius growing so the extension reaches); order-aware 1-DOF root pick so a root-at-seed isn't resized away; a clear "line is tangent — no second crossing" message replaces an opaque coincidence crash; the bagrut corpus reworded to the strict form. The live resume pointer and parked threads (area constraint, production proxy deploy, coord-validation campaign) live in **[docs/09-implementation-plan.md](docs/09-implementation-plan.md) (Status line)** and **[docs/PROJECT-MEMORY.md](docs/PROJECT-MEMORY.md) (session log)** — read those first. **1203 tests green, build clean** on branch `rebuild-foundation` (pushed to GitHub `dcodish/geo_builder`). Most recent fixes (all the same inscribed-quad-in-a-right-triangle figure): ([ADR-075](docs/06-decisions.md#adr-075)) **"AB tangent at F" on an EXISTING line** now emits a tangency *constraint* (`set-perpendicular`, radius ⟂ the existing line) that flexes the circle, instead of re-creating A,B as markers on a new tangent (which crashed with a dependency cycle); ([ADR-076](docs/06-decisions.md#adr-076)) **"F, G, H on AB, AC, CB"** (N points pairwise on N segments) now parses deterministically (was an unreliable LLM escalation); and ([ADR-077](docs/06-decisions.md#adr-077)) **"the diameter from F cuts AC at E"** now parses and lands E ON the segment — a `set-line [A,E,C]` order constraint flexes the figure (F moves) so the crossing stays on the side. A **bare segment reference "AC" defaults to the segment** (not the infinite line / continuation); opt-outs are `המשך AC` (extension, E beyond) and `הישר AC` (infinite line). **Principle (general, currently only in the diameter rule):** a segment reference means the segment between its endpoints, not the continuation, unless the student says `המשך`. **Then (2026-06-22):** ([ADR-098](docs/06-decisions.md#adr-098)) a **free point on a circle whose two "המשך" secants must each reach another circle's far side** (operator: "C is not positioned to satisfy the input") is now SAMPLED to a valid placement, never driven across the tangent degeneracy — the verifier re-derives `extend-onto-circle` membership + a **hard directional-order check** (wrong-side → amber), the sampler/"show another" gate on a clean extension margin, and `execute` auto-advances the seed to the first config where every extension reaches the far side (this began folding `collinear-order` into the givens verifier). ([ADR-099](docs/06-decisions.md#adr-099)) **inscribing in an EXISTING named circle** (`מרובע EBAD חסום במעגל O` when O + the vertices already exist) now asserts membership per vertex (idempotent `point-on-circle`) + draws the polygon, instead of re-creating the circle (which redefined its centre → "'O' is already defined" → built-nothing). Both were the operator's bagrut-Q4 manual session; with both fixed the figure is geometrically correct (∠EDA=∠CBA holds). ([ADR-100](docs/06-decisions.md#adr-100)) **angle EQUALITY** `∠ABC = ∠DEF` (and ratio `= 2∠DEF`) now parses → `set-angle-ratio` (was not-understood; the engine already had the relation via similar-triangles). ([ADR-101](docs/06-decisions.md#adr-101)) the **DOF cue now reports SHAPE degrees of freedom** (modulo the place/rotate/scale similarity gauge) — `freeDofCount` subtracts the free gauge, so a figure rigid up to similarity reads **0** and "✓ fully determined" finally fires (was unreachable without absolute pinning). Q4 now reads **4** (was 8); lone square/SSS-triangle/circle read 0. `freeDofs` (samplable list) is unchanged, so "show another" still varies size/placement of a determined shape. **Then (2026-06-23):** ([ADR-103](docs/06-decisions.md#adr-103)) a **constraint can now drive a circle's CENTRE, not only its radius** — `recruitFreeDofs`/`ancestors` surfaced a circle's free radius but never its free centre, so a size given on points riding two intersecting circles (Q4: `CD=36`, then `CE⟂AB` with `CD=36,DE=18`) failed "over-constrained" because the circle∩circle geometry caps |CD| (~8) with centres pinned a fixed gap apart. Fix surfaces the free centre as a drivable DOF (targeted, no ADR-095 over-recruit regression); `CD=36` and the full Q4 build (sizes before the ⟂). ([ADR-104](docs/06-decisions.md#adr-104)) **order-independence via deferral** — `replay` now retries still-failed CONSTRAINT-only facts against the completed figure to a fixpoint, so a constraint typed too early (e.g. `CE⟂AB` before `CD=36, DE=18`) auto-defers past the givens that pin it; the full Q4 builds in any entry order. (A direct stronger solver — heavy Halton global multi-start — was tried for the bare under-determined case and didn't converge in 8-DOF; reverted. Deferral works because the complete given set is determinate.) **Limit:** a genuinely under-determined partial figure (sizes never entered) stays pending until the determining givens arrive. **Then (2026-06-24):** the long-parked **coordinate-validation campaign** is BUILT ([ADR-109](docs/06-decisions.md#adr-109), `src/validation/`) — a *differential* check alongside the invariants campaign + givens verifier: seeded recipes **pin their base points** so the frame is fixed, an **independent closed-form oracle** (imports nothing from the engine) recomputes the derived coordinates, and they're diffed directly (engine must match one whole valid branch config). Dev/CI-only (under-determined production figures have no unique ground truth); applies only to 0-shape-DOF figures (~11% of real logged figures — a sharp tool for the determined slice, not a broad net). Zero engine changes (read-only consumer of `build`/`evaluate` → no risk to existing behaviour). **Finding: the engine matches the oracle to ~machine epsilon — worst residual 5.6e-15 over a 13,760-figure sweep (tol 1e-4)**, i.e. coordinate output incl. the numeric solver paths is exact to double precision on every covered construct. **Then (2026-06-24): theorem-vocabulary gap closed** — an operator audit ("is there no דלתון?") of the bagrut theorem list vs. the construct vocabulary. ([ADR-110](docs/06-decisions.md#adr-110)) **named shapes as parser MACROS** — **kite·דלתון**, **isosceles·שווה שוקיים**, **equilateral·שווה צלעות**, **isosceles trapezoid·טרפז שווה שוקיים**, **triangle midsegment·קטע אמצעים** — each decomposes to already-supported commands (quad/triangle/trapezoid + equal-side/midpoint constraints) so the constraint solver does the work, **no new engine construct** (theorem detection re-derives the type from geometry); i18n term corrected `עפיפון`→`דלתון`. ([ADR-111](docs/06-decisions.md#adr-111)) **regular polygon** — a generic `{type:'polygon';ids}` command + `polyEdges` + a `regularPolygon` rule placing n equally-spaced vertices on a hidden free-radius circle (He `מחומש/משושה/…`, En `pentagon…dodecagon`, `regular N-gon`; n=3→equilateral, n=4→square, n≥5→generic; irregular n>4 → the LLM segment-loop net). ([ADR-112](docs/06-decisions.md#adr-112), amends ADR-101) **DOF-cue fix** — a pinned parametric (on-circle/on-segment) vertex now contributes **0** DOF, so the ladder reads exactly **free quad 4 → cyclic quad 3 → inscribed square 0 → regular polygon 0** (corrected a pre-existing over-report on every inscribed shape; "✓ fully determined" now fires). **1322 tests green, build clean.** **Then (2026-06-24):** ([ADR-113](docs/06-decisions.md#adr-113)) a **constraint can now flex a shape DOF reached *through* a parametric point** — operator-reported `GE⊥AB` on a rhombus (E mid AB, G on the extension of diagonal BD) failed "over-constrained"; the figure is constructible only if the rhombus angle flexes below 60° so G lands strictly beyond D (at 60° the unique solution is the degenerate G=D), but the drivable-ancestor walk **stopped at the free param carrier G** and never reached the rhombus angle (carried by G's parent D), so the recruiter could only slide G onto D. (A `git bisect` proved it never worked — back to 2026-06-17 the same figure failed with "D and G would be at the same point".) Fix: in `drivable` mode (failure-path recruiter only) record a free on-segment carrier **and keep walking past it** to the DOFs behind its segment; regulariser-safe, general for the class. Locked by scenario `perpendicular-from-midpoint-flexes-rhombus`. **1323 tests green, build clean.** ([ADR-114](docs/06-decisions.md#adr-114)) **"isosceles" picks a SOFT default equal-pair that yields to an explicit one** — `משולש שווה שוקיים ABC` hard-coded `|AB|=|AC|` ("apex = first vertex"), so the student's own `AB=BC` stacked into an **equilateral** triangle they never asked for; "isosceles" only asserts SOME two sides equal — which pair is the student's to state (ADR-052). Fix: the macro tags its default equality `soft`, and `replay` drops it when an explicit `set-equal` on the same triangle is given, so the stated pair wins (lone isosceles still draws `|AB|=|AC|`). Scoped to the isosceles triangle; equilateral/kite/iso-trapezoid stay hard. Locked by scenario `isosceles-explicit-pair-overrides-default`. **1324 tests green, build clean.** ([ADR-115](docs/06-decisions.md#adr-115)) **a tangency declared against an EXISTING circle is a CONSTRAINT, never a re-built circle** — operator session `fn2wt71w` (`AB ו AD משיקים למעגל O` on a kite whose circle O already inscribes BCD) crashed `'O' is already defined`; a **log-class audit** (scan `logs/debug-log.jsonl` for the whole `already defined` class → reconstruct each → replay on HEAD) showed 9/11 historical instances already fixed and the crash itself a **stale dev server** (the running server predated the ADR-107 placement fix — the operator's "restart" instinct was right), but on HEAD the figure was **silently verifier-amber**: `cornerTangentCircle` rebuilt circle O (free centre on the bisector + `circle-through` the foot) and re-radiused it, kicking the inscribed B,C,D off. The audit surfaced a **sibling** — `incircle` ("משולש DEF חוסם את המעגל O") did the same to a circumcircle. Fix: an existing-circle guard on each — `cornerTangentCircle` emits a per-arm tangency constraint (`set-perpendicular(O,tip,vertex,tip)`); `incircle` builds the DUAL (3 free touch points → tangent at each → vertices as pairwise tangent intersections, deterministic — the foot-on-circle inversion over-constrains the 3rd side; *a green verifier is necessary but not sufficient — check per-step `status`*). Locked by scenarios `corner-tangent-on-existing-circle` + `triangle-circumscribes-existing-circle`. **1326 tests green, build clean.** ([ADR-116](docs/06-decisions.md#adr-116) + [ADR-115](docs/06-decisions.md#adr-115) Am.) **arc-measure term `קשת`/arc + implicit-circle tangent** — bagrut-Q4 manual test (session `6ai22ulh`): (1) the textbook given `⌢DE = 2⌢CE` had **no arc term** → a new `arcEquality` rule maps an arc to its central angle (arc XY ≡ ∠XOY) → `set-angle-ratio` (bilingual `קשת`/`arc`/`⌢`, equality or ratio, circle resolved implicitly); (2) `AB ו AD משיקים למעגל` with **no name** fell through ADR-115's named-only guard and spawned spurious tangent feet **E**, K + an auxiliary circle P, **hijacking the label E** → the guard now resolves the circle implicitly (named OR the one circle, ADR-029) so the unnamed tangent constrains O and creates no points. Clean Q4 build verifies, arc DE = 2·arc CE holds (∠DOE = 2∠COE). Locked by scenario `arc-ratio-and-implicit-tangent-q4`. **1327 tests green, build clean.** ([ADR-118](docs/06-decisions.md#adr-118)) **AREA as a first-class measure & constraint** — given (`שטח המשולש ABC הוא 13`/`SABC = 13`), ratio (`SABC/SDEF = 3/4`, `גדול פי 2 מ`, `רבע מ`, `היחס…הוא r`), and a letter label (`נסמן את שטח ABCD ב-S`); bilingual + compact `S`-notation, on any polygon. Built end-to-end (shoelace `polygonArea`, `area`/`area-ratio` constraints via `driveOrCheck`, `measure-area` lowered like `measure-length`, parser rule before the shape rules, verifier, centroid label, DOF cue). **Principle (scale vs shape):** a lone absolute area pins the figure's SCALE only (invisible after the similarity fit, ADR-101 — an equilateral triangle stays equilateral, just resized); area reshapes only as a ratio or a 2nd absolute given. Locked by scenarios `area-absolute-sets-scale-not-shape` + `area-ratio-reshapes`. **1344 tests green, build clean.** ([ADR-117](docs/06-decisions.md#adr-117)) **a named triangle shape on an inscribe carries its equal sides** — `ABC משולש שווה צלעות חסום במעגל` (equilateral inscribed) drew a GENERIC inscribed triangle: `inscribedPolygon` handled quad shapes but ignored the triangle shape word, and "שווה צלעות" isn't a `SHAPE_LEFTOVER` token so it was silently dropped (neither constrained nor escalated). Fix: detect equilateral/isosceles and append the ADR-110 macros' equal-side constraints in every branch, flexing the on-circle vertices into shape (verified |AB|=|BC|=|CA|); plain inscribed triangle unchanged. Locked by scenario `equilateral-triangle-inscribed`. **1332 tests green, build clean.** **Then (2026-06-25, work PC):** ([ADR-119](docs/06-decisions.md#adr-119)) **a chord's endpoints lie ON the circle in ANY phrasing** — operator session `sflkyd0r`: after `AB קוטר במעגל`, `CD ו AF מיתרים המקבילים זה לזה` ("CD and AF are parallel chords") parsed to two PLAIN segments + `set-parallel` and DROPPED the `מיתר`/chord membership (C,D,A,F became free points, not on circle O). Root cause: `parse` is first-match-wins, `parallelConstraint` (plain segments only) runs BEFORE the `chord` rule and claims the utterance; and `chord` itself handles only one chord. Fix: a centralised post-pass `withChordMembership` asserts `point-on-circle` for every SEGMENT endpoint in a chord-flavoured utterance with a resolvable circle (idempotent; a circle centre is excluded so "radius OE" keeps O off; a chord's midpoint isn't a segment endpoint) — general across ∥/⟂ chords. Locked by scenario `parallel-chords-keep-circle-membership`. *(A separate word-equality gap "X ו Y שווים" was raised and the operator declared it NON-actionable — students spell equality out as `AB=BC`.)* **1345 tests green, build clean.** **Then (2026-06-25, work PC):** ([ADR-120](docs/06-decisions.md#adr-120)) **the `△` triangle glyph is a parser keyword + toolbar button** — operator: "do we support congruent/similar triangles? … we're missing the triangle symbol." Congruence (`חופף`/`≅`, SSS) and similarity (`דומה`/`~`, AA) were ALREADY supported ([ADR-032](docs/06-decisions.md#adr-032)) — `משולש ABC חופף למשולש GHT` builds both triangles + makes GHT congruent to ABC (verified). Added `△`/`▲` to the symbol palette + the base `triangle` rule keyword (so `△ABC` builds a triangle, the construction counterpart of `∠ABC`) + the `twoTriangles` strip; catalog examples switched to the `△ABC ≅ △DEF` form. Locked by scenario `congruent-triangles-word-form`. **1346 tests green, build clean.** **Then (2026-06-25, work PC):** ([ADR-121](docs/06-decisions.md#adr-121)) **area subscript notation `S_{ABC}` / `S_ABC` + an `S_{}` toolbar button** — operator: "how would a student enter areas? can we have an `S_{}` format?". Area (ADR-118) read only the glued `SABC`; the textbook `S_{ABC}` didn't parse. Fix: a `normalizeAreaSubscript` pass in `parse` rewrites `S_{ABC}`/`S_ABC` → `SABC` (scoped to `S_` + 3–4 uppercase labels, so every area path inherits it and nothing else is touched) + an `S_{}` palette button (caret lands between the braces). Glued + verbose forms unchanged. Locked by `area-subscript.test.ts` + catalog-coverage. **1355 tests green, build clean.** **Then (2026-06-25, work PC):** ([ADR-122](docs/06-decisions.md#adr-122)) **swap two labels (A ↔ B) + a token-aware utterance relabel** — operator (chord figure): wanted C and D on opposite sides; "show another" can't (a relabeling isn't a different shape — swap is the right tool) and rename C→D errored "target-taken"; the temp-label workaround corrupted the display (`CC1`→`DD1`, a substring `split/join` relabel bug). Fix: a `swap(a,b)` store action (exchange two labels everywhere, one undo, positions visibly switch); `relabelUtterance` token-aware (replaces the substring relabel in rename/merge so `C1` is never corrupted); a "Swap endpoints" action on the **segment** menu (`SceneSegment` carries endpoint ids) + a typed `parseSwap` ("swap C and D" / "החלף בין C ל-D"); `swap` removed from `parseRename` (it used to route there → the error). Locked by `swap.test.ts` + `swap-parse.test.ts`. **1371 tests green, build clean.** **Then (2026-06-25, work PC):** ([ADR-121](docs/06-decisions.md#adr-121) Am. + [ADR-123](docs/06-decisions.md#adr-123)) **area-ratio on a kite — parse fix + a FORCED coincidence is allowed (with a notice)** — operator session `id4dn4a2`: `S_{ACD}=4S_{NCE}` didn't parse (a coefficient glued to the marker broke `\b`-before-`S` in the ref regex AND `normalizeAreaSubscript`; fixed with `(?<![A-Za-z])S`), and `שטח NCE = רבע שטח ACD` errored "O and N would be at the same point". Diagnosis: △NCE~△ACD is structural (right kite ⇒ ∠ADC=90°=∠NEC, shared ∠C) ⇒ area ratio ¼ ⟺ CN/CA=½; AC is a diameter (Thales) ⇒ O is its midpoint ⇒ CN/CA=½ places N EXACTLY on O. The solver was fine (0.3/0.4/0.6/0.7 converge) — only the coincidence guard hard-failed at the N=O value. Fix (operator's rule): a FORCED coincidence is ALLOWED with a notice (`evaluateCore` collects `coincidences`; store/UI surface "X and Y converge"), while DEFAULT collisions stay avoided (sampler + `chooseComposition` prefers a coincidence-free placement, errors only when both sides stack). Justified here because O was never user-defined. The earlier abandoned recruiter/similarity-reduction work proved unnecessary — the guard was the only blocker. Locked by scenario `area-ratio-converges-points-allowed`. **Then (same session)** the coincidence unmasked a freeze — `meetsRequirements` still required `pointsDistinct` (N=O fails it) → `autoResolve`→`findValidConfig` looped ~136 replays (~5 s each) → hang; fixed by `pointsDistinct(c,pos,allowed)` skipping the figure's `coincidences` (meetsRequirements/resample pass `fig.coincidences`), locked by `coincidence.test.ts`. _(Perf follow-up: a single `replay` of this coupled figure is ~1.5 s — the area-ratio over-recruits DOFs; usable but a leaner recruitment would speed it up.)_ **1378 tests green, build clean.** ([ADR-124](docs/06-decisions.md#adr-124)) **`extend-onto-circle` onto an EXISTING target point is a CONSTRAINT, never a silent no-op** — operator "check why C-A-D was not respected" (session `v7veg7sc`): `המשך CA חותך מעגל P ב D` did **nothing** because D already existed (from the earlier "chord AD") — `addObj` keeps the first definition and the shared-endpoint extend path pushes no constraints, so the directional C→A→D coupling was dropped (collinear residual 0.38). Fix: an existing extend target is now a **constraint** (collinear + beyond-the-2nd-letter order), driving a free on-circle D's θ to the far crossing; new targets unchanged. Locked by an `extend-onto-circle.test.ts` unit test. The engine change was **committed verbatim by a concurrent session** (`8b098fa`); revalidated intact. *A deeper finding (proved by tangent-chord algebra + a 15k-sample engine-free sweep): the operator's exact figure is **over-determined** — "AD tangent to O at A" + "CB tangent to P at B" make C-A-D collinear impossible non-degenerately, so the step now reports `over-constrained` honestly. The end-to-end scenario for that exact sequence is **PARKED** pending the operator's check of the bagrut source (is "AD tangent" a given or the conclusion?).* **Then (2026-06-30):** ([ADR-163](docs/06-decisions.md#adr-163)) **a "right triangle"'s right-angle vertex is an unstated default that yields to an explicit angle** — `ABC משולש ישר זוית` then `זווית ABC = 90` was refused "over-constrained" because the right angle is pinned structurally at the LAST vertex C; a `replay` pre-scan now reorders the triangle ids so an explicitly-90° vertex becomes the structural right-angle vertex (same ADR-052/ADR-114 default-yields-to-statement shape), so the stated angle holds as a passing check. ([ADR-164](docs/06-decisions.md#adr-164)) **a single-vertex angle `∠B = 90` / `זווית B = 90`** now resolves its two arms from the figure when the vertex has exactly two edges (one possible angle); with a different count it returns a new `ambiguous-angle` clarification ("name all three letters") — a third `ParseResult` variant that does NOT escalate to the LLM. **Operator follow-up on the same fix:** ([ADR-163](docs/06-decisions.md#adr-163) Am.) the reseat now also moves the right-angle **knee mark** to the reseated vertex (it was left at the un-reseated last vertex C → the operator's "weird image" of two knees); ([ADR-165](docs/06-decisions.md#adr-165)) a **constraint that morphs a named shape into a different one** (`טרפז ישר זווית ABCD` + `∠ABC = 90` → a rectangle) is now **allowed but flagged amber** (the givens verifier raises `figure.v.trapezoidMorph` when a declared trapezoid's both opposite-side pairs become parallel) — the operator's allow-but-flag choice, since ADR-157 only guards re-declaring a shape WORD, not a constraint reshape. **Then (2026-06-30, bagrut Q9):** ([ADR-166](docs/06-decisions.md#adr-166)) **two SEGMENTS that meet must meet ON the segments, not the continuation** — `AE ו BF נפגשים בנקודה G` (rectangle with equilateral triangles built on its sides) built clean but MIRRORED: the apexes sampled OUTWARD so the segments diverge, and `line-line-intersection` (the infinite-line crossing) placed G on the backward extension (param ≈ −1). Two coupled root causes, both fixed: the parser flags a plain meet `onSeg` (no `המשך`/`הישר`) → the verifier (`figure.v.meetOnSegment`, amber) + `meetsRequirements` require the crossing within both segments; AND the apex side — an unstated DISCRETE DOF (ADR-052) the continuous sampler reached only ~2% of seeds — becomes an explorable **reflection DOF** encoded in the seed's high bits (`reflectableFreePoints`/`applyReflections`/`withReflectMask`), so `firstSatisfyingSeed`/`findValidConfig` mirror the apexes inward and bring the crossings onto the segments. **Am. (operator follow-up):** the emergent `EGFH` rhombus is now DETECTED — `onHostEdges` splits an `onSeg` crossing's operand segments (G↔A,E,B,F; the ADR-162 node-definition class for a new node kind), `viewRelations`/`detectShapes` sample the requirement-satisfying config (`firstSatisfyingSeed`, not seed 0), and a shared `convergedSamples` filter drops numerically-diverged solves (~2/16 equilateral-apex seeds blow up) that masked the forced shape. Locked by scenario `segment-meet-lands-on-segments`. **Then (2026-07-01):** ([ADR-167](docs/06-decisions.md#adr-167)) the **implicit-edge universe is now GEOMETRIC, not a whitelist of point kinds** — a deep-review of "why do emergent shapes/lines shown on the diagram but never declared keep going undetected?" The universe `figureEdges` was `pointNeighbors + onHostEdges + visibleLineEdges`, where `onHostEdges` was a hand-maintained list of KINDS allowed to split their host (on-segment/midpoint/foot, and — added by ADR-166 — `onSeg` intersection); the operator's own note called each addition "the node-definition issue, **again**." Root fix: `onHostEdges` deleted, replaced by `collinearSplits` — for every drawn segment/polygon-edge, any point that is COLLINEAR on it in every sample AND within its span in some valid config splits it, **regardless of construction kind** (an intersection, foot, midpoint, `set-line` collinear point, or purely emergent coincidence all split the carrier they land on; sub-segments like `G–C` become first-class edges). The edge universe stays topological/generous; the "forced in every sample" classifier still gates which cycles are real shapes — so a first attempt requiring within-span in EVERY sample regressed the ADR-166 rhombus (the sampler visits mirror configs where a crossing sits past an endpoint) and the collinear-in-every / within-in-some split restores it. Engine-only per the operator's steer (renderer/build untouched); the sub-segment **highlight** (App resolves shape edges to stored `segment` objects only, `Figure` lights by object id) is a separate renderer concern left as a follow-up. Locked by `collinear-splits.test.ts` + scenario `emergent-shapes-through-crossings` (rectangle + two equilateral triangles: emergent ABH, CDG, EGFH all detected). **1667 tests green, build clean.** **Then (2026-07-01, same session, renderer — operator opted into the highlight + raised the clutter problem):** ([ADR-167](docs/06-decisions.md#adr-167) Am.) (1) **sub-segment highlight** — a detected shape's boundary is now highlighted as point-PAIRS (`Figure`'s new `highlightEdges` prop strokes each pair between its endpoints), so an edge that is only a portion of a longer drawn segment (`G–C` ⊂ `EC`, no object of its own) lights up — the "CDG segments not highlighted correctly" fix (`transform.ts` gained `toWorld`); (2) **hover-to-focus for "view relations"** — a rich figure marked with every equal tick/arc at once is unreadable/mis-read, so the relations layer is now HOVER-DRIVEN: the resting figure is clean, and pointing at a side/angle ON THE CANVAS reveals ONLY its equality class (`relationAt` picks the class under the cursor — a segment by ⟂ distance, an angle by pointing into its wedge; `relationsForPick` narrows to it; the hovered length class also gets accent strokes). Direct manipulation, no chip; a hint tells the student to hover. Pure pickers unit-tested (`relation-marks.test.ts`); engine untouched. (3) **corner value drawn once** — hovering the emergent rhombus showed `60° 60°` at each 60° corner (the corner is forced-60° under two IDs, `∠AFD` and `∠GFH`, since `H`∈`FA` and `G`∈`FD`); `relationMarks` now keys a definite-angle value by its WEDGE (vertex + two ray directions) not by point IDs, so the duplicate is deduped (arc-suppression too). **1672 tests green, build clean.** **Then (2026-07-01, session `sub2ys2a`):** ([ADR-169](docs/06-decisions.md#adr-169)) **a trapezoid's height drops to the OPPOSITE PARALLEL BASE** — `CE גובה בטרפז` (on a right trapezoid ABCD) escalated to the LLM and returned not-understood while `CD גובה` works on a triangle. The `altitude` rule resolved the opposite side with triangle-only logic (the apex's two neighbours must join into a triangle); in a trapezoid the apex C's neighbours B,D are a **diagonal**, not an edge, so no triangle → not-handled. Fix: the parser gains a figure-derived `parallels` hint (vertex-disjoint parallel edge-pairs via a new `parallelEdgePairs` engine helper — the same category of derived hint as `neighbors`/`circleMembers`), and `altitude` drops the height to the **unique opposite parallel base** (C on DC ∥ AB → foot on AB, CE ⟂ AB); a triangle (empty `parallels`) or a parallelogram (two parallel pairs → two valid heights) defers per [ADR-052](docs/06-decisions.md#adr-052). Triangle altitudes unchanged. Locked by scenario `altitude-in-trapezoid-drops-to-opposite-base` + `phase4.test.ts`. **1681 tests green, build clean.** **Then (2026-07-04): Phase 6a — the live theorem-surfacing feed — is BUILT** ([ADR-208](docs/06-decisions.md#adr-208)). A pure, read-only spine `src/theorems/` (sibling of `detectShapes`/`detectRelations`, engine untouched): `detectTheorems` folds an authored `THEOREM_TABLE` over a **coordinate-free** `MatchCtx` (facts + dependency graph + symbolic hints `circles`/`neighbors`/`tangents`; no `replay`/`evaluate`, so the feed runs LIVE each step). "Help, don't reveal" is enforced **structurally** — the derived-premise theorems (similarity 68/69/70/71, bisector-ratio 76) are simply not in the table, so they can't surface. Salience (headline listed individually vs background folded per family) + tier (green `certain`/amber `possible`) tame the flood; each entry attributes to its completing fact + marks `● new` on the current step. Two root fixes over the Q5–Q7 corpus: dual diameter-form recognition (a `diameter` command OR a `set-collinear` through the circle centre) and dual tangent-construction recognition (a `via:'tangent'` spec OR the Thales aux-circle fingerprint). The App carries a live feed (tier dots, ● badges, per-family background fold, click-a-row → highlight the PREMISE objects) + a Display-options toggle; it's computed in-App via `useMemo` (coordinate-free → no store action needed, unlike the sampled `detectShapes`) and emergent-shape triggers ride the "detect shapes" layer when on. Gate met: `integrity`(118)/`matchers`/`corpus`(Q5–Q7 step-gated `expectSurfaced ⊆ feed` + `mustNotSurface ∩ feed = ∅`) green; full suite + build clean. Next milestone: **Phase 6b** (B-series booklet corpus + parallels/median/midsegment families + similarity/bisector as gated *converse-recognition* prompts). **Then (2026-07-05):** ([ADR-228](docs/06-decisions.md#adr-228)) **size givens unified across shapes** — (A) `O_1`/`O_{1}` point-label subscripts normalise to the glued `O1` (a point token is a letter + GLUED digits, so the underscore had truncated the label to `O`); (B) a circle sized by its **circumference** (`שהיקפו 6π` ⇒ r=C/2π=3) or **area** (`ששטחו 9π` ⇒ r=√(A/π)=3) lowers to a NUMERIC radius, reusing the fixed-radius path (a circle's size IS its radius — no new construct); (C) a polygon **perimeter** (`היקף ABC = 20`) / perimeter-ratio is now a first-class `perimeter` constraint (the sibling of area, ADR-118) — `set-perimeter` drives Σ of the sides to the given and the verifier checks it. `היקף` is one Hebrew word for circumference AND perimeter, disambiguated by whether the utterance names a circle. Locked by scenarios `circle-circumference-sizes-radius` + `polygon-perimeter-sizes-figure`. **2237 tests green, build clean.** **Then (2026-07-05, cont.):** ([ADR-229](docs/06-decisions.md#adr-229)) **freeze-and-co-drive** — a free point claimed by one constraint can now co-drive a SECOND failing constraint on its spare DOF (`SolveDirective.also`), solved over a FROZEN bake of the valid subsystems (never the whole carrier soup) with deterministic multi-start host seeds; `recruitFreeDofs` stops at the first fully-valid config (a sibling order-constraint's failed experiments were undoing found solutions). Unlocks the "one point, two conditions" class — bagrut Q11 (two tangents from apex B at C,D + "A on the extension of BD") builds end-to-end to the closed form (|AB|=30, |BC|=18, |AD|=12). Locked by scenario `two-tangents-apex-collinear-with-pinned-point`. **2264 tests green, build clean.** **Then (2026-07-05, cont.):** ([ADR-230](docs/06-decisions.md#adr-230)) **a size given on a circle's RADIUS drives the radius, never the free centre** — operator "check why the diagram in test failed" (dev-log session `gzswxmq3`): two tangent circles with `O1M=9`/`O2M=16` (M the touch point, so the givens ARE the two radii) + two tangents from N to O1 failed `over-constrained: |O1M|=9 cannot hold`, 2nd tangent non-⟂. Root cause: `set-distance |centre·P|` where P lies on the circle is a RADIUS given, but with the free radius BUSY driving the tangency coincide, `driveOrCheck` reached neither the radial point (not a carrier) nor the radius (unavailable) and fell through to the free CENTRE — which can't change `|O1M|` — bloating the tangent solve to 6 coupled DOF that settled in compromise basins (|O1M|→6.9). Fix: route a size given on a BUSY tangency radius to the radius (pin it, via the shared `applyRadiusGiven`) and hand the orphaned coincide's centre-gap to a free centre (`keepTangencyDriven` now fires as soon as ANY tangency radius is pinned, since the remaining free radius alone can't satisfy a fixed centre gap); an AVAILABLE free radius (two INTERSECTING circles) is deliberately left flexible so the recruiter grows it without breaking `circle-circle-intersection`. Locked by scenario `tangent-circle-size-given-drives-radius-not-centre`. **2267 tests green, build clean.** **Then (2026-07-06):** an operator-commissioned **full review** (robustness of ADR-228/229/230 + measured profiling + detection-layer architecture; client-server for detection REJECTED — Web Worker + shared samples is the right split) followed by **[ADR-231](docs/06-decisions.md#adr-231)** — **[docs/17-design-rules.md](docs/17-design-rules.md)** (the operator-authority doctrine every future bug-fix session must read: class-first diagnosis, patch tripwires, chokepoint registry, mechanisms M1–M4) **plus the fixes**: M1 existing-id lowering (a statement about an EXISTING point is a constraint in every case — the prod `'O' is already defined` class, `fn34ptei`, closed at the mechanism); M2 ownership/order-independence (structural deferrable predicate, unowned-coincide re-home, (F)-bake directive restore, minimal-first staged recruiting, and the **HOIST** dual of ADR-104 — **Q11 with sizes typed LAST now builds to the same closed form**); M3 perf (evaluate purity memo, ONE shared budgeted sample core for relations+shapes, determined-figure single-sample, resample budget); honesty fixes (perimeter-beside-a-circle escalates instead of dropping the given; the radius reroute keeps the stated length label; ⟂ reflection anchors are slot-free). Locked by scenarios `existing-point-statements-lower-to-constraints`, `q11-sizes-last-order-independence`, `perpendicular-helper-flips-mirrored-slot` + the M1 class matrix in `redefine-existing-point.test.ts`. **Then (2026-07-06): save/load a figure file — FR-HS-10 REALISED + a fixtures regression net** ([ADR-232](docs/06-decisions.md#adr-232)) — `src/store/figureFile.ts` serializes the session to a portable `.geo.json` (the replay inputs: facts with utterance+lowered commands, seed, dialed radii, display styling — NEVER positions; an LLM step never re-escalates on load; `cmd.branch` keeps the alternative); sidebar save/load buttons; load = ONE undoable `set` (never destructive), graceful bilingual refusals (`schemaVersion`-gated). Companion net `src/__tests__/fixtures.test.ts`: every `fixtures/*.geo.json` replays through the REAL load path asserting build-green + verifier-clean + parser-drift (stored utterances must still lower to the stored commands) — a manual session's saved figure becomes permanent regression coverage by dropping the file in the folder (seeded with 4 corpus figures). Locked by `figure-file.test.ts` + the net. **2299 tests green, build clean.** **Then (2026-07-06):** ([ADR-233](docs/06-decisions.md#adr-233)) **a tangent's external-APEX role is assigned by circle MEMBERSHIP (semantic), not by "the label that already exists" (proxy)** — operator session `pr1y4i70` ("tried to create a tangent on C with no success until I changed syntax"): after `ACD חסום במעגל` (A,C,D on the circle), `BA משיק למעגל` was read by `tangentFromExternal` as a tangent FROM A (the one existing label) to a NEW touch B — but A is ON the circle, so the Thales aux-circle on OA is internally tangent at A and the touch B **collapsed onto A** (degenerate). The unclosed on-circle-endpoint + NEW-off-circle-endpoint member of the ADR-081/082 family. Fix (both semantic): `tangentFromExternal` defers when its would-be apex is a circle member (an on-circle point is the TOUCH), and `tangentLine` — which then draws the tangent AT A — now materialises the OTHER named endpoint B as a ±offset slider (so no stated point is dropped, §6 honesty). Sibling audit: `tangentsFromExternal` names its apex only via explicit "from E" (not this class); the July-5 `מעגל O1 וO2 משיקים… בנקודה E` mis-parse is a DIFFERENT class (circle-circle tangency grabbed by a tangent-line rule), filed not bundled. Locked by `tangent-on-circle-endpoint.test.ts` (mirrored He/En) + scenario `segment-tangent-at-on-circle-endpoint-new-far-end`. **2304 tests green, build clean.** ([ADR-234](docs/06-decisions.md#adr-234)) **an explicit equality that names a shape's SOFT default PINS it (commits), not a "already drawn" no-op** — operator session `z4v1zza3`: `משולש שווה שוקיים` drew (the isosceles bakes a soft apex-A pair `|AB|=|AC|`, ADR-138, that "show equal length" correctly does NOT report — the pair is genuinely unstated per ADR-052), then `AB=AC` — the student *saying which sides are equal* — errored "already exists." The engine WAS enforcing `|AB|=|AC|` at every seed; the bug was the commit gate: `AB=AC` names the same pair the soft default already drew, so the figure doesn't move → `dryRunOutcome` read `empty` → "already on the figure — nothing to add," swallowing the student's choice. Class (M4): a student's explicit statement that coincides with a soft default is genuine new information (soft→forced: unreported/cyclable → reported/pinned) even with no coordinate delta. Fix: `dryRunOutcome` gains a semantic "produced" case (sibling of `reveals`/`dataOnly`) — a `set-equal` naming an enabled shape-variant's not-yet-stated pair counts as produced (pure `pinsSoftVariant`, both kite/isosceles, mirrored order, excludes already-stated pairs + the pairless midsegment; cost off the common path). Now: isosceles → nothing forced; `AB=AC` → commits (no move, ADR-138 suppression) → "show equal length" reports the pair; the apex ANGLE stays a free DOF. Filed not fixed (out of scope): a duplicate explicit `set-equal` isn't deduped, so re-typing `AB=AC` a *second* time shifts the figure ~0.75. Locked by `pins-soft-variant.test.ts` + `dry-run.test.ts` + scenario `isosceles-pin-soft-pair` (App.submit-faithful gate). **2314 tests green, build clean.** **Then (2026-07-06, work PC — PLANNING ONLY, no code): theorem discovery v2 replan ACCEPTED and decision-complete — [docs/18-theorem-relevance-plan.md](docs/18-theorem-relevance-plan.md) is the authoritative Phase-6b+ plan; ⟵ RESUME with slice T1 on operator go.** Operator dissatisfaction ("theorems that should appear don't appear; ordering seems random") diagnosed to four root causes: **R1** only 43/109 catalog theorems are in `THEOREM_TABLE` (congruence 18–21, midsegments 62–67, medians 15–17, bisector 78–81 all absent — the operator's own "3 equal segments → חפיפה" example cannot surface); **R2** the Declared/Entailed/Observed evidence paths are matcher-local and `detectRelations`/ADR-224 similar-classes never reach the theorem spine; **R3** ranking is salience→recency→numeric-id with ZERO order assertions anywhere; **R4** no principles/intent layer (the 2-entry concepts feed is the embryo). The plan: **T1** disposition map (every 07 id: `tabled|no-reveal|planned|needs-construct|out-of-scope`, integrity-tested totality) + wire the FULL 25-question ground-truth corpus (only Q5–Q7 wired today) + a session-replay audit harness over `logs/debug-log.jsonl`+fixtures → **T2** catalog fill by measured priority on a shared evidence-predicate library → **T3** named rank bands + authored `pointedness` + `subsumes` edges + per-step `top`/`before` corpus ranking assertions → **T4** the observed lane (`MatchCtx.observed`: relations, similar classes, special lines; ADR-231 shared sample core) → **T5** the **principles lane** (concepts renamed; operator-authored teacher tips "whenever X is given/emerges, think about Y", cataloged in a new 10-pedagogy.md section with a catalog↔table integrity guard; intent archetypes as a boosting subspecies). **All decisions resolved by the operator (2026-07-06, one-by-one):** full catalog · converses = amber recognition prompts the moment the property side is stated · ranking is ballpark-importance-first, discrete bands, NO percentages · **default discovery level stays L1** (the dial's affordance carries discovery — L2 recommendation declined) · intent hints APPROVED (family-direction questions, never instantiated objects, amber, ≤2) · subsumed generics fold under a "מכוסה על ידי #X" label · principles catalog lives in 10-pedagogy.md · the **L2/L3 cutoff accepted**: L2 = provable from the constructs' definitions with zero coordinates (DRAWN gate; theorem-step entailments only by per-case operator approval, the ADR-220 precedent), L3 = known only by measuring the sampled drawings. **Then (2026-07-06, same day): slice T1 BUILT ([ADR-235](docs/06-decisions.md#adr-235)):** the full-catalog **coverage disposition map** (`src/theorems/coverage.ts`, 120 ids, totality + policy-equivalence guards in `integrity.test.ts`), the **measured T2 fill order** (`reports/theorem-fill-order.md` — top absent demand 80, 15–17, 24, 101, 31, 78, 62, 23), the **22-question B-series corpus membership gate** (`b-corpus.test.ts`, givens-only utterances through the real pipeline; today's misses documented as asserted-absent `gaps` so a T2 fix forces the bucket flip — never silent), and the **feed-audit harness** (`src/theorems/audit.ts` + env-gated runner → `reports/theorem-audit.md`). The wiring enumerated T2's evidence worklist (tangency-at-existing-point → tangent bundle; `set-concyclic` → 87; line-through-centre → 103; arc-equality → 92; kite⊃rhombus → 37/38; named-centre circle → 97/98) and recorded four parser findings (⊥-compound mis-lowering; inscribed-kite word dropped; `droppedNewLabels` flags the area marker `S` → needless prod LLM escalation; headless `G on line BD` crash). **⟵ RESUME with T2 (coverage fill) in the measured order.** **Then (2026-07-06, home PC — the operator's last prod diagram, remote events log session `sq9lt4fj`):** ([ADR-238](docs/06-decisions.md#adr-238)) **a driven carrier must not PARK at its own constraint's collapse point** — the two-tangent-circles figure (tangents from N through the shared touch M) wedged N ON M (the regularised-nearest point of its manifold), so the operator's radius sizes (`O1M=9`, `O2M=16`) were refused; fixed by an **anti-collapse barrier RETRY** in both driven solvers (retry-only — the primary descent is untouched, previously-green basins bit-for-bit) **plus HOIST now runs from the PENDING state** (the order-independence rescue was gated behind `!pending`, leaving the complete given set "deferred" forever); both entry orders now build the closed form (radii 9/16, |NM|=√(r1·r2)=12). ([ADR-239](docs/06-decisions.md#adr-239)) **the COMMON-TANGENT construct** (`AB משיק משותף לשני המעגלים` — was an LLM escalation): two-point variant (touch per circle + radius-⟂-tangent, the macro pattern) and at-the-shared-touch variant (drawn tangent at M); the unstated touch↔circle **pairing is a `softPair` default swapped by a later explicit membership** (M4 defaults-yield, the ADR-163 pre-scan shape). Locked by scenarios `shared-touch-tangents-sizes-last`, `common-tangent-two-circles`, `common-tangent-at-shared-touch` + `adr-238-239.test.ts`. **Then (same day, operator "fix all now"): the four findings fixed at root ([ADR-236](docs/06-decisions.md#adr-236))** — the shared `LINE_CUT` clause (⊥/∥ cut-compounds bind in both languages, unbindable cut-verbs escalate instead of half-parsing/D-grabbing), the inscribed **kite** lowers to its ADR-138 `shape-variant` (+ `quadFacts` reads 4-id shape-variants so 87 keeps firing; B8's 37/38 moved gaps→expect), `droppedNewLabels` normalizes + masks the area marker `S` (no more needless LLM escalations for `S_{ABC}=13`/`O_1`), and a NEW point named onto a line is created as a free slider (+ a `constraintIsPending` guard so dangling refs can never crash replay). Locked by `adr-236.test.ts`. **Then (same day): the reloaded two-tangent-circles figure — a size given on a similarity-gauge figure is a SCALE statement ([ADR-237](docs/06-decisions.md#adr-237)):** `O2M=9` on the full bagrut figure (tangent pairs from N to BOTH circles + A-B-N collinear) was refused although ADR-230 pinned the radius correctly — no idle centre existed for the tangency gap and the 9-DOF solve can't find what is a pure scale move; the step failure path now ends in a closed-form `scaleRescue` (k = stated/measured over every free DOF; LAST resort after the recruiter, try-and-verify, pinned-coordinate bail), and the second size closes the figure via the M2 orphan re-home (|O1O2| = 25 exactly). Locked by scenario `size-given-scales-similarity-gauge-figure`. **Then (2026-07-06, home PC): the saved-file / edit honesty triple** — ([ADR-241](docs/06-decisions.md#adr-241)) **an edited step re-lowers against its OWN position's PREFIX context** (editing `AB קוטר`→`AC קוטר` used the end-state context where the ⊥-step's C "existed", so the diameter lowered to a bare collinearity and A,C floated with every row ✓); `replaceGroup` gained the submit path's seed-validity search, and the diameter rule's existing-endpoints branch now asserts **membership, not just collinearity** (existence ≠ membership — the no-edit sibling `מעגל O / BD⊥AC / AC קוטר` was equally broken). ([ADR-240](docs/06-decisions.md#adr-240)) **a multi-subject membership statement asserts EVERY named point** (`A ו C נמצאות על המעגל` used to lower to `point-on-circle A` alone — the operator's exported figure file carried the partial lowering to every machine) — `pointOnCircle` reads the ADR-076 uppercase-label-list subject + the carrier defer ("D על המיתר AB במעגל O" is on the CHORD now), and the **LLM commit path re-checks `droppedNewLabels`** (the escalation seam used to re-enter the same single-subject grammar and silently commit the identical partial parse). ([ADR-242](docs/06-decisions.md#adr-242)) **load-time honesty audit** (`src/store/loadAudit.ts`) — a loaded `.geo.json` replays its SAVED lowering by design (ADR-232), so load now runs a budgeted **drift + dropped-labels** audit against the CURRENT parser and raises a persistent note naming suspect rows (✎ on a row is the per-step re-lower). Locked by scenarios `diameter-edit-rereads-at-position` (the harness gained an `{ edit: { step, to } }` step form), `diameter-on-existing-free-points`, `multi-point-on-circle-membership` + `multi-point-on-circle.test.ts` / `load-audit.test.ts`. **Then (2026-07-06, home PC): two CONCENTRIC circles ([ADR-244](docs/06-decisions.md#adr-244))** — operator: "שני מעגלים בעלי מרכז משותף O gives me just one circle" (bagrut Q6); circle IDENTITY was the centre letter, so a second same-centre circle was unrepresentable (the He phrase dead-ended at the LLM whose 2nd circle command collapsed into a RESIZE; the En phrase HALF-parsed to one circle; "המעגל החיצוני/הפנימי" silently attached to the one circle). Fix at the reference-model class: the **pair macro** (`circle-O` bound OUTER + `circle-O-2` bound INNER via a new `set-radius-order` — role binding at creation is pure gauge, both radii stay free DOFs), **qualifier resolution as a parse-seam post-pass** (the ADR-119 chokepoint — every circle rule gains חיצוני/פנימי/גדול/קטן · outer/inner/larger/smaller at once; stated membership disambiguates; otherwise a new `ambiguous-circle` clarification, the ADR-164 pattern), **`circleMembers` re-keyed by circle id**, and the **radius-order requirement riding the existing verifier/`meetsRequirements` gate** (`figure.v.radiusOrder` — sampler/"show another" never swap the pair; a contradicted order reads amber). Sizing rides ordinary distance givens (`OA=4`; ADR-230's routing was already per-membership); paired radius sliders label outer/inner. Locked by scenario `two-concentric-circles-q6` + `concentric.test.ts` + a catalog entry.

## Project memory (rule)

All durable project context lives **in this repo** — it syncs via Dropbox, so it travels to any machine David works from. The assistant's machine-local memory does **not** travel, so **never rely on it for this project.** At session start, read [`docs/PROJECT-MEMORY.md`](docs/PROJECT-MEMORY.md). Record decisions in the ADR log ([`docs/06-decisions.md`](docs/06-decisions.md)), status in the plan ([`docs/09-implementation-plan.md`](docs/09-implementation-plan.md)), and operational notes in `docs/PROJECT-MEMORY.md` — not only in local memory.

## Documentation

Full project docs live in [`docs/`](docs/) — vision, functional + non-functional requirements, design/architecture, glossary, the decision log (ADRs), the [pedagogy charter](docs/10-pedagogy.md) (what the tool should teach + the construction→theorem trigger map that drives Phase 6), and an [architecture-as-a-compiler lens](docs/11-architecture-as-compiler.md) (the NL→commands→evaluation→render pipeline as a front-end + constraint interpreter + back-end, and where new work slots in). Start at [`docs/README.md`](docs/README.md). These are the authoritative, detailed source; this file is the quick orientation and points there for depth. **Keep both in sync as the design evolves**, and add an ADR to `docs/06-decisions.md` for any significant decision.

## Testing & definition of ready

Test strategy and per-step acceptance gates live in [`docs/08-testing-strategy.md`](docs/08-testing-strategy.md). **Working rule for this repo: do not report a feature or build step as "ready" until its acceptance gate passes** — tests green, `tsc`/build clean, results shown honestly (no skipped/`.only` specs hiding gaps). The engine is pure and deterministic and is tested hardest; the LLM fallback is mocked (no live API calls in tests). The **stability** regression (existing points must not jump when a fact is added) is a first-class test.

**Rule (NO autonomous Anthropic API / Haiku calls — operator policy):** never fire a live Anthropic/Haiku API call on your own, even though the key in `.env.local` makes it technically possible. **Only the operator authorises a live API call.** To test the LLM fallback or a freeform decomposition, act as the **oracle yourself using your own session model (Opus 4.8, subscription, free)** — reason out the canonical command lines the LLM should emit, then verify they parse + build through the real `parse → replay` path. Opus ≈ Haiku is accepted as good enough; escalate to a live Haiku call **only with explicit operator approval**, and only when the operator's live results diverge from your Opus prediction (i.e. it's suspected Haiku-specific). See `docs/PROJECT-MEMORY.md` (Operational notes) for the full rationale + the sanctioned one-off live-call pattern.

**Rule (root cause over symptom — NEVER PATCH):** When diagnosing a reported bug, fix the **root cause**, never the surface symptom. **We do not write patches and we do not special-case the one input that errored — full stop.** Always find the core engine feature that failed and fix *that*, even when it is a big, risky change. The size of the correct fix is not a reason to avoid it. Concretely: (1) reproduce and trace *why* it happens before changing code (the debug log + a throwaway repro script); (2) fix at the layer where the defect actually originates — a missing keyword in the shared set, a wrong solver-routing branch, a missing engine capability — rather than special-casing the one figure that exposed it; a true root fix usually clears sibling bugs too (e.g. the mixed-carrier routing fix covers a whole class of figures, not just the one reported); (3) a green test on the reported case is necessary but **not sufficient** — ask "can this same class of bug still happen elsewhere?"; (4) **a narrow/local patch is not an acceptable outcome.** If the proper fix looks large or you are unsure what the core feature is or how far the fix should reach, **stop and ask the operator** — do not quietly ship a patch instead. The big correct fix is always the right thing; when in doubt, surface the scope and let the operator decide, never default to the symptom fix; (5) state the root cause in the commit message. **How to comply — the operational method (class-first diagnosis, patch tripwires, the chokepoint registry, the designed mechanisms M1–M4, perf rules, and the escalation template) is dictated in [docs/17-design-rules.md](docs/17-design-rules.md). Read it before fixing any reported bug; it has operator authority.**

**Rule (reported bugs become regression scenarios — NON-NEGOTIABLE):** whenever the operator reports a problem and it is diagnosed from the debug log (`logs/debug-log.jsonl`), the fix is **not complete** until the *exact utterance sequence* they entered is captured as an end-to-end scenario in [`src/__tests__/scenarios.test.ts`](src/__tests__/scenarios.test.ts) (and indexed in [`docs/test-scenarios.md`](docs/test-scenarios.md)) — replayed through the real parse-with-context → fact list → `replay` path and asserted. Analysis + fix + unit test is **not enough**: the operator's actual sequence must become permanent regression coverage so what worked before keeps working. Steps that escalate to the LLM are recorded as the canonical commands the log shows (the LLM is mocked). This is in addition to, not a replacement for, the per-fix unit test.

## Commands

- `npm run dev` — Vite dev server
- `npm run build` — `tsc -b` typecheck then `vite build`
- `npm test` — Vitest (watch mode). Single file: `npx vitest run <path>`. Single test by name: `npx vitest run -t "<name>"`
- Path alias `@/` → `src/` (keep `tsconfig.json` and `vite.config.ts` in sync)

## The 3-D sibling app — `src3d/` → themathbible.com/3d-builder/

A SECOND app in this repo (plan: [docs/20-space-vectors-tool.md](docs/20-space-vectors-tool.md), ACCEPTED; decisions: [docs/06b-decisions-3d.md](docs/06b-decisions-3d.md), ids `ADR-3D-NNN`) for the bagrut **space/vectors** question (Q2): geometric `u,v,w` vectors on solids + algebraic R³ lines/planes. **Isolation rules (docs/20 §12) have operator authority:** `src3d/` never imports from `src/` (patterns are COPIED); the 2-D locale files/ADR log/status text are never touched by 3-D work; 3-D decisions go in 06b and status updates go HERE, in this section only. Entry `3d.html`, build `npm run build:3d` (own config `vite.config.3d.ts`, base `/3d-builder/`, output `dist-3d/` — deploy renames `3d.html`→`index.html`); dev = the normal `npm run dev`, page `/3d.html`; tests ride the one shared vitest suite. **NO CAS — hard boundary (D3):** anything needing symbolic solving beyond 1–2-DOF numeric root-finds goes back to the operator. The curriculum has **no cross product** — internal use only, never student-facing. **State: V0 + V1 BUILT (2026-07-06, ADR-3D-001/002).** V0 — `Vec3` engine (cube/box/right-prism with ADR-052 free dims, on-segment points incl. ratio `AK=2KA'`), orthographic-orbit SVG renderer with textbook dashed hidden edges (numeric outward normals, convex rule), zundo store (derive-on-demand, keep-prior-on-error), He/En parser rules (prime `′`→`'` canonical; oblique-prism + bad-ratio honesty refusals), minimal RTL shell. V1 — the **geometric-vector lane**: `נסמן: AB=u…` basis naming; **CLAIMS** (the student's answer, verified across 4 seeded configurations, refused `claim-refuted` when wrong — never a driver): `vec-eq` (`AM = ½u+½v+5/3w`), `perp-plane` (`CA' מאונך למישור BC'D`), `collinear3` (`על ישר אחד`); centroid (`מפגש התיכונים`); the span-defined point `P על AM כך ש-KP = αu+βv` (Greek = unknown scalars, t driven CLOSED-FORM via one Cramer decomposition — the no-CAS boundary held; `spanPoint` runs before `onSegment` + a Greek guard so the condition can never silently degrade to a free point); auxiliary segments auto-draw (claims/planes/triangles, idempotent) and dash by the textbook interior rule (midpoint inside a solid, or on only back-facing faces). **GATE MET: 2020-Q2 א–ב + 2023-Q2 א–ב reproduce end-to-end from typed He/En utterances** (`src3d/__tests__/scenarios3.test.ts`), wrong answers refused. ADR-3D-003: named vectors render as their own teal arrow (tail→head overlay, arrowhead AT the head, notation label with arrow-above + underline); V0+V1 deployed to themathbible.com/3d-builder/ + a home-page rubric (2026-07-06). **V2 — the algebraic lane (ADR-3D-004): GATE MET, 2022-Q2's full chain He+En** — planes by equation with ONE symbolic parameter (`LinExpr`, `two-params` refused), the 45° given pins it by grid+bisection root-finding (roots = branches; `no-roots` = honest contradiction), membership (`נמצאת על אחד המישורים`) SELECTS the branch (else the seed cycles ±1; unpinned = sampled free DOF), `resolve3` returns positions+planes+lines+param and the renderer consumes it whole; feet on plane/line closed-form with ⟂ knee marks; ℓ echoed on-canvas in parametric form; scalar claims (`AB=3`, `שטח ABC=4.5`) multi-seed verified; a size claim on a free-dim solid refused `size-on-solid` (lane boundary). Parser: `parseLinearEq` all-or-nothing; **`ℓ` is not `\w` — never `\b` after a line name** (explicit lookaheads). Plane-visualization fixes (ADR-3D-004 Am. 1+2, operator screenshots): intersecting planes' patches share a focus ON their fold with an axis ALONG it (crossing guaranteed), the fold draws as an implicit seam before ℓ is named, a stated plane-angle gets a dihedral arc + value, and **patches extend to cover every point lying on the plane** (fold-anchored asymmetric extents; non-members never inflate). **Save/load (ADR-3D-005):** portable `.geo3.json` (replay inputs only, load = one undoable set, strict refusals) + **the `fixtures3/` regression net** — a saved manual session dropped in the folder becomes permanent coverage (all-facts-OK + parser-drift asserted; regenerate seeds with `GEN_FIXTURES3=1`). 125 src3d tests; `tsc -b` + both builds clean. **V3 — parameters in lines (ADR-3D-006): GATE MET, 2024-Q2 He+En** — typed parametric line `הישר ℓ: x = (-1,5,-11) + t(m-1, 5-m, -2)` (`Line3Def` parametric kind, LinExpr components, `parseParamExpr`); `parseLinearEq` handles `(m+6)z` (all-or-nothing, poison marker `§` — recorded trap: a `' '` marker became a NUL byte and broke exact-match edits); the ⟂ given pins m via **minima-scan + ternary root-finding** (`touchZeroRoots` — a non-negative residual never sign-changes; roots cross-filtered against ALL pinning givens); m=−5 unique, A=ℓ∩π=(2,0,−10) via `line-plane-point` (parallel ⇒ refused `line-misses-plane`, never a fake point); the **never-parallel probe is a family-level claim** (`dir(m)·n(m)` has no zero over the scan — sign changes AND touches refute); `on-line` membership verified (`not-on-line`) — accepting `B(5,-5,-9) על הישר ℓ` IS the ד-trap discovery. 145 src3d tests; vite build:3d clean. **V4 — the coordinate-injection PIVOT (ADR-3D-007): GATE MET — ALL FOUR corpus exams (2020/2022/2023/2024) now reproduce end-to-end He+En.** A coordinate statement about an EXISTING point is a GIVEN (M1): `point3` on a taken id lowers to a pivot PIN (partial `A(3,n,p)` — letters null, only numerics constrain; a NEW point with letters refuses `symbolic-new-point`); `נתון: v = (10,-5,0), u = (5,5,-5), P(0,4,6)` parses as one injection list. The pivot = **numeric similarity+dims least-squares** (`solve3.ts`: LM + central-difference Jacobian, 8-rotation seed-rotated multi-start, restart-polish; translate+axis-angle+log-scale+solid dims; vector pins transform without translation). **Under-determination welcome:** the 2020 prism height (never injected) stays a free resampled DOF while K=(−3,4,7) stands still. **Reflection = the discrete branch:** both mirrors solved; sign givens (`שיעור ה-z של C' חיובי`) select (else the seed cycles); `sign-unsatisfiable`/`injection-unsatisfiable` honest refusals. Planes THROUGH POINTS (Newell, post-pivot pass) + `plane-eq` claims (`המישור KBC: x+2y+3z-26=0`, scalar multiples pass). **Claim REL_TOL 1e-7→2e-5** — the numeric-Jacobian floor (~1e-6 in unpinned directions) sits under it, wrong answers (≥0.5 off) far above. 169 src3d tests; `tsc -b` + `vite build:3d` clean. **V5 — breadth + polish (ADR-3D-008)** — 6 more 572 exams read (10 total; **headline gap → V7: points by general vector combinations** `DF⃗=(k/2)DB⃗+kDC⃗`, 4 of 6 exams); the 2019 cube wired as a gate (a `line-through` two points + FINAL line∩plane fill pass, colon ratios `AE:EC=2:1`, angle-between-segments ≤90° claims, length-ratio claims — K=(2,4,2) ✓ He+En); right pyramid (apex above the CIRCUMCENTRE, oblique refuses); `catalog3.ts` + in-app panel + the guard test (every entry parses He+En); **LLM fallback via the SHARED proxy** — a `tool:'3d'` body field selects `llmShared3`'s prompt (server/parseHandler is the one place binding both apps; client `llm3.ts`+`submitSteps` re-parse every returned line; PAR-10 contract test; prod posts to `/geo-builder/api/parse`); responsive canvas, PNG export, DOF cue (`freeDofCount3`). **V6 — solids of revolution (ADR-3D-009, D4's committed scope)** — cylinder/cone/sphere (NOT in the 572 corpus — they live in the 571 paper; built against formula-sheet canonicals): unstated radius/height are FREE sampled DOFs, stated ones pin; unnamed centres stay unnamed (no invented labels); sampled-outline rendering (front-solid/back-dashed circles, silhouette generators, the sphere's exact orthographic silhouette, dashed axis); volume/`שטח המעטפת`/`שטח הפנים` π-claims verified from the formula sheet, refusals `no-such-solid`/`free-size-claim`. 270 src3d tests; `tsc -b` + builds clean. **⟵ RESUME with V7 (points defined by general vector combinations + the coefficient-pinning conditions — the widened corpus's headline gap) per docs/20; 571-paper validation of the revolution block also filed.**

## Target architecture

Natural language → commands → constructive evaluation → rendered figure. Build it in this order (the first step is the make-or-break risk):

### 1. Constructive / dependency-graph engine (the core)

Replaces the old template solver. Every object is **defined in terms of earlier objects**, classified by degrees of freedom:

- **Free point** (2 DOF) — placed, draggable.
- **Point-on-object** (1 DOF) — a parameter `t` along a segment/line/circle. This is what makes "G on AD" representable — the thing the old model couldn't express.
- **Derived point** (0 DOF) — intersection, midpoint, foot of perpendicular; fully determined by its parents.

Key properties:

- **Evaluation is topological** (dependency order) → coordinates. No template matching.
- **Branches are the "alternatives" feature.** A line∩circle or circle∩circle has 0/1/2 solutions; each derived object stores a **branch index**. "Show another configuration" increments the branch index and re-evaluates.
- **Stability is structural, not heuristic.** DOF parameters and branch indices persist across steps, so adding a constraint re-evaluates downstream objects without perturbing earlier choices — the figure doesn't jump. (No `previousPositions`/`fitState` threading like the old solver.)
- **Over-constraint detection** is general: if a step can't be satisfied, flag it, keep the prior figure, surface a "that contradicts…" message.

### 2. Rendering — hand-rolled SVG via React

The engine is the single source of truth; React renders SVG declaratively from its computed output (points, segments, polygons, circles, angle arcs, right-angle marks, equal-side ticks, labels, dashed special lines; animate free-point moves; pan/zoom + fit). **No imperative reconciler** (that layer existed in the old code only because JSXGraph was imperative — JSXGraph is gone). The renderer is a **swappable layer** — it could be replaced (e.g. with Mafs) without touching the engine.

### 3. Input layer — parser-first, API fallback

A clean `utterance → command[]` boundary. A **deterministic Hebrew/English grammar parser runs first** (free, offline, handles the common simple inputs); only ambiguous phrasings **escalate to the Claude API** (`claude-haiku-4-5` — sufficient for bounded structured parsing, ~10× cheaper than Opus/Fable). The engine never knows which parser produced the commands.

**API key handling:** the key must live **only in a server-side proxy** — never shipped to the browser (the old code's `VITE_ANTHROPIC_API_KEY`-in-browser approach is abandoned). Cost controls: prepaid credit cap, Console spend limit, gated proxy (per-class code) + per-IP rate limit, low `max_tokens`, lean prompt.

### 4. Theorems (v1)

`detect(figure)` predicates run over the computed model and surface relevant theorems as the student builds.

## v1 scope

- **Shapes:** triangle, quadrilateral, circle
- **Points:** free, on-segment/on-line/on-circle, intersection, midpoint, foot
- **Constraints:** distance, angle, right-angle, parallel, perpendicular, equal-segments
- **Special lines:** height, median, angle bisector, perpendicular bisector, midsegment
- **Alternatives toggle** + **theorem panel**

## Conventions to carry forward

- **No fixed assumptions — every unstated magnitude is a free DOF, not a fixed value ([ADR-052](docs/06-decisions.md#adr-052)).** A student enters only what the question shows; the tool must assume no size/angle/position/proportion unless it was stated (a number, an angle, or a relation that forces it). A default value is allowed as a *starting* point so the figure can be drawn, but it must change on "show another configuration" or when a later constraint forces it — a fixed default silently asserts a given the question never gave (the same cardinal sin as drawing a figure that violates the givens). Conformance smell: a value counted by `rawMovableDof` but absent from `freeDofs` (so never sampled) is a default masquerading as fixed.
- **RTL Hebrew is the default.** All user-facing strings go through `useTranslation`/`t()` (`src/i18n/`, `locales/he.json` + `en.json`). Toggling language updates `document.documentElement.dir`. The parser and any LLM fallback handle both Hebrew and English input.
- **Deterministic element IDs** (e.g. `seg-AB`, `poly-ABC`) so re-issuing the same command is idempotent — preserve this in the new model.
- **Stack:** React + Vite + Zustand + TypeScript. State/undo can use Zustand (+ `zundo` for temporal undo/redo, as the old store did).
