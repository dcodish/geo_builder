/**
 * Placeholder shell during the from-scratch rebuild.
 * The real UI (canvas + input + theorem panel) is built in step 2 of the
 * build order. Old UI lives in /archive for reference.
 */
export default function App() {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'system-ui, sans-serif',
        color: '#475569',
      }}
    >
      <p>Geo Builder — rebuild in progress.</p>
    </div>
  );
}
